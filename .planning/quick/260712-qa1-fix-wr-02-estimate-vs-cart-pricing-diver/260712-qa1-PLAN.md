---
phase: quick-260712-qa1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/App.tsx
  - src/__tests__/print.test.tsx
autonomous: true
requirements: [WR-02]
must_haves:
  truths:
    - "In fixed-bag mode, a grid color with no drill mapped for the current shape contributes $0 to the displayed supply total (matching the Shopify cart, which drops it)."
    - "Mapped colors bill identically to before in fixed-bag mode — no cost/quantity regression."
    - "The optimized-bags branch and the WR-01/WR-02 banner effect are untouched."
    - "A regression test proves estimate == cart ($0) for an unmapped-shape color in fixed-bag mode, and non-zero-unchanged for a mapped color."
  artifacts:
    - "src/App.tsx — exported pure helper calculateFixedBagCost(code, shape, count, bagSize, packetCost) + fixed-bag else branch rewired to use it"
    - "src/__tests__/print.test.tsx — new describe block asserting mapping-aware fixed-bag cost"
  key_links:
    - "calculateFixedBagCost gates on hasVariantMapping(code, shape) — unmapped => zero packets/purchase/costExact/costSafety"
    - "row.costSafety => safetyDrillCostCents (App.tsx:986) => displayed total; unmapped row.costSafety === cart priceColorPack(packColor(...)) === 0"
---

<objective>
Fix WR-02: the fixed-bag cost branch in `src/App.tsx` bills a non-zero cost for a grid
color that has NO drill variant mapped for the currently selected shape, while the Shopify
cart drops that same color — so the displayed total diverges from the cart. Make the
fixed-bag branch mapping-aware (mirroring the already-correct optimized branch) so an
unmapped-shape color is a $0 line in BOTH modes, then lock the behavior with a regression
test asserting estimate == cart.

Purpose: Eliminate the last known estimate-vs-cart divergence — the core promise of the
"trustworthy pricing" milestone. Phase 15 fixed only the banner wording (WR-01/WR-02);
this closes the cost-behavior half that was deferred as pre-existing in 15-REVIEW.md.

Output: A mapping-aware pure helper + rewired fixed-bag branch, plus a Vitest regression
test. `npx tsc --noEmit` and `npm test` both pass.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

# The fix site (fixed-bag else branch ~941-957) and the reconciliation total (~980-990)
@src/App.tsx

# The mapping-aware predicate (already imported at App.tsx:7) — hasVariantMapping at line 5114
@src/engine/variants.ts

# The mapping-aware primitive the optimized branch AND the cart already use — packColor
# returns an empty pack ($0) for an unmapped color; priceColorPack prices it to 0
@src/engine/bagPlanner.ts

# The cart that already drops unmapped colors (checkout.ts:44-50) — the estimate must reconcile to this
@src/engine/checkout.ts

# Existing calculateSafetyPurchase tests — the sibling primitive; add the new tests here
@src/__tests__/print.test.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add mapping-aware fixed-bag cost helper and rewire the else branch</name>
  <files>src/App.tsx</files>
  <behavior>
    New exported pure helper `calculateFixedBagCost(code: string, shape: 'square' | 'round', count: number, bagSize: number, packetCost: number)` returning `{ safety, packets, purchase, costExact, costSafety }`:
    - Unmapped shape (hasVariantMapping(code, shape) === false), e.g. code '471' + shape 'square', count 350, bagSize 200, packetCost 0.25: returns safety 385 (the +10% drill COUNT is preserved to match the optimized branch's drill-count column), packets 0, purchase 0, costExact 0, costSafety 0.
    - Unmapped shape code '798' + shape 'round', count 350: packets 0, purchase 0, costExact 0, costSafety 0.
    - Mapped shape code '310' + shape 'square', count 350, bagSize 200, packetCost 0.25 (unchanged from today): safety 385, packets 2, purchase 400, costExact ~0.4375 ((count/bagSize)*packetCost), costSafety ~0.5 (packets*packetCost).
  </behavior>
  <action>
Add a new exported pure function `calculateFixedBagCost` in `src/App.tsx`, placed immediately after `calculateSafetyPurchase` (currently ends at line 66) so the two fixed-bag primitives sit together and are both unit-testable from `../App`. It takes `(code, shape, count, bagSize, packetCost)` and returns `{ safety, packets, purchase, costExact, costSafety }`.

Implementation contract:
- First compute `const metrics = calculateSafetyPurchase(count, bagSize)` (reuse the existing primitive — do not duplicate the +10% math).
- If `!hasVariantMapping(code, shape)`: return the mapping-aware $0 line — `safety: metrics.safety`, `packets: 0`, `purchase: 0`, `costExact: 0`, `costSafety: 0`. Keep `safety` equal to `metrics.safety` (NOT zero): this mirrors the optimized branch, which keeps a non-zero drill-count in the Safety Margin column but zeroes the purchasable bags and cost for an unmapped color. Add a short comment stating this emits a $0 line for a color with no drill in the selected shape, matching the cart (checkout.ts drops it) and the optimized branch (packColor returns an empty pack).
- Else (mapped): return exactly today's fixed-bag math so mapped colors are byte-for-byte unchanged — `safety: metrics.safety`, `packets: metrics.packets`, `purchase: metrics.purchase`, `costExact: (count / bagSize) * packetCost`, `costSafety: metrics.packets * packetCost`.

`hasVariantMapping` is already imported at App.tsx:7 and `calculateSafetyPurchase` is module-scope in the same file — no new imports.

Then rewire the fixed-bag `else` branch (currently App.tsx:941-957). Replace the inline `const metrics = calculateSafetyPurchase(...)`, `costExact`, `costSafety` computation and the returned object with a single call: `const fixed = calculateFixedBagCost(code, drillStyle, count, drillBagSize, drillPacketCost);` and return an object preserving the EXACT existing row shape and field names — `code, count, name, hex, safety: fixed.safety, packets: fixed.packets, purchase: fixed.purchase, costExact: fixed.costExact, costSafety: fixed.costSafety, bagsText, optimizedBags: null, hasUnpricedSize: false`. For `bagsText`, emit `` `${fixed.packets} bag(s)` `` when `fixed.packets > 0`, otherwise `'None'` (so the unmapped $0 line reads 'None', matching the optimized branch's empty-pack bagsText; mapped colors always have packets > 0 since counts come from used grid cells, so their text is unchanged).

Do NOT touch the optimized branch (the `if (optimizeBagsCost)` arm, ~919-940), the reconciliation block (safetyDrillCostCents/totalCostSafety, ~983-990), or the WR-01/WR-02/DATA-01 banner derivation and effect (~992-1034). The banner already surfaces unmapped-shape colors correctly; this change only zeroes their COST so the total matches the cart.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm test</automated>
    Also confirm the wiring exists: `grep -c 'calculateFixedBagCost' src/App.tsx` returns 2 or more (one definition + at least one call in the else branch), and `grep -c 'hasVariantMapping' src/App.tsx` is unchanged from before plus the new usage inside the helper.
  </verify>
  <done>
    `calculateFixedBagCost` is exported from src/App.tsx, gates on `hasVariantMapping`, and returns a $0/zero-quantity line for unmapped-shape colors while leaving mapped colors identical. The fixed-bag else branch calls it and preserves the row shape (code, count, name, hex, safety, packets, purchase, costExact, costSafety, bagsText, optimizedBags:null, hasUnpricedSize:false). `npx tsc --noEmit` passes and the existing suite still passes (no regressions). Behavior is proven by Task 2's tests.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add estimate == cart regression tests for the fixed-bag branch</name>
  <files>src/__tests__/print.test.tsx</files>
  <action>
Add a new `describe('Fixed-bag cost is mapping-aware (WR-02)')` block to `src/__tests__/print.test.tsx`. Extend the existing top imports: import `calculateFixedBagCost` from `../App` (alongside the current `calculateSafetyPurchase` import), and import `packColor` and `priceColorPack` from `../engine/bagPlanner` (the exact primitives the Shopify cart uses in checkout.ts, so the assertion reconciles the estimate to the cart).

Define a standard price table for the tests: `const priceDb = { 200: 0.25, 500: 0.55, 1000: 0.8, 2000: 1.4 };`.

Write these cases:

1. Unmapped-shape color is a $0 line (471 + square). Call `calculateFixedBagCost('471', 'square', 350, 200, 0.25)` and assert `costExact === 0`, `costSafety === 0`, `packets === 0`, `purchase === 0`, and `safety === 385` (the +10% drill count is preserved, matching the optimized branch's Safety Margin column). Reference: 471 has an empty `square: {}` mapping in variants.ts.

2. Estimate reconciles to the cart for the unmapped color. Compute the cart's contribution the same way checkout.ts does — `const cartCost = priceColorPack(packColor('471', 'square', 350, priceDb), priceDb);` — assert `cartCost === 0`, and assert the estimate's `calculateFixedBagCost('471', 'square', 350, 200, 0.25).costSafety === cartCost` (both 0 — the divergence is closed). Also assert `packColor('471', 'square', 350, priceDb).packets === 0` to document that the cart drops the color.

3. Second empty pair (798 + round). Assert `calculateFixedBagCost('798', 'round', 350, 200, 0.25).costSafety === 0` and `packets === 0`. Reference: 798 has an empty `round: {}` mapping in variants.ts.

4. Mapped color is unchanged (regression guard). Call `calculateFixedBagCost('310', 'square', 350, 200, 0.25)` and assert `safety === 385`, `packets === 2`, `purchase === 400`, `costSafety` is `toBeCloseTo(0.5)` (2 * 0.25) and `costExact` is `toBeCloseTo(0.4375)` ((350/200) * 0.25) — i.e. the fix does NOT zero mapped colors.

Keep the existing `Safety margin calculations` describe block and the trailing comment intact. Do not modify any other test file.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm test</automated>
  </verify>
  <done>
    print.test.tsx contains a passing `Fixed-bag cost is mapping-aware (WR-02)` block that asserts (a) an unmapped-shape color (471/square and 798/round) yields a $0 fixed-bag line, (b) that $0 equals the cart's contribution via `priceColorPack(packColor(...))`, and (c) a mapped color (310/square) still bills its original non-zero cost/quantities. `npm test` runs green (all prior tests plus the new cases) and `npx tsc --noEmit` passes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Estimate UI -> Shopify cart | The displayed supply total is a price the user acts on; it MUST equal what the cart charges. A divergence is a pricing-integrity defect (the milestone's core concern). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-qa1-01 | Information Disclosure | fixed-bag cost branch (App.tsx sortedMatches) | high | mitigate | Make the branch mapping-aware via `calculateFixedBagCost` gating on `hasVariantMapping`, so an unmapped-shape color is a $0 line matching the cart — the displayed total no longer overstates cost. Locked by the Task 2 estimate==cart regression test. |
| T-qa1-02 | Tampering | mapped-color pricing (regression risk) | medium | mitigate | Preserve the exact existing fixed-bag math for mapped colors (safety/packets/purchase/costExact/costSafety); Task 2 case 4 asserts mapped colors are unchanged. |

No package-manager installs in this task (pure client-side TS edit + Vitest test) — no supply-chain (T-*-SC) threat applies.
</threat_model>

<verification>
- `npx tsc --noEmit` passes (no type errors from the new helper signature or the rewired branch).
- `npm test` passes: existing `Safety margin calculations` tests plus the new `Fixed-bag cost is mapping-aware (WR-02)` block are all green.
- Manual reasoning check: an unmapped-shape color's `row.costSafety` is 0, so it adds 0 to `safetyDrillCostCents` (App.tsx:986) and the displayed `totalCostSafety` matches the cart, which already drops the color.
</verification>

<success_criteria>
- Fixed-bag mode: an unmapped-shape color (e.g. 471 with drillStyle 'square') is billed $0, matching the Shopify cart and the optimized branch.
- Mapped colors in fixed-bag mode are byte-for-byte unchanged (no cost or quantity regression).
- Optimized branch and the WR-01/WR-02/DATA-01 banner are untouched.
- A regression test asserts estimate == cart ($0) for the unmapped color and non-zero-unchanged for a mapped color.
- `npx tsc --noEmit` and `npm test` both pass.
</success_criteria>

<output>
Create `.planning/quick/260712-qa1-fix-wr-02-estimate-vs-cart-pricing-diver/260712-qa1-SUMMARY.md` when done.
</output>
