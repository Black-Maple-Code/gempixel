---
phase: quick-260713-01y
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/engine/symbols.ts
  - src/engine/__tests__/symbols.test.ts
autonomous: true
requirements: [QUICK-260713-01y]
must_haves:
  truths:
    - "Grid + Symbols view leads with distinct non-alphanumeric shape glyphs for the most-common colors"
    - "No digit (0-9) ever appears as a chart symbol in any palette"
    - "Ambiguous capitals B, G, I, O, Q, S, Z never appear as symbols; only unambiguous letters, and only AFTER all glyphs are exhausted"
    - "A 118-color chart is covered by glyphs alone — no letters, no multi-char combos in the common case"
    - "Legend symbols still match grid symbols (allocation frequency-desc + tie-break unchanged)"
    - "The multi-char overflow combo only triggers when a palette exceeds the full single-symbol pool (rare last-ditch uniqueness net, preserving B4)"
  artifacts:
    - src/engine/symbols.ts
    - src/engine/__tests__/symbols.test.ts
  key_links:
    - "CURATED_SYMBOLS = [...GLYPH_SYMBOLS, ...SAFE_LETTER_SYMBOLS] feeds generateSymbolAllocation -> symbolMap -> viewer.ts fillText + export.ts + App.tsx legend"
    - "Identifier rename (LETTER/NUMBER/WINGDING -> GLYPH/SAFE_LETTER) touches only symbols.ts self-import and symbols.test.ts imports (grep-confirmed: no other importers)"
---

<objective>
Redesign the Grid + Symbols allocation pool so distinct, monochrome shape-glyphs lead the chart and unambiguous capital letters are a last resort — with NO digits anywhere. This fixes confusing letter/number iconography at small cell size (O/0, I/1, Z/2, S/5, B/8) and the ugly two-char "glyph+digit" combos on large palettes.

Design is ALREADY DECIDED ("Symbols-first, letters last resort"). Implement exactly to spec — do not re-open the design.

Purpose: A 100-200 color chart reads cleanly at ~10px cells with visually-distinct glyphs, no digits, no combos in the common case, and legend-matches-grid.
Output: Rebuilt `src/engine/symbols.ts` (new ordered pool + renamed exports) and rewritten `src/engine/__tests__/symbols.test.ts` invariants. Purely presentational — no worker / pricing / persistence impact.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@src/engine/symbols.ts
@src/engine/__tests__/symbols.test.ts
@CLAUDE.md

# Reference only — consumers use symbolMap / symbolFontPx / getContrastColor, NOT the tier names.
# Do NOT edit these; they must keep working unchanged:
@src/engine/viewer.ts   # draws grid symbols via ctx.fillText, ~L386-395 (bold {symbolFontPx}px 'Outfit', sans-serif)
@src/engine/export.ts   # draws symbols in exported chart + legend
@src/App.tsx            # legend uses symbolMap[c.dmc]
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild symbols.ts — symbols-first ordered pool, no digits, letters last</name>
  <files>src/engine/symbols.ts</files>
  <action>
Rebuild the ordered pool in `src/engine/symbols.ts` as `CURATED_SYMBOLS = [...GLYPH_SYMBOLS, ...SAFE_LETTER_SYMBOLS]`. Rename the exports: remove `LETTER_SYMBOLS` and `NUMBER_SYMBOLS` entirely (NO digits in the pool anywhere), and replace `WINGDING_SYMBOLS` with a much larger `GLYPH_SYMBOLS`. Add a new `SAFE_LETTER_SYMBOLS`.

1) `GLYPH_SYMBOLS` — a LARGE, ordered, de-duplicated pool of visually-distinct, MONOCHROME, text-presentation BMP glyphs that render reliably in canvas fillText under 'Outfit', sans-serif. ORDER most-distinct / boldest FIRST; put subtler look-alikes later; DEDUPE any pair that is near-identical at ~10px. Target ~120-150 unique glyphs so a 118-color chart is covered by glyphs ALONE (and most of the 200-color kit too). Categories to draw from (spec): geometric shapes filled + outline, directional triangles / arrows (single + double), star / asterisk dingbats, card suits, circled and boxed operators.

   - KEEP the entire current proven-safe base verbatim (it already ships in production): ♣ ♦ ♥ ♠ ▲ ▼ ◆ ● ■ ★ ◀ ▶ ❖ ⬤ △ ▽ ◇ ○ □ ☆ ◁ ▷ ⬡ ⭘ ◎ ⊕ ⊖ ⊗ ⊘ ⊙ ⊚ ⊛ ⊜ ⊞ ⊟ ⊠ ✦ ✧ ⬢ ⬣ ⭓ ⭔ ▰ ▱ † ‡ — then EXPAND it substantially.
   - Recommended expansion (append, then dedupe near-identical pairs; drop any that render as tofu/box during the Task-2 in-browser check): filled/outline geometrics ⬟ ⬥ ⬧ ⬦ ◈ ◊ ◢ ◣ ◤ ◥ ◸ ◹ ◺ ◿ ; card-suit outline variants ♤ ♡ ♢ ♧ ; half / quarter circles ◐ ◑ ◒ ◓ ◔ ◕ ◖ ◗ ◍ ◌ ; partitioned squares ◧ ◨ ◩ ◪ ◫ ▤ ▥ ▦ ▧ ▨ ▩ ; quadrant squares / circles ◰ ◱ ◲ ◳ ◴ ◵ ◶ ◷ ; circled operators ⊝ ⊡ ; star / florette dingbats ✶ ✷ ✵ ✱ ✲ ✻ ✼ ❂ ❉ ❊ ❋ ✹ ; single arrows ← → ↑ ↓ ↖ ↗ ↘ ↙ ; double arrows ⇐ ⇒ ⇑ ⇓ ⇔ ⇕ ; small triangles ▴ ▾ ◂ ▸ ; crosses / bars ✚ ✜ ✠ ✢ ⧈ ⧇ .
   - EXCLUDE any emoji-presentation / color-emoji-prone glyph. Keep only text-default variants. Specifically do NOT include: ☀ ☂ ❤ ⚑ ✈ ☃ ☁ ⚐ ⭐ ⭕ ⬛ ⬜ ❄ ✅ ❌ ➕ ➖ ➗ ✔ ✖ ❇ ✴ ✳ (these force emoji rendering). Prefer the well-supported Geometric Shapes (U+25xx), math-operator (U+22xx), Arrows (U+21xx), and text-default Dingbats (U+27xx) blocks — avoid astral / SMP code points (BMP only).

2) `SAFE_LETTER_SYMBOLS` — unambiguous capitals ONLY, used after glyphs are exhausted. Exactly, in order: A, C, D, E, F, H, J, K, L, M, N, P, R, T, U, V, W, X, Y (19 letters). This EXCLUDES B, G, I, O, Q, S, Z (the look-alike / digit-confusable letters).

3) `CURATED_SYMBOLS = [...GLYPH_SYMBOLS, ...SAFE_LETTER_SYMBOLS]`. No `NUMBER_SYMBOLS`, no digits anywhere.

KEEP UNCHANGED: `generateSymbolAllocation` (frequency-desc sort + alphabetical `localeCompare` tie-break), the `>poolSize` multi-char base+suffix overflow fallback (`base = CURATED_SYMBOLS[index % poolSize]`, `suffix = Math.floor(index / poolSize) >= 1`), `symbolFontPx`, and `getContrastColor`. The overflow combo now only fires when a palette exceeds the whole (much larger) single-symbol pool — an acceptable rare last-ditch uniqueness net that preserves the B4 guarantee.

Update the file's top doc comment to describe the NEW order: most-used colors get distinct shape-glyphs first, unambiguous capitals only as a last resort, no digits.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>symbols.ts exports GLYPH_SYMBOLS (~120-150 unique text-default BMP glyphs, proven base kept), SAFE_LETTER_SYMBOLS (A C D E F H J K L M N P R T U V W X Y), and CURATED_SYMBOLS = glyphs then letters with no digits. LETTER_SYMBOLS/NUMBER_SYMBOLS removed. Allocation + overflow + symbolFontPx logic byte-for-byte behavior preserved. tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite symbols.test.ts invariants + verify full suite and build</name>
  <files>src/engine/__tests__/symbols.test.ts</files>
  <action>
Update the imports at the top of the test file: remove `LETTER_SYMBOLS`, `NUMBER_SYMBOLS`, `WINGDING_SYMBOLS`; add `GLYPH_SYMBOLS`, `SAFE_LETTER_SYMBOLS`. (Grep already confirms these three old identifiers appear ONLY in symbols.ts and this test file — no other files to update.)

REPLACE the two assertions that encode the OLD order with new invariants:
  - DELETE the `is ordered Letters (A-Z) -> Numbers (0-9) -> Wingdings` test.
  - DELETE the `assigns the most-frequent color a plain letter, starting at A` test.
  - The existing `contains at least 80 distinct...` test: keep the all-unique check, but raise the length assertion to `> 118`.

ADD new invariant tests:
  - Pool size grew: `CURATED_SYMBOLS.length` is greater than 118, and `new Set(CURATED_SYMBOLS).size === CURATED_SYMBOLS.length` (all unique).
  - Symbols-first: `CURATED_SYMBOLS[0]` is a NON-alphanumeric glyph — assert `/[A-Za-z0-9]/.test(CURATED_SYMBOLS[0]) === false`.
  - No digits anywhere: `CURATED_SYMBOLS.every(s => !/[0-9]/.test(s))`.
  - Glyph tier is letter-free: `GLYPH_SYMBOLS.every(g => !/[A-Za-z]/.test(g))`.
  - Letters last & filtered: `SAFE_LETTER_SYMBOLS` equals `['A','C','D','E','F','H','J','K','L','M','N','P','R','T','U','V','W','X','Y']`; assert none of `['B','G','I','O','Q','S','Z']` are members; and the letter tier appears strictly AFTER all glyphs — `CURATED_SYMBOLS.slice(0, GLYPH_SYMBOLS.length)` equals `GLYPH_SYMBOLS` and `CURATED_SYMBOLS.slice(GLYPH_SYMBOLS.length)` equals `SAFE_LETTER_SYMBOLS`.

KEEP the frequency-desc allocation test and the alphabetical tie-break test as-is — they already reference `CURATED_SYMBOLS[0..3]` so they auto-track the new first glyphs. KEEP the `symbolFontPx` and `getContrastColor` tests unchanged.

FIX the B4 `>pool` uniqueness test so it regenerates from the NEW (larger) pool size instead of hard-coding 82 / 200:
  - Compute `const poolSize = CURATED_SYMBOLS.length;` and drive the test with a count safely above it: `const codeCount = poolSize + 20;`.
  - Assert all `codeCount` assigned symbols are unique (`new Set(...).size === codeCount`).
  - First `poolSize` colors keep their single curated glyph: `allocation['COLOR_000'] === CURATED_SYMBOLS[0]` and `allocation[COLOR_{poolSize-1}] === CURATED_SYMBOLS[poolSize-1]`.
  - The color at index `poolSize` triggers the overflow branch: its symbol equals `` `${CURATED_SYMBOLS[0]}1` ``, is not equal to `CURATED_SYMBOLS[0]`, and has length > 1. (These references auto-track the new pool.)

Then run the full verification below.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm test && npm run build</automated>
  </verify>
  <done>Rewritten symbols.test.ts imports GLYPH_SYMBOLS/SAFE_LETTER_SYMBOLS; old-order assertions removed; new invariants (length > 118, all-unique, [0] non-alphanumeric, no digits anywhere, glyph tier letter-free, SAFE_LETTER_SYMBOLS excludes B/G/I/O/Q/S/Z and sits strictly after glyphs) all pass; B4 overflow test derives from the new poolSize and still exercises the multi-char branch. tsc clean, full suite green (~244), build succeeds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Purely presentational, client-side glyph selection. No user input parsing, no new external data, no package installs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01y-01 | Information Disclosure | canvas glyph rendering | low | accept | Deterministic in-browser glyph draw; no data leaves client. No change to attack surface vs. shipping symbols.ts. |
</threat_model>

<verification>
Executor MUST confirm all of:
- (a) `npx tsc --noEmit` is clean (no type errors from the rename).
- (b) `npm test` is green — rewritten symbols.test.ts plus the full suite (~244 tests).
- (c) `npm run build` succeeds.
- (d) Assertions hold: `CURATED_SYMBOLS` contains NO digits, has no alphanumerics before the letter tier (`CURATED_SYMBOLS[0]` is a shape glyph; `SAFE_LETTER_SYMBOLS` sits strictly after all glyphs), and `CURATED_SYMBOLS.length > 118`. (Enforced by the new tests in Task 2.)
- (e) IN-BROWSER VISUAL CHECK (required, executor must perform): run `npm run dev`, load an image with 100+ colors, switch to the Grid + Symbols view. Confirm: symbols are distinct shape-glyphs (no confusing letters/digits), NO digit and NO two-char "glyph+digit" combo appears in the common case, and the legend symbols match the grid symbols exactly. Drop and replace any glyph that renders as a tofu/□ box or as a color emoji. (The orchestrator will also verify in-browser after execution.)
</verification>

<success_criteria>
- symbols.ts rebuilt: GLYPH_SYMBOLS (large, ordered, deduped, text-default, ~120-150) + SAFE_LETTER_SYMBOLS (19 unambiguous capitals) + CURATED_SYMBOLS = glyphs-then-letters, zero digits.
- generateSymbolAllocation / overflow fallback / symbolFontPx behavior preserved (B4 uniqueness net intact).
- All old-order test assertions replaced with the new symbols-first invariants; imports updated to the new export names.
- tsc clean, full suite green, build succeeds, and the in-browser Grid + Symbols view reads cleanly with distinct glyphs and matching legend.
</success_criteria>

<output>
Create `.planning/quick/260713-01y-redesign-grid-symbols-allocation-distinc/260713-01y-SUMMARY.md` when done.
</output>
