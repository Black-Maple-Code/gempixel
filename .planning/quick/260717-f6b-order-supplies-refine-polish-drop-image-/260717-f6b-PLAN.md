---
id: 260717-f6b
title: "Order/Supplies/Refine polish batch (8 fixes incl. canvas pricing)"
status: complete
mode: quick
branch: claude/gsd-ui-phase-20-70b245
---

# Quick Task 260717-f6b — Order/Supplies/Refine polish batch

Eight user-reported items across three screens + the pricing engine. Orchestrator
acts as executor with live browser verification.

1. **Remove Image wrap** finish option (Order) — `OrderScreen.tsx`.
2. **Order flow + Title Case** — cap section width (max-w-[1180px] centered) so it
   doesn't sprawl on wide monitors; Title-Case the download/cart CTAs — `OrderScreen.tsx`.
3. **Back = button** — make the wizard Back a bordered button, not bare text — `App.tsx`.
4. **Supplies sticky** — the order-summary panel must stay pinned while the drill
   table scrolls. Root cause: the earlier Refine `h-full` bounding broke natural
   scroll; make height step-aware (h-full only on Refine) — `App.tsx`.
5. **Realistic canvas price** — small canvases priced ~$3.73 (way under market);
   add a per-vendor `minPrice` floor + raise the curve — `engine/checkout.ts`.
6. **Advanced overflow** — Advanced content was hard to reach; resolved by the rail's
   internal scroll + moving Drill shape out — `RefineScreen.tsx`.
7. **Promote Drill shape** out of Advanced into main controls — `RefineScreen.tsx`.
8. **"Drills" label** over the count on size cards — `SizeCard.tsx`.

## Verification
- `npx tsc --noEmit`; full `npx vitest run` (392 tests).
- Live browser on Refine (Drills label, drill shape, Advanced), Supplies (sticky),
  Order (wrap gone, Title Case, Back button, width cap at 1500px).
