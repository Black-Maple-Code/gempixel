---
task: 260717-02w
type: execute
mode: quick
wave: 1
depends_on: []
autonomous: true
files_modified:
  - src/App.tsx
  - src/__tests__/integration.test.tsx
requirements:
  - size-selection-crops-image#2   # non-blocking note #2 (un-highlighted default card)

must_haves:
  truths:
    - "After any image upload, exactly one RefineScreen size card (Medium) is highlighted immediately — no un-highlighted 'no size selected' state."
    - "The default post-upload grid is crop-free for every aspect ratio (unchanged intent)."
    - "An exact-3:2 upload still defaults to 80×53 — byte-identical to pre-change behavior."
  artifacts:
    - "src/App.tsx: loadImageFile derives its default dims via aspectAwareGrid on the Medium tier."
    - "src/__tests__/integration.test.tsx: on-load-default highlight + byte-identical guard test."
  key_links:
    - "loadImageFile default dims === refineSizePresets Medium dims (both call aspectAwareGrid(80,53,imgW,imgH)) → RefineScreen selected={cols===preset.cols && rows===preset.rows} at RefineScreen.tsx:118 lands on Medium."
---

<objective>
Reconcile the two grid-anchoring strategies in `src/App.tsx` so the default post-upload
grid corresponds to a RefineScreen preset card (Medium), fixing the un-highlighted-card UX
inconsistency flagged in `.planning/debug/resolved/size-selection-crops-image.md` non-blocking
note #2.

Two anchoring strategies currently coexist:
1. **Image-load auto-adjust** (`loadImageFile`, `src/App.tsx:989–1006`): anchors the WIDTH
   budget — keeps `cols` fixed and grows `rows` (`newRows = round(cols / ar)`), so a 2:3
   portrait defaults to 80×120.
2. **AR-aware presets** (`aspectAwareGrid` @ `src/App.tsx:114`, `REFINE_SIZE_PRESETS` @
   `:90`, `refineSizePresets` @ `:1333`, `onSelectSize` @ `:1364`): anchors the LONG-AXIS
   budget — Medium for that same portrait renders as 53×80.

Both are crop-free, but the initial post-load dims (80×120) match NO preset card's dims, so
RefineScreen's selected-highlight (`selected={cols === preset.cols && rows === preset.rows}`,
`RefineScreen.tsx:118`) lands on no card until the user taps one — reading as "no size
selected."

Purpose: Make the on-load default run the SAME `aspectAwareGrid` long-axis mapping for the
default tier (Medium) so the default post-upload grid exactly equals the Medium preset card's
dims — the correct card highlights immediately, still with no crop.

Output: A one-line-of-logic reconciliation in `loadImageFile` (reusing the existing
`aspectAwareGrid`/`REFINE_SIZE_PRESETS` helpers — no parallel sizing path), plus a guard test.
</objective>

<context>
@.planning/STATE.md
@.planning/debug/resolved/size-selection-crops-image.md

# Source under change (already in-context for the executor; do not re-read wholesale):
# - src/App.tsx: REFINE_SIZE_PRESETS (90–95), aspectAwareGrid (114–131),
#   loadImageFile (979–1037; on-load dims block 989–1006, setMatchInputs 1019),
#   refineSizePresets (1333–1351), onSelectSize (1364–1375).
#   State inits: cols=80 (195), rows=53 (196), unit='grid' (227 — only ever set to 'grid'
#   at 398/439), widthInput='80' (228), heightInput='53' (229).
# - src/features/screens/RefineScreen.tsx:118 — selected highlight comparison.
# - src/ui/SizeCard.tsx:55 — renders aria-pressed={selected}.
# - src/features/screens/UploadScreen.tsx — file input id="upload-file-input", type="file".
# - Test harness reference: src/__tests__/integration.test.tsx (upload-simulation pattern
#   with FileReader/Image/getContext/createImageBitmap stubs at 288–373; Custom-size toggle
#   + #refine-width/#refine-height read at 351–370).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Anchor the on-load default grid to the Medium tier via aspectAwareGrid</name>
  <files>src/App.tsx</files>
  <action>
Reconcile `loadImageFile`'s on-load dimension logic with the AR-aware presets by reusing the
existing `aspectAwareGrid` helper on the default (Medium) tier.

Step 1 — add a default-tier constant immediately after `REFINE_SIZE_PRESETS`
(`src/App.tsx:95`). Export it (mirrors `aspectAwareGrid`/`REFINE_SIZE_PRESETS` visibility) and
derive it from the existing array (do NOT hardcode a duplicate 80/53 literal):

  export const DEFAULT_REFINE_PRESET =
    REFINE_SIZE_PRESETS.find((p) => p.tag === 'BEST') ?? REFINE_SIZE_PRESETS[1];

(`tag: 'BEST'` is Medium; the `?? REFINE_SIZE_PRESETS[1]` fallback keeps the type non-optional
under `strict`. Add a short comment: this is the default post-upload size tier; anchoring the
on-load default to it via aspectAwareGrid makes the initial grid equal the Medium card so
RefineScreen highlights it immediately.)

Step 2 — replace the entire on-load width-anchor block in `loadImageFile` (`src/App.tsx`,
the `// Adjust dimensions to match aspect ratio` comment through `setRows(newRows);`,
lines 989–1006). The three `unit === 'cm' | 'inch'` branches are dead code (`unit` is only
ever `'grid'` — `setUnit` is called only at 398/439, both with `'grid'`), so drop them.
Compute the default dims through `aspectAwareGrid` on the Medium tier and set BOTH axes plus
BOTH input strings (matching how `onSelectSize` at :1364 sets cols/rows/widthInput/heightInput
together):

  const { cols: defaultCols, rows: defaultRows } = aspectAwareGrid(
    DEFAULT_REFINE_PRESET.cols,
    DEFAULT_REFINE_PRESET.rows,
    img.naturalWidth,
    img.naturalHeight,
  );
  setCols(defaultCols);
  setRows(defaultRows);
  setWidthInput(String(defaultCols));
  setHeightInput(String(defaultRows));

Add a comment explaining WHY: default the post-upload grid to the recommended Medium size TIER
mapped through aspectAwareGrid so the grid AR follows the image (no crop) AND the dims equal
the Medium RefineScreen card exactly — so its selected-highlight lands on load (reconciles the
two anchoring strategies; fixes size-selection-crops-image.md note #2). An exact-3:2 image
reproduces 80×53 byte-for-byte because aspectAwareGrid(80,53,·) returns the preset dims at
ar=1.5.

Step 3 — update the match-inputs commit (`src/App.tsx:1019`) to fire the worker with the new
default dims instead of the old `cols`/`newRows`:

  setMatchInputs({ image: img, cols: defaultCols, rows: defaultRows });

Leave `setImage(img)`, `setExcludedColors(new Set())`, `setSelectedPreset('custom')`, and the
recent-images block unchanged. Do NOT touch `aspectAwareGrid`, `REFINE_SIZE_PRESETS`,
`refineSizePresets`, `onSelectSize`, the custom-size handlers, RefineScreen, or the ingest
engine — the reconciliation is upstream dims only.
  </action>
  <verify>
    <automated>cd "C:/Users/rickf/OneDrive/Documents/Claude Coding/gempixel/.claude/worktrees/gsd-plan-phase-20-b72521" && npx tsc --noEmit</automated>
  </verify>
  <done>
`npx tsc --noEmit` is clean. `loadImageFile` computes default dims via
`aspectAwareGrid(DEFAULT_REFINE_PRESET.cols, DEFAULT_REFINE_PRESET.rows, img.naturalWidth,
img.naturalHeight)`, sets cols/rows/widthInput/heightInput to those dims, and commits
`setMatchInputs({ image, cols: defaultCols, rows: defaultRows })`. No fork of the sizing path;
no `unit` cm/inch branches remain in `loadImageFile`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add the on-load-default highlight + byte-identical guard test and run verification</name>
  <files>src/__tests__/integration.test.tsx</files>
  <action>
Add a focused integration test to `src/__tests__/integration.test.tsx` (the jsdom suite that
already renders `<App/>`, mocks CanvasViewer + MatcherClient, and stubs createImageBitmap in
its `beforeEach`). Model the upload simulation EXACTLY on the existing test "automatically
adjusts height to stay in ratio…" (lines 288–373): stub `FileReader` and `Image` via
`vi.stubGlobal` (Image with `naturalWidth`/`naturalHeight` and a `set src` that calls
`onload` on a macro-task), stub `HTMLCanvasElement.prototype.getContext` to return the mock 2D
context, find `container.querySelector('input[type="file"]')` (UploadScreen's
`#upload-file-input`), set `files`, and `dispatchEvent(new Event('change', { bubbles: true }))`,
then `await vi.waitFor(...)` for the load to settle. Restore `getContext` at the end
(the suite intentionally does not `unstubAllGlobals`).

Helper to read live cols/rows after upload: within `[data-step-panel="2"]`, click the
`'Custom size'` button (find by trimmed textContent, per the analog test) and read
`#refine-width`/`#refine-height` `.value`; and locate the Medium size card via
`Array.from(step2.querySelectorAll('button[aria-pressed]'))` whose textContent includes
`'Medium'`.

Add two `it(...)` cases inside the existing describe:

1. **"a 2:3 portrait upload defaults to the Medium tier (53×80) and highlights the Medium card
   on load"** — upload an Image with naturalWidth=1000, naturalHeight=1500 (getImageData mock
   sized 1000×1500). Assert:
     - `#refine-width` value === `'53'` and `#refine-height` value === `'80'` — i.e. live
       cols/rows === `aspectAwareGrid(80, 53, 1000, 1500)` (long-axis budget 80 on the vertical
       axis; short axis `Math.round(80 * (1000/1500))` = 53).
     - the Medium card's `aria-pressed` === `'true'`, and the Small and Large cards'
       `aria-pressed` === `'false'` (exactly one card highlighted).
   (This case FAILS against the pre-Task-1 width-anchor path, which defaults this portrait to
   80×120 → no card highlighted — it is the reconciliation guard.)

2. **"an exact-3:2 upload keeps the 80×53 default (byte-identical) and highlights Medium"** —
   upload an Image with naturalWidth=3000, naturalHeight=2000. Assert `#refine-width` === `'80'`
   and `#refine-height` === `'53'` (unchanged from prior behavior), and the Medium card's
   `aria-pressed` === `'true'`. This pins the byte-identical guarantee for already-3:2 inputs.

Keep each case self-contained (its own FileReader/Image/getContext stubs, like the analog),
and restore `getContext` before returning.
  </action>
  <verify>
    <automated>cd "C:/Users/rickf/OneDrive/Documents/Claude Coding/gempixel/.claude/worktrees/gsd-plan-phase-20-b72521" && npx tsc --noEmit && npx vitest run src/__tests__/App.test.tsx src/__tests__/integration.test.tsx src/features/screens/__tests__/RefineScreen.test.tsx</automated>
  </verify>
  <done>
`npx tsc --noEmit` clean, and all three suites pass: `src/__tests__/App.test.tsx` (existing
`aspectAwareGrid` pure-math guards still green), `src/__tests__/integration.test.tsx` (the two
new cases: portrait → 53×80 + Medium highlighted; exact-3:2 → 80×53 + Medium highlighted), and
`src/features/screens/__tests__/RefineScreen.test.tsx` (aria-pressed highlight test unchanged).
  </done>
</task>

</tasks>

<verification>
Run from WORKTREE_ROOT
(`C:/Users/rickf/OneDrive/Documents/Claude Coding/gempixel/.claude/worktrees/gsd-plan-phase-20-b72521`):

1. `npx tsc --noEmit` → no errors.
2. `npx vitest run src/__tests__/App.test.tsx src/__tests__/integration.test.tsx src/features/screens/__tests__/RefineScreen.test.tsx`
   → all pass, including the two new integration cases.
3. (Optional full regression) `npm test` → suite green (no regressions elsewhere).

Manual sanity (not required for the gate): upload a portrait photo → the Medium card is
outlined/tinted immediately with no tap; the preview shows the whole image (no crop).
</verification>

<success_criteria>
- The on-load default grid is produced by `aspectAwareGrid(DEFAULT_REFINE_PRESET.cols,
  DEFAULT_REFINE_PRESET.rows, img.naturalWidth, img.naturalHeight)` — a single reused sizing
  path, no fork of `aspectAwareGrid`/`REFINE_SIZE_PRESETS`.
- Immediately after any upload, exactly one RefineScreen size card (Medium) is highlighted
  (`aria-pressed="true"`); the "no size selected" un-highlighted state is gone.
- The default remains crop-free for all aspect ratios (behavior preserved).
- Exact-3:2 uploads still default to 80×53 (byte-identical guard test passes).
- `npx tsc --noEmit` clean; App, integration, and RefineScreen Vitest suites green.
</success_criteria>

<output>
This is a quick task — no SUMMARY file is required. On completion, `/gsd-quick` records the
task in `.planning/STATE.md` (Quick Tasks Completed) and commits the change atomically.
</output>