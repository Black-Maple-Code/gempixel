---
task: 260717-02w
type: execute
mode: quick
status: complete
subsystem: refine-sizing
tags: [refine, sizing, aspect-ratio, ux]
key-files:
  modified:
    - src/App.tsx
    - src/__tests__/integration.test.tsx
commits:
  - f9fab53  fix(20): anchor on-load default grid to Medium tier via aspectAwareGrid
  - b2f7a1f  test(20): guard on-load default highlight and byte-identical 3:2 sizing
requirements:
  - size-selection-crops-image#2
completed: 2026-07-17
---

# Quick Task 260717-02w: Reconcile default post-upload grid to a preset card Summary

Reconciled `loadImageFile`'s on-load grid sizing with the AR-aware preset path so the default
post-upload dims equal the Medium (BEST) RefineScreen card exactly — its selected-highlight now
lands immediately on upload with no crop, eliminating the "no size selected" state (fixes
size-selection-crops-image.md note #2).

## What changed

**Task 1 — `src/App.tsx` (commit f9fab53):**
- Added exported `DEFAULT_REFINE_PRESET`, derived from `REFINE_SIZE_PRESETS` via
  `find(p => p.tag === 'BEST') ?? REFINE_SIZE_PRESETS[1]` (Medium; no duplicated literal,
  type stays non-optional under strict).
- Replaced the on-load width-anchor block in `loadImageFile` with a single
  `aspectAwareGrid(DEFAULT_REFINE_PRESET.cols, DEFAULT_REFINE_PRESET.rows, img.naturalWidth,
  img.naturalHeight)` call, setting `cols`, `rows`, `widthInput`, and `heightInput` together
  (mirrors `onSelectSize`). Dropped the dead `unit === 'cm' | 'inch'` branches (`unit` is only
  ever `'grid'`).
- Updated `setMatchInputs` to fire the worker with the new `defaultCols`/`defaultRows`.

**Task 2 — `src/__tests__/integration.test.tsx` (commit b2f7a1f):**
- "a 2:3 portrait upload defaults to the Medium tier (53×80) and highlights the Medium card on
  load" — asserts `#refine-width`='53', `#refine-height`='80', and exactly one card highlighted
  (Medium `aria-pressed`='true'; Small/Large 'false'). This case fails against the pre-Task-1
  80×120 width-anchor path — it is the reconciliation guard.
- "an exact-3:2 upload keeps the 80×53 default (byte-identical) and highlights Medium" — asserts
  `#refine-width`='80', `#refine-height`='53', Medium `aria-pressed`='true'.

## Verification

- `npx tsc --noEmit` → clean.
- `npx vitest run src/__tests__/App.test.tsx src/__tests__/integration.test.tsx src/features/screens/__tests__/RefineScreen.test.tsx`
  → 3 files passed, 48 tests passed (includes the 2 new integration cases). The stderr lines
  during the run are from pre-existing intentional error-path tests (ERR-01 banner, download
  failure), not failures.

## Deviations from Plan

None — plan executed exactly as written.

## Self-Check: PASSED
- src/App.tsx modified (DEFAULT_REFINE_PRESET + aspectAwareGrid on-load path) — FOUND
- src/__tests__/integration.test.tsx modified (2 new cases) — FOUND
- Commit f9fab53 — FOUND
- Commit b2f7a1f — FOUND
