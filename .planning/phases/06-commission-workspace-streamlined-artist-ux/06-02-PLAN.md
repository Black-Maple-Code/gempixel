---
phase: 06-commission-workspace-streamlined-artist-ux
plan: "02"
type: execute
wave: 2
depends_on:
  - "06-01-PLAN.md"
files_modified:
  - src/App.tsx
  - src/__tests__/App.test.tsx
autonomous: true
requirements:
  - ARTIST-02
must_haves:
  truths:
    - "D-03: Sidebar panel is consolidated into a clean 4-step wizard interface: Upload, Size/Style, Legend/Palette, and Quoting/Checkout."
    - "Wizard displays active step progress markers (1, 2, 3, 4) with visual indicators and connector lines."
    - "Wizard provides back and next navigation buttons at the bottom footer of the Left Sidebar."
    - "Wizard allows progression if either a raw image is loaded OR if an active/saved project has been loaded (disabled={!image && !currentProjectId})."
  artifacts:
    - path: "src/App.tsx"
      provides: "wizardStep state controls, progress indicator header layout, conditional panel step render, and footer navigation triggers"
    - path: "src/__tests__/App.test.tsx"
      provides: "Integration tests verifying wizard panel switching, progress indicators, and upload validation locks"
  key_links: []
---

<objective>
Redesign sidebar controls into a simplified 4-step wizard workflow, replacing the tab-based navigation with a linear layout progression that guides the artist step-by-step.
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-commission-workspace-streamlined-artist-ux/06-CONTEXT.md
@.planning/phases/06-commission-workspace-streamlined-artist-ux/06-RESEARCH.md
@.planning/phases/06-commission-workspace-streamlined-artist-ux/06-PATTERNS.md
@.planning/phases/06-commission-workspace-streamlined-artist-ux/06-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement wizard step tracking state and progress indicators header</name>
  <files>src/App.tsx</files>
  <read_first>src/App.tsx</read_first>
  <action>Modify src/App.tsx to add wizardStep Preact state (number, defaulting to 1) replacing the old controlTab state. Render the visual progress track header at the top of the Left Sidebar. The header should display circular markers labeled '1', '2', '3', '4' corresponding to Steps 1-4. Render connector lines between markers, and apply styled active/completed states using glassmorphism borders and indigo text glows.</action>
  <verify>Check that the app mounts and tsc compiles without errors. <automated>npx tsc --noEmit</automated></verify>
  <acceptance_criteria>
    - wizardStep state variable initialized
    - Wizard progress indicator track rendered at top of Left Sidebar
    - Active/completed step styling displays correctly
  </acceptance_criteria>
  <done>Wizard step state and progress header implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Reorganize sidebar panels into conditional wizard steps</name>
  <files>src/App.tsx</files>
  <read_first>src/App.tsx</read_first>
  <action>Reorganize the controls in Left Sidebar in src/App.tsx to display settings inputs conditionally based on active wizardStep (implementing Decision D-03): (Step 1: Upload) drag-drop file zone, fit/crop option, and recent uploads gallery; (Step 2: Size) grid size presets, cols/rows inputs, physical conversion results, and square/round toggles; (Step 3: Palette) base kit filter, DMC legend list table, color highlight hooks, and exclusions count; (Step 4: Quote) pricing database config table, safety margin select, price markup estimation card, canvas redirects, and DiamondDrillsUSA checkout cart permalinks.</action>
  <verify>Verify tsc compiles with zero errors. <automated>npx tsc --noEmit</automated></verify>
  <acceptance_criteria>
    - Old tab selectors are removed from Left Sidebar
    - Control sections are mapped to their respective wizard panels
    - UI correctly isolates visible inputs to the current active step
  </acceptance_criteria>
  <done>Sidebar controls consolidated into conditional wizard panels.</done>
</task>

<task type="auto">
  <name>Task 3: Integrate footer navigation triggers and validation locks</name>
  <files>src/App.tsx</files>
  <read_first>src/App.tsx</read_first>
  <action>Add a navigation footer bar at the bottom of the Left Sidebar panel in src/App.tsx. Render a Back button (visible when wizardStep > 1) and a Next button (visible when wizardStep < 4). Hook up next/back click handlers. Crucially, the Next button on Step 1 must allow progression if either a raw image is loaded OR if an active/saved project has been loaded (e.g. check !image && !currentProjectId to disable), preventing loaded commissions from being blocked.</action>
  <verify>Check compilation safety. <automated>npx tsc --noEmit</automated></verify>
  <acceptance_criteria>
    - Back/Next footer buttons render correctly in sidebar bottom
    - Decrement and increment actions transition panels successfully
    - Step 1 Next button is unlocked if active project is loaded even when image is null
  </acceptance_criteria>
  <done>Wizard footer navigation controls and validation locks integrated.</done>
</task>

<task type="auto">
  <name>Task 4: Add wizard navigation and transition unit tests</name>
  <files>src/__tests__/App.test.tsx</files>
  <read_first>src/App.tsx</read_first>
  <action>Modify src/__tests__/App.test.tsx to add unit tests asserting: (1) step progression using Back/Next footer buttons; (2) Next button validation lock when both image and project ID are missing; (3) progression is allowed for loaded projects with null image; (4) display isolation of active step options.</action>
  <verify>Run the Vitest suite verifying the wizard tests. <automated>npx vitest run src/__tests__/App.test.tsx</automated></verify>
  <acceptance_criteria>
    - Unit tests verify wizard transition logic
    - Step 1 validation lock is tested under empty, loaded image, and loaded project states
    - All tests run and pass successfully
  </acceptance_criteria>
  <done>Wizard routing and transition tests implemented and passed.</done>
</task>

</tasks>
