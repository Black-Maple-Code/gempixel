---
phase: 06-commission-workspace-streamlined-artist-ux
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/App.tsx
  - src/__tests__/App.test.tsx
autonomous: true
requirements:
  - ARTIST-01
must_haves:
  truths:
    - "D-01: User can save project schema capturing active dimensions, styles, base kit, safety margin, calculator cost entries, exclusions, and custom supplier URLs."
    - "D-02: User can persist projects locally using a compressed DMC grid index array (~4KB) and downsized thumbnail data URL (~10KB) without crashing localStorage 5MB quota."
    - "D-04: User can switch active layouts and delete projects inside a collapsible 'My Commissions' portfolio drawer in the Left Sidebar."
    - "CanvasViewer initializes and canvas mounts successfully if either raw 'image' OR loaded project 'matchResult' is present, allowing saved layouts (which lack raw photos) to draw."
  artifacts:
    - path: "src/App.tsx"
      provides: "Split-key localStorage project registry database hooks, Commissions drawer switcher, and Save project dialog layout"
    - path: "src/__tests__/App.test.tsx"
      provides: "Unit tests verifying project saving, loading, list rendering, and local storage deletion"
  key_links: []
---

<objective>
Build local storage database and project switcher for multiple commission projects, allowing artists to save, manage, switch, and delete layouts locally without exceeding the localStorage quota limits.
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-commission-workspace-streamlined-artist-ux/06-CONTEXT.md
@.planning/phases/06-commission-workspace-streamlined-artist-ux/06-RESEARCH.md
@.planning/phases/06-commission-workspace-streamlined-artist-ux/06-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement workspace serialization database models and local storage helpers</name>
  <files>src/App.tsx</files>
  <read_first>.planning/phases/06-commission-workspace-streamlined-artist-ux/06-RESEARCH.md</read_first>
  <action>Define ProjectSummary and ProjectData interfaces in App.tsx (implementing Decisions D-01 and D-02). Add helper functions to save, load, and delete projects. The saving logic must generate a UUID, compile active matching counts, dimensions, styles, costs, and exclusions into a flat index array (1D number array), and capture a small canvas thumbnail data URL. Store summary objects in 'gempixel_workspace_registry' and full details separately under 'gempixel_project_${id}'. Crucially, decouple canvas mounting and CanvasViewer initialization in App.tsx from raw 'image' state, enabling them to mount and initialize if either 'image' OR 'matchResult' is present, ensuring that loaded projects (which only restore grid data and lack raw image data) can render their canvas preview.</action>
  <verify>Run tsc --noEmit to verify model definitions. <automated>npx tsc --noEmit</automated></verify>
  <acceptance_criteria>
    - ProjectSummary and ProjectData types defined
    - Canvas viewer initializes and canvas element mounts when image is null but project matchResult is present
    - Split-key localStorage handlers save and delete projects cleanly
    - Recalculation handles restored grid index data safely
  </acceptance_criteria>
  <done>Split-key local storage database functions implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate 'My Commissions' switcher drawer in sidebar</name>
  <files>src/App.tsx</files>
  <read_first>src/App.tsx</read_first>
  <action>Modify src/App.tsx to add the collapsible 'My Commissions' details drawer at the very top of the Left Sidebar (implementing Decision D-04). Render the list of stored projects loaded from the summary registry. Each project row should display the thumbnail, name, modification timestamp, and a Hover '×' button to trigger deletion. Hook up click handlers to load the selected project configuration instantly and redraw the active viewport.</action>
  <verify>Check that the app mounts and tsc/vite compiles without errors. <automated>npx tsc --noEmit</automated></verify>
  <acceptance_criteria>
    - Collapsible Commissions drawer is visible in sidebar
    - Stored projects render with details and thumbnails
    - Loading a project triggers viewport canvas redraw
  </acceptance_criteria>
  <done>Commissions switcher drawer integrated into Left Sidebar.</done>
</task>

<task type="auto">
  <name>Task 3: Render Save Commission modal input dialog</name>
  <files>src/App.tsx</files>
  <read_first>src/App.tsx</read_first>
  <action>Add a 'Save Current Commission' button inside the switcher drawer. Add a backdrop overlay modal dialog in src/App.tsx that displays when the save action is triggered. Prompt the user for a client name (defaulting to e.g. 'Commission Layout 1'). Clicking 'Save' calls the local storage helper to serialize and persist the current layout state, then updates the active project summary list.</action>
  <verify>Run build validation checking typescript compiler status. <automated>npx tsc --noEmit</automated></verify>
  <acceptance_criteria>
    - Clicking save CTA opens name input overlay dialog
    - Input form updates name state and saves successfully
    - Saving adds the new project summary entry to Left Sidebar switcher list
  </acceptance_criteria>
  <done>Save commission modal overlay dialog created.</done>
</task>

<task type="auto">
  <name>Task 4: Add unit tests for project storage and switching</name>
  <files>src/__tests__/App.test.tsx</files>
  <read_first>src/App.tsx</read_first>
  <action>Modify src/__tests__/App.test.tsx to add unit tests asserting: (1) project summary addition to registry; (2) complete state recovery upon loading a project ID (verifying canvas mounts successfully without raw image); (3) removal of data keys and registry entries on project deletion; (4) workspace reset to default config on reset/new action.</action>
  <verify>Run the new tests via vitest. <automated>npx vitest run src/__tests__/App.test.tsx</automated></verify>
  <acceptance_criteria>
    - Unit tests verify split-key localStorage persistence
    - Switching and loading project configurations are verified
    - All tests run and pass successfully
  </acceptance_criteria>
  <done>Storage and switching unit tests implemented and passed.</done>
</task>

</tasks>
