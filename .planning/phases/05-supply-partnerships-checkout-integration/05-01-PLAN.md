---
phase: 05-supply-partnerships-checkout-integration
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/engine/checkout.ts
  - src/App.tsx
  - src/engine/__tests__/checkout.test.ts
autonomous: true
requirements:
  - PARTNER-01
must_haves:
  truths:
    - "Canvas partner redirect URL compiler replaces width, height, shape, and size parameter templates successfully."
    - "UI provides an 'Order Custom Sized Canvas' button that opens the generated URL in a new tab."
    - "Configured base URL template defaults to a working supplier URL and is customizable by the user via a settings menu."
  artifacts:
    - path: "src/engine/checkout.ts"
      provides: "compileCanvasPartnerUrl function replacing template bracket tokens with width, height, shape, and size"
    - path: "src/App.tsx"
      provides: "Order Canvas button in Quote tab, template inputs in Settings dropdown, and state persistence to localStorage"
    - path: "src/engine/__tests__/checkout.test.ts"
      provides: "Unit tests verifying compileCanvasPartnerUrl under various template formats and parameters"
  key_links:
    - from: "src/App.tsx"
      to: "src/engine/checkout.ts"
      via: "importing compileCanvasPartnerUrl"
      pattern: "import .*compileCanvasPartnerUrl.* from '\\./engine/checkout'"
    - from: "src/engine/__tests__/checkout.test.ts"
      to: "src/engine/checkout.ts"
      via: "importing compileCanvasPartnerUrl"
      pattern: "import .*compileCanvasPartnerUrl.* from '../checkout'"
---

<objective>
Build custom canvas partner redirect link integrations passing sizing parameters, allowing users to redirect to canvas suppliers with canvas dimensions automatically pre-filled.
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-supply-partnerships-checkout-integration/05-CONTEXT.md
@.planning/phases/05-supply-partnerships-checkout-integration/05-RESEARCH.md
@.planning/phases/05-supply-partnerships-checkout-integration/05-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement canvas partner redirect compiler utility</name>
  <files>src/engine/checkout.ts</files>
  <read_first>.planning/phases/05-supply-partnerships-checkout-integration/05-RESEARCH.md</read_first>
  <action>Create the file src/engine/checkout.ts (following the service structure of closest analog src/engine/color.ts). Implement compileCanvasPartnerUrl function which accepts CanvasRedirectOptions containing baseUrlTemplate, widthCm, heightCm, and shape. Replace {width}, {height}, {shape}, and {size} (which maps to e.g. "{width}x{height}") in the template, properly encoding query parameters via encodeURIComponent. Add logic to validate that the compiled output is a valid URL using the native browser URL API, logging error if validation fails.</action>
  <verify>Run tsc --noEmit to check compilation correctness. <automated>npx tsc --noEmit</automated></verify>
  <acceptance_criteria>
    - src/engine/checkout.ts exists and compiles successfully
    - compileCanvasPartnerUrl replaces template strings correctly
    - URL parsing validation handles invalid URLs safely
  </acceptance_criteria>
  <done>Canvas partner redirect utility implemented in src/engine/checkout.ts.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate Canvas Redirect UI controls and button</name>
  <files>src/App.tsx</files>
  <read_first>src/App.tsx</read_first>
  <action>Modify src/App.tsx to add the canvas redirect UI. Bind canvasTemplate state hook (persisted to localStorage as "gempixel_canvas_template") defaulting to: "https://www.heartfuldiamonds.com/products/custom-diamond-painting-kit?width={width}&height={height}&shape={shape}". Inside the Quote tab layout, under "Partnerships & Ordering", add the "Order Custom Sized Canvas" button. When clicked, compile the active canvas size (computed width/height in centimeters or grid units, converted properly if necessary) and shape (square vs. round), and open the compiled URL in a new browser tab.</action>
  <verify>Check that the app mounts and tsc/vite compiles without errors. <automated>npx tsc --noEmit</automated></verify>
  <acceptance_criteria>
    - App component contains persistent canvasTemplate setting
    - "Order Custom Sized Canvas" button is visible under Quote tab
    - Clicking the button redirects to the compiled partner page in a new window/tab
  </acceptance_criteria>
  <done>Canvas Redirect UI settings and CTA button integrated.</done>
</task>

<task type="auto">
  <name>Task 3: Write tests for canvas partner URL compiler</name>
  <files>src/engine/__tests__/checkout.test.ts</files>
  <read_first>src/engine/checkout.ts</read_first>
  <action>Create src/engine/__tests__/checkout.test.ts (following closest test analog src/engine/__tests__/color.test.ts). Write unit tests targeting compileCanvasPartnerUrl verifying: (1) replacement of {width}, {height}, {shape}, and {size} tokens in the template URL; (2) correctness of percent-encoding of substituted values; (3) graceful handling of malformed template formats.</action>
  <verify>Run the new tests via vitest. <automated>npx vitest run src/engine/__tests__/checkout.test.ts</automated></verify>
  <acceptance_criteria>
    - src/engine/__tests__/checkout.test.ts is created
    - Tests assert the correct replacement of all template variables
    - All tests run and pass successfully
  </acceptance_criteria>
  <done>Canvas URL compiler unit tests created and passed.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- Run the test suite: npx vitest run src/engine/__tests__/checkout.test.ts
- Verify that TypeScript compilation succeeds without errors: npx tsc --noEmit
</verification>
