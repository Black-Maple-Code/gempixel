---
phase: 05-supply-partnerships-checkout-integration
plan: "02"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - src/engine/variants.ts
  - src/engine/checkout.ts
  - src/App.tsx
  - src/engine/__tests__/checkout.test.ts
autonomous: true
requirements:
  - PARTNER-02
must_haves:
  truths:
    - "Dye lot optimizer groups drill counts strictly according to package thresholds: 200 bags only when <= 800, bulk bags only when > 800."
    - "Shopify Add-to-Cart permalink includes all variant IDs and quantities concatenated correctly with a return_to parameter."
    - "Affiliate referral parameters are appended using URL query parameters and Shopify cart attributes."
    - "Warning is shown to the user when the compiled link length exceeds 2000 characters."
    - "Fallback links to standard product pages are returned for missing static variant mappings."
  artifacts:
    - path: "src/engine/variants.ts"
      provides: "DRILL_VARIANTS static lookup mapping containing Shopify Variant database IDs for 200, 500, 1000, 2000 bags"
    - path: "src/engine/checkout.ts"
      provides: "optimizeBags and compileShopifyCartLink functions matching DMC/shapes to Shopify checkout URLs"
    - path: "src/App.tsx"
      provides: "Order Drills button, affiliate tag inputs, error fallbacks for unmapped items, and localStorage state persistence"
    - path: "src/engine/__tests__/checkout.test.ts"
      provides: "Unit tests verifying optimizeBags rules, compileShopifyCartLink formatting, affiliate query params, and unmapped fallback handles"
  key_links:
    - from: "src/engine/checkout.ts"
      to: "src/engine/variants.ts"
      via: "importing DRILL_VARIANTS"
      pattern: "import .*DRILL_VARIANTS.* from '\\./variants'"
    - from: "src/App.tsx"
      to: "src/engine/checkout.ts"
      via: "importing compileShopifyCartLink"
      pattern: "import .*compileShopifyCartLink.* from '\\./engine/checkout'"
    - from: "src/engine/__tests__/checkout.test.ts"
      to: "src/engine/checkout.ts"
      via: "importing optimizeBags and compileShopifyCartLink"
      pattern: "import .*from '../checkout'"
---

<objective>
Implement Diamond Drills USA shopping cart link compiler with affiliate referral tracking, including static variant lookup tables, package optimization rules to prevent mixing dye lots, and UI controls for affiliate integration.
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-supply-partnerships-checkout-integration/05-CONTEXT.md
@.planning/phases/05-supply-partnerships-checkout-integration/05-RESEARCH.md
@.planning/phases/05-supply-partnerships-checkout-integration/05-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Compile Shopify Variant lookup table dictionary</name>
  <files>src/engine/variants.ts</files>
  <read_first>.planning/phases/05-supply-partnerships-checkout-integration/05-RESEARCH.md</read_first>
  <action>Create src/engine/variants.ts (following the config pattern of closest analog src/engine/palette.ts). Define DRILL_VARIANTS lookup mapping from DMC color codes and physical shapes (square vs. round) to Shopify Variant database IDs (for sizes 200, 500, 1000, 2000) using the structures and sample mappings provided in the research document.</action>
  <verify>Run tsc --noEmit to check compilation correctness. <automated>npx tsc --noEmit</automated></verify>
  <acceptance_criteria>
    - src/engine/variants.ts is created
    - DRILL_VARIANTS holds mappings for sample DMC codes (e.g. 150, 310) across shapes and bag sizes
    - TypeScript compiles cleanly without errors
  </acceptance_criteria>
  <done>Shopify Variant lookup table compiled.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Shopify Cart Permalink Compiler and Optimizer</name>
  <files>src/engine/checkout.ts</files>
  <read_first>src/engine/checkout.ts</read_first>
  <action>Modify src/engine/checkout.ts (following the transform pattern of closest analog src/engine/color.ts) to implement: (1) optimizeBags function enforcing dye lot rules (if <= 800 drills, only use 200-count bags; if > 800 drills, combine 500/1000/2000 bags and avoid 200-count); (2) compileShopifyCartLink function taking items (dmcCode, shape, count), affiliateTag, and affiliateApp ("ref", "rfsn", "none"). Concatenate Shopify variant IDs and quantities into the permalink URL format. Append affiliate query parameters and Shopify cart attributes. Return compiler results containing the URL, isUrlTooLong flag (if > 2000 chars), and lists of unmapped items with standard product handles.</action>
  <verify>Run tsc --noEmit to check compiler types. <automated>npx tsc --noEmit</automated></verify>
  <acceptance_criteria>
    - optimizeBags implements the exact <= 800 and > 800 dye lot packaging logic
    - compileShopifyCartLink builds valid Shopify permalink URLs with checkout redirects and affiliate codes
    - Unmapped items are returned with standard product handles for fallbacks
  </acceptance_criteria>
  <done>Shopify cart compiler and dye lot optimizer implemented.</done>
</task>

<task type="auto">
  <name>Task 3: Integrate checkout cart compiler controls into UI</name>
  <files>src/App.tsx</files>
  <read_first>src/App.tsx</read_first>
  <action>Modify src/App.tsx to add the cart checkout integration. Add affiliateTag state (persisted as "gempixel_affiliate_tag") and affiliateApp state (persisted as "gempixel_affiliate_app", default "ref"). Under the Quote sidebar tab, add an "Order Drills from Diamond Drills USA" button. When clicked, compile the cart items based on the active project's palette and quantities. If some colors are unmapped in DRILL_VARIANTS, display a warning dialog listing unmapped codes with direct links to their Diamond Drills USA search/product pages so the user can add them manually. If the final URL length exceeds 2000 characters, show a warning and split or offer fallback product handle checkouts.</action>
  <verify>Check compilation and run the app to make sure it loads and compiles without errors. <automated>npx tsc --noEmit</automated></verify>
  <acceptance_criteria>
    - UI provides affiliate settings inputs in the Quote panel config dropdown
    - "Order Drills from Diamond Drills USA" button triggers link generation
    - UI warning is shown for unmapped colors and long URLs
  </acceptance_criteria>
  <done>Checkout controls and affiliate settings integrated into the UI.</done>
</task>

<task type="auto">
  <name>Task 4: Write tests for Shopify permalink compiler and optimizer</name>
  <files>src/engine/__tests__/checkout.test.ts</files>
  <read_first>src/engine/__tests__/checkout.test.ts</read_first>
  <action>Open src/engine/__tests__/checkout.test.ts (following closest test analog src/engine/__tests__/color.test.ts). Write unit tests to verify: (1) optimizeBags boundary rules (<= 800 only uses 200 bags, > 800 combines 500/1000/2000 and avoids 200 bags); (2) compileShopifyCartLink correctly formats variant IDs and quantities; (3) affiliate and tracking parameters are appended properly as query params and cart attributes; (4) unmapped fallback handles are correctly populated for missing colors.</action>
  <verify>Run the test suite. <automated>npx vitest run src/engine/__tests__/checkout.test.ts</automated></verify>
  <acceptance_criteria>
    - Vitest unit tests verify all Shopify integration capabilities
    - All tests run and pass successfully
  </acceptance_criteria>
  <done>Shopify integration and dye lot optimizer unit tests implemented and passed.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- Run the test suite: npx vitest run src/engine/__tests__/checkout.test.ts
- Verify that TypeScript compilation succeeds without errors: npx tsc --noEmit
</verification>
