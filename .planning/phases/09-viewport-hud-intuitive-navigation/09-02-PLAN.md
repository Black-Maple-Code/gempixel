---
phase: 09-viewport-hud-intuitive-navigation
plan: "02"
type: execute
wave: 2
depends_on:
  - "09-01-PLAN.md"
files_modified:
  - src/App.tsx
  - src/__tests__/App.test.tsx
autonomous: true
requirements:
  - NAV-01
  - NAV-02
  - NAV-03
must_haves:
  truths:
    - "N-03: A new viewportMode selector and zoom control overlay is rendered inside the canvas workspace as a horizontal, floating glassmorphic Viewport HUD."
    - "N-04: The HUD stops pointer events propagation (onClick, onPointerDown) to prevent panning the canvas underneath when clicking overlay buttons."
    - "N-05: A '⚠️ Low Zoom' warning is displayed inside the Viewport HUD in symbols mode when the calculated scale falls below the symbol threshold (zoomScale * 16 < 10)."
    - "N-06: The top-bar wizard stepper navigation is removed from the Main Canvas Area header."
    - "N-07: A sticky wizard navigation footer is placed at the bottom of the left sidebar containing progress dots for the 4 steps, '< Back' and 'Next Step >' actions."
    - "N-08: Ingestion settings in Step 1 and Palette Optimization settings in Step 2 are grouped inside styled, collapsible details/summary cards conforming to 09-UI-SPEC.md (using text-[10px] or text-sm, never text-[11px])."
    - "N-09: Pure CSS hover tooltips are added for HUD buttons and settings inputs without introducing any third-party JavaScript tooltip engines."
    - "N-10: The Back button in the sticky footer is assigned id='wizard-back-btn' and the Next button is assigned id='wizard-next-btn' for E2E testing safety."
  artifacts:
    - path: "src/App.tsx"
      provides: "zoomScale state, combined floating viewport HUD overlay, responsive sticky left-sidebar navigation layout, step progress dots, and collapsible details cards with hover tooltip helpers."
    - path: "src/__tests__/App.test.tsx"
      provides: "Aligned step navigation selectors and text matches, and mocked CanvasViewer stubs to support testing step navigation."
  key_links:
    - from: "src/App.tsx"
      to: "src/engine/viewer.ts"
      via: "subscribing to onZoomChange callback and triggering zoomIn/zoomOut/resetZoom"
    - from: "src/App.tsx"
      to: "src/index.css"
      via: "applying glassmorphic, tooltip, and collapsible details classes"
---

<objective>
Refactor App.tsx sidebar and canvas container layouts to integrate the sticky navigation footer stepper and floating Viewport HUD. Implement collapsible group panels with hover tooltips conforming to typography specifications, and align the test suite accordingly.
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-viewport-hud-intuitive-navigation/09-CONTEXT.md
@.planning/phases/09-viewport-hud-intuitive-navigation/09-RESEARCH.md
@.planning/phases/09-viewport-hud-intuitive-navigation/09-PATTERNS.md
@.planning/phases/09-viewport-hud-intuitive-navigation/09-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor App.tsx Sidebar Layout, Sticky Navigation, Collapsible Cards, and Tooltips</name>
  <files>src/App.tsx</files>
  <read_first>src/App.tsx</read_first>
  <action>Modify `src/App.tsx` to implement N-06, N-07, N-08, N-09, and N-10.
1. Remove the old header bar containing the progress stepper (lines 2257-2324).
2. Remove `overflow-y-auto` from the `aside` sidebar container, and structure it as a flex column.
3. Wrap all setting steps and the portfolio drawer in a flex-grow scrollable container: `<div className="flex-1 overflow-y-auto py-4 flex flex-col gap-4">`.
4. Group Step 1 inputs under a collapsible `<details open className="text-[10px] text-slate-400 bg-slate-950/20 p-2 rounded border border-slate-850/40 cursor-pointer">` with summary "Ingestion Settings" and helper tooltips. (Conforms to UI-SPEC by using text-[10px] instead of text-[11px]).
5. Group Step 2 inputs under a collapsible details panel (using text-[10px] or text-sm) with summary "Palette Optimization Settings" and helper tooltips.
6. Add a sticky wizard navigation footer at the bottom of the sidebar. Render progress dots 1-4 with active/completed styling. Clicking a dot jumps to that step if it is valid (Step 1 is always valid, Step 2 is valid if image/activeProjectId is present, Steps 3/4 are valid if matchResult is present).
7. Implement Back and Next buttons in the sticky footer: Back button text `< Back` with `id="wizard-back-btn"`, Next button text `Next Step >` (Next button has `id="wizard-next-btn"`).</action>
  <verify>Verify App component compiles correctly. <automated>npx tsc --noEmit</automated></verify>
  <done>Sticky footer wizard, collapsible cards, and tooltips integrated in App.tsx sidebar.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate Floating Viewport HUD Overlay in App.tsx and Connect Viewer callbacks</name>
  <files>src/App.tsx</files>
  <read_first>src/App.tsx</read_first>
  <action>Modify `src/App.tsx` to implement N-03, N-04, and N-05.
1. Add `zoomScale` state: `const [zoomScale, setZoomScale] = useState(1.0);`.
2. In the CanvasViewer initialization `useEffect`, subscribe to `viewerRef.current.onZoomChange` to update `zoomScale`.
3. Remove the old floating selectors and zoom controls (lines 2350-2387).
4. Render a horizontal floating HUD overlay at the top-center of the canvas workspace container. Stop click and pointer-down propagation (`e.stopPropagation()`) on the HUD wrapper.
5. In the HUD, render: (a) 3-way view mode selector (Grid, Symbols, Original) with custom tooltips ("Canvas colors", "Colors + Symbols", "Original photo"); (b) Zoom In, Zoom Out, and Fit to Screen buttons connected to `viewerRef.current?.zoomIn()`, `zoomOut()`, `fitToContainer()` with tooltips; and (c) Low Zoom warning badge ("⚠️ Low Zoom") with tooltip "Zoom in to view symbol overlays (disabled at <10px cell size)" when `viewportMode === 'symbols' && zoomScale * 16 < 10`.</action>
  <verify>Verify App compiles. <automated>npx tsc --noEmit</automated></verify>
  <done>Floating viewport HUD overlay and CanvasViewer zoom callbacks connected in App.tsx.</done>
</task>

<task type="auto">
  <name>Task 3: Align App Tests to Wizard Buttons and Mock CanvasViewer</name>
  <files>src/__tests__/App.test.tsx</files>
  <read_first>src/__tests__/App.test.tsx</read_first>
  <action>Modify `src/__tests__/App.test.tsx` to align with next/back footer buttons and HUD. Mock `zoomIn`, `zoomOut`, and `resetZoom` inside the test mocks for `CanvasViewer`. Adjust assertions that search for text content `Back` to search for `< Back` instead. Ensure all existing tests pass successfully.</action>
  <verify>Run the full test suite. <automated>npm test</automated></verify>
  <done>Wizard test cases and CanvasViewer mocks aligned in App.test.tsx.</done>
</task>

</tasks>
