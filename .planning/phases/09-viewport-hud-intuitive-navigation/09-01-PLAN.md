---
phase: 09-viewport-hud-intuitive-navigation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/engine/viewer.ts
  - src/index.css
autonomous: true
requirements:
  - NAV-02
  - NAV-03
must_haves:
  truths:
    - "N-01: CanvasViewer in viewer.ts exposes public methods zoomIn(), zoomOut(), and resetZoom(). Zooming triggers the public onZoomChange(scale: number) callback."
    - "N-02: index.css includes floating glassmorphic container styles for Viewport HUD, pure CSS hover tooltips, and collapsible details summary caret transition animations."
  artifacts:
    - path: "src/engine/viewer.ts"
      provides: "Public zoomIn, zoomOut, resetZoom methods, and onZoomChange callback on CanvasViewer."
    - path: "src/index.css"
      provides: "Glassmorphic HUD styles, CSS hover tooltips, and details caret animations."
  key_links:
    - from: "src/App.tsx"
      to: "src/engine/viewer.ts"
      via: "will import and invoke zoom APIs"
    - from: "src/App.tsx"
      to: "src/index.css"
      via: "will apply glassmorphic, tooltip, and collapsible details classes"
---

<objective>
Expose programmatic zoom methods and scale change notification callbacks on the canvas viewer class. Set up utility styling rules in the global stylesheet for viewport HUD containers, hover tooltips, and accordion disclosure arrows.
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-viewport-hud-intuitive-navigation/09-CONTEXT.md
@.planning/phases/09-viewport-hud-intuitive-navigation/09-RESEARCH.md
@.planning/phases/09-viewport-hud-intuitive-navigation/09-PATTERNS.md
@.planning/phases/09-viewport-hud-intuitive-navigation/09-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend CanvasViewer in viewer.ts with Zoom APIs and Callback</name>
  <files>src/engine/viewer.ts</files>
  <read_first>src/engine/viewer.ts</read_first>
  <action>Modify `src/engine/viewer.ts` to implement N-01. Expose public `onZoomChange` callback property: `public onZoomChange?: (scale: number) => void;`. Implement public methods: (1) `zoomIn()` which zooms centered in the canvas viewport center (`canvas.width / 2`, `canvas.height / 2`) by a factor of `1.25`; (2) `zoomOut()` which zooms centered in the canvas viewport center by a factor of `0.8`; (3) `resetZoom()` which resets the scale to fit the container. Trigger the `onZoomChange` callback at the end of `handleZoom` and `fitToContainer` whenever the scale adjusts.</action>
  <verify>Verify canvas viewer compiles correctly. <automated>npx tsc --noEmit</automated></verify>
  <done>Zoom API controls and scale callback added to CanvasViewer class.</done>
</task>

<task type="auto">
  <name>Task 2: Define CSS Classes for Glassmorphism, CSS Tooltips, and Caret Animations in index.css</name>
  <files>src/index.css</files>
  <read_first>src/index.css</read_first>
  <action>Modify `src/index.css` to support N-02. Define Tailwind/CSS rules for the floating glassmorphic HUD: container positioning, backdrop blur, border, shadow, and mobile overrides. Add pure CSS hover tooltip rules using `group` and absolute coordinates. Style details/summary panels to hide browser default summary carets and animate custom carets (e.g. rotating 90 degrees when open).</action>
  <verify>Verify stylesheet compiles within the Vite process. <automated>npm run build</automated></verify>
  <done>Glassmorphic, tooltip, and details accordion transition styles added to index.css.</done>
</task>

</tasks>
