---
phase: 08-canvas-export-vendor-integration
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/engine/checkout.ts
  - src/App.tsx
  - src/engine/__tests__/checkout.test.ts
autonomous: true
requirements:
  - VENDOR-01
  - EXPORT-02
must_haves:
  truths:
    - "V-01: Replace static PrintKK redirect with three custom canvas-only rolled print vendors: Lumaprints (primary default), Prodigi, and FinerWorks."
    - "V-02: Base pricing tiers for rolled canvas are mapped for each vendor (Lumaprints: 12x16 = $6.50, 16x20 = $8.50, 20x28 = $12.00, 40x60 = $28.00; Prodigi: 12x16 = $9.00, 16x20 = $11.50, 20x28 = $16.00, 40x60 = $35.00; FinerWorks: 12x16 = $11.00, 16x20 = $14.00, 20x28 = $19.50, 40x60 = $42.00)."
    - "V-03: Sizing is converted to square inches based on selected unit (grid cell / cm / inch). Sizing conversions: grid cell = cols/10 in, cm = cm/2.54 in, inch = exact."
    - "V-04: Pricing uses linear interpolation between adjacent tiers when within tier limits, matching exact tiers if dimensions match, and falling back to the custom rate Width * Height * Sq. Inch Rate if out of bounds (below minimum or above maximum tiers)."
    - "V-05: Step 3 includes a 'Canvas Print Partner' dropdown selecting between Lumaprints, Prodigi, and FinerWorks, dynamically updating the calculated product price and shipping rate."
    - "V-06: Renders dynamic Sizing Advice based on the active unit and dimensions. Combined layout view offset ($X$) is 14 cells (grid unit), 1.4 inches (inch unit), or 3.56 cm (cm unit), advising ordering a larger canvas size to fit the margin legends. Separate layout view advises ordering the exact canvas size."
  artifacts:
    - path: "src/engine/checkout.ts"
      provides: "calculateCanvasCost function, VENDOR_REGISTRY mapping, and type structures for PricingPoint and VendorConfig."
    - path: "src/App.tsx"
      provides: "State for selectedVendor, dropdown selection input in Step 3, dynamic price/shipping recalculation, and rendering of the helper advice boxes containing the sizingAdvice copy text."
    - path: "src/engine/__tests__/checkout.test.ts"
      provides: "Unit tests verifying calculateCanvasCost tier mapping, linear interpolation, custom square inch rate fallbacks, and unit conversion logic."
  key_links:
    - from: "src/App.tsx"
      to: "src/engine/checkout.ts"
      via: "import and invoke calculateCanvasCost"
---

<objective>
Refactor the pricing engine to support multiple canvas vendors (Lumaprints, Prodigi, FinerWorks) with tier matching, linear interpolation, and custom square-inch pricing fallback. Integrate the new multi-vendor selector and a dynamic sizing advice panel into Step 3 of the UI workspace wizard.
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-canvas-export-vendor-integration/08-CONTEXT.md
@.planning/phases/08-canvas-export-vendor-integration/08-RESEARCH.md
@.planning/phases/08-canvas-export-vendor-integration/08-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add vendor pricing models and interpolation functions in checkout.ts</name>
  <files>src/engine/checkout.ts</files>
  <read_first>.planning/phases/08-canvas-export-vendor-integration/08-RESEARCH.md</read_first>
  <action>Modify src/engine/checkout.ts to implement V-01, V-02, V-03, and V-04. Define the `PricingPoint` and `VendorConfig` interfaces. Declare the `VENDOR_REGISTRY` mapping with configurations for `lumaprints`, `prodigi`, and `finerworks`. Implement the `calculateCanvasCost(width, height, unit, vendorKey)` function using standard unit conversions (grid/10 for inches, cm/2.54 for inches), exact matches lookup, linear interpolation between tiers, and a fallback to square-inch rates if the calculated area falls outside standard bounds. Ensure all calculations return prices rounded to 2 decimal places.</action>
  <verify>Verify checkout engine compiles correctly. <automated>npx tsc --noEmit</automated></verify>
  <acceptance_criteria>
    - VENDOR_REGISTRY contains correct base prices, shipping costs, and custom square-inch rates for Lumaprints, Prodigi, and FinerWorks.
    - calculateCanvasCost converts grid and cm dimensions to inches correctly.
    - calculateCanvasCost returns exact tier prices when dimensions match exactly, interpolates correctly between adjacent tiers, and falls back to area * rate for out-of-bounds custom sizes.
  </acceptance_criteria>
  <done>Pricing engine and multiple vendor configurations implemented in src/engine/checkout.ts.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor App.tsx for Multi-Vendor Selector and Dynamic Sizing Advice</name>
  <files>src/App.tsx</files>
  <read_first>src/App.tsx</read_first>
  <action>Modify src/App.tsx to implement V-05 and V-06. Add `selectedVendor` state initialized to 'lumaprints'. In Step 3, replace the static PrintKK section with a select element labeled "Canvas Print Partner" with options for Lumaprints, Prodigi, and FinerWorks. Bind the select element to `selectedVendor`, and on change, update the `canvasBaseCost` and `canvasShippingEstimate` using the VENDOR_REGISTRY defaults and `calculateCanvasCost`. Implement a React useMemo hook for `sizingAdvice` which calculates the X offset (14 cells, 1.4 inches, or 3.56 cm) to recommend appropriate sizes for separate canvas versus combined canvas sheets. Render the sizing advice in a prominent helper card in Step 3.</action>
  <verify>Verify App components compile correctly. <automated>npx tsc --noEmit</automated></verify>
  <acceptance_criteria>
    - selectedVendor state correctly updates base costs and shipping estimates upon vendor selection.
    - Canvas Print Partner dropdown renders with Lumaprints as default, alongside Prodigi and FinerWorks.
    - Helper card displays accurate sizing advice for both separate and combined modes with appropriate units.
  </acceptance_criteria>
  <done>Multi-vendor selector and dynamic sizing advice integrated in App.tsx.</done>
</task>

<task type="auto">
  <name>Task 3: Refactor checkout unit tests to cover vendor pricing logic</name>
  <files>src/engine/__tests__/checkout.test.ts</files>
  <read_first>src/engine/__tests__/checkout.test.ts</read_first>
  <action>Modify src/engine/__tests__/checkout.test.ts to verify the pricing engine. Add unit tests asserting: (1) correct mapping and exact tier pricing calculations for Lumaprints; (2) correct linear interpolation calculations (e.g. 16x16 yielding $7.50 for Lumaprints); (3) custom rate fallbacks for out-of-bounds dimensions; (4) proper unit conversion handling for grid units, centimeters, and inches; and (5) Prodigi and FinerWorks calculations correctly reflect their respective rates.</action>
  <verify>Run the updated checkout tests. <automated>npx vitest run src/engine/__tests__/checkout.test.ts</automated></verify>
  <acceptance_criteria>
    - All pricing engine and unit conversion tests pass successfully.
    - Linear interpolation is verified mathematically.
    - Custom square-inch fallbacks are verified.
  </acceptance_criteria>
  <done>Pricing engine tests added and verified.</done>
</task>

</tasks>
