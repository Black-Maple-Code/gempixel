---
phase: 08-canvas-export-vendor-integration
plan: "02"
type: execute
wave: 2
depends_on:
  - "08-01-PLAN.md"
files_modified:
  - src/App.tsx
  - src/index.css
files_created:
  - src/engine/export.ts
  - src/engine/__tests__/export.test.ts
autonomous: true
requirements:
  - EXPORT-01
must_haves:
  truths:
    - "E-01: PNG Export Engine exports a borderless high-resolution PNG image of only the grid cells and symbol overlays (cellScale = 20px default)."
    - "E-02: Combined Canvas Sheet exports a high-resolution PNG containing a left margin legend (140px), a dashed guideline, the centered grid canvas, another dashed guideline, and a right margin legend (140px)."
    - "E-03: Combined sheet rendering uses a dynamic height calculation (max of grid height vs legend required height) to prevent vertical legend cropping."
    - "E-04: PNG downloads are initiated asynchronously via canvas.toBlob and short-lived Object URLs (with revocation timeouts to avoid memory leaks)."
    - "E-05: Home printer legend print layout hides the main application shell and the canvas viewer completely, formatting only the check legend (DMC code, swatch, symbol) for portrait letter/A4 pages."
    - "E-06: Print styling enforces background colors and borders using -webkit-print-color-adjust: exact and print-color-adjust: exact and avoids page breaks inside items using break-inside: avoid."
  artifacts:
    - path: "src/engine/export.ts"
      provides: "functions drawCanvasOnly, drawCombinedCanvasSheet, and triggerCanvasDownload using native offscreen canvas drawing."
    - path: "src/App.tsx"
      provides: "button triggers in Step 3 for 'Download Canvas Grid (PNG)', 'Download Combined Canvas Sheet (PNG)', and 'Print Legend Sheet (Paper)' with class layout toggle hook."
    - path: "src/index.css"
      provides: "@media print overrides for print-only-legend-mode body formatting, table/list checklist rules, and page break avoidance."
    - path: "src/engine/__tests__/export.test.ts"
      provides: "Unit tests verifying export canvas width/height allocations and rendering offsets."
  key_links:
    - from: "src/App.tsx"
      to: "src/engine/export.ts"
      via: "imports and invokes drawCanvasOnly, drawCombinedCanvasSheet, and triggerCanvasDownload"
    - from: "src/App.tsx"
      to: "src/index.css"
      via: "uses body class toggles styled in index.css"
---

<objective>
Implement high-resolution client-side PNG export engine (Option C) enabling download of the borderless canvas grid only versus a combined canvas sheet with margin legends. Implement home printer legend print capabilities leveraging portrait print media stylesheet configurations.
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-canvas-export-vendor-integration/08-CONTEXT.md
@.planning/phases/08-canvas-export-vendor-integration/08-RESEARCH.md
@.planning/phases/08-canvas-export-vendor-integration/08-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement high-resolution offscreen canvas drawing and download logic</name>
  <files>src/engine/export.ts</files>
  <read_first>.planning/phases/08-canvas-export-vendor-integration/08-RESEARCH.md</read_first>
  <action>Create a new file src/engine/export.ts (following the offscreen drawing structure and canvas draw loops of closest analog src/engine/viewer.ts from PATTERNS.md). Implement: (1) `drawCanvasOnly` drawing cells and symbols at default scale 20px using native canvas context APIs; (2) `drawCombinedCanvasSheet` allocating left and right 140px margins, drawing color check swatches, text labels, centering the grid canvas vertically, and drawing dashed guidelines; (3) `triggerCanvasDownload` which performs canvas toBlob PNG serialization, creates an object URL, clicks a temporary anchor tag, and schedules revocation via setTimeout to prevent memory leaks. Import `getContrastColor` from src/engine/symbols.ts to format high-contrast text overlays.</action>
  <verify>Verify export engine compiles successfully. <automated>npx tsc --noEmit</automated></verify>
  <acceptance_criteria>
    - drawCanvasOnly creates a borderless scaled grid with high-contrast text symbol overlays.
    - drawCombinedCanvasSheet allocates left and right legends of 140px, sets canvas height to the max of grid versus legend height to prevent cropping, and draws vertical dashed boundaries.
    - triggerCanvasDownload initiates PNG download and revokes Object URLs after clicking.
  </acceptance_criteria>
  <done>Export canvas rendering and download pipeline implemented in src/engine/export.ts.</done>
</task>

<task type="auto">
  <name>Task 2: Add export and print button actions in App.tsx</name>
  <files>src/App.tsx</files>
  <read_first>src/App.tsx</read_first>
  <action>Modify src/App.tsx to implement E-04 and E-05. In Step 3 under Order & Actions, add three new buttons: "Download Canvas Grid (PNG)", "Download Combined Canvas Sheet (PNG)", and "Print Legend Sheet (Paper)". Implement action click handlers that compile options and call `drawCanvasOnly` or `drawCombinedCanvasSheet` and download them via `triggerCanvasDownload`. Implement `printLegendSheetOnly` to toggle class `print-only-legend-mode` on the document body before triggering `window.print()`, cleaning it up using an `afterprint` event listener. Render the printable checklist container `.legend-checklist-print-container` in the main body (hidden on screen but exposed on print) listing all active legend colors in a checklist layout.</action>
  <verify>Verify App.tsx compiles correctly. <automated>npx tsc --noEmit</automated></verify>
  <acceptance_criteria>
    - PNG download actions draw the correct canvases and prompt the download dialog.
    - Print Legend Sheet button toggles body class and opens browser print dialog.
    - The printable legend container renders checklist data correctly.
  </acceptance_criteria>
  <done>Export actions and print triggers integrated into App.tsx.</done>
</task>

<task type="auto">
  <name>Task 3: Implement printer CSS stylesheet rules in index.css</name>
  <files>src/index.css</files>
  <read_first>src/index.css</read_first>
  <action>Modify src/index.css to implement E-06. Under `@media print`, configure formatting rules for `body.print-only-legend-mode`. Force background to white and color to black. Hide all application components including the print canvas sheet itself. Display only the `.legend-checklist-print-container` formatting the list into a 3-column grid checklist. Add `break-inside: avoid` on list items and apply `print-color-adjust: exact` to swatches to ensure background colors are printed correctly on home print sheets.</action>
  <verify>Verify CSS compiles and packages build correctly. <automated>npx tsc --noEmit</automated></verify>
  <acceptance_criteria>
    - Print layout hides normal app shell, sidebar, and canvas.
    - Legend list displays as a multi-column portrait grid checklist.
    - Swatch background colors render on print using print-color-adjust.
    - Checklist items do not break across page boundaries.
  </acceptance_criteria>
  <done>Home printer portrait layout styles added to index.css.</done>
</task>

<task type="auto">
  <name>Task 4: Add exporter unit tests</name>
  <files>src/engine/__tests__/export.test.ts</files>
  <read_first>src/engine/export.ts</read_first>
  <action>Create a new test file src/engine/__tests__/export.test.ts (following the structure and mock canvas contexts of closest analog test src/engine/__tests__/viewer.test.ts from PATTERNS.md). Write unit tests to verify: (1) `drawCanvasOnly` generates correct width and height dimensions; (2) `drawCombinedCanvasSheet` allocates correct width including margins; (3) `drawCombinedCanvasSheet` overrides height when the required legend size exceeds grid size; and (4) draw methods handle mock canvas rendering contexts properly without throwing errors.</action>
  <verify>Run the new exporter tests. <automated>npx vitest run src/engine/__tests__/export.test.ts</automated></verify>
  <acceptance_criteria>
    - All export engine unit tests pass.
    - Correct canvas sizing and height adjustments are verified.
  </acceptance_criteria>
  <done>Exporter unit tests implemented and verified.</done>
</task>

</tasks>
