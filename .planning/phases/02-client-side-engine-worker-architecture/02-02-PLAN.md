---
phase: 02-client-side-engine-worker-architecture
plan: "02"
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - src/engine/matcher.worker.ts
  - src/engine/worker-client.ts
  - src/engine/__tests__/worker.test.ts
autonomous: true
requirements:
  - ENGINE-03
  - ENGINE-04
must_haves:
  truths:
    - "Web Worker processes the pixel color-matching loop asynchronously, posting progress updates back to the client"
    - "In-flight worker computations can be successfully aborted, discarding partial matching runs"
    - "RGBA-to-DMC match cache persists across runs and clears only when the active candidate palette changes"
  artifacts:
    - path: "src/engine/matcher.worker.ts"
      provides: "Web Worker process listening for match/abort events, computing CIEDE2000 matches in batch rows, and caching results"
    - path: "src/engine/worker-client.ts"
      provides: "Client manager that initializes worker, tracks active palette hashes, requests match runs, handles aborts, and triggers callbacks"
    - path: "src/engine/__tests__/worker.test.ts"
      provides: "Unit tests verifying worker lifecycle, messaging protocols, abort requests, progress events, and caching behavior"
  key_links:
    - from: "src/engine/worker-client.ts"
      to: "src/engine/types.ts"
      via: "importing DmcColor interface"
      pattern: "import .*from '\\./types'"
    - from: "src/engine/matcher.worker.ts"
      to: "src/engine/color.ts"
      via: "importing matchPixelGrid or findClosestDmc functional matches"
      pattern: "import .*from '\\./color'"
    - from: "src/engine/__tests__/worker.test.ts"
      to: "src/engine/worker-client.ts"
      via: "importing MatcherClient wrapper class"
      pattern: "import .*from '\\.\\./worker-client'"
---

<objective>
Implement the Web Worker background processing architecture for CIEDE2000 color matching. This includes creating the persistent Web Worker listener, row-by-row batch processing with abort checking, worker messaging wrapper client, and RGBA-to-DMC match lookup caching with palette-based invalidation.
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-client-side-engine-worker-architecture/02-CONTEXT.md
@.planning/phases/02-client-side-engine-worker-architecture/02-RESEARCH.md
@.planning/phases/02-client-side-engine-worker-architecture/02-PATTERNS.md
@.planning/phases/02-client-side-engine-worker-architecture/02-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement background Web Worker matcher and caching loop</name>
  <files>src/engine/matcher.worker.ts</files>
  <read_first>.planning/phases/02-client-side-engine-worker-architecture/02-RESEARCH.md</read_first>
  <action>Create src/engine/matcher.worker.ts. This worker script runs inside the worker thread. Implement: (1) message listener parsing incoming messages for match and abort commands; (2) RGBA-to-DMC cache Map storing matched codes; (3) clearCache option processing; (4) matching loop that processes pixels in row batches, checking an isAborted flag at the start of each row, and using the RGBA numeric cache key to look up matches (bypassing the blending and distance loops on cache hit). When a cache miss occurs, perform alpha blending, find the closest DMC color code via CIEDE2000, and save it in the cache. Post progress percentages during execution, and post final result matches and counts back to the client.</action>
  <verify>Ensure worker compiles successfully under Vite configurations.</verify>
  <acceptance_criteria>
    - src/engine/matcher.worker.ts exists and imports matching logic from color.ts
    - The worker processes messages asynchronously without throwing script parse errors
    - Cache lookups bypass matching calculations on RGBA hits
    - Abort requests successfully stop row processing loops
  </acceptance_criteria>
  <done>Worker matching loop and cache implementation finished.</done>
</task>

<task type="auto">
  <name>Task 2: Build worker client wrapper manager class</name>
  <files>src/engine/worker-client.ts</files>
  <read_first>src/engine/matcher.worker.ts</read_first>
  <action>Create src/engine/worker-client.ts. Implement the MatcherClient class. The constructor receives a Worker URL and instantiates the persistent worker. Implement: (1) match(pixels, candidates, onProgress, onComplete) method. Calculate candidate active palette hash. Compare with the previous hash; if different, send clearCache: true, and update the saved hash. Send the abort signal, followed by the match payload; (2) terminate() terminating the persistent worker.</action>
  <verify>Verify class structure compiles successfully with npx tsc --noEmit</verify>
  <acceptance_criteria>
    - src/engine/worker-client.ts exists and compiles cleanly
    - MatcherClient constructor initializes a persistent Web Worker
    - match() tracks active candidate palette hashes and automatically sets clearCache
    - match() handles abort events and sets message callback hooks
  </acceptance_criteria>
  <done>Worker client wrapper manager class created successfully.</done>
</task>

<task type="auto">
  <name>Task 3: Implement unit tests for worker execution, cancellation, and cache invalidation</name>
  <files>src/engine/__tests__/worker.test.ts</files>
  <read_first>src/engine/worker-client.ts</read_first>
  <action>Create src/engine/__tests__/worker.test.ts. Write Vitest unit tests verifying: (1) MatcherClient successfully launches the worker (running Vitest with Web Worker support, mocking Worker if needed in the test environment); (2) match executes color matching successfully and returns matched arrays; (3) sending abort stops active computations; (4) RGBA cache lookups persist across consecutive match() calls on the same palette, and are cleared when the candidate palette array changes.</action>
  <verify>Run the tests using npx vitest run src/engine/__tests__/worker.test.ts to verify all worker messaging, cancellation, and cache assertions pass.</verify>
  <acceptance_criteria>
    - src/engine/__tests__/worker.test.ts exists and executes successfully
    - Unit tests cover worker lifecycle, abort callbacks, and cache invalidation criteria
    - All tests pass successfully
  </acceptance_criteria>
  <done>Unit tests for worker messaging and cache validity created and passed.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- Run the test suite: npx vitest run src/engine/__tests__/worker.test.ts
- Verify that TypeScript compilation succeeds without errors: npx tsc --noEmit
</verification>
