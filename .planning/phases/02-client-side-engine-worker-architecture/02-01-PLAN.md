---
phase: 02-client-side-engine-worker-architecture
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/engine/ingest.ts
  - src/engine/__tests__/ingest.test.ts
autonomous: true
requirements:
  - INGEST-01
  - INGEST-02
  - INGEST-03
  - INGEST-04
must_haves:
  truths:
    - "Physical dimensions convert accurately to grid cells based on 2.5mm/drill metric and 10 dots/inch imperial density"
    - "Cropping correctly computes horizontal/vertical offset bounds using the Cover/Crop aspect ratio preservation mode"
    - "Box Sampling averages RGBA values correctly across the cropped source image cells"
  artifacts:
    - path: "src/engine/ingest.ts"
      provides: "Aspect ratio cropping bounds, metric/imperial size calculations, and Box Sampling area averaging downscaler"
    - path: "src/engine/__tests__/ingest.test.ts"
      provides: "Unit tests verifying physical-to-dot conversions, crop offset coordinates, and correct downscaling outputs"
  key_links:
    - from: "src/engine/__tests__/ingest.test.ts"
      to: "src/engine/ingest.ts"
      via: "importing calculateCropBounds, boxSampleImage, physicalToGridDimensions"
      pattern: "import .*from '\\.\\./ingest'"
---

<objective>
Implement the client-side file ingestion, sizing, cropping, and box-sampling downscaling algorithms. This includes building the calculations for physical size conversion, cover aspect ratio crops, and the pixel block averaging downscaler.
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-client-side-engine-worker-architecture/02-CONTEXT.md
@.planning/phases/02-client-side-engine-worker-architecture/02-RESEARCH.md
@.planning/phases/02-client-side-engine-worker-architecture/02-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement client-side sizing, aspect ratio crop bounds, and box sampling downscaling</name>
  <files>src/engine/ingest.ts</files>
  <read_first>.planning/phases/02-client-side-engine-worker-architecture/02-RESEARCH.md</read_first>
  <action>Create src/engine/ingest.ts. Implement: (1) physicalToGridDimensions(width, height, unit) converting physical size to dots based on 2.5mm per drill (metric) or 10 drills per inch (imperial), rounding to the nearest integer; (2) calculateCropBounds(srcWidth, srcHeight, targetCols, targetRows) which calculates the bounding box of the source image to crop, keeping the aspect ratio of the target grid centered on the original image (Cover/Crop mode); (3) boxSampleImage(srcPixels, srcWidth, srcHeight, targetCols, targetRows) which takes a flat Uint8ClampedArray of source pixels, applies the calculated crop boundary, divides the cropped region into targetCols x targetRows cells, and averages the RGBA values of each block, returning a new flat Uint8ClampedArray of size targetCols * targetRows * 4.</action>
  <verify>Ensure that the file compiling passes with npx tsc --noEmit</verify>
  <acceptance_criteria>
    - src/engine/ingest.ts exists and compiles without errors
    - physicalToGridDimensions converts metric and imperial dimensions to integer grid cells
    - calculateCropBounds correctly determines cropping cropWidth, cropHeight, and offsets
    - boxSampleImage aggregates blocks of pixel data and averages color channels without empty divisions
  </acceptance_criteria>
  <done>Sizing and Box Sampling downscaler implemented successfully.</done>
</task>

<task type="auto">
  <name>Task 2: Write unit tests for sizing conversions, cropping offsets, and Box Sampling</name>
  <files>src/engine/__tests__/ingest.test.ts</files>
  <read_first>src/engine/ingest.ts</read_first>
  <action>Create src/engine/__tests__/ingest.test.ts. Write Vitest unit tests verifying: (1) physicalToGridDimensions matches the 2.5mm and 10 dots/inch constraints with standard test sizes; (2) calculateCropBounds correctly centers crops horizontally and vertically for mismatched source/target aspect ratios; (3) boxSampleImage correctly downsamples mock pixel arrays, averaging multiple pixels into single grid cells, and preserving channel boundaries.</action>
  <verify>Run the newly created tests using npx vitest run src/engine/__tests__/ingest.test.ts to verify all tests pass successfully.</verify>
  <acceptance_criteria>
    - src/engine/__tests__/ingest.test.ts exists and runs on Vitest
    - Unit tests verify conversion boundaries, aspect ratio cropping bounds, and Box Sampling averages
    - Test runner reports all tests pass
  </acceptance_criteria>
  <done>Unit tests created and verified passing.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- Run the test suite: npx vitest run src/engine/__tests__/ingest.test.ts
- Verify that TypeScript compilation succeeds without errors: npx tsc --noEmit
</verification>
