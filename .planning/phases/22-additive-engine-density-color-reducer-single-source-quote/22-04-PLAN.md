---
phase: 22-additive-engine-density-color-reducer-single-source-quote
plan: 04
type: execute
wave: 2
depends_on: ["22-03"]
files_modified:
  - src/features/match/useDiamondArtMatch.ts
  - src/features/match/__tests__/useDiamondArtMatch.test.tsx
autonomous: true
requirements: [REFINE-04]
must_haves:
  truths:
    - "useDiamondArtMatch exposes detectedColorCount = the distinct DMC codes in the RAW matched grid (Object.keys(rawMatchResult.counts).length), 0 when there is no match yet (D-04)"
    - "detectedColorCount recomputes ONLY on a new raw match (worker re-run); it does NOT change when smoothing/reduce post-process inputs change — the Phase 23 slider max stays stable under the user (D-04)"
    - "the reduce step is added to the post-process pipeline in the canonical order raw → smoothMatches(strength) → reduceToColorCount(target), reduction LAST (D-05)"
    - "the reduce step is gated to a no-op default (enableReduce falsy / targetColorCount absent) so App's current matchResult and total are BYTE-IDENTICAL this phase — reduceToColorCount is not even called on the default path (SC5, Pitfall 6)"
    - "the new MatchInputs fields are OPTIONAL so existing hook consumers (App) compile unchanged; detectedColorCount is a purely additive MatchState field"
  artifacts:
    - "detectedColorCount field on MatchState + its useMemo derivation in src/features/match/useDiamondArtMatch.ts"
    - "optional enableReduce/targetColorCount inputs + gated reduce step in the post-process useMemo"
    - "hook coverage in src/features/match/__tests__/useDiamondArtMatch.test.tsx"
  key_links:
    - "detectedColorCount is derived in a useMemo keyed ONLY on rawMatchResult (NOT matchResult) so it is invariant under smoothing/reduce — the coupled-controls anti-pattern D-04 forbids"
    - "the gated reduce step imports reduceToColorCount from engine/color.ts (built in 22-03) and runs AFTER smoothMatches (D-05)"
---

<objective>
Expose `detectedColorCount` from `useDiamondArtMatch` as a purely additive read-only field (the distinct DMC codes in the RAW matched grid, D-04), and add the target-N reduce step to the hook's post-process pipeline in the canonical `raw → smooth → reduce` order (D-05) — gated to a no-op default so App's current behavior is byte-identical this phase (SC5). This is the hook seam Phase 23's color-count slider (REFINE-04) wires into; Phase 22 only lands the additive engine support.

Purpose: `detectedColorCount` gives Phase 23 a stable slider max (invariant under edge-cleanup/color drags), and the gated reduce step means flipping REFINE-04 on in Phase 23 is a wiring change, not a pipeline change.
Output: additive `detectedColorCount` on `MatchState`, optional `enableReduce`/`targetColorCount` on `MatchInputs`, a gated reduce step, and hook test coverage.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-additive-engine-density-color-reducer-single-source-quote/22-CONTEXT.md
@.planning/phases/22-additive-engine-density-color-reducer-single-source-quote/22-RESEARCH.md
@.planning/phases/22-additive-engine-density-color-reducer-single-source-quote/22-03-SUMMARY.md
</context>

<artifacts_produced>
NEW symbols this phase introduces (exclude from source-drift verification):
- `src/features/match/useDiamondArtMatch.ts` (additive): `detectedColorCount` (MatchState field), `enableReduce?` + `targetColorCount?` (optional MatchInputs fields), a gated reduce step
- consumes `reduceToColorCount` (from 22-03, engine/color.ts)
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expose detectedColorCount + add gated reduce step to useDiamondArtMatch (D-04/D-05, additive)</name>
  <read_first>
    - src/features/match/useDiamondArtMatch.ts (WHOLE FILE — MatchInputs, MatchState, the post-process useMemo at :217-249 with the substitution→smoothing order and the `matches === rawMatchResult.matches` identity return; the gating precedent enableSubstitution/enableSmoothing)
    - src/engine/color.ts (reduceToColorCount signature { codes, counts, mergedCount } + MERGE_GUARD_DELTA_E — built in 22-03; the fn this step calls)
    - src/engine/smoothing.ts:58-72 (smoothMatches, which runs BEFORE reduce in the D-05 canonical order)
    - .planning/phases/22-additive-engine-density-color-reducer-single-source-quote/22-RESEARCH.md (Pitfalls 5 + 6 — detectedColorCount must key on rawMatchResult; the reduce step must be gated no-op)
  </read_first>
  <files>src/features/match/useDiamondArtMatch.ts</files>
  <behavior>
    - After a raw match completes with counts {A:2, B:1}, detectedColorCount === 2 (distinct raw codes)
    - Before any match, detectedColorCount === 0 (no rawMatchResult)
    - Changing smoothingStrength / enableSmoothing (post-process) does NOT change detectedColorCount (it is keyed on rawMatchResult only)
    - With the default inputs (App passes neither enableReduce nor targetColorCount), the reduce step is NOT invoked and matchResult is byte-identical to the pre-change hook (existing tests stay green)
    - The post-process order is substitution (existing) → smoothMatches (existing) → reduceToColorCount (new, gated last)
  </behavior>
  <action>
    Edit `src/features/match/useDiamondArtMatch.ts` — all changes additive (do NOT alter the worker effect, restore seam, or existing gating):
    (a) Import `reduceToColorCount` from `../../engine/color` (alongside the existing `substituteLowCountColors` import).
    (b) On `MatchInputs`, add two OPTIONAL fields (optional so App and every existing consumer compile unchanged, SC5): `enableReduce?: boolean;` (JSDoc: target-N color reduction, off by default; Phase 23 REFINE-04 slider flips it on) and `targetColorCount?: number;` (JSDoc: the color-count slider target; ignored unless enableReduce and < the current distinct count).
    (c) On `MatchState`, add `detectedColorCount: number;` (JSDoc: distinct DMC codes in the RAW matched grid, measured before smoothing/reduction; recomputes only on a worker re-run — the stable Refine slider max, D-04).
    (d) Derive it in a NEW useMemo keyed ONLY on `rawMatchResult`: `const detectedColorCount = useMemo(() => Object.keys(rawMatchResult?.counts ?? {}).length, [rawMatchResult]);` (Pitfall 5 — never derive it off `matchResult`/post-process memo or the slider max jumps under the user).
    (e) In the existing post-process useMemo (after the smoothing block, D-05 order = reduction LAST), add a gated reduce step: only when `enableReduce === true` AND `typeof targetColorCount === 'number' && Number.isFinite(targetColorCount)` call `reduceToColorCount(matches, counts, activeCandidates, targetColorCount)` and reassign `matches`/`counts` from its `codes`/`counts`. Do NOT call reduceToColorCount on the default path (so `matches` stays === rawMatchResult.matches when nothing else ran, preserving the byte-identical identity return, Pitfall 6). Destructure the two new inputs alongside the existing ones and add `enableReduce`, `targetColorCount` to the post-process useMemo dependency array (so Phase 23 re-renders on slider change). Return `detectedColorCount` in the hook's returned object.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `MatchState` includes `detectedColorCount: number` and the hook returns it
    - `detectedColorCount` is computed in a useMemo whose dependency array is exactly `[rawMatchResult]` (not matchResult)
    - `MatchInputs` adds `enableReduce?` and `targetColorCount?` as OPTIONAL fields (App.tsx compiles without passing them)
    - `reduceToColorCount` is invoked only inside an `enableReduce && Number.isFinite(targetColorCount)` guard, AFTER the smoothing block
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>detectedColorCount is exposed (raw-keyed), the gated reduce step is wired last in the pipeline, new inputs are optional, and App compiles unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Add hook coverage — detectedColorCount value + stability under post-process (D-04)</name>
  <read_first>
    - src/features/match/__tests__/useDiamondArtMatch.test.tsx (WHOLE FILE — the hoisted MatcherClient mock that completes synchronously with counts {A:2,B:1}; the `mount(getInputs)` harness + `settle(h, predicate)` poller; the beforeEach OffscreenCanvas/createImageBitmap seams)
    - src/features/match/useDiamondArtMatch.ts (detectedColorCount derivation — Task 1)
    - .planning/codebase/TESTING.md (jsdom pragma, hoisted mocks, settle/poll pattern)
  </read_first>
  <files>src/features/match/__tests__/useDiamondArtMatch.test.tsx</files>
  <action>
    Add a `describe('detectedColorCount (D-04)')` block to the existing hook test, reusing the `mount` harness + `settle` poller and the mocked MatcherClient (which completes with counts `{ A: 2, B: 1 }`). Cases: (1) after a match settles, `state.detectedColorCount === 2` (distinct raw codes); (2) before any image is provided, `detectedColorCount === 0`; (3) STABILITY — mount with an image and `enableSmoothing: true, smoothingStrength: 3`, settle, capture detectedColorCount, then rerender with a different `smoothingStrength` (e.g. 1) and assert `detectedColorCount` is UNCHANGED (still 2) — proving it is keyed on the raw match, not the post-process (the coupled-controls guard). Provide a full `MatchInputs` object in each `getInputs` (the two new fields are optional — omit them or set enableReduce:false to exercise the default no-op path). Do not assert reduce-merge correctness here (fake codes 'A'/'B' are not in the palette so no merge occurs) — reducer merge behavior is covered by 22-03's color.test.ts; this task covers the additive detectedColorCount contract only.
  </action>
  <verify>
    <automated>npx vitest run src/features/match/__tests__/useDiamondArtMatch.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/features/match/__tests__/useDiamondArtMatch.test.tsx` exits 0
    - a test asserts detectedColorCount === 2 after a match completes and === 0 before any match
    - a test asserts detectedColorCount is unchanged when smoothingStrength changes (stability / raw-keyed)
    - the existing hook tests in the file still pass (additive, no regression)
  </acceptance_criteria>
  <done>Hook coverage proves detectedColorCount equals the raw distinct count and stays stable under post-process changes; full hook suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| rawMatchResult → detectedColorCount / reduce step | a null or malformed rawMatchResult (restore path) and a non-finite targetColorCount (future slider) cross into the derived count + the reduce call |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22-H1 | DoS (render path) | detectedColorCount derivation | low | mitigate | `Object.keys(rawMatchResult?.counts ?? {}).length` — null-safe, returns 0 for no match; never throws. |
| T-22-H2 | Tampering (integrity) | gated reduce step | low | mitigate | reduce runs only under `enableReduce && Number.isFinite(targetColorCount)`; the pure reduceToColorCount already degrades-not-crashes on degenerate input (22-03). Default path never calls it → App byte-identical (SC5). |
| T-22-H3 | Tampering (supply chain) | package.json | low | mitigate | Zero new dependencies (imports existing engine/color). Review gate: no new package.json dependency. |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0 (strict; App.tsx compiles without passing the new optional inputs).
- `npx vitest run src/features/match/__tests__/useDiamondArtMatch.test.tsx` exits 0.
- `npm test` exits 0; total test count only grows vs baseline; existing App/integration tests unaffected (SC5 — matchResult byte-identical on the default path).
- `git diff` shows only additive changes to the hook (worker effect / restore seam untouched).
- No new dependency in package.json.
</verification>

<success_criteria>
- `useDiamondArtMatch` exposes `detectedColorCount` (raw distinct count) that is stable under post-process changes — the stable Phase 23 slider max (D-04, SC4 engine support).
- The reduce step is wired last in the canonical `raw → smooth → reduce` order and gated to a byte-identical no-op default (D-05, SC5).
- New MatchInputs fields are optional; App compiles unchanged; the full suite stays green.
</success_criteria>

<output>
Create `.planning/phases/22-additive-engine-density-color-reducer-single-source-quote/22-04-SUMMARY.md` when done.
</output>
