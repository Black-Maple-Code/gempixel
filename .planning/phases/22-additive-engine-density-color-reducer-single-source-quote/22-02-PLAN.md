---
phase: 22-additive-engine-density-color-reducer-single-source-quote
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/engine/checkout.ts
  - src/engine/quote.ts
  - src/engine/__tests__/quote.test.ts
autonomous: true
requirements: [QUOTE-02, QUOTE-03]
must_haves:
  truths:
    - "buildOrderQuote returns an OrderQuote whose totalCents === sumCents(lineItems.map(li => li.cents)) BY CONSTRUCTION — the itemized line items always sum EXACTLY to the total in integer cents (QUOTE-02, D-06)"
    - "the tax line item is 0 cents and labeled 'calculated at vendor checkout' via TAX_RATE_ESTIMATE=0 — no figure reads as a finalized charge (QUOTE-03, D-07)"
    - "shipping is ONE combined 'Shipping (est.)' line whose cents === toCents(VENDOR_REGISTRY[vendor].baseShipping + DRILLS_BASE_SHIPPING) and carries a 'rates as of {RATES_AS_OF}' note (QUOTE-03, D-08)"
    - "a null/non-finite canvasBaseCost yields canvasPriced=false and a 0-cent canvas line without throwing — never a $0/NaN phantom (Phase 15 lineage)"
    - "quote.ts is unreferenced by App.tsx this phase (additive; imported only by its new test) — App keeps its current total assembly (SC5 strangler)"
  artifacts:
    - src/engine/quote.ts
    - src/engine/__tests__/quote.test.ts
    - "DRILLS_BASE_SHIPPING + RATES_AS_OF constants added to src/engine/checkout.ts"
  key_links:
    - "quote.ts consumes OrderSupplyPlan.optimizedCostCents (does NOT re-pack drills, D-06) + VENDOR_REGISTRY[vendor].baseShipping + DRILLS_BASE_SHIPPING, and routes ALL money through money.ts toCents/sumCents"
---

<objective>
Build the single integer-cents customer quote selector (`engine/quote.ts`) that composes drills + canvas + one combined shipping line + a $0 tax estimate into exactly one `OrderQuote` whose itemized line items always sum EXACTLY to the total (QUOTE-02). Every estimate carries a label and a dated "rates as of" provenance (QUOTE-03). It consumes the already-reconciled `planOrderSupply` drills cents (does not re-pack — D-06), the curated canvas base + shipping, all through `money.ts`.

Purpose: Phase 23's Supplies "Est. total" and the Order total both read this one selector, so they can never diverge — the `planOrderSupply` single-source pattern raised one level.
Output: `src/engine/checkout.ts` (add `DRILLS_BASE_SHIPPING`, `RATES_AS_OF`), `src/engine/quote.ts` (new selector), `src/engine/__tests__/quote.test.ts` (new).

This is ADDITIVE and MUST NOT be wired into App.tsx (Phase 23) — App keeps its current `totalCostSafetyCents` assembly untouched (SC5).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-additive-engine-density-color-reducer-single-source-quote/22-CONTEXT.md
@.planning/phases/22-additive-engine-density-color-reducer-single-source-quote/22-RESEARCH.md
@.planning/codebase/CONVENTIONS.md
</context>

<artifacts_produced>
NEW symbols this phase introduces (exclude from source-drift verification):
- `src/engine/quote.ts`: `buildOrderQuote`, `OrderQuote` (interface), `QuoteLineItem` (interface), `TAX_RATE_ESTIMATE`
- `src/engine/checkout.ts` (additive): `DRILLS_BASE_SHIPPING`, `RATES_AS_OF`
- `src/engine/__tests__/quote.test.ts`
(Sibling plans add density.ts, color.ts reducer, and the hook detectedColorCount fields.)
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Add DRILLS_BASE_SHIPPING + RATES_AS_OF constants to checkout.ts (D-08, QUOTE-03)</name>
  <read_first>
    - src/engine/checkout.ts:105-164 (VENDOR_REGISTRY with per-vendor baseShipping: lumaprints 4.99 / finerworks 5.50 — the curated rate layer the new constants sit beside)
    - .planning/codebase/CONVENTIONS.md (SCREAMING_SNAKE_CASE module constants; JSDoc on exported symbols; inline comments explain WHY)
  </read_first>
  <files>src/engine/checkout.ts</files>
  <action>
    In `src/engine/checkout.ts`, immediately beside `VENDOR_REGISTRY` (same curated cost layer), add two additive exported constants (D-08): `export const DRILLS_BASE_SHIPPING = 5.00;` with a JSDoc noting it is the curated flat drills-shipping estimate — Diamond Drills USA genuinely charges $5 flat (free over $30), so treating drills shipping as free would read more favorably than reality; it is summed with the canvas vendor's `baseShipping` into ONE combined "Shipping (est.)" line in quote.ts. And `export const RATES_AS_OF = '2026-07-14';` (ISO date string) with a JSDoc noting it is the "rates as of" provenance for the curated shipping + canvas rates (QUOTE-03), dated to the newer of the two curated shipping inputs; flipping to live rates later (v5.0) updates this one constant. Do NOT modify VENDOR_REGISTRY values, calculateCanvasCost, or any existing signature — these are purely additive exports (SC5).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/engine/checkout.ts` exports `DRILLS_BASE_SHIPPING` === `5.00` and `RATES_AS_OF` (an ISO date string)
    - VENDOR_REGISTRY, calculateCanvasCost, CanvasVendor, normalizeVendor signatures are byte-unchanged (git diff shows only additions)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Two additive curated-rate constants exist beside VENDOR_REGISTRY; no existing checkout signature changed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create engine/quote.ts — single integer-cents quote selector (QUOTE-02, QUOTE-03, D-06/07/08)</name>
  <read_first>
    - src/engine/money.ts (toCents throws on non-finite; sumCents exact integer sum; fromCents; formatUSD — ALL quote math routes through these, QUOTE-02)
    - src/engine/bagPlanner.ts:455-547 (OrderSupplyPlan interface + planOrderSupply — the reconciled drills half; quote consumes optimizedCostCents, never re-packs, D-06)
    - src/engine/checkout.ts:105-218 (VENDOR_REGISTRY[vendor].baseShipping, calculateCanvasCost returns number|null, CanvasVendor union, and the new DRILLS_BASE_SHIPPING/RATES_AS_OF from Task 1)
    - src/App.tsx:1143-1148 (current totalCostSafetyCents assembly — what quote.ts SUPERSEDES in Phase 23; note App has NO tax line today and uses a single canvasShippingEstimate — quote.ts deliberately differs and must NOT be wired in now, SC5)
    - .planning/phases/22-additive-engine-density-color-reducer-single-source-quote/22-RESEARCH.md (Pattern 2 proposed interface + Pitfall 4 nullable canvas cost)
  </read_first>
  <files>src/engine/quote.ts</files>
  <behavior>
    - buildOrderQuote({ supplyPlan, canvasBaseCost, vendor }) returns { lineItems, totalCents, ratesAsOf, canvasPriced }
    - totalCents === sumCents(lineItems.map(li => li.cents)) for every fixture (QUOTE-02 line-sum equality)
    - drills line cents === supplyPlan.optimizedCostCents (consumed, not recomputed)
    - shipping line cents === toCents(VENDOR_REGISTRY[vendor].baseShipping + DRILLS_BASE_SHIPPING); labeled "Shipping (est.)"; note === `rates as of ${RATES_AS_OF}`; estimate true
    - tax line cents === 0; labeled "Tax"; note "calculated at vendor checkout"; estimate true
    - canvasBaseCost null OR non-finite → canvasPriced false, canvas line 0 cents, NO throw; total still equals the line-sum
    - canvasBaseCost finite → canvas line cents === toCents(canvasBaseCost); canvasPriced true; note `rates as of ${RATES_AS_OF}`
    - a .005 dollar boundary fixture confirms integer cents only (toCents half-up) — no floats leak into totalCents
  </behavior>
  <action>
    Create `src/engine/quote.ts` as a pure engine module (named exports; no Preact/DOM). Import `toCents, sumCents` from `./money`; `OrderSupplyPlan` type from `./bagPlanner`; `CanvasVendor, VENDOR_REGISTRY, DRILLS_BASE_SHIPPING, RATES_AS_OF` from `./checkout`. Export `const TAX_RATE_ESTIMATE = 0;` (D-07 — a single knob so a future live/estimated rate is a one-line change; the label attaches, not a percentage). Export `interface QuoteLineItem { key: 'drills' | 'canvas' | 'shipping' | 'tax'; label: string; cents: number; estimate: boolean; note?: string; }` and `interface OrderQuote { lineItems: QuoteLineItem[]; totalCents: number; ratesAsOf: string; canvasPriced: boolean; }`. Export `buildOrderQuote(input: { supplyPlan: OrderSupplyPlan; canvasBaseCost: number | null; vendor: CanvasVendor }): OrderQuote`. Implementation (all integer cents): drillsCents = `input.supplyPlan.optimizedCostCents` (already reconciled integer cents — consume, do NOT call packColor, D-06); canvasPriced = `input.canvasBaseCost != null && Number.isFinite(input.canvasBaseCost)`; canvasCents = canvasPriced ? `toCents(input.canvasBaseCost)` : 0 (Pitfall 4 — a null/non-finite canvas cost must be SURFACED via canvasPriced, never silently thrown into toCents which throws on non-finite, and never a silent $0 line); shippingCents = `toCents(VENDOR_REGISTRY[input.vendor].baseShipping + DRILLS_BASE_SHIPPING)` (one combined line, D-08); taxCents = `toCents((drillsCents + canvasCents + shippingCents) / 100 * TAX_RATE_ESTIMATE)` which is 0 while TAX_RATE_ESTIMATE=0 but keeps the tax line routed through money.ts (D-07, QUOTE-02 line-sum holds). Build lineItems in the order drills, canvas, shipping, tax with the exact labels/notes/estimate flags from the behavior block (drills estimate false; canvas/shipping/tax estimate true; canvas + shipping note `rates as of ${RATES_AS_OF}`; tax note 'calculated at vendor checkout'). Return `{ lineItems, totalCents: sumCents(lineItems.map(li => li.cents)), ratesAsOf: RATES_AS_OF, canvasPriced }` so totalCents equals the line-sum BY CONSTRUCTION. Do NOT reference or import this selector from App.tsx (SC5 — additive, unused until Phase 23).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/engine/quote.ts` exports `buildOrderQuote`, `OrderQuote`, `QuoteLineItem`, `TAX_RATE_ESTIMATE`
    - buildOrderQuote reads `supplyPlan.optimizedCostCents` and contains NO call to `packColor`/`planColorSupply` (grep-clean of drill re-packing)
    - all four line cents flow through `toCents`; the total is `sumCents(lineItems.map(...))` (grep confirms no raw `+` float total)
    - App.tsx does not import from `./engine/quote` (grep-clean — additive this phase)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>quote.ts compiles; single integer-cents selector composes drills+canvas+combined-shipping+$0-tax; total equals line-sum by construction; unreferenced by App.</done>
</task>

<task type="auto">
  <name>Task 3: Create quote.test.ts — line-sum equality + estimate labels + null-canvas guard (QUOTE-02/03)</name>
  <read_first>
    - src/engine/quote.ts (module under test — Task 2)
    - src/engine/__tests__/bagPlanner.test.ts (how OrderSupplyPlan fixtures are shaped; how integer-cents assertions read)
    - src/engine/checkout.ts:127-152 (VENDOR_REGISTRY baseShipping values to assert shipping math against)
    - .planning/codebase/TESTING.md (node env, inline fixtures, exact toEqual for structural results)
  </read_first>
  <files>src/engine/__tests__/quote.test.ts</files>
  <action>
    Create `src/engine/__tests__/quote.test.ts` (`import { describe, it, expect } from 'vitest'`; import `buildOrderQuote, TAX_RATE_ESTIMATE` from `../quote`; import `sumCents` from `../money`; import `VENDOR_REGISTRY, DRILLS_BASE_SHIPPING, RATES_AS_OF` from `../checkout`). Build a minimal inline `OrderSupplyPlan` fixture (only the fields buildOrderQuote reads must be real — set `optimizedCostCents` to a known integer like 1234; other required OrderSupplyPlan fields can be zero/empty to satisfy the type). Add `describe('buildOrderQuote (QUOTE-02/03)')` with `it` cases: (1) LINE-SUM EQUALITY — across several fixtures (varying optimizedCostCents, both vendors, priced + null canvas) assert `quote.totalCents === sumCents(quote.lineItems.map(li => li.cents))`; (2) TAX (D-07) — the `tax` line item is `0` cents, label 'Tax', note 'calculated at vendor checkout', and `TAX_RATE_ESTIMATE === 0`; (3) COMBINED SHIPPING (D-08) — the `shipping` line cents equals the integer cents of `VENDOR_REGISTRY[vendor].baseShipping + DRILLS_BASE_SHIPPING` for lumaprints (4.99+5.00) and finerworks (5.50+5.00), label 'Shipping (est.)', note contains `RATES_AS_OF`; (4) NULL CANVAS (Pitfall 4) — `canvasBaseCost: null` → `canvasPriced === false`, canvas line `0` cents, NO throw, total still equals line-sum; also a non-finite (Infinity) canvasBaseCost behaves the same; (5) DRILLS PASS-THROUGH — the drills line cents equals the fixture's `optimizedCostCents`; (6) INTEGER-CENTS BOUNDARY — a fixture exercising a `.005`-boundary canvas cost confirms `toCents` half-up (no float leak). Use exact `toBe`/`toEqual` (integer/structural), not `toBeCloseTo`.
  </action>
  <verify>
    <automated>npx vitest run src/engine/__tests__/quote.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/engine/__tests__/quote.test.ts` exits 0
    - a test asserts `totalCents === sumCents(lineItems.map(li => li.cents))` across multiple fixtures (QUOTE-02)
    - a test asserts the tax line is 0 cents + 'calculated at vendor checkout' (D-07) and shipping is the combined baseShipping+DRILLS_BASE_SHIPPING with a RATES_AS_OF note (D-08)
    - a test asserts null/non-finite canvasBaseCost → canvasPriced false, 0-cent canvas line, no throw
  </acceptance_criteria>
  <done>quote.test.ts passes; line-sum equality, tax/shipping labeling + provenance, and the null-canvas guard are all proven.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| curated rate/plan inputs → money math | canvasBaseCost (nullable), supplyPlan.optimizedCostCents, and vendor cross into integer-cents composition; a tampered/restored price or an out-of-union vendor must not white-screen or emit a phantom |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22-Q1 | DoS (render path) | buildOrderQuote canvas cost | medium | mitigate | canvasBaseCost null/non-finite is guarded to canvasCents=0 + canvasPriced=false BEFORE toCents (which throws on non-finite) — never white-screens; covered by the null-canvas test. |
| T-22-Q2 | Tampering (data integrity) | line-item vs total divergence | medium | mitigate | totalCents === sumCents(lineItems) by construction; all lines via money.ts toCents; tax/canvas/shipping never a silent $0 phantom (canvasPriced surfaces a bad price, Phase 15 lineage). Line-sum-equality test enforces it. |
| T-22-Q3 | Tampering (supply chain) | package.json | low | mitigate | Zero new dependencies (composition of money.ts/bagPlanner/checkout). Review gate: no new package.json dependency. |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0.
- `npx vitest run src/engine/__tests__/quote.test.ts` exits 0.
- `npm test` exits 0; total test count only grows vs baseline (SC5).
- `git grep -n "engine/quote" src/App.tsx` returns nothing (additive; not wired into App this phase).
- `git diff` shows checkout.ts changes are additive only (VENDOR_REGISTRY / calculateCanvasCost unchanged).
- No new dependency in package.json.
</verification>

<success_criteria>
- The customer quote is computed in exactly one place in integer cents; Supplies "Est. total", Order total, and the itemized line-item sum are equal by construction (QUOTE-02).
- Tax is $0 labeled "calculated at vendor checkout"; shipping is one combined "Shipping (est.)" line; both carry a dated "rates as of" provenance — no figure reads as finalized (QUOTE-03, D-07/D-08).
- quote.ts is additive and unreferenced by App.tsx; the full suite stays green (SC5).
</success_criteria>

<output>
Create `.planning/phases/22-additive-engine-density-color-reducer-single-source-quote/22-02-SUMMARY.md` when done.
</output>
