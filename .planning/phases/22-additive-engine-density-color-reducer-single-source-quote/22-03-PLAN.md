---
phase: 22-additive-engine-density-color-reducer-single-source-quote
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/engine/color.ts
  - src/engine/__tests__/color.test.ts
autonomous: true
requirements: [REFINE-04]
must_haves:
  truths:
    - "reduceToColorCount is deterministic: the same input yields identical output, AND shuffling the input counts key order yields identical output (no Object.key-order dependence — D-02)"
    - "equidistant surviving shades break the tie on the LOWEST DMC code via a numeric-then-lexical comparator (handles '310' vs 'B5200'); ties decided on EXACT distance equality, no epsilon band (D-02)"
    - "the Delta-E guard is an absolute veto: a rare color with no within-guard neighbor is SKIPPED and the next-rarest tried; targetN is a CEILING so mergedCount may legitimately exceed targetN (D-03)"
    - "targetN >= detected distinct count returns grid + counts unchanged (no-op), and a degenerate/empty grid never throws (degrade-not-crash)"
    - "the max original→final per-cell CIEDE2000 shift is <= MERGE_GUARD_DELTA_E — the 'no visible change' contract bound (D-01 mitigation)"
    - "reduceToColorCount reuses getColorDistance (CIEDE2000, same weights as matchColor) and merges the globally-rarest surviving color into the nearest already-used shade (D-01)"
  artifacts:
    - "reduceToColorCount + MERGE_GUARD_DELTA_E + compareDmcCode exported from src/engine/color.ts"
    - "reducer describe block added to src/engine/__tests__/color.test.ts"
  key_links:
    - "reduceToColorCount is the target-N reshape of substituteLowCountColors (same { codes, counts } shape) but with a stable total order — Phase 23's color slider and the hook (22-04) feed one merged count to viewer/legend/cart/quote"
---

<objective>
Land the pure, deterministic, Delta-E-guarded target-N color reducer `reduceToColorCount` in `engine/color.ts` (beside `substituteLowCountColors` + `getColorDistance`), plus its full test suite. It merges the globally-rarest surviving color into the CIEDE2000-nearest already-used shade with a stable lowest-DMC-code tie-break, recomputing counts each pass until distinct === targetN or the guard blocks all further merges (D-01/D-02/D-03).

Purpose: this is the engine keystone REFINE-04 needs — one authoritative merged count fed to viewer, legend, cart, and quote. Phase 22 lands the pure fn + tests as an isolated engine-only commit; the hook exposure + gated pipeline step is 22-04 (Wave 2); the UI slider is Phase 23.
Output: `reduceToColorCount` + `MERGE_GUARD_DELTA_E` + `compareDmcCode` in `src/engine/color.ts`; a reducer describe block in `src/engine/__tests__/color.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-additive-engine-density-color-reducer-single-source-quote/22-CONTEXT.md
@.planning/phases/22-additive-engine-density-color-reducer-single-source-quote/22-RESEARCH.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
</context>

<artifacts_produced>
NEW symbols this phase introduces (exclude from source-drift verification):
- `src/engine/color.ts` (additive): `reduceToColorCount`, `MERGE_GUARD_DELTA_E`, `compareDmcCode`
- reducer describe block in `src/engine/__tests__/color.test.ts`
(Sibling plans add density.ts, quote.ts/checkout consts, and the hook detectedColorCount/enableReduce/targetColorCount fields.)
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add reduceToColorCount + MERGE_GUARD_DELTA_E + compareDmcCode to color.ts (D-01/D-02/D-03)</name>
  <read_first>
    - src/engine/color.ts:125-195 (getColorDistance CIEDE2000 wrapper the reducer reuses; substituteLowCountColors — the REFERENCE shape { codes, counts } to mirror, and the exact Object.keys/highCountCodes[0] non-determinism at :168-179 to AVOID)
    - src/engine/types.ts (DmcColor { dmc, name, hex, r, g, b, lab, kits }, LabCoordinates { l, a, b })
    - src/App.tsx:1111-1118 (the numeric-then-lexical code comparator to REUSE: parseInt both; if both numeric compare numerically, else localeCompare — Pitfall 2, DMC codes are not all numeric)
    - .planning/phases/22-additive-engine-density-color-reducer-single-source-quote/22-RESEARCH.md (Pattern 1 proposed signature + Determinism Details + Pitfalls 1/2/3)
  </read_first>
  <files>src/engine/color.ts</files>
  <behavior>
    - reduceToColorCount(gridCodes, counts, activeCandidates, targetN, guard=MERGE_GUARD_DELTA_E) → { codes, counts, mergedCount }
    - DETERMINISM: identical inputs → identical output; passing counts whose keys are inserted in a shuffled order → identical output (sort by comparator before selecting, never rely on Object.keys order)
    - TIE-BREAK: two surviving shades equidistant from the rare color → the one with the LOWER DMC code (compareDmcCode) absorbs; a '310' vs 'B5200' equidistant pair resolves to '310'; distance ties are EXACT (===), no epsilon
    - GUARD VETO: if the rarest color's nearest surviving neighbor distance > guard, that color is skipped and the next-rarest is tried; when EVERY surviving color's nearest neighbor is beyond guard, the loop stops
    - CEILING: mergedCount >= targetN always; equals targetN when merges are available; EXCEEDS targetN when the guard blocks the remainder (this is correct, not a bug — D-03)
    - NO-OP: surviving distinct <= targetN → returns { codes: copy of gridCodes, counts: copy, mergedCount: surviving } unchanged
    - DEGRADE: empty grid / empty counts / codes missing from candidates never throw (skip un-mappable codes; treat as blocked/surviving)
    - NEAREST-USED: the absorbing shade is always an ALREADY-USED (surviving) shade selected by getColorDistance over the rare color's lab
  </behavior>
  <action>
    In `src/engine/color.ts`, add (all additive — do NOT modify any existing signature, SC5):
    (a) `export const MERGE_GUARD_DELTA_E = 10;` with a JSDoc: a fixed-conservative CIEDE2000 absolute merge veto (D-03); PROVISIONAL value — the mechanics are locked but the number is tunable in REFINE-06 (v4.x); the no-visible-change bound test caps worst-case merge shift at exactly this value. Do NOT make it a runtime-tunable UI knob here.
    (b) `export function compareDmcCode(a: string, b: string): number` — the numeric-then-lexical total order reused from App.tsx:1112-1118: parseInt(a,10)/parseInt(b,10); if both are numbers (not NaN) return numA-numB, else return a.localeCompare(b). This is the stable tie-break basis (D-02) and is exported so the tie-break test asserts it directly.
    (c) `export function reduceToColorCount(gridCodes: string[], counts: Record<string, number>, activeCandidates: DmcColor[], targetN: number, guard: number = MERGE_GUARD_DELTA_E): { codes: string[]; counts: Record<string, number>; mergedCount: number }`. Algorithm (mirror substituteLowCountColors' { codes, counts } shape but with a STABLE total order — the pure engine, no Preact/DOM):
      1. Build a code→DmcColor map from activeCandidates (skip/ignore any grid code not present in candidates — it cannot be distance-computed; treat it as permanently surviving/blocked, degrade-not-crash).
      2. surviving = the set of codes with counts[code] > 0. If surviving.length <= targetN (or targetN < 1 is coerced to a sane floor of 1), return { codes: [...gridCodes], counts: { ...counts }, mergedCount: surviving.length } (no-op, reference-safe).
      3. Maintain a working counts object and a merge map (rare code → absorbing code). Loop:
         a. Build the sorted surviving list ordered by compareDmcCode ascending (sort ONCE per iteration over the tens–low-hundreds entries — never let Object.keys order decide anything, Pitfall 1).
         b. rarest = the surviving color with the minimum working count; on equal counts the lower dmcCode wins (i.e. iterate the compareDmcCode-sorted list and pick the first achieving the min count). Skip colors already marked blocked this run.
         c. Among the OTHER surviving shades (exclude the rare one and any un-mappable), find the nearest by getColorDistance(rare.lab, s.lab) under the total order (distance ascending, then compareDmcCode ascending) using strict `<` for distance and an EXPLICIT compareDmcCode tie-break on EXACT-equal distance (no epsilon — Pitfall/D-02). If the rare color has no mappable other surviving shade, mark it blocked.
         d. If the best nearest distance > guard, mark the rare color BLOCKED for this run and continue to the next-rarest non-blocked color (absolute veto, skip-and-continue — D-03; NEVER merge-anyway).
         e. Otherwise merge: record rare→nearest in the merge map, remove rare from surviving, add rare's count into the nearest's working count, and recompute the surviving set.
         f. Stop when surviving.length === targetN OR every surviving color is blocked.
      4. Apply the accumulated merge map to gridCodes (follow chains so a cell whose color was merged into a shade that was later itself merged resolves to the final surviving code), producing newCodes; recompute newCounts by tallying newCodes.
      5. Return { codes: newCodes, counts: newCounts, mergedCount: (distinct codes with count>0 in newCounts) }.
    Return a NEW codes array + counts object (never mutate inputs). Reuse getColorDistance (do not re-implement CIEDE2000). Do NOT introduce a count-based tie-break (counts mutate → non-determinism, Pitfall 1) and do NOT use an epsilon distance band.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/engine/color.ts` exports `reduceToColorCount`, `MERGE_GUARD_DELTA_E` (=== 10), and `compareDmcCode`
    - reduceToColorCount reuses `getColorDistance` (grep confirms no second CIEDE2000/differenceCiede2000 impl inside the fn) and returns `{ codes, counts, mergedCount }`
    - the fn contains no epsilon distance band and no count-based tie-break (tie-break is compareDmcCode only)
    - existing exports (matchColor, matchPixelGrid, substituteLowCountColors, getColorDistance) are byte-unchanged (git diff additive only)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>reduceToColorCount + MERGE_GUARD_DELTA_E + compareDmcCode compile as additive exports; iterative-recompute + absolute-veto + stable tie-break implemented; inputs never mutated.</done>
</task>

<task type="auto">
  <name>Task 2: Add the reducer test suite to color.test.ts (determinism, tie-break, guard, ceiling, no-op, no-visible-change bound)</name>
  <read_first>
    - src/engine/color.ts (reduceToColorCount/MERGE_GUARD_DELTA_E/compareDmcCode — Task 1)
    - src/engine/__tests__/color.test.ts (existing structure: inline mockCandidates: DmcColor[] with real lab coords, beforeEach clearCache(), nested describe, node env)
    - .planning/phases/22-additive-engine-density-color-reducer-single-source-quote/22-RESEARCH.md (Test Strategy → color.test.ts reducer cases; Pitfalls 1/2/3)
    - .planning/codebase/TESTING.md (inline fully-shaped DmcColor fixtures; toEqual exact / toBeCloseTo for float)
  </read_first>
  <files>src/engine/__tests__/color.test.ts</files>
  <action>
    Extend `src/engine/__tests__/color.test.ts` with a `describe('reduceToColorCount (SC4 / REFINE-04 support)')` block. Build inline fully-shaped `DmcColor` fixtures with real, deliberately-spaced `lab` coordinates so distances are predictable (include at least: two near-identical shades within MERGE_GUARD_DELTA_E of a rare color, one far shade beyond the guard, and a numeric+named code pair such as '310' and 'B5200' placed equidistant from a rare color for the tie-break case). Add `it` cases: (1) DETERMINISM — same input twice → `toEqual` identical output; AND build the same `counts` with keys inserted in a shuffled order and assert the output `toEqual`s the canonical run (guards Object-key-order, Pitfall 1); (2) TIE-BREAK — an equidistant '310' vs 'B5200' pair → the rare color merges into '310' (lower DMC code via compareDmcCode); also a direct `compareDmcCode('310','B5200')` / `compareDmcCode('310','40')` assertion proving numeric-then-lexical order (Pitfall 2); (3) GUARD VETO — a rare color whose only neighbors are beyond MERGE_GUARD_DELTA_E is SKIPPED while a within-guard rare color merges; when all remaining are beyond guard the loop stops; (4) TARGET CEILING — `mergedCount >= targetN` always; equals targetN when merges are available; EXCEEDS targetN when the guard blocks the rest (assert a fixture where mergedCount > targetN, Pitfall 3) — do NOT assert `mergedCount === targetN` unconditionally; (5) NO-OP — `targetN >= distinct count` returns grid + counts unchanged (`toEqual` the input, grid length preserved); (6) NO-VISIBLE-CHANGE BOUND (D-01 mitigation) — after reduction, for every grid cell compute getColorDistance(originalColor.lab, finalColor.lab) and assert the MAX is `<= MERGE_GUARD_DELTA_E`; (7) PURITY — output grid length === input grid length, and no code appears in the output that was not in the input candidate set (no invented colors), and the input arrays/objects are not mutated. Use `toEqual`/`toBe` for structural/integer results and `toBeCloseTo`/`<=` comparisons for CIEDE2000 distances.
  </action>
  <verify>
    <automated>npx vitest run src/engine/__tests__/color.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/engine/__tests__/color.test.ts` exits 0
    - the suite includes a shuffled-key-order determinism case, an equidistant '310' vs 'B5200' tie-break case, a guard-veto skip case, a targetN-exceeded ceiling case, a no-op case, and a max-shift <= MERGE_GUARD_DELTA_E bound case
    - the suite asserts input arrays/objects are not mutated and grid length is preserved
  </acceptance_criteria>
  <done>The reducer describe block passes; determinism, tie-break, guard veto, target ceiling, no-op, and no-visible-change bound are all proven.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| post-match grid/counts + targetN → reducer | gridCodes/counts come from the matcher; targetN comes from a (future) UI slider; a degenerate grid or an out-of-range/non-finite targetN must not throw or loop forever in the render path |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22-R1 | Tampering (integrity) | non-deterministic merge → legend/cart/quote disagree | medium | mitigate | Total order on immutable dmcCode (compareDmcCode), EXACT distance ties (no epsilon), fixed CIEDE2000 weights via getColorDistance; sort surviving list before every selection (never Object.keys order). Shuffled-key determinism test enforces it (D-02). |
| T-22-R2 | DoS (render path) | reducer on degenerate grid / bad targetN | medium | mitigate | Degrade-not-crash: surviving <= targetN (or targetN coerced to floor 1) returns input unchanged; un-mappable codes are skipped not thrown; the loop terminates because each merge strictly shrinks the surviving set and blocked colors are never retried (mirrors smoothMatches/packColor). |
| T-22-R3 | Tampering (supply chain) | package.json | low | mitigate | Zero new dependencies (reuses culori via getColorDistance). Review gate: no new package.json dependency. |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0 (strict; noUnusedLocals/noUnusedParameters).
- `npx vitest run src/engine/__tests__/color.test.ts` exits 0.
- `npm test` exits 0; total test count only grows vs baseline (SC5).
- `git diff src/engine/color.ts` shows additive changes only (no existing export modified).
- No new dependency in package.json.
</verification>

<success_criteria>
- `reduceToColorCount` is deterministic (stable DMC-code tie-break, exact ties, fixed weights), an absolute-veto guard with skip-and-continue, and a target ceiling (mergedCount may exceed targetN) — SC4 engine support.
- The no-visible-change bound (max original→final CIEDE2000 shift <= MERGE_GUARD_DELTA_E) is proven by test (D-01 mitigation).
- All changes are additive to color.ts; the full suite stays green (SC5).
</success_criteria>

<output>
Create `.planning/phases/22-additive-engine-density-color-reducer-single-source-quote/22-03-SUMMARY.md` when done.
</output>
