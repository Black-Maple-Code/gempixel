---
phase: 22-additive-engine-density-color-reducer-single-source-quote
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/engine/density.ts
  - src/engine/__tests__/density.test.ts
  - src/engine/export.ts
  - src/engine/viewer.ts
autonomous: true
requirements: [QUOTE-01]
must_haves:
  truths:
    - "gridToInches(120,160) returns { widthIn: 12, heightIn: 16 } — every inch figure derives from grid dims through one 2.5mm/dot (10 dots/inch) source (QUOTE-01)"
    - "gridToInches(cols,rows).widthIn === cols/10 reconciles EXACTLY with calculateCanvasCost's grid→/10 conversion — a single density source, no second constant (10.16)"
    - "formatInches(n) rounds to 1 decimal place and returns a string (matches App.tsx fmt precedent)"
    - "the engine phase-marker stale comments are removed from export.ts/viewer.ts and the full Vitest suite stays green (SC5)"
  artifacts:
    - src/engine/density.ts
    - src/engine/__tests__/density.test.ts
  key_links:
    - "density.ts DOTS_PER_INCH=10 is the same divisor calculateCanvasCost uses (width/10, checkout.ts:184-190); the reconciliation test binds them so canvas cost and displayed size never disagree"
---

<objective>
Create the single 2.5mm/dot density source (QUOTE-01): a pure `engine/density.ts` that maps grid dimensions (cols/rows) to physical inches at 10 dots/inch, reconciled EXACTLY with the existing `calculateCanvasCost` grid→/10 precedent so there is one — and only one — density path. Also close the engine-side stale phase-marker comments left by Phase 20's theme retirement.

Purpose: Phase 23's SizeCards, canvas cost, and any inch label all read this one helper, so no hard-coded mock inch label can survive and no second density constant can fork the truth.
Output: `src/engine/density.ts` (new) + `src/engine/__tests__/density.test.ts` (new); two stale engine comment markers deleted.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-additive-engine-density-color-reducer-single-source-quote/22-CONTEXT.md
@.planning/phases/22-additive-engine-density-color-reducer-single-source-quote/22-RESEARCH.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
</context>

<artifacts_produced>
NEW symbols this phase introduces (exclude from source-drift verification — they do not exist pre-phase):
- `src/engine/density.ts`: `DOTS_PER_INCH`, `MM_PER_DOT`, `gridToInches`, `formatInches`
- `src/engine/__tests__/density.test.ts`
(Sibling plans add: `reduceToColorCount`/`MERGE_GUARD_DELTA_E`/`compareDmcCode` in color.ts, `buildOrderQuote`/`OrderQuote`/`QuoteLineItem`/`TAX_RATE_ESTIMATE` in quote.ts, `DRILLS_BASE_SHIPPING`/`RATES_AS_OF` in checkout.ts, `detectedColorCount`/`enableReduce`/`targetColorCount` in useDiamondArtMatch.ts.)
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create engine/density.ts — the single 2.5mm/dot density source (QUOTE-01)</name>
  <read_first>
    - src/engine/checkout.ts:172-218 (calculateCanvasCost — the existing implicit density: `unit === 'grid'` divides width/10, height/10; the new helper must reconcile EXACTLY)
    - src/App.tsx:273-278 (the current inline `cols / 10` density + `fmt = (n) => (Math.round(n * 10) / 10).toString()` precedent the helper standardizes)
    - src/engine/smoothing.ts:1-28 (pure-engine module + JSDoc shape to mirror: named exports, no Preact/DOM)
    - .planning/codebase/CONVENTIONS.md (SCREAMING_SNAKE_CASE constants, camelCase verb-first fns, named exports only)
  </read_first>
  <files>src/engine/density.ts</files>
  <behavior>
    - gridToInches(120, 160) → { widthIn: 12, heightIn: 16 }
    - gridToInches(cols, rows).widthIn === cols / 10 for any finite cols (reconciles with calculateCanvasCost grid→/10)
    - gridToInches with a non-finite cols/rows (NaN/Infinity from tampered dims) degrades to 0 for that axis, never NaN/throw
    - formatInches(12) → "12"; formatInches(15.44) → "15.4"; formatInches(15.45) → "15.5" (1-dp round-half-up via Math.round)
    - DOTS_PER_INCH === 10 and MM_PER_DOT === 2.5 are exported constants
  </behavior>
  <action>
    Create `src/engine/density.ts` as a pure engine module (no Preact/DOM/persistence; named exports only; JSDoc on each export per CONVENTIONS.md). Export const `DOTS_PER_INCH = 10` and const `MM_PER_DOT = 2.5`. Add a JSDoc note on `DOTS_PER_INCH` explaining that 2.5mm/dot is exactly 10.16 dots/inch, deliberately rounded to 10 to stay identical to the app's long-standing `/10` convention (checkout.ts calculateCanvasCost width/10, App.tsx cols/10) — this is the single reconciled density source; do NOT "correct" it to 10.16 or a second density path forks (QUOTE-01). Export `gridToInches(cols: number, rows: number): { widthIn: number; heightIn: number }` returning `{ widthIn: cols / DOTS_PER_INCH, heightIn: rows / DOTS_PER_INCH }`, but guard each axis with a finite check (a non-finite input degrades to 0, never NaN — mirrors the fail-soft render-path lineage) while returning `cols / DOTS_PER_INCH` unchanged for all finite inputs so the value stays byte-identical to calculateCanvasCost's `width / 10`. Export `formatInches(inches: number): string` returning `(Math.round(inches * 10) / 10).toString()` (1-dp display string, matching the App.tsx:276 fmt). Return a plain number from gridToInches (callers do math on the number; formatInches produces the label) — do not fold formatting into gridToInches. Do NOT introduce any second density constant (e.g. 10.16) or a `cm`/`inch` branch here — this helper is grid→inch only (QUOTE-01, one source).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/engine/density.ts` exists and exports `DOTS_PER_INCH`, `MM_PER_DOT`, `gridToInches`, `formatInches`
    - `DOTS_PER_INCH === 10` and `MM_PER_DOT === 2.5` (no 10.16 constant anywhere in the file)
    - `gridToInches` returns a plain `{ widthIn: number; heightIn: number }` (no formatting inside it)
    - `npx tsc --noEmit` exits 0
    - the file contains no `import` from `preact`, no DOM reference, no `cm`/`inch` conversion branch
  </acceptance_criteria>
  <done>density.ts compiles; gridToInches/formatInches/constants exported; single grid→inch source with no second density path.</done>
</task>

<task type="auto">
  <name>Task 2: Create density.test.ts — reconciliation with calculateCanvasCost (QUOTE-01)</name>
  <read_first>
    - src/engine/density.ts (the module under test — created in Task 1)
    - src/engine/checkout.ts:172-218 (calculateCanvasCost grid→/10; the reconciliation target)
    - src/engine/__tests__/smoothing.test.ts (co-located test conventions: `import { describe, it, expect }`, node env, nested describe, exact `toEqual` for deterministic structural results)
    - .planning/codebase/TESTING.md (Vitest, co-located __tests__, node env, `toBeCloseTo` for float / `toEqual` for exact)
  </read_first>
  <files>src/engine/__tests__/density.test.ts</files>
  <action>
    Create `src/engine/__tests__/density.test.ts` following the co-located Vitest convention (`import { describe, it, expect } from 'vitest'`; import `gridToInches`, `formatInches`, `DOTS_PER_INCH` from `../density`; import `calculateCanvasCost` from `../checkout`). Add a `describe('density helper (QUOTE-01)')` with these `it` cases: (1) `gridToInches(120,160)` deep-equals `{ widthIn: 12, heightIn: 16 }` via `toEqual`; (2) a reconciliation case — for a grid like 200x280, assert `gridToInches(200,280).widthIn === 200 / DOTS_PER_INCH` and `.heightIn === 280 / DOTS_PER_INCH`, AND that the derived area `widthIn * heightIn` equals the internal grid→inch area calculateCanvasCost uses for the same grid (reconstruct `(200/10)*(280/10)` and confirm it matches — the point is one density source, so name in a comment that both derive from /10); (3) a non-finite guard case — `gridToInches(NaN, 160).widthIn` is `0` (not NaN) and does not throw; (4) `formatInches` rounding — `formatInches(12)` is `"12"`, `formatInches(15.44)` is `"15.4"`, `formatInches(15.45)` is `"15.5"`. Use exact `toEqual`/`toBe` for these integer/structural results (not `toBeCloseTo`).
  </action>
  <verify>
    <automated>npx vitest run src/engine/__tests__/density.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/engine/__tests__/density.test.ts` exits 0
    - the suite asserts `gridToInches(120,160)` equals `{ widthIn: 12, heightIn: 16 }`
    - the suite asserts `gridToInches(cols,rows).widthIn === cols / DOTS_PER_INCH` (reconciliation with the /10 density calculateCanvasCost uses)
    - the suite covers the non-finite guard (NaN → 0, no throw) and formatInches 1-dp rounding
  </acceptance_criteria>
  <done>density.test.ts passes; reconciliation with calculateCanvasCost's /10 is asserted; single density source proven.</done>
</task>

<task type="auto">
  <name>Task 3: Delete the stale engine theme-param comment markers (SC5 cleanup)</name>
  <read_first>
    - src/engine/export.ts:80-91 (first marker sits above `ctx.font = \`bold ${symbolFontPx(...)}...\``)
    - src/engine/export.ts:170-178 (second marker, same pattern)
    - src/engine/viewer.ts:388-396 (third marker, same pattern)
    - src/engine/symbols.ts:145 (symbolFontPx(basePx, symbol) — takes NO theme param; confirms the markers are stale)
  </read_first>
  <files>src/engine/export.ts, src/engine/viewer.ts</files>
  <action>
    Delete the two stale single-line comment markers in `src/engine/export.ts` (at ~line 85 and ~line 174) and the one in `src/engine/viewer.ts` (at ~line 392) that flag the already-completed Phase 20 theme-param removal. Each marker sits directly above a `ctx.font = \`bold ${symbolFontPx(...)}...\`` line, and `symbolFontPx(basePx, symbol)` (symbols.ts:145) takes NO `theme` argument — so this is a comment-only deletion with zero signature or behavior change (honors the strangler rule: engine-only, App.tsx left untouched — its marker at App.tsx:627 is a UI-file concern deferred to Phase 23/25). BEFORE deleting each line, confirm the following `ctx.font`/`symbolFontPx(...)` call passes only two arguments (basePx expression + symbol) and NO `theme` argument; if any call actually still passes a `theme` argument, STOP and leave that marker in place (defensive — do not force churn). Delete only the comment lines; do not touch the `ctx.font` lines or any surrounding code.
    <!-- planner-discipline-allow: PHASE 22: remove theme param -->
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "remove theme param" src/engine/export.ts src/engine/viewer.ts` returns no matches (both engine markers gone)
    - no `ctx.font`/`symbolFontPx(...)` call line was altered (grep confirms `symbolFontPx(` call sites still present and unchanged in count)
    - `npx tsc --noEmit` exits 0
    - `npm test` exits 0 (comment-only deletion keeps the full suite green)
  </acceptance_criteria>
  <done>The two engine stale phase-marker comments are removed; tsc + full suite green; no code/signature change.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| grid dims → density math | cols/rows originate from validated size controls, but a tampered/restored project could carry a non-finite dimension into gridToInches |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22-D1 | Tampering (data integrity) | gridToInches(cols,rows) | low | mitigate | Finite-guard each axis (non-finite → 0, never NaN); finite inputs pass through as `cols/10` unchanged so reconciliation with calculateCanvasCost holds. Covered by the NaN-guard test case. |
| T-22-D2 | Tampering (supply chain) | package.json | low | mitigate | Zero new dependencies this phase (density.ts is browser-native math). Review gate: diff introduces no new package.json dependency. |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0 (strict mode: noUnusedLocals/noUnusedParameters/noImplicitReturns).
- `npx vitest run src/engine/__tests__/density.test.ts` exits 0.
- `npm test` exits 0 and the total test count only grows vs the phase-start baseline (SC5).
- `git diff --stat` shows only density.ts, density.test.ts, export.ts, viewer.ts touched — App.tsx untouched (strangler).
- No new dependency in package.json.
</verification>

<success_criteria>
- One density source exists: `gridToInches` derives inches from grid dims at 10 dots/inch and reconciles exactly with calculateCanvasCost's `/10` (QUOTE-01).
- No second density constant (10.16) or duplicate grid→inch path exists.
- The two engine phase-marker stale comments are gone (SC5 tidy); tsc + full suite green.
</success_criteria>

<output>
Create `.planning/phases/22-additive-engine-density-color-reducer-single-source-quote/22-01-SUMMARY.md` when done.
</output>
