---
phase: 26-interim-customer-fulfillment-canvas-png-packet-diamond-drill
plan: 04
type: execute
wave: 4
depends_on: [26-03]
files_modified:
  - src/App.tsx
  - src/__tests__/App.test.tsx
autonomous: true
requirements: [ORDER-04, ORDER-05]
must_haves:
  truths:
    - "The frame-scope error banners (match error + action error) render in the Atelier light warn-on-light recipe — the only user-visible remnant of the retired dark fulfillment path is cleaned (D-08)."
    - "After the re-token, no live (non-print) surface uses the dark-slate 900/950 background family, pure-white text, or the dark-rose banner background — the hard D-08 grep-gate passes (D-08)."
    - "The 240+ Vitest suite stays green and tsc is clean (SC5)."
  artifacts:
    - "src/App.tsx :: both frame-scope error banners re-tokened to `border border-warn text-warn bg-panel-2`"
    - "src/App.tsx :: Save Project Modal re-tokened to Atelier light (ids + behavior preserved)"
    - "src/App.tsx :: the stale frame-scope comment that named the deleted dark shell by its old utility class reworded"
    - "src/__tests__/App.test.tsx :: dark-shell signature guard updated to the post-re-token invariant"
  key_links:
    - "The re-tokened actionError banner is the single error surface for BOTH the re-homed canvas-download failures (ORDER-04) and the cart too-long/unmapped condition (ORDER-05, refactored in 26-03)."
  prohibitions:
    - statement: "No live (non-print) surface may use the dark-slate 900/950 background family, pure-white text, or the dark-rose banner background after this plan (hard D-08 grep-gate)."
      status: enforced
      verification: "grep -rnE 'bg-slate-9(00|50)|text-white|rose-950' over live source (src excluding __tests__ / *.test.*) returns ZERO matches."
    - statement: "Re-token only — the Save Project Modal keeps its ids (save-project-name-input / save-project-submit / save-project-cancel) and behavior; no functional change."
      status: enforced
      verification: "the save-modal ids are unchanged; App.test save/load tests stay green."
    - statement: "Print-mode CSS / @media print rules are out of scope for the grep-gate and are not modified (print-only, not live UI)."
      status: enforced
      verification: "git diff shows no change to index.css print rules; the grep-gate greps live source only."
    - statement: "No new npm dependency."
      status: enforced
      verification: "git diff --exit-code package.json package-lock.json clean."
---

<objective>
Fold in the D-08 cleanup (WR-04, `resolves_phase:26`): re-token the last dark-slate remnants
of the retired fulfillment path to the Atelier light system and enforce the hard grep-gate.
With the two coupled modals deleted in 26-03, the remaining live dark surfaces are the two
frame-scope error banners (the only *user-visible* remnant — they can show on a real
match/worker or download/cart error), the still-live Save Project Modal (a live surface the
grep-gate's "any live surface" wording also covers), and one stale comment that still names the
deleted dark shell by its old utility class.

Purpose: satisfy D-08's hard grep-gate — no dark-slate 900/950 background family, no pure-white
text, and no dark-rose banner background on ANY live (non-print) surface — so the strangler
close leaves a single, consistent Atelier light skin.

Planner note (discovery): D-08 enumerates the two modals + the two error banners, but its
grep-gate is worded "any live surface." The still-live **Save Project Modal** (`App.tsx`
~1853–1890) and a stale frame-scope **comment** (~1495) both carry the retired tokens, so this
plan re-tokens the Save Modal (ids/behavior preserved) and rewords the comment to make the hard
gate genuinely pass file-wide. This is the honest reading of "no … on any live surface," not a
scope expansion beyond D-08's stated gate.

Planner note (grep-gate reachability — enumeration verified): a planning-time run of the D-08
enumeration grep over live source —
`grep -rnE 'bg-slate-9(00|50)|text-white|rose-950' src --include='*.tsx' --include='*.ts' | grep -v '__tests__' | grep -v '\.test\.'`
— confirms EVERY current live occurrence is confined to two files: `src/App.tsx` (the two error
banners ~1506/1511, the stale comment ~1495, the Save Project Modal ~1855–1876, and the two
coupled modals ~1661–1746 / ~1757–1839) and `src/features/wizard/steps/Step3Canvas.tsx` (deleted
wholesale by 26-03). No live match exists anywhere else in `src`. Because 26-03 (this plan's
dependency) deletes `Step3Canvas.tsx` AND the two coupled modals, the only remnants 26-04 must
clean are the banners + Save Modal + comment — all in `src/App.tsx`, already in `files_modified`.
The hard gate is therefore achievable entirely within this plan's scope. Task 1 re-runs the
enumeration FIRST (before re-tokening); if 26-03 or any later change has introduced an out-of-scope
match, the executor widens `files_modified` (or records it) rather than hitting an unsatisfiable gate.

Output: re-tokened banners + Save Modal + reworded comment; an updated dark-shell render guard;
a passing grep-gate; green suite.
</objective>

<artifacts_this_phase_produces>
No NEW runtime symbols and no deletions of exports. Changes are token/class re-skins on
existing live surfaces plus a test-guard update. Surfaces touched:
- The two frame-scope error banners (match error + action error) in `src/App.tsx`.
- The Save Project Modal in `src/App.tsx` (class tokens only; ids preserved).
- The stale frame-scope comment in `src/App.tsx` (~1495).
- The dark-shell signature test in `src/__tests__/App.test.tsx` (~1358).
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-interim-customer-fulfillment-canvas-png-packet-diamond-drill/26-CONTEXT.md
@.planning/phases/26-interim-customer-fulfillment-canvas-png-packet-diamond-drill/26-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-token the error banners + Save Project Modal to Atelier light; reword the stale shell comment (D-08)</name>
  <files>src/App.tsx</files>
  <read_first>
    - src/App.tsx — read: the two frame-scope error banners (match-error ~1505–1509 and
      action-error ~1510–1522, including its dismiss button), the stale comment ~1495 that
      names the deleted dark shell by its old utility class, and the Save Project Modal
      ~1853–1890 (overlay scrim, panel, heading, helper text, the name `<input>` with id
      `save-project-name-input`, and the Save/Cancel buttons with ids `save-project-submit` /
      `save-project-cancel`).
    - .planning/phases/26-.../26-UI-SPEC.md — the Color section (accent reserved list; the warn
      color `--warn` #A85A11) and the D-08 grep-gate paragraph, which fixes the banner recipe as
      `border border-warn text-warn bg-panel-2`; and the Error-state copy row.
    - src/features/screens/OrderScreen.tsx — the Atelier light surface tokens to match
      (`bg-panel` / `bg-panel-2`, `text-ink`, `text-muted`, `text-faint`, `border-border`,
      `rounded-[var(--radius-card)]` / `rounded-[var(--radius-control)]`, focus `border-accent`).
  </read_first>
  <action>
    FIRST — enumerate before re-tokening (grep-gate reachability, W2): run the D-08 enumeration
    grep over live source —
    `grep -rnE 'bg-slate-9(00|50)|text-white|rose-950' src --include='*.tsx' --include='*.ts' | grep -v '__tests__' | grep -v '\.test\.'`
    — and confirm every match is confined to the surfaces this plan owns in `src/App.tsx` (the two
    frame-scope error banners, the Save Project Modal, and the stale ~1495 comment). Planning-time
    enumeration already verified this holds once 26-03 has deleted `Step3Canvas.tsx` + the two
    coupled modals (see the objective's reachability note). If ANY live match falls OUTSIDE those
    App.tsx surfaces (a new file, or a surface 26-03 did not delete), STOP and either widen this
    plan's `files_modified` to include that file and re-token it, or record it explicitly — do not
    leave the hard gate unsatisfiable. Then proceed with the re-token below.

    Re-token BOTH frame-scope error banners to the Atelier warn-on-light recipe from the
    UI-SPEC: `border border-warn text-warn bg-panel-2` (dark-amber caution text on a light
    panel), keeping their existing layout utilities (positioning, spacing, `no-print`,
    rounding) and, on the action-error banner, its dismiss button and behavior. Replace the
    retired dark banner background and light-on-dark text — the match-error banner keeps its
    "Couldn't process the image: …" message; the action-error banner keeps the App-provided
    message. Recolor the dismiss "×" control from the retired rose tone to a warn/muted tone.

    Re-token the Save Project Modal to Atelier light, changing ONLY class tokens (preserve the
    ids `save-project-name-input`, `save-project-submit`, `save-project-cancel` and all
    behavior): the panel becomes a light card (`bg-panel-2` / `bg-panel`, `border-border`,
    `rounded-[var(--radius-card)]`), the heading + body use `text-ink` / `text-muted` (no
    gradient-clip white heading — a plain `text-ink` semibold heading), the name input uses the
    Atelier input recipe (`border-border bg-panel … text-ink focus:border-accent`), the Save
    button uses the accent primary treatment (match the `Button variant="primary"` look —
    `bg-accent text-on-accent`), and Cancel uses a neutral light treatment (`bg-panel`/`bg-panel-2`
    `border-border text-ink`). For the full-screen overlay scrim, use a neutral dark scrim that
    is NOT in the slate 900/950 utility family (e.g. an ink/black translucent overlay) so the
    modal still dims the background while clearing the grep-gate.

    Reword the stale frame-scope comment (~1495) so it no longer contains the deleted shell's
    old utility class name — describe the removed dark 3-column shell by name/role, not by its
    former class, so the source grep-gate is clean.

    Do NOT modify any @media print rule or print-only container; the grep-gate is scoped to
    live (non-print) surfaces.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - The enumeration grep was run FIRST and every live match is confined to the App.tsx surfaces
      this plan owns (error banners + Save Project Modal + ~1495 comment); any match outside those
      surfaces was resolved by widening `files_modified` (and re-tokening it) or recorded before
      re-tokening — the hard gate is not left unsatisfiable.
    - Both error banners' container classes include `border-warn`, `text-warn`, and `bg-panel-2`
      and no longer use a dark banner background or light-on-dark text.
    - The Save Project Modal renders with Atelier light tokens; its three ids
      (`save-project-name-input`, `save-project-submit`, `save-project-cancel`) are unchanged.
    - The ~1495 comment no longer contains the deleted shell's old utility class name.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>
    The two error banners and the Save Project Modal render in Atelier light; the stale shell
    comment is reworded; the Save Modal's ids/behavior are unchanged; tsc is green.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update the dark-shell render guard + enforce the hard D-08 grep-gate (D-08)</name>
  <files>src/__tests__/App.test.tsx</files>
  <read_first>
    - src/__tests__/App.test.tsx — the dark-shell signature test (~1357–1365): it currently
      filters elements carrying the deleted-shell background AND `text-slate-100` together and
      explicitly treats "retained modal backdrops" as out of scope. After 26-03's modal
      deletions + this plan's re-token, that "retained backdrop" rationale is obsolete.
    - .planning/phases/26-.../26-UI-SPEC.md — the D-08 grep-gate rule ("any live (non-print)
      surface"), to phrase the strengthened guard.
  </read_first>
  <action>
    Update the dark-shell signature test to the post-re-token invariant: assert that NO rendered
    live element carries the dark-slate 900/950 background family (the Save modal now renders
    Atelier-light, and both fulfillment modals are gone), and refresh its comment so it no
    longer claims retained dark modal backdrops are out of scope. Keep it a rendered-DOM guard
    (querySelectorAll on the mounted App) complementing the source grep-gate.

    Then run the hard D-08 grep-gate as the verification: over live source only (all of `src`
    EXCLUDING `**/__tests__/**` and `*.test.*`), the patterns for the dark-slate 900/950
    background family, pure-white text, and the dark-rose banner background must each return
    ZERO matches. Print-mode `@media print` rules and print-only hidden containers are out of
    scope. If any live-source match remains, fix it before completing.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/App.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rnE 'bg-slate-9(00|50)|text-white|rose-950' src --include='*.tsx' --include='*.ts' | grep -v '__tests__' | grep -v '\.test\.'` returns ZERO lines (hard grep-gate).
    - The dark-shell signature test asserts the post-re-token invariant (no live dark-slate
      900/950 background element) and its stale "retained backdrop" comment is refreshed.
    - `npx vitest run` is green with total passing >= 240.
    - `npx tsc --noEmit` exits 0.
    - `git diff --exit-code package.json package-lock.json` clean.
  </acceptance_criteria>
  <done>
    The hard D-08 grep-gate passes on live source, the render-level dark-shell guard reflects the
    new single-skin invariant, and the suite is green — the strangler close leaves one
    consistent Atelier light skin.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App error string → error banner (text node) | the re-tokened banners still render the message as a text node only |
| user input → Save Project Modal input | the modal name field is plain text bound to state; no HTML sink |

## STRIDE Threat Register (ASVS L1; block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-26-10 | Injection | re-tokened error banners | low | mitigate | Re-token is class-only; the banner stays a text node (never dangerouslySetInnerHTML). |
| T-26-11 | Tampering | Save Project Modal behavior during re-skin | low | mitigate | Only class tokens change; ids + handlers preserved; save/load tests stay green. |

No high-severity threats — nothing blocks. Pure presentational re-skin + a test-guard update on
a 100%-client-side app; no auth, no PII transmission, no secrets, no new external surface.
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0.
- `npx vitest run` green, total passing >= 240.
- Hard grep-gate: `grep -rnE 'bg-slate-9(00|50)|text-white|rose-950' src` over live source
  (excluding tests) returns zero matches.
- `git diff` shows no change to @media print rules; no new dependency.
</verification>

<success_criteria>
The last dark-slate remnants of the retired fulfillment path are re-tokened to Atelier light,
the hard D-08 grep-gate passes on live source, and the suite is green — the phase's single UI
tree wears one consistent light skin (SC4 honesty presentation, SC5 clean close).
</success_criteria>

<output>
Create `.planning/phases/26-interim-customer-fulfillment-canvas-png-packet-diamond-drill/26-04-SUMMARY.md` when done. Note the Save-Project-Modal re-token (the D-08 "any live surface" discovery beyond the two enumerated modals).
</output>
