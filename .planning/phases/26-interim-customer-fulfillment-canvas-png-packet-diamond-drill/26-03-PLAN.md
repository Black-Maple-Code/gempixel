---
phase: 26-interim-customer-fulfillment-canvas-png-packet-diamond-drill
plan: 03
type: execute
wave: 3
depends_on: [26-02]
files_modified:
  - src/App.tsx
  - src/features/wizard/steps/Step3Canvas.tsx
  - src/features/screens/flags.ts
  - src/features/screens/__tests__/flags.test.ts
  - src/__tests__/App.test.tsx
  - src/__tests__/print.test.tsx
autonomous: true
requirements: [ORDER-04, ORDER-05]
must_haves:
  truths:
    - "Single UI tree: Step3Canvas.tsx and flags.ts no longer exist; panel-3 renders SuppliesScreen unconditionally (no strangler ternary) — SC5, D-02."
    - "The Diamond Drills USA cart has exactly one call site (App handleShopifyCheckout → OrderScreen); the dormant duplicate in Step3Canvas is gone (SC3/SC5, D-01/D-02)."
    - "The too-long / unmapped-cart branch surfaces via the actionError banner (the deleted Checkout Warning modal's replacement), not a dark-slate modal (D-08)."
    - "The full Vitest suite stays green (>= 240) and tsc is clean after deletion (SC5)."
  artifacts:
    - "src/App.tsx :: panel-3 collapsed to <SuppliesScreen /> only; no Step3Canvas import; no flags import; no checkoutWarning / resourcesModalOpen state; no dead Step3Canvas-only handlers"
    - "DELETED: src/features/wizard/steps/Step3Canvas.tsx"
    - "DELETED: src/features/screens/flags.ts"
    - "DELETED: src/features/screens/__tests__/flags.test.ts"
  key_links:
    - "panel-3 collapse removes the last flags.ts consumer (USE_NEW_SUPPLIES) → flags.ts is safe to delete (D-02)."
    - "handleShopifyCheckout too-long/unmapped branch → setActionError (banner) + still opens the cart for mapped colors + sets cartOpened (replaces the deleted Checkout Warning modal, D-08)."
  prohibitions:
    - statement: "Deletion must NOT orphan the re-homed live features — the PNG export + drill cart already live on OrderScreen (26-02) before this delete runs (strangler ships green, D-02)."
      status: enforced
      verification: "This plan depends_on 26-02; OrderScreen renders the four downloads + cart CTA at step 4 independently of Step3Canvas."
    - statement: "No second live Diamond Drills USA cart button anywhere after deletion (single Order call site, D-01)."
      status: enforced
      verification: "grep compileShopifyCartLink usage across src/ resolves to exactly one App call site wired to OrderScreen; SuppliesScreen has none."
    - statement: "Keep every state value still consumed by planOrderSupply / buildOrderQuote / handleShopifyCheckout / the print containers (priceDb, selectedVendor, canvasBaseCost, canvasShippingEstimate, drillStyle, affiliateTag, affiliateApp, unmappedLog, savingsHeadline). Remove ONLY members tsc flags as unused."
      status: enforced
      verification: "npx tsc --noEmit exits 0; buildOrderQuote / planOrderSupply / the supply-report container still compile and render."
    - statement: "Do NOT alter the print CSS or the print DOM containers / the 25-05 Ctrl+P canvas-grid print path — this plan removes only dead Step3Canvas-coupled code."
      status: enforced
      verification: "git diff shows no change to index.css print rules and no change to the canvas print-mode wiring."
---

<objective>
Complete the strangler close (SC5, D-02). Now that OrderScreen owns the reachable fulfillment
actions (26-02), delete the dormant legacy fulfillment UI and its config gate — reaching a
single UI tree with no dual fulfillment path. This plan depends on 26-02, so the live PNG
export + drill cart are never orphaned (they already render on the Order step before this
delete runs).

Deletions (D-02): `Step3Canvas.tsx`, the panel-3 strangler ternary (collapse to
`SuppliesScreen`), the `flags.ts` module + its test (the last legacy branch is gone), the two
coupled dark-slate fulfillment modals (Artist Resources — already dead, no opener exists; and
Checkout Warning — dies with the re-homed checkout path), and their now-orphaned state and
handlers. The Checkout Warning modal's too-long / unmapped-cart branch is refactored to
surface through the existing `actionError` banner (D-08; the banner is re-tokened to Atelier
light in 26-04).

Purpose: finalize ORDER-04/ORDER-05 as single, reachable paths and satisfy SC5 (legacy
Step3Canvas export/checkout wiring fully superseded, no dual UI), keeping the suite green.

Output: the deletions above + a tsc-driven cleanup of dead Step3Canvas-only members + retired
Step3Canvas-coupled tests. The D-08 grep-gate + banner/Save-modal re-token is 26-04.
</objective>

<artifacts_this_phase_produces>
DELETIONS in this plan (source-grounding: these symbols/files cease to exist):
- FILE `src/features/wizard/steps/Step3Canvas.tsx`
- FILE `src/features/screens/flags.ts` (exports USE_NEW_UPLOAD / USE_NEW_REFINE /
  USE_NEW_SUPPLIES / USE_NEW_ORDER)
- FILE `src/features/screens/__tests__/flags.test.ts`
- App state `resourcesModalOpen` / `setResourcesModalOpen` (dead — no opener) and the Artist
  Resources modal JSX
- App state `checkoutWarning` / `setCheckoutWarning` and the Checkout Warning modal JSX
- Any App handler/state/setter that tsc reports unused after the delete (candidates:
  `printReport`, `printLegendSheetOnly`, `sizingAdviceData`, and Step3Canvas-only setters).
  KEEP everything still consumed elsewhere.

No NEW runtime symbols. (The re-homed handlers + drawLegendOnly were introduced in 26-01/26-02.)
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-interim-customer-fulfillment-canvas-png-packet-diamond-drill/26-CONTEXT.md
@.planning/phases/26-interim-customer-fulfillment-canvas-png-packet-diamond-drill/26-PATTERNS.md
@src/features/wizard/steps/Step3Canvas.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete Step3Canvas + flags.ts + coupled modals; collapse panel-3; refactor cart-error path; tsc-driven dead-member cleanup (D-01/D-02/D-08)</name>
  <files>src/App.tsx, src/features/wizard/steps/Step3Canvas.tsx, src/features/screens/flags.ts</files>
  <read_first>
    - src/App.tsx — read: the imports (Step3Canvas at 19, USE_NEW_SUPPLIES at 20); the
      `resourcesModalOpen` state (212) + the Artist Resources modal (1659–1752) — note NO
      `setResourcesModalOpen(true)` opener exists anywhere (grep confirms), so the modal is
      already dead; the `checkoutWarning` state (1208–1212), `handleShopifyCheckout`
      (1214–1259, especially the 1254–1255 `setCheckoutWarning` branch and the 1257
      `window.open`), and the Checkout Warning modal (1755–1852); the panel-3 block
      (1614–1647) with the `USE_NEW_SUPPLIES ? <SuppliesScreen/> : <Step3Canvas .../>` ternary;
      the Step3Canvas prop block (1618–1645) listing which App members it consumed; the print
      handlers `printReport` (993) / `printLegendSheetOnly` (1054) and the print DOM containers
      (~1891 legend-checklist, ~1920 supply-report) — leave the containers + print CSS alone.
    - src/features/wizard/steps/Step3Canvas.tsx — its fulfillment block (268–317) is the
      reference for what already re-homed; then the file is deleted.
    - src/features/screens/flags.ts — all four flags are `true`; panel-3 is the last consumer.
    - .planning/phases/26-.../26-CONTEXT.md — D-02 (deletion ownership + what to delete), D-08
      (the too-long/unmapped branch surfaces via the actionError banner once the modal is gone).
    - Prior precedent: STATE.md decision for 25-01 ("tsc-driven dead-state grep-clean").
  </read_first>
  <action>
    1. Panel-3: replace the `USE_NEW_SUPPLIES ? <SuppliesScreen {...suppliesProps}/> :
       <Step3Canvas .../>` ternary with `<SuppliesScreen {...suppliesProps} />` only. Remove
       the `Step3Canvas` import and the `USE_NEW_SUPPLIES` import.
    2. Delete the file `src/features/wizard/steps/Step3Canvas.tsx`.
    3. Delete the file `src/features/screens/flags.ts`.
    4. Delete the Artist Resources modal JSX and its `resourcesModalOpen` /
       `setResourcesModalOpen` state (it is already unreachable — no opener).
    5. Delete the Checkout Warning modal JSX and the `checkoutWarning` / `setCheckoutWarning`
       state. Refactor `handleShopifyCheckout`'s guarded branch (formerly `setCheckoutWarning`):
       keep the unmapped-colors-log persistence and the `isUrlTooLong` computation; on the
       too-long / unmapped case, STILL open the cart for the mapped colors via
       `window.open(result.url, '_blank', 'noopener,noreferrer')`, set `cartOpened` true, AND
       call `setActionError(...)` with an honest, text-only message naming the condition (e.g.
       that some colors are not in the direct-add catalog and should be added manually at
       Diamond Drills USA, and/or that a very long cart link may need a smaller batch). Do NOT
       reintroduce a modal; the banner is the sanctioned surface (D-08). Preserve
       `noopener,noreferrer` exactly.
    6. Run `npx tsc --noEmit`. For each `noUnusedLocals` / `noUnusedParameters` error, remove
       the dead member — but ONLY if it is genuinely orphaned (was consumed solely by the
       deleted Step3Canvas / modals). Likely-dead: `printReport`, `printLegendSheetOnly`,
       `sizingAdviceData`, and Step3Canvas-only setters (e.g. `setSelectedVendor`,
       `setCanvasBaseCost`, `setCanvasShippingEstimate`, `updatePriceDb`, `setAffiliateTag`,
       `setAffiliateApp`). KEEP every value still consumed by `planOrderSupply`,
       `buildOrderQuote`, `handleShopifyCheckout`, or the print DOM containers (`priceDb`,
       `selectedVendor`, `canvasBaseCost`, `canvasShippingEstimate`, `drillStyle`,
       `affiliateTag`, `affiliateApp`, `unmappedLog`, `savingsHeadline`). Do NOT touch the print
       CSS or the print DOM containers, and do NOT touch the 25-05 Ctrl+P canvas-grid print path.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -f src/features/wizard/steps/Step3Canvas.tsx` (file gone).
    - `test ! -f src/features/screens/flags.ts` (file gone).
    - `grep -rn "Step3Canvas\|from './features/screens/flags'\|USE_NEW_" src/ --include=*.tsx --include=*.ts` returns no live (non-test-comment) references in App.tsx.
    - `grep -n "checkoutWarning\|resourcesModalOpen" src/App.tsx` returns ZERO matches.
    - `grep -n "compileShopifyCartLink" src/` shows exactly one usage site in App.tsx (the cart handler) — no SuppliesScreen usage.
    - `handleShopifyCheckout` no longer calls `setCheckoutWarning`; it calls `setActionError`
      on the too-long/unmapped branch and still uses `window.open(..., '_blank', 'noopener,noreferrer')`.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>
    Step3Canvas.tsx + flags.ts are deleted, panel-3 renders SuppliesScreen only, both dark
    fulfillment modals and their state are gone, the cart-error path surfaces via the
    actionError banner, dead Step3Canvas-only members are cleaned, and tsc is green — single UI
    tree, no dual fulfillment path.
  </done>
</task>

<task type="auto">
  <name>Task 2: Retire the Step3Canvas-coupled tests; keep the suite green (D-02)</name>
  <files>src/features/screens/__tests__/flags.test.ts, src/__tests__/App.test.tsx, src/__tests__/print.test.tsx</files>
  <read_first>
    - src/features/screens/__tests__/flags.test.ts — retire with flags.ts.
    - src/__tests__/App.test.tsx — the `it.skip('...TODO(26)...')` Step3Canvas-coupled tests:
      the commission-quote / per-bag-price-grid tests (~190, ~234), the unmapped-colors-log
      settings test (~600), the canvas-download W5 test (~793, targets "Download Canvas Grid
      (PNG)" in Step3Canvas), and the checkout W4 / WR-02 log-guard tests (~810–853, target the
      "Order Drills" button in Step3Canvas). Also the dark-shell signature test (~1358–1364)
      references retained modal backdrops — leave it for 26-04 (which owns the re-token) unless
      it fails here, in which case only note it.
    - src/__tests__/print.test.tsx — the `it.skip('...WR-01...')` per-bag-price test (~327)
      that reads prices via the legacy Step3Canvas "Prices per Bag Size ($)" grid.
    - Precedent: the Phase 23 aside-retargeting (retire strangler-obsolete tests with an inline
      note; preserve behavior still exercised elsewhere).
  </read_first>
  <action>
    Delete `src/features/screens/__tests__/flags.test.ts` (its subject is gone).

    In App.test.tsx and print.test.tsx, retire the Step3Canvas-coupled `it.skip` tests that
    can no longer have a canvas-first home (the per-bag-price-grid, commission-quote, and
    unmapped-colors-log settings tests) — remove them with a one-line inline strangler-retirement
    note (mirroring the Phase 23 aside precedent), NOT leaving dangling `it.skip` referencing a
    deleted component.

    For the two checkout log-guard tests (W4 / WR-02) and the canvas-download W5 test: PREFER
    retargeting them onto the now-reachable OrderScreen fulfillment CTAs — the cart CTA
    (`onCartCheckout` → handleShopifyCheckout, exercising the corrupt/wrong-type log guard) and
    a canvas download CTA (exercising the actionError-on-failure path) — so guard/error coverage
    is preserved rather than dropped. If retargeting is impractical under jsdom (e.g. window.open
    stubbing), retire them with the same inline note. Either way the suite must be green with no
    reference to a deleted component.
  </action>
  <verify>
    <automated>npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -f src/features/screens/__tests__/flags.test.ts`.
    - `grep -rn "Step3Canvas" src/__tests__/` returns no references to the deleted component
      (only, at most, historical inline notes that do not import or query it).
    - `grep -rn "it.skip" src/__tests__/App.test.tsx src/__tests__/print.test.tsx` shows no
      remaining skip that references the deleted Step3Canvas price-grid / Order-Drills / settings UI.
    - `npx vitest run` is green with total passing >= 240.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>
    flags.test.ts is gone; every Step3Canvas-coupled test is either retargeted onto the
    OrderScreen fulfillment path (preserving guard/error coverage) or retired with an inline
    note; the suite is green with no reference to deleted code.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → external cart (new tab) | the single re-homed `compileShopifyCartLink` handoff; deletion must not weaken it |
| App error string → error banner (text node) | the too-long/unmapped cart condition now surfaces as a text-only banner message |

## STRIDE Threat Register (ASVS L1; block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-26-07 | Info disclosure / reverse-tabnabbing | cart new-tab open in the refactored branch | medium | mitigate | Preserve `window.open(url, '_blank', 'noopener,noreferrer')` verbatim in the refactored too-long/unmapped path. |
| T-26-08 | Injection | actionError banner now also carries the cart-condition message | low | mitigate | Message is a fixed, app-authored string rendered as a text node (no HTML sink); it does not interpolate untrusted cart HTML. |
| T-26-09 | Tampering | accidental removal of consumed pricing/bag state during tsc cleanup | low | mitigate | Acceptance criteria + prohibitions require keeping every value consumed by planOrderSupply/buildOrderQuote/handleShopifyCheckout; tsc 0 confirms nothing live was cut. |

No high-severity threats — nothing blocks. Deletion-only + one honest banner refactor; no new
external surface, no auth, no PII transmission, no secrets.
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0.
- `npx vitest run` green, total passing >= 240.
- Step3Canvas.tsx, flags.ts, flags.test.ts deleted; panel-3 renders SuppliesScreen only.
- Exactly one `compileShopifyCartLink` call site (App → OrderScreen); no Supplies cart.
- `git diff` shows no change to index.css print rules / the Ctrl+P canvas-grid path.
</verification>

<success_criteria>
Single UI tree, no dual fulfillment path: Step3Canvas + flags.ts are gone, panel-3 is
collapsed, both dark fulfillment modals are removed, the cart-error path is an honest banner,
and the suite is green (SC5). ORDER-04/ORDER-05 now each have exactly one reachable path.
</success_criteria>

<output>
Create `.planning/phases/26-interim-customer-fulfillment-canvas-png-packet-diamond-drill/26-03-SUMMARY.md` when done. Record which tsc-flagged members were removed and whether the checkout log-guard tests were retargeted or retired.
</output>
