---
phase: 13-performance-off-main-thread-decode
plan: 02
type: execute
wave: 2
depends_on: [13-01]
files_modified:
  - src/App.tsx
autonomous: true
requirements: [PERF-01]

must_haves:
  truths:
    - "During the async decode/resample interval the loading overlay shows an INDETERMINATE bar labeled 'Preparing image…', then flips to the DETERMINATE 'Matching colors: {progress}%' the moment the worker's first onProgress fires (D-09)"
    - "The loading overlay never co-displays with the matchError banner — loading is cleared on error upstream and the {loading && …} / {matchError && …} gates remain mutually exclusive (D-09)"
    - "The match-error banner copy is stage-agnostic ('Couldn't process the image: {matchError}') so a decode-stage message reads correctly, and stays a plain JSX text child (D-10, ASVS V5)"
  artifacts:
    - src/App.tsx
  key_links:
    - "App.tsx consumes the hook's new loadingPhase signal (from Plan 13-01) at the useDiamondArtMatch destructure (line ~395) to choose indeterminate vs determinate overlay"
    - "the reactive matchError signal already carries worker-side decode failures (D-10 routing lives in the hook/worker from Plan 13-01); this plan only generalizes the display copy"
---

<artifacts_this_phase_produces>
Symbols/edits introduced by this plan (all in `src/App.tsx`):

- Destructure `loadingPhase` from `useDiamondArtMatch(...)` (extends the existing destructure at line ~395).
- Loading overlay (lines ~1642-1650): phase-branched markup — indeterminate bar + "Preparing image…" when `loadingPhase === 'preparing'`; determinate `width:{progress}%` bar + "Matching colors: {progress}%" when `loadingPhase === 'matching'`.
- Match-error banner (line ~1659): copy generalized to "Couldn't process the image: {matchError}".

REUSED (not produced): the `loadingPhase`/`loading`/`progress`/`error` signals themselves (produced in Plan 13-01), the existing overlay container + bar markup, and the Phase 11 error-banner element.
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/13-performance-off-main-thread-decode/13-CONTEXT.md
@.planning/phases/13-performance-off-main-thread-decode/13-PATTERNS.md
@.planning/phases/13-performance-off-main-thread-decode/13-01-PLAN.md
</context>

<objective>
Wire the D-09 phase-labeled loading overlay and the D-10 stage-agnostic error-banner copy in `src/App.tsx`, consuming the `loadingPhase` signal added to the hook in Plan 13-01.

Purpose: The relocation makes an async decode/resample interval visible before the worker's match progress starts. D-09 requires one overlay that reads "Preparing image…" (indeterminate) during that interval and flips to "Matching colors: {progress}%" (determinate) on the first progress tick; D-10 requires the banner copy to read correctly for a decode-stage failure, not just a match failure.
Output: `src/App.tsx` overlay branches on `loadingPhase` and the banner copy is generalized — with the "spinner never co-displays with banner" invariant intact.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: Branch the loading overlay on loadingPhase (indeterminate "Preparing image…" → determinate "Matching colors: {progress}%")</name>
  <files>src/App.tsx</files>
  <read_first>
    - src/App.tsx:1642-1661 (the loading overlay block 1643-1650 and the matchError banner 1657-1661)
    - src/App.tsx:385-410 (the useDiamondArtMatch destructure at ~395 — where loadingPhase must be pulled in)
    - src/features/match/useDiamondArtMatch.ts (the MatchState surface with the new loadingPhase field from Plan 13-01)
    - .planning/phases/13-performance-off-main-thread-decode/13-PATTERNS.md (§ src/App.tsx — overlay label at line 1648)
  </read_first>
  <behavior>
    - When loading is true and loadingPhase is 'preparing', the overlay shows an indeterminate bar and the label "Preparing image…" (no percentage).
    - When loading is true and loadingPhase is 'matching', the overlay shows a determinate bar whose width is {progress}% and the label "Matching colors: {progress}%".
    - The overlay is gated by {loading && …} exactly as before, so it disappears on completion or error and never overlaps the matchError banner.
  </behavior>
  <action>
    Add `loadingPhase` to the `useDiamondArtMatch(...)` destructure at line ~395 (alongside `loading`, `progress`, `error: matchError`). In the loading overlay (lines 1643-1650), branch on `loadingPhase`. For `'preparing'`: render the same bar container (`w-48 bg-slate-800 h-2 rounded-full overflow-hidden`) with an indeterminate inner bar — use a full-width inner bar with `animate-pulse` (e.g. `className="bg-indigo-500 h-full w-full animate-pulse"`) instead of a `style={{ width: '{progress}%' }}` fill — and the label span text "Preparing image…". For `'matching'`: keep the existing determinate inner bar (`style={{ width: \`${progress}%\` }}`) and the existing label "Matching colors: {progress}%". Keep the outer `{loading && ( … )}` gate and the overlay container classes unchanged so the mutual-exclusion-with-banner invariant holds. Do not otherwise alter the overlay layout.
  </action>
  <verify>
    <automated>grep -q "Preparing image" src/App.tsx && grep -q "loadingPhase" src/App.tsx && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Source: `App.tsx` destructures `loadingPhase` from `useDiamondArtMatch`.
    - Source: the overlay renders "Preparing image…" with an indeterminate (non-`{progress}%`-width) bar when `loadingPhase === 'preparing'`, and the existing `width:{progress}%` bar + "Matching colors: {progress}%" when `'matching'`.
    - Source: the outer `{loading && ( … )}` gate is unchanged; no new co-display path with the matchError banner is introduced.
    - Behavior: `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>The single loading overlay is phase-labeled: indeterminate "Preparing image…" during decode, determinate "Matching colors: {progress}%" on the first worker progress tick, never co-displaying with the error banner.</done>
</task>
<task type="auto">
  <name>Task 2: Generalize the match-error banner copy to a stage-agnostic string (D-10)</name>
  <files>src/App.tsx</files>
  <read_first>
    - src/App.tsx:1652-1661 (the matchError banner block — copy at line 1659; the security comment at 1652-1656 that mandates text-only content)
    - .planning/phases/13-performance-off-main-thread-decode/13-PATTERNS.md (§ src/App.tsx — banner copy at line 1659)
  </read_first>
  <behavior>
    - The banner text reads "Couldn't process the image: {matchError}" for both decode-stage and match-stage failures.
    - {matchError} is rendered as a plain JSX text child (no dangerouslySetInnerHTML), so a crafted worker error string cannot inject markup.
  </behavior>
  <action>
    In the matchError banner (line ~1659), change the leading copy from "Color matching failed:" to "Couldn't process the image:" — the `{matchError}` interpolation and surrounding element/classes are unchanged. Keep it a plain JSX text child; do NOT introduce `dangerouslySetInnerHTML` (preserve the security invariant noted in the 1652-1656 comment). Optionally update that comment to say the banner now covers decode-stage failures too, but keep the text-only mandate wording intact.
  </action>
  <verify>
    <automated>grep -q "process the image" src/App.tsx && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Source: the banner leading copy is "Couldn't process the image:" and the literal "Color matching failed:" no longer appears in `src/App.tsx`.
    - Source: the banner content remains a plain JSX text child; no `dangerouslySetInnerHTML` is added.
    - Behavior: `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>One stage-agnostic banner reads correctly for both decode-stage and match-stage failures, staying text-only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worker/decode error string → matchError banner | The `{matchError}` string (now including decode-stage failures) is rendered in the UI and must remain a plain JSX text child. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-01 | Tampering | generalized matchError banner copy | low | mitigate | The copy change keeps `{matchError}` a plain JSX text child; no `dangerouslySetInnerHTML` is introduced (ASVS V5 output encoding). This is the same T-13-01 tracked in Plan 13-01, guarded here at the display site. |
| T-13-SC | Tampering | npm/pip/cargo installs | low | accept | Zero package installs — copy/markup edits only. Supply-chain surface unchanged. |

No high-severity findings; nothing blocks under ASVS L1 `security_block_on: high`.
</threat_model>

<verification>
- `grep -q "Preparing image" src/App.tsx` and `grep -q "process the image" src/App.tsx` both succeed; `grep "Color matching failed" src/App.tsx` returns nothing.
- `npx tsc --noEmit` exits 0.
- `npm run build` succeeds; `npm test` stays green (no App test regressions).
</verification>

<success_criteria>
- The one loading overlay is indeterminate "Preparing image…" during decode and determinate "Matching colors: {progress}%" during match, gated so it never co-displays with the error banner (D-09).
- The error banner reads "Couldn't process the image: {matchError}" for decode and match failures alike and stays text-only (D-10).
- Typecheck, build, and test suite green.
</success_criteria>

<output>
Create `.planning/phases/13-performance-off-main-thread-decode/13-02-SUMMARY.md` when done.
</output>