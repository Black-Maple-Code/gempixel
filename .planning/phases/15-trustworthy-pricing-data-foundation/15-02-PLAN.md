---
phase: 15-trustworthy-pricing-data-foundation
plan: 02
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - src/engine/money.ts
  - src/engine/bagPlanner.ts
  - src/engine/__tests__/money.test.ts
  - src/engine/__tests__/bagPlanner.test.ts
  - src/App.tsx
autonomous: true
requirements: [PRICE-01, PRICE-02, PRICE-03]

must_haves:
  truths:
    - "defaultPacketCost(type, 500) returns a value strictly between defaultPacketCost(type, 200) and defaultPacketCost(type, 1000) for every drill type (never the 5000 bulk tier)."
    - "The cost minimizer never selects a bag size because its price is missing; a missing price is treated as Infinity, never $0."
    - "A color whose plan can only be covered by an unpriced size is flagged (hasUnpricedSize=true) and surfaced via the existing actionError banner, never priced as a $0 line."
    - "All money math routes through the integer-cents helper; the displayed drill/canvas line items sum exactly to the displayed total (no float drift)."
  artifacts:
    - "src/engine/money.ts: toCents, fromCents, sumCents, formatUSD (round-half-up, matching the existing Math.round(x*100)/100 convention)"
    - "src/engine/bagPlanner.ts: ColorPack + ColorSupplyRow gain hasUnpricedSize: boolean and unpricedSizes: number[]"
    - "src/engine/bagPlanner.ts: defaultPacketCost has an explicit 500 tier derived from a single canonical size list"
    - "src/engine/__tests__/money.test.ts: reconciliation test (sum of line-item cents === total cents)"
  key_links:
    - "priceColorPack computes in integer cents via money.ts and no longer uses `|| 0` for a missing price."
    - "minCostBulk's priceOf treats a missing price as Infinity (was `?? 0`), so a phantom-free size can never win the search."
    - "App.tsx sums per-line costSafety in integer cents so line items reconcile to totalCostSafety; an effect surfaces any hasUnpricedSize color via setActionError."
  prohibitions:
    - "No `priceDb[size] ?? 0` or `priceDb[...] || 0` remains in bagPlanner.ts."
    - "The optimizer must NOT self-select an unpriced size; no $0 line may reach a quote for a real billable item."
    - "Do NOT downgrade minCostBulk's exact bounded search to a greedy loop or add an LP solver."
    - "Do NOT reconcile the two separate price scales (defaultPacketCost vs priceDb) — only add the missing 500 tier."
---

<objective>
Land the three pricing-accuracy fixes as pure engine changes: (PRICE-01) add the missing 500-count tier to `defaultPacketCost` so a 500 bag is no longer priced at the 5000-bulk tier; (PRICE-02) stop the cost minimizer from treating a missing price as $0 — a missing price becomes `Infinity` (never self-selected), and a color that can only be covered by an unpriced size is flagged via a new `hasUnpricedSize` signal and surfaced, never shown as a $0 line; (PRICE-03) introduce a single canonical integer-cents money helper and route pricing through it so itemized line items always reconcile exactly to the displayed total.

Purpose: Every downstream number (Phase 16 optimizer surfacing, Phase 17 service fee + order packet) compounds on these figures, so they must be correct and test-guarded first (Pitfalls 6, 7, 8).
Output: `engine/money.ts` (+ tests), corrected `bagPlanner.ts` (500 tier, Infinity-not-$0, hasUnpricedSize), and a minimal App.tsx wiring that reconciles totals and surfaces unpriced colors.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/research/PITFALLS.md
@.planning/research/ARCHITECTURE.md

@src/engine/bagPlanner.ts
@src/engine/__tests__/bagPlanner.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the canonical integer-cents money helper (engine/money.ts) with a reconciliation test</name>
  <files>src/engine/money.ts, src/engine/__tests__/money.test.ts</files>
  <read_first>
    - src/engine/checkout.ts (existing rounding convention: `Math.round(x * 100) / 100` at ~L193, ~L203, ~L207)
    - src/engine/bagPlanner.ts (existing `Math.round(cost * 100) / 100` at priceColorPack ~L155)
  </read_first>
  <behavior>
    - toCents(8.5) === 850; toCents(4.99) === 499; toCents(0.1) + toCents(0.2) === toCents(0.3) (=== 30) — no float drift.
    - toCents uses round-half-up (Math.round semantics, matching the existing convention): toCents(0.005) === 1, toCents(1.005) === 101.
    - toCents on a non-finite input (NaN, Infinity) THROWS (fail loud — a NaN must never silently become a $0/NaN cent value).
    - fromCents(850) === 8.5; formatUSD(850) === '$8.50'.
    - sumCents([850, 499, 16, 14]) === 1379 and equals the total for a line-item reconciliation.
  </behavior>
  <action>
    Create src/engine/money.ts as a pure module (no Preact/DOM/persistence) exporting: `toCents(dollars: number): number` = an EPSILON-SAFE round-half-up so representation error does not swallow a half-cent — first guard `if (!Number.isFinite(dollars)) throw new RangeError(...)`, then return `Math.round((dollars * 100) + (dollars >= 0 ? Number.EPSILON : -Number.EPSILON) * 100)` (a decimal-safe equivalent, e.g. `Math.round(Number((dollars * 100).toFixed(6)))`, is acceptable — pick whichever you find cleanest, but it MUST make `toCents(1.005) === 101` and `toCents(0.005) === 1` true). Do NOT use the naive `Math.round(dollars * 100)`: `1.005 * 100 === 100.4999999999…` in IEEE-754, so the naive form returns 100 — exactly the float money bug PRICE-03 exists to prevent, and a TDD executor would stall on a red test. `fromCents(cents: number): number` = `cents / 100`; `sumCents(values: number[]): number` = integer sum; `formatUSD(cents: number): string` = `'$' + (cents / 100).toFixed(2)`. money.ts is now the single canonical money authority and SUPERSEDES the ad-hoc `Math.round(x*100)/100` calls in checkout.ts/bagPlanner.ts for cents math — it keeps round-half-up semantics but fixes the representation-error edge cases the ad-hoc form has. Create src/engine/__tests__/money.test.ts covering the behaviors above, including an explicit reconciliation test that builds a realistic set of line-item dollar amounts (e.g. canvas 8.50, shipping 4.99, several drill costs), converts each to cents, and asserts `sumCents(lineItemCents)` equals the cents of the intended grand total — proving line items sum exactly with no float drift.
  </action>
  <acceptance_criteria>
    - money.test.ts asserts round-half-up correctness on the float-edge cases: `toCents(1.005) === 101` AND `toCents(0.005) === 1` (guards against the naive `Math.round(dollars*100)` that returns 100/0).
    - money.test.ts asserts `toCents(0.1) + toCents(0.2) === toCents(0.3)`.
    - money.test.ts asserts `toCents(NaN)` throws (and `toCents(Infinity)` throws).
    - money.test.ts contains a reconciliation assertion where `sumCents(lineItemCents) === totalCents` for a multi-line fixture.
    - `npx vitest run src/engine/__tests__/money.test.ts` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/engine/__tests__/money.test.ts</automated>
  </verify>
  <done>engine/money.ts is the single canonical integer-cents helper (round-half-up, NaN-guarded) and its test proves reconciliation and drift-free summation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fix the 500 tier, kill the $0-as-free bug, and add the hasUnpricedSize signal in bagPlanner.ts</name>
  <files>src/engine/bagPlanner.ts, src/engine/__tests__/bagPlanner.test.ts</files>
  <read_first>
    - src/engine/bagPlanner.ts (ColorPack ~L26-31; packColor ~L40-73; minCostBulk `priceOf = priceDb[size] ?? 0` ~L89; priceColorPack `priceDb[Number(size)] || 0` ~L153; defaultPacketCost ~L199-226; ColorSupplyRow ~L159-165)
    - src/engine/__tests__/bagPlanner.test.ts (the whole-object empty-pack assertion at ~L54; defaultPacketCost block ~L132-140)
    - src/engine/money.ts (from Task 1)
  </read_first>
  <behavior>
    - PRICE-01: for every type in {standard, ab, glow, crystal}, defaultPacketCost(type, 500) is strictly greater than defaultPacketCost(type, 200) and strictly less than defaultPacketCost(type, 1000). Existing assertions (200/1000/2000/5000 values) still hold.
    - PRICE-02 (never $0-self-select): with PRICE_DB = { 200: 2, 500: 4, 1000: 7 } (2000 deliberately absent), packColor('150','square',2100,...) does NOT choose the free 2000 size; it covers with priced sizes only ({ 1000: 2, 500: 1 }) and reports a finite cost. The 2000 size is never present in bySize purely because it is unpriced.
    - PRICE-02 (surface unplannable): a color whose only available size is unpriced (e.g. '150' round has only {200}; PRICE_DB with no 200 price) yields hasUnpricedSize=true and is NOT reported as a $0-cost line.
    - Empty pack still returns hasUnpricedSize=false, unpricedSizes=[] (an unknown/zero-count color is legitimately empty, not unpriced).
  </behavior>
  <action>
    In bagPlanner.ts: define a single canonical size list (e.g. `const BAG_SIZES = [200, 500, 1000, 2000] as const`, with 5000 as the explicit bulk fallback) and rewrite `defaultPacketCost` to resolve its value from one per-type table keyed by size so no tier can be half-added again — insert the 500 tier with a value strictly between each type's 200 and 1000 tier (suggested: standard 0.55, ab 0.70, glow 0.90, crystal 1.00; the exact figure is at your discretion provided the strictly-between invariant holds for all four types), keeping the existing 200/1000/2000 values and the 5000-else fallback (3.0/4.0/5.0/6.0). Extend `ColorPack` with `hasUnpricedSize: boolean` and `unpricedSizes: number[]`; populate them on every return path (empty pack => false/[], pack200 and minCostBulk => computed). Change `minCostBulk`'s `priceOf` from `priceDb[size] ?? 0` to `priceDb[size] ?? Infinity` so an unpriced size can never be the chosen optimum; after the search, if the best plan still contains (or structurally required) an unpriced size, set hasUnpricedSize=true and record the offending sizes in unpricedSizes; exclude unpriced sizes from the candidate set so a priced plan is preferred when one exists. In `priceColorPack`, replace `priceDb[Number(size)] || 0` with an explicit lookup that routes through money.ts integer cents (toCents/sumCents/fromCents) and treats a missing price as an unpriced condition (do not silently add $0). Re-expose `hasUnpricedSize` (and unpricedSizes) on `ColorSupplyRow` via `planColorSupply` (OR of the exact + safety packs). KEEP the exact bounded search intact — do not convert it to a greedy loop. Update the two existing bagPlanner tests that assert exact object shape: the whole-object empty-pack assertion (~L54) must include the new `hasUnpricedSize: false, unpricedSizes: []` fields, and extend the defaultPacketCost block to add 500-tier assertions.
  </action>
  <acceptance_criteria>
    - bagPlanner.test.ts asserts, for all four types, `defaultPacketCost(t,500) > defaultPacketCost(t,200)` AND `defaultPacketCost(t,500) < defaultPacketCost(t,1000)`.
    - bagPlanner.test.ts asserts the missing-2000 fixture packs to `{ 1000: 2, 500: 1 }` (never `{ 2000: 1 }` at $0) with a finite cost.
    - bagPlanner.test.ts asserts a color coverable only by an unpriced size returns `hasUnpricedSize === true` and is not reported at $0.
    - No `?? 0` or `|| 0` price fallback remains in bagPlanner.ts (both replaced by Infinity/explicit-unpriced handling).
    - `npx vitest run src/engine/__tests__/bagPlanner.test.ts` exits 0 and the existing dye-lot / cost-min / cart==estimate tests still pass.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/engine/__tests__/bagPlanner.test.ts</automated>
  </verify>
  <done>defaultPacketCost has a correct 500 tier, the minimizer never self-selects an unpriced size, hasUnpricedSize threads to ColorSupplyRow, pricing runs on integer cents, the bounded search is preserved, and the bagPlanner suite is green.</done>
</task>

<task type="auto">
  <name>Task 3: Reconcile the displayed total in cents and surface unpriced colors via the existing banner (App.tsx)</name>
  <files>src/App.tsx</files>
  <read_first>
    - src/App.tsx (actionError banner + setActionError ~L103, ~L1692-1700; sortedMatches derivation ~L900-964; safetyDrillCost / totalCostSafety ~L966-972)
    - src/engine/money.ts and src/engine/bagPlanner.ts (from Tasks 1-2: sumCents/toCents/fromCents, planColorSupply's hasUnpricedSize)
  </read_first>
  <action>
    In App.tsx: compute `safetyDrillCost` and `totalCostSafety` (~L970-972) by summing the per-line costs in integer cents via money.ts (toCents on each `row.costSafety`, canvasBaseCost, canvasShippingEstimate; `sumCents`; then `fromCents` once) so the displayed itemized line items sum exactly to the displayed total (PRICE-03 reconciliation). Add a `useEffect` (do NOT set state during render) that watches the derived rows and, when any planned color has `hasUnpricedSize` true, calls `setActionError` with a plain-text message naming the affected DMC code(s) (e.g. "Some colors have an unpriced bag size and were left out of the total: 471, 798 — price them to include an accurate cost."), reusing the existing Phase 11 actionError banner (plain JSX text child — never dangerouslySetInnerHTML, per T-11-07); such a color must never render a $0 cost as if it were free. Keep this a minimal wiring change — do NOT add new UI components or restructure the legend.
  </action>
  <acceptance_criteria>
    - The displayed `totalCostSafety` equals the integer-cents sum of the displayed line items (canvas base + shipping + per-color costSafety) — no float-drift cent mismatch.
    - When a color has `hasUnpricedSize`, the existing actionError banner surfaces it by DMC code (plain text), and no $0 line is presented for that billable color.
    - The unpriced surfacing runs from an effect, not during render.
    - `npx tsc --noEmit` clean and `npm test` exits 0 (>= 178 tests, App/integration/print suites still green).
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && npm test</automated>
  </verify>
  <done>The total reconciles to the sum of displayed line items in cents, unpriced colors are surfaced through the existing banner (never as $0), and tsc + the full suite are green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Editable priceDb / project import -> bagPlanner | The per-bag price table is user-editable and restorable; missing/negative/NaN prices feed the cost minimizer and the displayed total. |
| bagPlanner/money -> displayed quote | Line-item costs and the grand total are rendered as money the user trusts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-03 | Tampering | minCostBulk / priceColorPack `?? 0`/`|| 0` | high | mitigate | Treat a missing price as Infinity (never $0); a color coverable only by an unpriced size is flagged (hasUnpricedSize) and surfaced, never emitted as a self-selected $0 line. |
| T-15-04 | Denial of Service | toCents integer-cents math | medium | mitigate | `toCents` throws on non-finite input (NaN/Infinity) so a bad price fails loud instead of silently producing NaN/$0 cents in the total. |
| T-15-05 | Information Disclosure | Displayed total vs itemized lines | medium | mitigate | Total is summed from the same integer-cents line items, so a customer-facing total cannot silently disagree with the visible items. |
| T-15-SC | Tampering | npm/pip/cargo installs | low | accept | No new packages introduced this plan (money.ts is first-party code). |
</threat_model>

<verification>
- `npx vitest run src/engine/__tests__/money.test.ts` — reconciliation + NaN guard + drift-free summation.
- `npx vitest run src/engine/__tests__/bagPlanner.test.ts` — 500 tier strictly between 200/1000; unpriced never self-selected; hasUnpricedSize surfaced.
- `npx tsc --noEmit && npm test` — full baseline (>= 178) green with the App.tsx wiring.
</verification>

<success_criteria>
- A 500-count bag prices at its own tier (between 200 and 1000), never the 5000 bulk tier.
- No $0 line reaches a quote for a real billable item; an unpriced size is excluded/flagged and never self-selected as cheapest.
- Itemized line items sum exactly to the displayed total (integer-cents reconciliation).
</success_criteria>

<artifacts>
## Artifacts this plan produces
- `engine/money.ts` — `toCents` (epsilon-safe round-half-up: `toCents(1.005) === 101`, not the naive `Math.round(x*100)`), `fromCents`, `sumCents`, `formatUSD` (canonical integer-cents helper; supersedes the ad-hoc `Math.round(x*100)/100` for cents math)
- `engine/__tests__/money.test.ts` — reconciliation + NaN-guard + drift tests
- `bagPlanner.ColorPack` / `bagPlanner.ColorSupplyRow` — new `hasUnpricedSize: boolean` + `unpricedSizes: number[]` fields
- `bagPlanner.defaultPacketCost` — explicit 500 tier from a single canonical size list
- `bagPlanner.minCostBulk` / `priceColorPack` — missing price => Infinity/explicit-unpriced (the `?? 0`/`|| 0` bug removed), pricing on integer cents
- App.tsx — cents-reconciled `safetyDrillCost`/`totalCostSafety` + an effect surfacing hasUnpricedSize colors through the existing actionError banner
</artifacts>

<output>
Create `.planning/phases/15-trustworthy-pricing-data-foundation/15-02-SUMMARY.md` when done.
</output>
