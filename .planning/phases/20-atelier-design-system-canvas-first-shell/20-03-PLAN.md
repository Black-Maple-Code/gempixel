---
phase: 20-atelier-design-system-canvas-first-shell
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/wizard/stepMeta.ts
  - src/features/wizard/StepBar.tsx
  - src/features/wizard/AtelierShell.tsx
  - src/features/wizard/__tests__/StepBar.test.tsx
autonomous: true
requirements: [SHELL-01, SHELL-02]
must_haves:
  truths:
    - "SC3: A single horizontal 4-step bar (Upload → Refine → Supplies → Order) is the only navigator component — no sidebars, hamburger, or page-flip wizard; labels come from one STEP_META map."
    - "SC4 (gating UX): Locked/upcoming steps render aria-disabled='true', are removed from tab order, and show a static tooltip; the current step is aria-current='step'; all four stay visible; a locked tap never navigates."
    - "SC5: StepBar and AtelierShell are pure/props-only (App stays the state owner); new unit tests cover the a11y + gating contract; the 240+ Vitest suite stays green."
  artifacts:
    - "src/features/wizard/stepMeta.ts — the single STEP_META map ({1 Upload, 2 Refine, 3 Supplies, 4 Order})"
    - "src/features/wizard/StepBar.tsx — pure StepBar component + StepBarProps"
    - "src/features/wizard/AtelierShell.tsx — top-bar chrome (pixel-logo + Newsreader wordmark + StepBar + Save pill)"
    - "src/features/wizard/__tests__/StepBar.test.tsx — a11y/gating unit tests"
  key_links:
    - "STEP_META ↔ StepBar labels ↔ AtelierShell (one source of step labels/order; the Phase 23 semantic remap is a data edit here)"
    - "WizardApi {step, canEnter, goTo} props ↔ StepBar render (reads step state, never owns it — D-01)"
    - "D-12 gating (aria-disabled + out-of-tab-order + aria-current + tooltip) ↔ locked-step visual states in the UI-SPEC step-nav table"
---

<objective>
Build the two pure chrome components the canvas-first shell needs — the single horizontal 4-step `StepBar` and the `AtelierShell` top bar — plus the `STEP_META` single-source label map, with the full D-12 gating/a11y contract and unit tests. No App.tsx wiring here (that is Plan 04); these are self-contained new files so they build in parallel with the dark-mode and font work.

Purpose: SHELL-01 (the one-and-only navigator, structurally enforced by there being a single nav component) and the gating half of SHELL-02/D-12. Establishes STEP_META (D-02) so the Phase 23 semantic remap is a data edit, not another pass through the 2449-line App.tsx.
Output: `stepMeta.ts`, `StepBar.tsx`, `AtelierShell.tsx`, and `StepBar.test.tsx`, all pure/props-only.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-atelier-design-system-canvas-first-shell/20-CONTEXT.md
@.planning/phases/20-atelier-design-system-canvas-first-shell/20-PATTERNS.md
@.planning/phases/20-atelier-design-system-canvas-first-shell/20-UI-SPEC.md
@src/features/wizard/useWizard.ts
@src/features/wizard/steps/Step1Ingest.tsx
</context>

<artifacts_this_phase_produces>
Symbols this plan creates (source-grounding exclusion list — NEW, not drift):
- `STEP_META` (exported from `src/features/wizard/stepMeta.ts`): the ordered map of step index → `{ index, label }` with labels `Upload` · `Refine` · `Supplies` · `Order` (D-02). Also exports the locked-step tooltip copy per step (`Upload an image to unlock` for Refine, `Compute a match to unlock` for Supplies).
- `StepBar` + `StepBarProps` (exported from `src/features/wizard/StepBar.tsx`): props `{ step, canEnter, goTo }` (WizardApi slice) plus an optional `stale?` prop reserved for Plan 05 (accepts and renders nothing yet if omitted).
- `AtelierShell` (exported from `src/features/wizard/AtelierShell.tsx`): props `{ children, step, canEnter, goTo, onSave, canSave }` — renders the top bar (logo + wordmark + StepBar + Save pill) and its `children` below.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: STEP_META map + the pure StepBar navigator with D-12 gating and a11y</name>
  <files>src/features/wizard/stepMeta.ts, src/features/wizard/StepBar.tsx</files>
  <behavior>
    - Renders exactly 4 step items in order: Upload, Refine, Supplies, Order (labels from STEP_META).
    - Current step: circle filled + number + bold ink label + `aria-current="step"`.
    - Completed step (index < current): green filled circle + check mark; label muted; connector before it is "passed" (green).
    - Upcoming/locked step (`canEnter(index)` false): `aria-disabled="true"`, `tabindex="-1"` (out of tab order), muted label with reduced-opacity container, connector ahead uses the muted `#D8D0BC` treatment, and a static tooltip from STEP_META (e.g. Refine → "Upload an image to unlock").
    - Clicking an enabled/completed step calls `goTo(index)`; clicking a locked step does NOT call `goTo` and does not navigate (no toast, no dead-end).
  </behavior>
  <read_first>
    - `src/features/wizard/useWizard.ts` (the `WizardApi` interface — `step`, `canEnter(step)`, `goTo(step)`; note `goTo` already bypasses gating under `isTestEnv`).
    - `src/features/wizard/steps/Step1Ingest.tsx:1-51` (the pure-props child contract to copy: props in, no local state mirroring engine state).
    - `.planning/phases/20-atelier-design-system-canvas-first-shell/20-PATTERNS.md` §"StepBar.tsx" (Analog B desktop step-nav markup to model; the gating upgrade from `disabled` to `aria-disabled`; the `.tooltip-group`/`.tooltip-box` idiom at src/index.css:591-615; the three inline label arrays STEP_META replaces).
    - `.planning/phases/20-atelier-design-system-canvas-first-shell/20-UI-SPEC.md` §"Step-nav visual states" table + §"Gating" + §Copywriting (step labels + locked-step tooltip copy) + §Color (accent reserved for current/completed circle + passed connector).
    - `.planning/phases/20-atelier-design-system-canvas-first-shell/20-CONTEXT.md` D-01, D-02, D-12.
  </read_first>
  <action>
    Create `stepMeta.ts` exporting `STEP_META` as the single ordered source of step index → label (`Upload`, `Refine`, `Supplies`, `Order`) plus each step's locked-step tooltip copy (Refine: `Upload an image to unlock`; Supplies: `Compute a match to unlock`; Upload/Order have none). This is the one place labels/order live (D-02).
    Create `StepBar.tsx` as a pure props-only component (`StepBarProps = { step, canEnter, goTo, stale? }`, reserving `stale?` for Plan 05 — accept it, render nothing for it now). Render the four steps from STEP_META with the three visual states from the UI-SPEC table, consuming semantic Atelier utilities/tokens (`bg-accent`, `text-on-accent`, `text-ink`, `text-muted`, `border-border`, the passed/ahead connector colors) — never hard-code Atelier hexes (Pattern: design tokens via var/@theme). Implement gating per D-12: locked steps get `aria-disabled="true"` + `tabIndex={-1}` and do not call `goTo`; the current step gets `aria-current="step"`; the static tooltip uses the existing tooltip idiom. Keep the component free of `dangerouslySetInnerHTML` — labels/tooltips render as plain text from STEP_META. Do NOT read or own step state; it comes entirely from props (App stays owner, D-01).
  </action>
  <acceptance_criteria>
    - `grep -c "STEP_META" src/features/wizard/stepMeta.ts` ≥ 1 and STEP_META enumerates the four labels in order Upload, Refine, Supplies, Order.
    - `grep -c "useState" src/features/wizard/StepBar.tsx` returns 0 (pure/props-only; no local state).
    - StepBar renders `aria-current="step"` on the current step and `aria-disabled="true"` + `tabIndex={-1}` on locked steps (asserted by the Task 3 tests).
    - `grep -c "dangerouslySetInnerHTML" src/features/wizard/StepBar.tsx` returns 0.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>STEP_META is the single label/order source; StepBar is a pure component rendering four gated, a11y-correct steps from tokens, owning no state.</done>
</task>

<task type="auto">
  <name>Task 2: AtelierShell top-bar chrome (logo + Newsreader wordmark + StepBar + Save pill)</name>
  <files>src/features/wizard/AtelierShell.tsx</files>
  <read_first>
    - `.planning/phases/20-atelier-design-system-canvas-first-shell/20-UI-SPEC.md` §"Design System"/§Typography/§Color (top-bar chrome: left = green 3×3 pixel-logo tile + "GemPixel" wordmark in Newsreader 21/600; center = StepBar; right = Save pill `#1B1A17` bg / `#F4F1E9` text — the one deliberate dark accent) + §"Interaction Contract" (Navigation).
    - `.planning/phases/20-atelier-design-system-canvas-first-shell/20-PATTERNS.md` §"AtelierShell.tsx" (pure-props idiom; Save-pill analog `App.tsx:1562-1571` `btn-chunk-2`; the `.gem-logo` pure-CSS 3×3 mark at src/index.css:169+) + §"Chunky button primitives".
    - `.planning/phases/20-atelier-design-system-canvas-first-shell/20-CONTEXT.md` D-01, D-10 (wordmark = Newsreader, Pixelify dropped), §Specifics (top-bar chrome details).
    - `src/features/wizard/steps/Step1Ingest.tsx:1-51` (pure-props contract).
  </read_first>
  <action>
    Create `AtelierShell.tsx` as a pure props-only component (`{ children, step, canEnter, goTo, onSave, canSave }`). Render the top bar: left = the green pixel-logo tile (reuse the existing `.gem-logo` pure-CSS 3×3 mark) + the "GemPixel" wordmark using the display/serif token so it renders in Newsreader (D-10) at the 21/600 spec; center = `<StepBar step canEnter goTo />`; right = a "Save" pill styled as the deliberate dark accent (`#1B1A17` bg via token, `#F4F1E9` text) wired to `onSave`, disabled when `!canSave`. Render `children` (the step panels + viewer, supplied by App in Plan 04) below the top bar. Consume Atelier tokens/utilities only — no hard-coded hexes except where the UI-SPEC explicitly fixes the dark Save pill. Keep it pure: no step state ownership, no engine calls. This component seeds minimal shell chrome in `src/features/wizard/` (formal `src/ui/` primitives are Phase 21 — do not create those here).
  </action>
  <acceptance_criteria>
    - `AtelierShell` imports and renders `StepBar`, passing `step`/`canEnter`/`goTo` straight through.
    - `grep -c "useState" src/features/wizard/AtelierShell.tsx` returns 0 (pure/props-only).
    - The wordmark uses the display/serif font token (Newsreader), not a hard-coded font-family and not Pixelify (`grep -c "Pixelify" src/features/wizard/AtelierShell.tsx` == 0).
    - `grep -c "dangerouslySetInnerHTML" src/features/wizard/AtelierShell.tsx` returns 0.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>AtelierShell renders the top-bar chrome (logo + Newsreader wordmark + StepBar + dark Save pill) and its children, pure/props-only, tokens-only.</done>
</task>

<task type="auto">
  <name>Task 3: StepBar a11y + gating unit tests</name>
  <files>src/features/wizard/__tests__/StepBar.test.tsx</files>
  <read_first>
    - `src/features/wizard/StepBar.tsx` (the component under test — Task 1) and `src/features/wizard/stepMeta.ts`.
    - `src/__tests__/App.test.tsx` (jsdom render/query idioms used across the suite — reuse the same testing-library / container-query style).
    - `.planning/phases/20-atelier-design-system-canvas-first-shell/20-RESEARCH.md` §5 "Step-nav a11y / tab-order" testing bucket (the exact assertions to lift into verification).
    - `.planning/phases/20-atelier-design-system-canvas-first-shell/20-CONTEXT.md` D-12.
  </read_first>
  <action>
    Add `StepBar.test.tsx` covering the D-12 contract with a props-driven render (supply a stub `canEnter`/`goTo`): (1) renders exactly 4 steps labeled Upload/Refine/Supplies/Order in order; (2) the current step has `aria-current="step"`; (3) a locked step (canEnter false) has `aria-disabled="true"` and `tabIndex=-1` (out of tab order); (4) clicking a locked step does NOT call `goTo`; (5) clicking an enabled/completed step DOES call `goTo` with that index. Keep tests deterministic and jsdom-only (no build/network).
  </action>
  <acceptance_criteria>
    - `npx vitest run src/features/wizard/__tests__/StepBar.test.tsx` exits 0 with the five assertions above passing.
    - `npx vitest run` exits 0 with ≥ 240 passing overall (new tests are additive — SC5).
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/features/wizard/__tests__/StepBar.test.tsx</automated>
  </verify>
  <done>StepBar's 4-step render, aria-current, locked aria-disabled/out-of-tab-order, and locked-tap-no-op are all asserted and green; overall suite ≥240.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App state → StepBar/AtelierShell props | Chrome components receive wizard state as read-only props (pure children); no privileged operation crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-06 | Tampering (injection) | StepBar/AtelierShell label + tooltip rendering | low | mitigate | Labels/tooltips render as plain text from the static STEP_META map; no `dangerouslySetInnerHTML`, no user-controlled markup in this chrome. Grep-gated to 0 occurrences. |
| T-20-07 | Elevation of Privilege | Locked-step gating | low | mitigate | Gating is enforced by not calling `goTo` on locked steps AND by `canEnter` in useWizard (defense-in-depth); a tampered DOM click cannot bypass the state machine's `canEnter` guard. Unit-tested (Task 3). |
</threat_model>

<verification>
- StepBar renders 4 steps in the STEP_META order with correct aria-current / aria-disabled / tabIndex states (Task 3 tests green).
- StepBar and AtelierShell contain no `useState` and no `dangerouslySetInnerHTML` (pure/props-only, no injection surface).
- `npx tsc --noEmit` exits 0; `npx vitest run` exits 0 with ≥ 240 passing (SC5).
</verification>

<success_criteria>
The single-navigator chrome exists as pure components: STEP_META is the one label/order source (D-02), StepBar enforces the full D-12 gating/a11y contract, AtelierShell renders the Atelier top bar, and the additive tests are green with the suite ≥240. App wiring is Plan 04.
</success_criteria>

<output>
Create `.planning/phases/20-atelier-design-system-canvas-first-shell/20-03-SUMMARY.md` when done.
</output>
