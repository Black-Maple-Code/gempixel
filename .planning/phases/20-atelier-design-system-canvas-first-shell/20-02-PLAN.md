---
phase: 20-atelier-design-system-canvas-first-shell
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
  - src/App.tsx
autonomous: true
requirements: [DESIGN-01]
must_haves:
  truths:
    - "SC1: A returning user always sees the Atelier light theme — the persisted gempixel_theme key is unconditionally removed on boot and nothing reads or writes it."
    - "SC1: No half-dark flash on reload — the index.html inline anti-FOUC boot script that stamped data-theme='dark' for returning dark users is deleted, and the hard-coded data-theme attribute is gone."
    - "SC1: The theme toggle UI and the usePersistentState('gempixel_theme') hook + its [data-theme] DOM effect are removed from App.tsx."
    - "SC5: The viewer CSS-var effect still pushes --drill-round-backing/--canvas-gap into the canvas (now always the Atelier light values); engine signatures unchanged; the 240+ Vitest suite stays green."
  artifacts:
    - "index.html — boot script deleted, <html lang=\"en\"> with no theme attribute"
    - "src/App.tsx — theme hook + [data-theme] effect + toggle UI removed; one localStorage.removeItem('gempixel_theme') at boot; viewer effect dep array no longer references theme"
  key_links:
    - "index.html boot script deletion ↔ D-08 'by construction' (once no [data-theme=dark] CSS exists, a stale attribute selects nothing)"
    - "App boot removeItem ↔ returning dark-mode users carry no residue"
    - "viewer CSS-var effect ↔ Atelier light --drill-round-backing/--canvas-gap (theme dep dropped, effect kept — the REAL theme→canvas path, not an engine param)"
---

<objective>
Rip out the dark-mode resurrection vectors in the App/HTML/boot layer so a returning dark-mode user can never see a half-dark UI again.

Purpose: The behavior half of DESIGN-01/SC1. Deletes the persisted-theme hook, the DOM `[data-theme]` effect, the toggle UI, AND — the non-obvious research finding — the `index.html` inline anti-FOUC boot script that re-stamps `data-theme="dark"` before Preact mounts. Pairs with Plan 01's CSS flatten (they touch different files and run in parallel; together they satisfy D-08 "SC1 by construction").
Output: `index.html` with no boot script and no theme attribute; `src/App.tsx` with the theme hook/effect/toggle removed, a one-time boot `removeItem`, and the viewer CSS-var effect retained but with `theme` dropped from its dependency array.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-atelier-design-system-canvas-first-shell/20-CONTEXT.md
@.planning/phases/20-atelier-design-system-canvas-first-shell/20-RESEARCH.md
@.planning/phases/20-atelier-design-system-canvas-first-shell/20-PATTERNS.md
</context>

<artifacts_this_phase_produces>
Symbols/wirings this plan creates (source-grounding exclusion list — NEW, not drift):
- `src/App.tsx`: one `localStorage.removeItem('gempixel_theme')` call at App init (D-06 boot hygiene; runs once, no ordering dependency once the index.html script is gone).
- The viewer CSS-var effect keeps its body but its dependency array loses `theme` (was `[theme, image, matchResult, drillStyle]` → `[image, matchResult, drillStyle]`), tagged `// PHASE 22: remove theme param`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Delete the index.html dark-mode boot script and theme attribute</name>
  <files>index.html</files>
  <read_first>
    - `index.html` lines 1-21 (line 2 hard-codes the theme attribute on `<html>`; lines 7-15 are the inline anti-FOUC boot script that reads the persisted theme key and stamps the attribute before Preact mounts).
    - `.planning/phases/20-atelier-design-system-canvas-first-shell/20-RESEARCH.md` §3c/§3d (the NEW finding: index.html is the real resurrection vector; delete the script; removeItem then has no ordering dependency) and Pitfall C.
    - `.planning/phases/20-atelier-design-system-canvas-first-shell/20-PATTERNS.md` §"index.html".
  </read_first>
  <action>
    Delete the entire inline `<script>…</script>` block in the `<head>` that reads the persisted theme key and sets `document.documentElement.dataset.theme` (lines 7-15). Simplify the opening tag on line 2 to `<html lang="en">` with no theme attribute. Do NOT leave a comment that names the removed script, the removed attribute, or the localStorage key — a negative grep counts comments. Leave the rest of the head (charset, viewport, title) and the body/module-script untouched.
  </action>
  <acceptance_criteria>
    - `grep -c "gempixel_theme" index.html` returns 0.
    - `grep -c "data-theme" index.html` returns 0.
    - `grep -c "dataset.theme" index.html` returns 0 (boot script fully removed).
    - `grep -c "src/main.tsx" index.html` still returns 1 (module entry intact).
  </acceptance_criteria>
  <verify>
    <automated>grep -c "data-theme" index.html; grep -c "gempixel_theme" index.html</automated>
  </verify>
  <done>index.html has no inline theme boot script, no theme attribute, and no reference to the abandoned localStorage key; the module entry is intact.</done>
</task>

<task type="auto">
  <name>Task 2: Remove the theme hook, DOM effect, and toggle from App.tsx; add boot removeItem; drop theme from the viewer effect deps</name>
  <files>src/App.tsx</files>
  <read_first>
    - `src/App.tsx` lines 160-170 (the `usePersistentState<'dark'|'light'>('gempixel_theme', …)` hook + the `document.documentElement.dataset.theme = theme` effect); lines 1437-1451 (the light/dark pill toggle button with `onClick={() => setTheme(...)}`); lines 556-562 (the viewer CSS-var effect: `getComputedStyle` → `setRoundBacking`/`setGridGap`, dep array `[theme, image, matchResult, drillStyle]`); the App-init region near the top of the component body (a safe place for the one-time removeItem).
    - `.planning/phases/20-atelier-design-system-canvas-first-shell/20-CONTEXT.md` D-05, D-06, D-07.
    - `.planning/phases/20-atelier-design-system-canvas-first-shell/20-RESEARCH.md` §3e (the theme→canvas path is this CSS-var effect, NOT an engine `theme` param — reconciles the D-07 "theme param" misnomer; keep the effect, drop the `theme` dep) and §3d (removeItem has no ordering dependency).
    - `.planning/phases/20-atelier-design-system-canvas-first-shell/20-PATTERNS.md` §"src/App.tsx (state owner)".
    - `src/hooks/__tests__/usePersistentState.test.ts` (confirm the generic hook is untouched — only the theme instance is removed).
  </read_first>
  <action>
    <!-- planner-discipline-allow: gempixel_theme -->
    Delete the `usePersistentState` theme instance and the DOM effect that stamps the theme attribute onto `document.documentElement` (the hook that persists the `'dark'|'light'` value under the abandoned localStorage key, and the effect that writes that value onto the document element as the theme attribute). Keep the `usePersistentState` hook module itself — it is used elsewhere; only this instance goes (D-05).
    Delete the light/dark pill toggle button block (the one whose `onClick` flips theme). Remove any now-unused `theme`/`setTheme` references so typecheck stays clean.
    Add a single unconditional `localStorage.removeItem('gempixel_theme')` at App init (top of the component body or in a mount-once effect) so returning users carry no dark residue (D-06). Guard it defensively (wrap in try/catch or reuse the existing safe-storage convention) consistent with the codebase's non-throwing storage boundaries.
    In the viewer CSS-var effect, KEEP the effect body (it pushes `--drill-round-backing`/`--canvas-gap` via `getComputedStyle` — the real theme→canvas mechanism, which now always resolves to Atelier light) and remove `theme` from its dependency array so it reads `[image, matchResult, drillStyle]`. Add a `// PHASE 22: remove theme param` marker on that effect (the marker belongs here, not on any engine signature — engine signatures are genuinely untouched, D-07/RESEARCH §3e).
  </action>
  <acceptance_criteria>
    - `grep -c "gempixel_theme" src/App.tsx` returns 1 (only the boot `removeItem` reference remains) and that single occurrence is the `removeItem` call (verify no `usePersistentState<…>('gempixel_theme'` and no `dataset.theme` assignment remain).
    - `grep -c "setTheme" src/App.tsx` returns 0 and `grep -c "dataset.theme" src/App.tsx` returns 0.
    - `grep -c "removeItem('gempixel_theme')" src/App.tsx` returns 1.
    - `grep -c "PHASE 22" src/App.tsx` ≥ 1 (marker on the viewer CSS-var effect).
    - `npx tsc --noEmit` exits 0 (no dangling theme/setTheme references).
    - `npx vitest run` exits 0 with ≥ 240 passing (SC5) — no test asserts on dark mode, so the rip is test-safe.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run</automated>
  </verify>
  <done>App.tsx has no theme hook, no [data-theme] effect, no toggle; a one-time boot removeItem clears the abandoned key; the viewer CSS-var effect is retained without the theme dep and marked for Phase 22; typecheck + full suite green (≥240).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → localStorage | Persisted `gempixel_theme` value is attacker/stale-controllable client state that previously drove a UI code path |
| index.html boot script → DOM | Pre-Preact inline script mutated `<html>` from persisted state |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-03 | Tampering | Persisted `gempixel_theme` localStorage key | low | mitigate | Unconditional boot `removeItem` + no code reads/writes the key anywhere afterward; grep-gated. A tampered value cannot resurrect a dark path because the CSS and boot script are gone. |
| T-20-04 | Elevation/Resurrection | index.html inline boot script | medium | mitigate | Delete the script entirely (the actual resurrection vector); `<html>` carries no theme attribute; SC1 satisfied by construction (D-08). |
| T-20-05 | Denial of Service (self) | removeItem in a storage-restricted context | low | accept | Wrapped in a non-throwing boundary consistent with the app's defensive storage convention; failure degrades silently, never crashes boot. |
</threat_model>

<verification>
- `grep -c "data-theme" index.html` == 0 and `grep -c "gempixel_theme" index.html` == 0.
- App.tsx: no theme hook / no `dataset.theme` / no `setTheme`; exactly one boot `removeItem('gempixel_theme')`; viewer CSS-var effect retained with `theme` dropped from deps + PHASE 22 marker.
- `npx tsc --noEmit` exits 0; `npx vitest run` exits 0 with ≥ 240 passing (SC5).
</verification>

<success_criteria>
Behavior half of DESIGN-01/SC1 satisfied: no persisted theme key survives boot, no inline boot script re-stamps a dark attribute, no toggle exists, and the theme→canvas effect keeps working against the now-unconditional Atelier light tokens. Full Vitest suite green (≥240).
</success_criteria>

<output>
Create `.planning/phases/20-atelier-design-system-canvas-first-shell/20-02-SUMMARY.md` when done.
</output>
