---
phase: 20-atelier-design-system-canvas-first-shell
plan: 05
type: execute
wave: 3
depends_on: ["20-04"]
files_modified:
  - src/App.tsx
  - src/features/wizard/StepBar.tsx
  - src/__tests__/App.test.tsx
autonomous: true
requirements: [SHELL-02]
must_haves:
  truths:
    - "SC4 (soft-invalidate, D-13): Editing a completed upstream step marks downstream steps stale, keeps the last-good match/supplies on screen (no data loss), and blocks advancing past a stale step."
    - "SC4: A single 'Recompute match' banner CTA (heading 'This step is out of date') surfaces via the existing actionError/banner convention; the expensive match runs once on intentional click — no silent worker re-fire."
    - "SC5: The soft-invalidate gating does not interfere with the linear forward-navigation tests or the locked-Next semantics; the 240+ Vitest suite stays green."
  artifacts:
    - "src/App.tsx — stale tracking (staleFromStep) + 'Recompute match' banner + advance-block-past-stale"
    - "src/features/wizard/StepBar.tsx — renders a stale marker on downstream steps via the reserved stale prop"
    - "src/__tests__/App.test.tsx — soft-invalidate test (upstream edit → banner + advance blocked → recompute clears)"
  key_links:
    - "App stale signal ↔ StepBar stale prop (downstream steps show out-of-date marker)"
    - "'Recompute match' CTA ↔ the one intentional match re-run (no auto re-fire — avoids the B2 abort-race)"
    - "last-good match/supplies retained on screen ↔ no data loss while stale"
---

<objective>
Add the soft-invalidate + recompute chrome (D-13): when the user edits a completed upstream step after a match exists, mark downstream steps stale, keep the last-good match/supplies visible, block advancing past the stale step, and surface one "Recompute match" banner CTA that re-runs the match exactly once on click.

Purpose: The soft-invalidate half of SHELL-02/SC4. It protects the expensive, abort-race-prone match (prior B2 context) from silent re-fires while never dead-ending or losing data. This is the last shell behavior; it threads the `stale` prop reserved in Plan 03 through StepBar.
Output: App.tsx stale tracking + recompute banner + advance-block; StepBar rendering the stale marker; a soft-invalidate test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-atelier-design-system-canvas-first-shell/20-CONTEXT.md
@.planning/phases/20-atelier-design-system-canvas-first-shell/20-UI-SPEC.md
@.planning/phases/20-atelier-design-system-canvas-first-shell/20-03-SUMMARY.md
@.planning/phases/20-atelier-design-system-canvas-first-shell/20-04-SUMMARY.md
@src/features/wizard/useWizard.ts
</context>

<artifacts_this_phase_produces>
Symbols this plan creates (source-grounding exclusion list — NEW, not drift):
- `src/App.tsx`: a `stale` signal (Claude's Discretion on shape — recommend `staleFromStep: number | null`, the earliest downstream step index that is out of date) derived when an upstream input (image / canvas size) changes after a match exists; the "Recompute match" banner (heading `This step is out of date`, CTA `Recompute match`) mounted via the existing `actionError`/banner surfacing convention; an advance-block that prevents moving PAST a stale step until recompute clears it.
- `src/features/wizard/StepBar.tsx`: renders a stale/out-of-date marker on downstream steps using the `stale?` prop reserved in Plan 03.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: App-level stale tracking, recompute banner, and advance-block-past-stale</name>
  <files>src/App.tsx</files>
  <read_first>
    - `src/App.tsx` — the wizard composition + AtelierShell wiring from Plan 04 (`20-04-SUMMARY.md`); the existing `actionError`/banner surfacing pattern (search for `actionError` usages to reuse the same defensive banner convention); the match trigger path (how the match is invoked today — the "Recompute match" CTA re-runs exactly this, once); the `image`/canvas-size/`matchResult` state.
    - `.planning/phases/20-atelier-design-system-canvas-first-shell/20-CONTEXT.md` D-13 (soft-invalidate + re-run prompt: mark downstream stale, keep last-good on screen, block advancing past stale, one Recompute CTA, no silent re-fire, no data loss) + §"Claude's Discretion" (precise `stale` shape + banner mount location are the planner's/executor's call).
    - `.planning/phases/20-atelier-design-system-canvas-first-shell/20-UI-SPEC.md` §"Soft-invalidate + recompute" + §Copywriting (banner heading `This step is out of date`, CTA `Recompute match`) + §"Error state" (existing defensive actionError convention).
    - `src/features/match/useDiamondArtMatch.ts` (the match pipeline the recompute triggers; note it is App-owned — panel toggling never touches it).
  </read_first>
  <action>
    Add a `stale` signal to App (recommend `staleFromStep: number | null`): when a completed upstream input changes after a `matchResult` already exists (e.g. a new image is loaded or the canvas size changes), set it to the earliest downstream step that is now out of date. Crucially, do NOT clear or recompute the existing match automatically — keep the last-good `matchResult`/supplies on screen (no data loss, no silent worker re-fire; this deliberately avoids the B2 abort-race).
    Surface ONE "Recompute match" banner using the existing `actionError`/banner convention (same non-throwing surfacing style), with heading `This step is out of date` and CTA `Recompute match`. The CTA invokes the existing match trigger exactly once and, on success, clears the stale signal.
    Block advancing PAST a stale step: while `staleFromStep` is set, the forward affordance to steps at/after that index is blocked (the user can still view stale downstream content but cannot treat it as current until recompute). Implement the block so it does NOT alter useWizard's `canEnter` (keep engine/state-machine indices frozen, D-04) and does NOT trip the linear forward-navigation tests (a fresh linear Upload→match→next flow never enters the stale state; stale only arises on an upstream EDIT after a match). Pass the stale signal down to `<AtelierShell>`/`<StepBar>` (Task 2 renders the marker).
  </action>
  <acceptance_criteria>
    - `grep -c "Recompute match" src/App.tsx` ≥ 1 and `grep -c "This step is out of date" src/App.tsx` ≥ 1 (the exact UI-SPEC copy).
    - The recompute CTA calls the existing match trigger (no NEW worker-invocation path introduced) and clears the stale signal on success — verified by Task 3's test.
    - No automatic match re-fire on upstream edit: the last-good `matchResult` remains set while stale (asserted in Task 3).
    - `grep -c "canEnter" src/features/wizard/useWizard.ts` unchanged (useWizard not modified) and `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Editing an upstream step after a match marks downstream stale, keeps the last-good match on screen, blocks advancing past the stale step, and shows one Recompute banner that re-runs the match once on click; useWizard indices untouched; typecheck clean.</done>
</task>

<task type="auto">
  <name>Task 2: StepBar renders the stale marker via the reserved stale prop</name>
  <files>src/features/wizard/StepBar.tsx</files>
  <read_first>
    - `src/features/wizard/StepBar.tsx` (the `stale?` prop reserved in Plan 03, currently accepted but unrendered) and `src/features/wizard/stepMeta.ts`.
    - `.planning/phases/20-atelier-design-system-canvas-first-shell/20-UI-SPEC.md` §"Soft-invalidate + recompute" + §"Step-nav visual states" (keep the stale marker visually distinct from locked/upcoming; it does not remove the step from the journey map).
    - `.planning/phases/20-atelier-design-system-canvas-first-shell/20-CONTEXT.md` D-12, D-13.
  </read_first>
  <action>
    Render an out-of-date marker on downstream steps at/after the stale index using the `stale` prop passed from App (shape agreed in Task 1). Keep it a small, token-based visual affordance (e.g. an amber/warn dot or `aria-describedby` note) that is visually distinct from the locked/upcoming state and does not remove the step from the visible journey map. Keep StepBar pure/props-only (no new state) and free of `dangerouslySetInnerHTML`. Do not change the existing gating/a11y behavior from Plan 03.
  </action>
  <acceptance_criteria>
    - StepBar renders a distinct stale marker when the `stale` prop indicates downstream steps are out of date (asserted in Task 3).
    - `grep -c "useState" src/features/wizard/StepBar.tsx` returns 0 (still pure) and `grep -c "dangerouslySetInnerHTML" src/features/wizard/StepBar.tsx` returns 0.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>StepBar shows an accessible, token-based stale marker on out-of-date downstream steps via the stale prop, staying pure and keeping the Plan 03 gating intact.</done>
</task>

<task type="auto">
  <name>Task 3: Soft-invalidate behavior test</name>
  <files>src/__tests__/App.test.tsx</files>
  <read_first>
    - `src/__tests__/App.test.tsx` (the jsdom render + navigation idioms, incl. the `#wizard-next-btn` and STEP_META-label finders from Plan 04) and the existing upload→match flow helpers used across the suite.
    - `.planning/phases/20-atelier-design-system-canvas-first-shell/20-CONTEXT.md` D-13; `.planning/phases/20-atelier-design-system-canvas-first-shell/20-UI-SPEC.md` §Copywriting (exact banner strings).
  </read_first>
  <action>
    Add a soft-invalidate test: drive to a state with a computed match, then edit a completed upstream step (e.g. reload a different image / change size) and assert (1) the "This step is out of date" banner + "Recompute match" CTA appear, (2) the last-good `matchResult`-derived content is still on screen (no data loss / no auto re-fire), (3) advancing past the stale step is blocked, and (4) clicking "Recompute match" runs the match once and clears the stale state (banner gone, advance unblocked). Keep the test deterministic (mock/stub the match trigger as the suite already does). Confirm the linear forward-navigation tests are unaffected.
  </action>
  <acceptance_criteria>
    - `npx vitest run src/__tests__/App.test.tsx` exits 0 with the new soft-invalidate assertions passing.
    - `npx vitest run` exits 0 with ≥ 240 passing overall (SC5); no prior navigation/index/locked-Next test regressed.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run</automated>
  </verify>
  <done>The soft-invalidate flow (upstream edit → stale banner + blocked advance + retained last-good match → recompute clears) is asserted and green, and the full suite stays ≥240.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user edit → match pipeline | An upstream edit could trigger repeated expensive worker runs (resource exhaustion / the prior B2 abort-race) if not gated |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-11 | Denial of Service (self) | Silent match re-fire on every upstream edit | medium | mitigate | Soft-invalidate marks stale but does NOT auto-recompute; the expensive match runs once only on the intentional "Recompute match" click (D-13), avoiding the B2 abort-race. Unit-tested (Task 3). |
| T-20-12 | Tampering / data loss | Discarding the last-good match on invalidation | low | mitigate | The last-good matchResult/supplies stay on screen while stale (no clear-on-edit); recompute replaces it only on success. Asserted in Task 3. |
</threat_model>

<verification>
- Upstream edit after a match → downstream steps stale, last-good match retained, advance-past-stale blocked, one "Recompute match" banner ("This step is out of date").
- Recompute runs the match once and clears the stale state; no automatic re-fire.
- StepBar shows an accessible stale marker; useWizard indices and locked-Next semantics unchanged.
- `npx tsc --noEmit` exits 0; `npx vitest run` exits 0 with ≥ 240 passing (SC5).
</verification>

<success_criteria>
Soft-invalidate half of SHELL-02/SC4 satisfied (D-13): edits soft-invalidate without data loss or silent re-fire, advancing past stale is blocked, and one Recompute CTA re-runs the match once — all with the suite green (≥240) and the state machine indices frozen.
</success_criteria>

<output>
Create `.planning/phases/20-atelier-design-system-canvas-first-shell/20-05-SUMMARY.md` when done.
</output>
