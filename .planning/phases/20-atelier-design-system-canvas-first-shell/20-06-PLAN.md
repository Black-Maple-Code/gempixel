---
phase: 20-atelier-design-system-canvas-first-shell
plan: 06
type: execute
wave: 4
depends_on: ["20-04", "20-05"]
files_modified:
  - src/App.tsx
  - src/features/wizard/AtelierShell.tsx
  - src/__tests__/App.test.tsx
  - src/index.css
autonomous: true
gap_closure: true
requirements: [SHELL-01, SHELL-02]

must_haves:
  truths:
    - "The Atelier top bar shows exactly one visible 'GemPixel' wordmark; no second wordmark renders in the left sidebar."
    - "On a desktop viewport the canvas/photo is visible without scrolling, and the page does not scroll beyond the viewport with a match loaded."
    - "Sidebars scroll internally (their own scrollbars); StepBar gating, Save pill, and soft-invalidate/recompute stay unregressed."
  artifacts:
    - "src/App.tsx — legacy sidebar brand cluster (gem-logo tile + GemPixel h1 + tagline) removed; collapse button + sidebar preserved."
    - "src/features/wizard/AtelierShell.tsx — shell root carries a definite viewport height (h-dvh / h-[100dvh]) + overflow-hidden."
    - "src/__tests__/App.test.tsx — the two wordmark assertions retargeted to the top-bar wordmark span."
  key_links:
    - "AtelierShell root definite height (h-dvh) -> row wrapper flex-1 min-h-0 overflow-hidden caps to viewport -> sidebars' inner overflow-y-auto engage -> canvas above the fold."
    - "App.test.tsx wordmark assertions target `header span.font-display` (not the first <h1>) so removing the sidebar h1 keeps the suite green."
---

<objective>
Close the two shell-layout gaps surfaced in Phase 20 UAT Test 4 (both severity: major, both user-confirmed, both chrome/layout-only). GAP 1: a duplicate "GemPixel" wordmark — the legacy left-sidebar control-panel header still renders its own brand cluster below the new AtelierShell top-bar wordmark. GAP 2: the canvas sits below the fold because the shell's flex chain has no definite height, so the page balloons to the always-mounted supply-list aside and the center canvas floats to ~y=855 (below the 800px fold).

Both root causes are already diagnosed with line-level evidence (see the two debug sessions and 20-UAT.md Gaps). This plan turns those diagnoses into two atomic, independent fixes plus the mandatory test retarget and a browser + suite verification. It does NOT rebuild, delete, or restructure the legacy Step1..4 bodies (that is Phase 23/25).

Purpose: Restore the UI-SPEC single-wordmark contract and the canvas-first above-the-fold shell (SHELL-01) without regressing StepBar gating, the Save pill, or soft-invalidate (SHELL-02).
Output: Updated src/App.tsx, src/features/wizard/AtelierShell.tsx, src/__tests__/App.test.tsx (and, if needed, src/index.css), with tsc clean, the full Vitest suite green, and a browser-verified single wordmark + above-the-fold canvas.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
# Diagnosed root causes (do NOT re-diagnose — implement the fix direction)
@.planning/phases/20-atelier-design-system-canvas-first-shell/20-UAT.md
@.planning/debug/duplicate-gempixel-wordmark.md
@.planning/debug/canvas-below-fold.md

# Single-wordmark + canvas-first contract
@.planning/phases/20-atelier-design-system-canvas-first-shell/20-UI-SPEC.md

# Files this plan edits
@src/App.tsx
@src/features/wizard/AtelierShell.tsx
@src/__tests__/App.test.tsx
@src/index.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove legacy sidebar brand cluster + retarget wordmark tests (GAP 1)</name>
  <files>src/App.tsx, src/__tests__/App.test.tsx</files>
  <action>
    Fix GAP 1 (duplicate wordmark) surgically — no Step 1 deletion.

    In src/App.tsx, inside the left-sidebar control-panel header row (the `<div className="flex justify-between items-center border-b ...">` at ~1297), REMOVE the entire brand-cluster wrapper `<div className="flex items-center gap-3">` at ~1298 and everything it contains: the `.gem-logo` 3x3 pixel tile (~1299-1303), the sidebar `<h1>` wordmark (~1305), and the "Diamond Painting Planner" tagline `<p>` (~1306). PRESERVE the collapse `<button>` (~1309-1317) and its parent header `<div>` — after removal the header row holds only the collapse button; keep its `justify-between items-center` classes so the lone button stays where it was. Do NOT touch the sidebar body, the "My Images" drawer, the wizard step panels, `<main>`, or the supply-list aside. Do NOT delete or restructure any Step1..4 body (Phase 23/25 scope).

    The AtelierShell top-bar wordmark span at src/features/wizard/AtelierShell.tsx:61 is the single intended wordmark — leave it exactly as-is.

    In src/__tests__/App.test.tsx, retarget BOTH wordmark assertions (the block at ~99-101 in the "mounts under blocked storage" test and ~109-111 in the "renders dashboard shell elements" test). Today each does `container.querySelector('h1')` and asserts `textContent === 'GemPixel'`; after this removal the first `<h1>` in DOM order becomes the print-only report title, so those assertions would fail. Change each to query the top-bar wordmark instead: `container.querySelector('header span.font-display')` (the `font-display` class is unique to the AtelierShell wordmark — StepBar uses no `font-display`), assert it is truthy and its `textContent === 'GemPixel'`. In addition, add one regression assertion in each of the two tests: assert that `Array.from(container.querySelectorAll('h1'))` contains NO element whose `textContent === 'GemPixel'` (the surviving print-only `<h1>` reads the full report title, not an exact match) — this guards against re-introducing a duplicate wordmark heading. Leave src/__tests__/print.test.tsx untouched (its report-title assertion is unaffected).

    Do not leave a code comment that quotes the removed heading or tagline copy.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/App.test.tsx && npx tsc --noEmit</automated>
    <human-check>In the dev server at desktop width, confirm exactly ONE visible "GemPixel" wordmark (the top-bar Newsreader wordmark beside the green pixel tile) — no second "GemPixel" heading and no "Diamond Painting Planner" tagline in the left sidebar; the collapse-sidebar button still works.</human-check>
  </verify>
  <done>The sidebar brand cluster (gem-logo tile + GemPixel h1 + "Diamond Painting Planner" tagline) is gone; the collapse button and the sidebar itself remain and still function; both retargeted assertions pass against `header span.font-display`; no `<h1>` in the app DOM has textContent exactly 'GemPixel'; tsc is clean.</done>
</task>

<task type="auto">
  <name>Task 2: Give the shell a definite viewport height so the canvas sits above the fold (GAP 2)</name>
  <files>src/features/wizard/AtelierShell.tsx, src/index.css</files>
  <action>
    Fix GAP 2 (canvas below the fold) — the `min-h-screen` vs `h-screen` flexbox gotcha. The single load-bearing change is on the shell root's height.

    In src/features/wizard/AtelierShell.tsx, change the shell root `<div>` className at line 52 from the indefinite-height `flex flex-col min-h-screen` to a DEFINITE viewport height plus clip: `flex flex-col h-dvh overflow-hidden`. Prefer `h-dvh` (dynamic viewport height, correct under a mobile URL bar); if Tailwind v4 in this build does not expose the `h-dvh` utility, use the arbitrary value `h-[100dvh]` instead. Do NOT change the `<header>` (already `shrink-0`). This one change gives the whole flex chain a definite height, so the already-correctly-authored row wrapper at src/App.tsx:1290 (`flex flex-1 min-h-0 overflow-hidden`) finally caps to the viewport and both sidebars' inner `overflow-y-auto` engage — returning the canvas above the fold. Do NOT restructure the row wrapper, `<main>` (App.tsx:1594), the center canvas column (App.tsx:1624), or the always-mounted supply-list `<aside>` (App.tsx:1867); their internal-scroll layout is already right and only failed because the parent height was indefinite.

    In src/index.css, inspect the `body` rule at ~118-125 (`@apply antialiased select-none min-h-screen; ...`). Leave the body's min-height floor as-is UNLESS it reintroduces page scroll beyond the viewport once the root is `h-dvh`; it should not, because the root now sets an exact `100dvh`. Do not add any rule that makes the page taller than the viewport.

    Optional, only if the canvas still visually floats low after the height fix: top-align the center canvas column (App.tsx:1624) by swapping `items-center` -> `items-start` and adding top padding. This is NOT required once the height is definite — do not apply it speculatively.

    Do not leave a code comment that quotes the previous root class name.
  </action>
  <verify>
    <automated>grep -cE 'h-dvh|h-\[100dvh\]' src/features/wizard/AtelierShell.tsx && npx tsc --noEmit && npx vitest run</automated>
    <human-check>In the dev server at desktop width (~1280x800), load an image so a match computes and the DMC supply list populates (183-207 rows). Confirm: (a) the canvas/photo is visible WITHOUT scrolling — its top is above the ~800px fold — and the whole page does NOT scroll beyond the viewport; (b) the left sidebar and right supply list scroll on their OWN inner scrollbars, not the whole page; (c) no regressions — StepBar gating, the dark "Save" pill, and soft-invalidate all still work (change width after a match -> amber "This step is out of date" + "Recompute match" banner, last-good canvas retained, Next blocked, Recompute clears it).</human-check>
  </verify>
  <done>The AtelierShell root uses a definite viewport height (`h-dvh` or `h-[100dvh]`) plus `overflow-hidden`; the grep returns >= 1; tsc is clean; the full Vitest suite is green; the body rule introduces no page scroll beyond the viewport.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client render -> DOM/CSS | This gap-closure is a pure presentational change: it removes DOM nodes and swaps Tailwind height classes. No new untrusted input, network call, storage schema, or parsing path is introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-06-01 | Tampering | Shell layout classes / sidebar DOM (AtelierShell root, App.tsx sidebar header) | low | accept | No new attack surface — CSS-class + DOM-structure change only, no input handling, no network, no persisted-data change. Layout-regression risk is covered by the full Vitest suite (`npx vitest run`) plus the blocking human-verify browser check (Task 3). |

No npm/pip/cargo install tasks in this plan, so no supply-chain (`T-{phase}-SC`) row is required.
</threat_model>

<verification>
- `npx tsc --noEmit` is clean.
- `npx vitest run` (full suite, including the two retargeted App.test.tsx wordmark assertions) is green.
- `grep -cE 'h-dvh|h-\[100dvh\]' src/features/wizard/AtelierShell.tsx` returns >= 1 (definite viewport height on the shell root).
- End-of-phase browser human-check (per `human_verify_mode: end-of-phase`; captured in the tasks' `<human-check>` blocks): at desktop width on the dev server — exactly ONE visible "GemPixel" wordmark; canvas/photo visible without scrolling with a match loaded; sidebars scroll internally; StepBar gating, Save pill, and soft-invalidate/recompute unregressed.
</verification>

<success_criteria>
- GAP 1 closed: exactly one "GemPixel" wordmark (the AtelierShell top-bar span); the legacy sidebar brand cluster is removed; the collapse button and sidebar remain functional.
- GAP 2 closed: the shell root has a definite viewport height; the page does not scroll beyond the viewport with a match loaded; the canvas is above the fold; sidebars scroll internally.
- No regressions: StepBar gating/a11y, Save pill, and D-13 soft-invalidate/recompute all still work.
- tsc clean; full Vitest suite green.
</success_criteria>

<output>
Create `.planning/phases/20-atelier-design-system-canvas-first-shell/20-06-SUMMARY.md` when done.
</output>
