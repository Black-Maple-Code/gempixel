---
phase: 20-atelier-design-system-canvas-first-shell
plan: 04
type: execute
wave: 2
depends_on: ["20-02", "20-03"]
files_modified:
  - src/App.tsx
  - src/__tests__/App.test.tsx
autonomous: true
requirements: [SHELL-01, SHELL-02]
must_haves:
  truths:
    - "SC3: The horizontal 4-step StepBar is the ONLY navigator — the desktop footer dot-nav and the desktop top step bar are both deleted and replaced by one AtelierShell/StepBar; no second step navigator remains."
    - "SC4: The single CanvasViewer is mounted once and never remounts on step changes — the four step panels are CSS-toggled siblings (hidden/display:none), not {step===n && …} unmount branches, and the viewer host stays above any step branch."
    - "D-04: The useWizard 1..4 indices and the locked-Next semantics are preserved; the primary forward CTA keeps id=wizard-next-btn so the existing navigation tests stay green."
    - "SC5: The existing viewer/legend/supply UI still functions inside the new shell and the 240+ Vitest suite stays green."
  artifacts:
    - "src/App.tsx — <AtelierShell> composed at shell scope, both legacy step-nav surfaces deleted, step panels converted to CSS-toggled siblings, primary CTA retains id=wizard-next-btn"
    - "src/__tests__/App.test.tsx — step-tab lookups updated from old labels to STEP_META labels; single-navigator + single-mount identity assertions"
  key_links:
    - "AtelierShell/StepBar ↔ App wizard state (App passes {step, canEnter, goTo}; App stays the owner — D-01)"
    - "id=wizard-next-btn on the new CTA ↔ ~30 existing navigation-test assertions (must be preserved — D-04)"
    - "step panels as hidden siblings ↔ CanvasViewer never remounting (SC4 falls out of tree position — D-14)"
---

<objective>
Wire the Plan 03 chrome into App.tsx: delete both legacy step-nav surfaces, compose `<AtelierShell>`/`<StepBar>` as the single navigator, and convert the four step panels from `{step===n && …}` unmount branches into CSS-toggled siblings so the single `<CanvasViewer>` never remounts on a step change. Reconcile the navigation tests that assert on the old step labels while preserving the locked-Next affordance.

Purpose: SHELL-01 (one-and-only navigator, structurally — D-03) and the single-mount half of SHELL-02/SC4 (D-14). This is the structural heart of the strangler shell: the legacy Step1..4 bodies stay 1:1 unchanged, only their mount/nav wrapper changes.
Output: an App.tsx that renders through AtelierShell with one nav component, CSS-toggled step panels, a preserved `wizard-next-btn`, and a green suite with label-reconciled tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-atelier-design-system-canvas-first-shell/20-CONTEXT.md
@.planning/phases/20-atelier-design-system-canvas-first-shell/20-PATTERNS.md
@.planning/phases/20-atelier-design-system-canvas-first-shell/20-UI-SPEC.md
@.planning/phases/20-atelier-design-system-canvas-first-shell/20-02-SUMMARY.md
@.planning/phases/20-atelier-design-system-canvas-first-shell/20-03-SUMMARY.md
@src/features/wizard/useWizard.ts
</context>

<artifacts_this_phase_produces>
Wirings this plan creates (source-grounding exclusion list — NEW, not drift):
- `src/App.tsx`: `<AtelierShell>` composition at shell scope wrapping the step panels + viewer; both legacy nav surfaces removed; the four `{wizard.step === n && <StepN…/>}` branches converted to always-rendered siblings toggled with `hidden`/`display:none`; the primary forward CTA retains `id="wizard-next-btn"`.
- `src/__tests__/App.test.tsx`: step-tab lookups migrated to STEP_META labels (Refine/Supplies/Order); a single-mount identity assertion (CanvasViewer constructed once across step changes); a single-navigator assertion.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Delete both legacy step-nav surfaces and compose AtelierShell as the single navigator</name>
  <files>src/App.tsx</files>
  <read_first>
    - `src/App.tsx` lines 1456-1573 (the sticky wizard footer with the dot-nav at 1471-1497 AND the `#wizard-next-btn` at 1499-1510; the desktop top step bar at 1519-1551 with the `btn-chunk` "Next Step" at 1553-1560 and the `btn-chunk-2` Save at 1562-1571). Also lines 2183-2238 (the MOBILE bottom tab bar — Setup/Canvas/Colors — which is a panel toggle, NOT a step nav: leave it untouched, it is Phase 24).
    - `.planning/phases/20-atelier-design-system-canvas-first-shell/20-PATTERNS.md` §"src/App.tsx" ("Nav surfaces to delete" — desktop top step bar + sticky dot-nav footer; the mobile bottom bar is a panel toggle, leave it) + §"AtelierShell.tsx" (Save-pill analog).
    - `.planning/phases/20-atelier-design-system-canvas-first-shell/20-CONTEXT.md` D-01, D-02, D-03, D-04.
    - `.planning/phases/20-atelier-design-system-canvas-first-shell/20-03-SUMMARY.md` (the AtelierShell/StepBar props contract, incl. the `onSave`/`canSave` shape).
    - `src/__tests__/App.test.tsx` lines around 236/387/429/487/516/526/532/596/600/675/747/831/848/962/1109 (every `#wizard-next-btn` navigation) and 182-184/242-244/258-259/275-277 (step-tab lookups by old `title` labels).
  </read_first>
  <action>
    Delete the two desktop step-nav surfaces (D-03): the sticky-footer dot-nav block (the `[1,2,3,4].map` dots) and the desktop top step bar (the `['Upload','Size','Colors','Supplies'].map` row). Replace them with a single `<AtelierShell step={wizard.step} canEnter={wizard.canEnter} goTo={wizard.goTo} onSave={…} canSave={!!matchResult}>` composition so `<StepBar>` is the sole step navigator (SHELL-01). App keeps ownership of wizard state and passes it down (D-01). Move the existing Save action (the `setSaveModalOpen(true)` handler from the old top-bar Save) into `onSave`.
    CRITICAL — preserve the locked-Next affordance (D-04): keep a primary forward CTA that carries `id="wizard-next-btn"`, calls `wizard.next`, and is `disabled={!(wizard.canEnter(wizard.step + 1) || isTestEnv)}` exactly as the old top-bar Next did. ~30 existing tests click `#wizard-next-btn` to advance steps; that id and its disabled semantics MUST survive (place the CTA inside AtelierShell or adjacent to StepBar). Do NOT change useWizard or its 1..4 indices.
    Do NOT delete or alter the mobile bottom tab bar (panel toggle, Phase 24). Do NOT touch the legacy Step1..4 bodies (Phase 23/25).
  </action>
  <acceptance_criteria>
    - `grep -c "wizard-next-btn" src/App.tsx` ≥ 1 (the locked-Next CTA survives with its id).
    - `grep -c "AtelierShell" src/App.tsx` ≥ 1 and `grep -c "StepBar" src/App.tsx` counts StepBar only via AtelierShell (no second nav surface): `grep -c "\.map" src/App.tsx` on the deleted nav rows returns 0 for the old `['Upload','Size','Colors','Supplies']` and `[1, 2, 3, 4].map` dot-nav (both gone).
    - `grep -c "'Upload', 'Size', 'Colors', 'Supplies'" src/App.tsx` returns 0 and the old dot-nav `[1, 2, 3, 4].map(step =>` nav block is removed.
    - The mobile bottom tab bar remains: `grep -c "Mobile Bottom Tab Bar" src/App.tsx` still returns 1.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Both desktop step-nav surfaces are gone; AtelierShell/StepBar is the single navigator; the primary CTA keeps id=wizard-next-btn with unchanged locked-Next semantics; the mobile panel-toggle bar and legacy step bodies are untouched; typecheck clean.</done>
</task>

<task type="auto">
  <name>Task 2: Convert the four step panels to CSS-toggled siblings and lock the single-mount viewer guarantee</name>
  <files>src/App.tsx</files>
  <read_first>
    - `src/App.tsx` lines 1295-1415 (the four `{wizard.step === n && <StepNIngest/Palette/Canvas/Export …/>}` gated panels); lines 507-533 (viewer teardown effect + the guarded `new CanvasViewer` init, `if (!viewerRef.current)`); lines 535-562 (data-sync effect + the CSS-var effect); and the canvas host location (`<canvas ref={canvasRef}>`, already always-mounted while an image exists, inside the `{(image || matchResult) ? …}` branch in `<main>`).
    - `.planning/phases/20-atelier-design-system-canvas-first-shell/20-PATTERNS.md` §"Single-mount viewer (D-14)" (canvas host is already always-mounted; convert the four panels from `&&`-unmount to `hidden`/`display:none` siblings; preserve the viewer init/teardown/data-sync effects untouched) + §"Canvas can't read CSS vars".
    - `.planning/phases/20-atelier-design-system-canvas-first-shell/20-RESEARCH.md` §4a (Preact never unmounts a node that stays in the tree; display:none is a style change, not a reconciliation removal) + §4b (the display:none 0×0-measurement gotcha: keep the viewer host itself always visible — only the surrounding step panels toggle; if a panel has a secondary measured element prefer visibility:hidden or re-fit on reveal).
    - `.planning/phases/20-atelier-design-system-canvas-first-shell/20-CONTEXT.md` D-14, D-04.
  </read_first>
  <action>
    Convert each of the four step panels from a `{wizard.step === n && <StepN …/>}` conditional (which unmounts on step change) into an always-rendered sibling wrapped in a container toggled by `hidden` (equivalently `display:none`) when `wizard.step !== n`. All four panels stay in the tree at all times; only their visibility toggles. This keeps everything around the single `<CanvasViewer>` stable so the viewer never remounts on a step change (SC4/D-14). Do NOT move the canvas host into a step branch; keep it above/outside the step panels (it is already always-mounted while an image exists). Leave the viewer init (`new CanvasViewer`, guarded once), teardown, and data-sync effects exactly as they are — the never-remount guarantee falls out of tree position, not new lifecycle code (no keep-alive/freeze lib, D-14).
    Watch the display:none measurement gotcha (RESEARCH §4b): the viewer host must remain visible (it is at shell scope, never a hidden sibling); if any step panel contains a secondary measured element that would size to 0×0 while hidden, prefer `visibility:hidden`/off-screen for that node or re-fit on reveal via the existing fit path — do NOT let a hidden panel drive the canvas to 0×0. Do NOT change any legacy Step body props/behavior (Phase 23).
  </action>
  <acceptance_criteria>
    - `grep -c "wizard.step === 1 &&" src/App.tsx` returns 0 and likewise for `=== 2/3/4` on the step PANELS (the four panels are no longer `&&`-gated; they are `hidden`-toggled siblings). (The unrelated `wizard.step === 3 && matchResult` mobile block may remain — scope this grep to the panel region 1295-1415.)
    - All four `<Step1Ingest/>`…`<Step4Export/>` renders are present unconditionally in the tree (grep each component name returns ≥1).
    - `npx tsc --noEmit` exits 0.
    - `npx vitest run` exits 0 with ≥ 240 passing (existing step-content tests still find their panels — SC5).
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run</automated>
  </verify>
  <done>The four step panels are always-mounted CSS-toggled siblings; the CanvasViewer host stays above any step branch and its lifecycle effects are unchanged; no panel drives the canvas to 0×0; suite green (≥240).</done>
</task>

<task type="auto">
  <name>Task 3: Reconcile navigation tests to STEP_META labels and assert single-navigator + single-mount identity</name>
  <files>src/__tests__/App.test.tsx</files>
  <read_first>
    - `src/__tests__/App.test.tsx` lines 182-184, 242-244, 258-259, 275-277 (step-tab lookups by old `title`/text: `'Palette & Optimize'`, `'Cost & Order'`) and the `#wizard-next-btn` navigation blocks (must keep working unchanged).
    - `.planning/phases/20-atelier-design-system-canvas-first-shell/20-RESEARCH.md` §5 (Single-mount identity test bucket: assert CanvasViewer is constructed once and not re-instantiated across step changes — spy on the constructor / `destroy`).
    - `src/engine/__tests__/viewer.test.ts` (how CanvasViewer is constructed/mocked in the suite, for the identity spy idiom).
    - `.planning/phases/20-atelier-design-system-canvas-first-shell/20-CONTEXT.md` D-02, D-04, D-14; `.planning/phases/20-atelier-design-system-canvas-first-shell/20-03-SUMMARY.md` (final STEP_META label strings).
  </read_first>
  <action>
    Update the step-tab lookups in App.test.tsx that reference the OLD labels to the new STEP_META labels (D-02 intentionally renamed the display labels; the useWizard 1..4 INDEX + locked-Next assertions do NOT change — D-04): step 2 `Palette & Optimize` → `Refine`, step 3 `Cost & Order` → `Supplies`, step 4 `Save` (tab) → `Order`, step 1 stays `Upload`. Migrate the `b.title === 'Cost & Order' || b.textContent…` finders to match the StepBar step button by its new label text. Leave every `#wizard-next-btn` navigation exactly as-is (that affordance was preserved in Task 1).
    Add two assertions: (1) single-navigator — after the redesign the old dot-nav / desktop top-bar step surfaces are absent and StepBar is the only step navigator present; (2) single-mount identity (SC4/D-14) — advancing across steps does NOT re-instantiate CanvasViewer (spy on the constructor or `destroy`; assert constructed once across a step change). Keep changes minimal and deterministic; do not weaken existing index/locked-Next assertions.
  </action>
  <acceptance_criteria>
    - `grep -c "Cost & Order" src/__tests__/App.test.tsx` returns 0 and `grep -c "Palette & Optimize" src/__tests__/App.test.tsx` returns 0 (old labels fully migrated).
    - The suite contains a single-mount identity assertion (CanvasViewer constructed once across a step change) and a single-navigator assertion.
    - `npx vitest run` exits 0 with ≥ 240 passing (SC5); every prior `#wizard-next-btn` navigation test still passes.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run</automated>
  </verify>
  <done>App.test.tsx uses STEP_META labels, asserts StepBar is the sole navigator and the viewer is single-mount across step changes, and the full suite is green (≥240) with the locked-Next tests intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App state → shell/panel rendering | Structural refactor of an existing tree; no new external input or privileged operation crosses this boundary (chrome only) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-08 | Denial of Service (self) | display:none step panels driving the canvas to 0×0 | medium | mitigate | Keep the viewer host always visible at shell scope; prefer visibility:hidden/re-fit for any secondary measured node (RESEARCH §4b). Guarded by the single-mount identity test + existing viewer tests. |
| T-20-09 | Tampering | Locked-Next / gating regression | low | mitigate | `id=wizard-next-btn` preserved with unchanged `canEnter`-driven disabled state; useWizard indices untouched; ~30 navigation tests + the index/locked-Next assertions stay green. |
| T-20-10 | Repudiation | (n/a — client-only chrome, no auditable transaction) | low | accept | No server, no transaction; nothing to log. |
</threat_model>

<verification>
- Both legacy step-nav surfaces deleted; `<AtelierShell>`/`<StepBar>` is the single navigator; `id=wizard-next-btn` preserved (SC3, D-03/D-04).
- Four step panels are always-mounted `hidden`-toggled siblings; CanvasViewer constructed once across step changes (SC4, D-14).
- Mobile bottom tab bar and legacy Step1..4 bodies untouched.
- `npx tsc --noEmit` exits 0; `npx vitest run` exits 0 with ≥ 240 passing (SC5).
</verification>

<success_criteria>
SHELL-01 satisfied structurally (one nav component), SC4 satisfied by construction (single-mount viewer + CSS-toggled panels), D-04 preserved (indices + locked-Next + wizard-next-btn), and the legacy viewer/legend/supply UI still functions inside the shell with the suite green (≥240).
</success_criteria>

<output>
Create `.planning/phases/20-atelier-design-system-canvas-first-shell/20-04-SUMMARY.md` when done.
</output>
