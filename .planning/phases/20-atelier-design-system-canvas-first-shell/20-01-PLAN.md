---
phase: 20-atelier-design-system-canvas-first-shell
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - src/main.tsx
  - vite.config.ts
  - src/index.css
  - src/engine/viewer.ts
  - src/engine/export.ts
  - src/engine/symbols.ts
autonomous: true
requirements: [DESIGN-01, DESIGN-02]
must_haves:
  truths:
    - "SC2: The UI renders from Atelier design tokens (bg #F4F1E9, accent green #0E6E5C, 8px spacing, card radius 12px, card shadow) with self-hosted Newsreader/Archivo/JetBrains Mono fonts."
    - "SC2: No external font request is made (both Google-Fonts imports deleted from src/index.css); the built CSS references no fonts.googleapis.com URL."
    - "SC2: No font-driven layout shift — Fontaine emits metric-matched fallback @font-face (size-adjust/ascent-override/descent-override) and each --font-* value references its generated fallback family."
    - "SC1 (CSS side): The dual [data-theme] skins are flattened onto :root Atelier light values; no [data-theme] selector remains in src/index.css."
    - "SC5: engine/* signatures are unchanged (only string-literal 'Outfit'→'Archivo Variable' repoint + PHASE 22 markers); the 240+ Vitest suite stays green."
  artifacts:
    - "src/main.tsx — four @fontsource JS imports above ./index.css"
    - "vite.config.ts — FontaineTransform.vite({ fallbacks, resolvePath }) wiring"
    - "src/index.css — :root Atelier tokens, @theme inline --font-serif/--font-sans/--font-mono (+ Fontaine fallback families), redefined --font-display, radius/shadow tokens"
    - "package.json — @fontsource-variable/newsreader, @fontsource-variable/archivo, @fontsource/jetbrains-mono (deps) + fontaine (devDep)"
  key_links:
    - "@theme inline --font-* values → Tailwind font-serif/font-sans/font-mono utilities → the rendered chrome"
    - "Fontaine-generated 'Family fallback' face ↔ the fallback family named inside each --font-* value (must match exactly or no-CLS silently fails)"
    - "Deleted Outfit @import ↔ the three engine canvas symbol-font literals (repointed to 'Archivo Variable' so symbols keep a loaded font)"
---

<objective>
Land the Atelier light design tokens and self-hosted, no-CLS webfont system, and reconcile the engine's canvas symbol-font literals so nothing is orphaned when the external font imports are deleted.

Purpose: DESIGN-02 (self-hosted fonts, no external request, no FOUT/CLS) and the CSS half of DESIGN-01 (flatten the dual `[data-theme]` skins to a single Atelier `:root`). This is the visual-skin foundation the rest of the phase renders against.
Output: `@fontsource` + `fontaine` wired through `main.tsx`/`vite.config.ts`, `src/index.css` carrying unconditional Atelier tokens + self-hosted font stacks, and the three engine symbol-font literals repointed (string-literal change only — engine signatures stay frozen per the strangler rule).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-atelier-design-system-canvas-first-shell/20-CONTEXT.md
@.planning/phases/20-atelier-design-system-canvas-first-shell/20-RESEARCH.md
@.planning/phases/20-atelier-design-system-canvas-first-shell/20-PATTERNS.md
@.planning/phases/20-atelier-design-system-canvas-first-shell/20-UI-SPEC.md
</context>

<artifacts_this_phase_produces>
Symbols/wirings this plan creates (source-grounding exclusion list — these are NEW, not drift):
- `src/main.tsx`: four `@fontsource` JS imports (Newsreader/Archivo variable `wght.css`, JetBrains Mono `400.css` + `700.css`) placed above the existing `import './index.css'`.
- `vite.config.ts`: `FontaineTransform.vite({ fallbacks, resolvePath })` added to the plugin array after `tailwindcss()`.
- `src/index.css` new/overridden `@theme inline` tokens: `--font-serif`, `--font-sans`, `--font-mono` (each including its Fontaine `'<Family> fallback'` family), redefined `--font-display`; new Atelier radius/shadow tokens (`--radius-card` 12px, `--radius-control` 8px, `--radius-pill` 20px, `--shadow-card` `0 40px 80px -30px rgba(0,0,0,0.5)` — names follow existing convention, Claude's Discretion).
- `package.json`: `@fontsource-variable/newsreader@5.2.10`, `@fontsource-variable/archivo@5.2.8`, `@fontsource/jetbrains-mono@5.2.8` (deps); `fontaine@0.8.0` (devDep).
- `// PHASE 22: remove theme param` markers on the three engine symbol-font literals.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Install self-hosted font packages and wire the font-load + no-CLS pipeline</name>
  <files>package.json, package-lock.json, src/main.tsx, vite.config.ts</files>
  <read_first>
    - `.planning/phases/20-atelier-design-system-canvas-first-shell/20-RESEARCH.md` §"Standard Stack" (exact versions + entrypoints), §1a (JS import in main.tsx), §2a (Fontaine `vite.config` wiring), and §"Package Legitimacy Audit" (all four packages Approved, no [ASSUMED]/[SUS] — no legitimacy checkpoint required).
    - `.planning/phases/20-atelier-design-system-canvas-first-shell/20-PATTERNS.md` §"src/main.tsx" and §"vite.config.ts" (current 9-line entrypoint; current 18-line config; plugin array `[preact(), tailwindcss()]`; the `test.environment: 'node'` block must be preserved).
    - `src/main.tsx` (current entrypoint), `vite.config.ts` (current config).
  </read_first>
  <action>
    Install exactly (versions per RESEARCH Standard Stack; audit already Approved): `@fontsource-variable/newsreader@5.2.10`, `@fontsource-variable/archivo@5.2.8`, `@fontsource/jetbrains-mono@5.2.8` as dependencies, and `fontaine@0.8.0` as a devDependency. Do NOT add any other UI/slider/font-loader/PDF dependency (Out-of-Scope constraint).
    In `src/main.tsx`, add the four `@fontsource` CSS-entrypoint JS imports (Newsreader `wght.css`, Archivo `wght.css`, JetBrains Mono `400.css` and `700.css`) ABOVE the existing `import './index.css'` line — JS import (Vite asset graph), NOT a CSS `@import` (dodges the Tailwind v4 external-URL resolution bug per RESEARCH §1a / Alternatives).
    In `vite.config.ts`, import `FontaineTransform` from `fontaine` and add `FontaineTransform.vite({ fallbacks: [...], resolvePath })` to the plugin array immediately after `tailwindcss()` — this is the no-CLS mechanism (D-11: `font-display: swap`, already baked into the @fontsource faces, plus Fontaine's auto-generated `size-adjust`/`ascent-override`/`descent-override` fallback metrics; NOT hand-authored metrics, NOT `font-display: optional`). Use the system fallbacks from RESEARCH §2a and a `resolvePath` that maps @fontsource's `url(./files/*.woff2)` id into `node_modules/@fontsource…`. Preserve the existing `test`, `resolve.alias` blocks unchanged. `resolvePath` is the one MEDIUM item — Task 3's build spike proves it; if Fontaine logs "could not resolve", adjust `resolvePath` to reconstruct the node_modules path (fallback path documented in RESEARCH §2b).
  </action>
  <acceptance_criteria>
    - `npm ls @fontsource-variable/newsreader @fontsource-variable/archivo @fontsource/jetbrains-mono fontaine` resolves all four at the pinned versions.
    - `grep -c "@fontsource" src/main.tsx` returns 4, and all four import lines appear on lines above the `./index.css` import (verify order).
    - `grep -c "FontaineTransform" vite.config.ts` is ≥ 1 and `grep -c "environment: 'node'" vite.config.ts` is still 1 (test block preserved).
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>All four packages installed at pinned versions; main.tsx imports the font entrypoints before index.css; vite.config wires Fontaine without disturbing the test/alias blocks; typecheck clean.</done>
</task>

<task type="auto">
  <name>Task 2: Flatten dark-mode CSS to a single Atelier :root, wire self-hosted font stacks, and add radius/shadow tokens</name>
  <files>src/index.css</files>
  <read_first>
    - `src/index.css` lines 1-175 (the two external font `@import` lines at top; the `:root, [data-theme="dark"]` block at 10-31; the `[data-theme="light"]` override at 33-53; the `@theme inline` block at 69-129 defining only `--font-display` at 88; `@layer base` body font at 136 and `code, pre, .font-mono` at 139-141).
    - `.planning/phases/20-atelier-design-system-canvas-first-shell/20-RESEARCH.md` §1b/§1c (delete both external imports; `--font-serif/sans/mono` override values WITH the Fontaine fallback family appended; redefine `--font-display` once on :root as the Newsreader variable family), §2b (the CSS-variable fallback-name gotcha — Fontaine will NOT auto-append the fallback to names declared in custom properties, so it must be written manually), §3a (the flatten is a value-source change, safe under Tailwind v4).
    - `.planning/phases/20-atelier-design-system-canvas-first-shell/20-UI-SPEC.md` §Color (exact Atelier hexes + Ink scale), §"Radius & Shadow" (card 12px, controls 7-8px, pills 20px, card shadow `0 40px 80px -30px rgba(0,0,0,0.5)`), §Typography (which faces own which role), §Spacing.
    - `.planning/phases/20-atelier-design-system-canvas-first-shell/20-CONTEXT.md` D-05, D-09, D-10 (drop Pixelify entirely; wordmark = Newsreader).
  </read_first>
  <action>
    Delete the two external Google-Fonts stylesheet `@import` lines at the very top of the file (the SC2 no-external-request gate). Delete the entire line — do NOT leave a placeholder comment that names the removed URL (a negative grep counts comments).
    Flatten the dual skins (D-05): collapse the shared dark block and the light override so a single unconditional `:root` carries the Atelier LIGHT values (the current light set: bg `#F4F1E9`, panel `#FCFAF4`, panel-2 `#FFFFFF`, border `#E4DECF`, border-2 `#EEE8D9`, ink `#1B1A17`, muted `#6B6459`, faint `#9A927D`, accent + accent-2 `#0E6E5C`, warn `#A85A11`, on-accent `#F4F1E9`, grid-dot transparent, canvas-gap `#E4DECF`, drill-round-backing `#C9BFA6`, btn shadows none). Remove both theme-scoped selector blocks entirely so no such selector remains anywhere in the file. Keep the theme-independent gem-palette `:root` block.
    Font wiring (D-09/D-10, RESEARCH §1c): inside the existing `@theme inline` block add `--font-serif`, `--font-sans`, `--font-mono`, each value a stack that starts with the real self-hosted family (`'Newsreader Variable'` / `'Archivo Variable'` / `'JetBrains Mono'`), then its Fontaine-generated fallback family (`'Newsreader Variable fallback'` etc. — the exact emitted name is confirmed by Task 3; adjust if Fontaine's default differs), then the system fallbacks. Redefine `--font-display` once on `:root` (not per-skin) to the Newsreader variable family so the wordmark + titles use Newsreader (D-10, Pixelify dropped). In `@layer base`, change the `body` font-family from the old display-sans literal to `var(--font-sans)` (Archivo now owns body/UI), and append the mono fallback family to the `code, pre, .font-mono` rule so it reads real family + `'JetBrains Mono fallback'` + `monospace`.
    Add Atelier radius + shadow tokens (Claude's Discretion on names, follow existing convention): `--radius-card: 12px`, `--radius-control: 8px`, `--radius-pill: 20px`, `--shadow-card: 0 40px 80px -30px rgba(0,0,0,0.5)`. The new chrome (Plan 03) consumes these so DESIGN-02's "radii/shadows" requirement is token-backed.
  </action>
  <acceptance_criteria>
    - `grep -c "fonts.googleapis.com" src/index.css` returns 0 (no external font request; comments included).
    - `grep -c "\[data-theme" src/index.css` returns 0 (dual skins fully flattened, no theme selector remains).
    - `grep -c "Pixelify" src/index.css` returns 0 (D-10).
    - `grep -c "Archivo Variable" src/index.css` ≥ 1 and `grep -c "Newsreader Variable" src/index.css` ≥ 1 and `grep -c "JetBrains Mono" src/index.css` ≥ 1.
    - `grep -Ec "fallback" src/index.css` ≥ 3 (a fallback family named inside each of --font-serif/--font-sans/--font-mono, per the §2b gotcha).
    - `grep -c "0 40px 80px -30px" src/index.css` ≥ 1 (card shadow token) and the file contains a `--bg` value of `#F4F1E9` resolving on `:root`.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>src/index.css has no external font import and no [data-theme] selector; Atelier light tokens live unconditionally on :root; --font-serif/sans/mono resolve to self-hosted stacks each naming a Fontaine fallback family; radius/shadow tokens exist; typecheck clean.</done>
</task>

<task type="auto">
  <name>Task 3: Repoint the engine canvas symbol-font literals and prove the no-CLS build spike</name>
  <files>src/engine/viewer.ts, src/engine/export.ts, src/engine/symbols.ts</files>
  <read_first>
    - `src/engine/viewer.ts:392`, `src/engine/export.ts:85`, `src/engine/export.ts:173` (the three hard-coded canvas symbol-font family literals inside `ctx.font = ...`), `src/engine/symbols.ts:22` (a comment naming the old family).
    - `.planning/phases/20-atelier-design-system-canvas-first-shell/20-RESEARCH.md` §0-finding + Pitfall D + Open Q1 (repoint to `'Archivo Variable'` — a string-literal change, NOT an engine signature change, so the strangler rule / D-07 is honored); §2 (build-spike: confirm Fontaine emits fallback `@font-face` with `size-adjust`/`ascent-override`/`descent-override`).
    - `.planning/phases/20-atelier-design-system-canvas-first-shell/20-CONTEXT.md` D-07 (engine signatures frozen; PHASE 22 markers).
    - `src/engine/__tests__/export.test.ts`, `src/engine/__tests__/viewer.test.ts` (confirm they don't assert on the font literal — the repoint must keep them green).
  </read_first>
  <action>
    In each of the three engine call sites, change ONLY the canvas symbol-font family string literal (currently the old display-sans name) to `'Archivo Variable'`, keeping the surrounding `ctx.font` template and `sans-serif` fallback and the `symbolFontPx(...)` call identical — this is a string-literal edit, not a signature or behavior change (D-07-safe; the strangler rule that engine signatures freeze until Phase 22 is intact). Add a `// PHASE 22: remove theme param` marker comment adjacent to each edited line. Update the `symbols.ts:22` comment so it no longer names the old family (rephrase to reference the body/UI face) so the negative grep is clean; no code change in symbols.ts.
    Then run the no-CLS build spike (RESEARCH §2): `npm run build`, and confirm the emitted dist CSS contains Fontaine's injected fallback `@font-face` metric overrides and no external font URL. If the build logs a Fontaine "could not resolve" warning or the dist CSS has no `size-adjust`/`ascent-override`, adjust `vite.config.ts` `resolvePath` (Task 1) until the overrides appear; only if Fontaine genuinely cannot resolve the woff2 fall back to hand-authored `@font-face` metrics named identically to the `--font-*` fallback families (RESEARCH §2b Alternative). Do NOT introduce a comment in any engine file that names the old font literal.
  </action>
  <acceptance_criteria>
    - `grep -rn "'Outfit'" src/engine/` returns 0 matches (all three literals repointed; the symbols.ts comment no longer names the old family).
    - `grep -c "Archivo Variable" src/engine/viewer.ts` ≥ 1 and `grep -c "Archivo Variable" src/engine/export.ts` is 2.
    - `grep -c "PHASE 22" src/engine/viewer.ts` ≥ 1 and `grep -c "PHASE 22" src/engine/export.ts` ≥ 1.
    - `npm run build` exits 0; the built CSS under `dist/` contains at least one `size-adjust` and one `ascent-override` (Fontaine fallback faces present) and contains no `fonts.googleapis.com` reference. (`grep -rc "size-adjust" dist/ | grep -qv ':0'` and `! grep -rq "fonts.googleapis.com" dist/`.)
    - `npx vitest run` exits 0 with ≥ 240 passing (SC5); the engine tests (export/viewer/symbols) are unchanged and green.
  </acceptance_criteria>
  <verify>
    <automated>npm run build && npx vitest run</automated>
  </verify>
  <done>Three engine symbol-font literals repointed to 'Archivo Variable' (string-only, signatures frozen, PHASE 22 markers); build emits Fontaine metric-fallback faces with no external font URL; full suite green ≥240.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → third-party font CDN | External Google-Fonts request (being removed) leaks client IP/User-Agent to a third party — a privacy-leak vector for this privacy-first, no-backend app |
| build → npm registry | New package installs cross the supply-chain boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-01 | Information Disclosure | External `@import` in src/index.css | medium | mitigate | Delete both Google-Fonts imports; self-host via @fontsource so no request leaves the client (Task 2, DESIGN-02/SC2). Grep-gated to 0 external font URLs in source and dist. |
| T-20-SC | Tampering (supply chain) | npm installs (@fontsource×3, fontaine) | high | mitigate | RESEARCH `## Package Legitimacy Audit` verified all four as established fontsource.org / UnJS packages, pinned exact versions, contents inspected locally — Approved, no [ASSUMED]/[SUS]/[SLOP], so no blocking human checkpoint required. Versions pinned in package.json. |
| T-20-02 | Tampering | Fontaine build transform output | low | accept | Fontaine injects pure CSS `@font-face` at build time (zero runtime JS); output is deterministic and grep-verified in Task 3. No runtime attack surface added. |
</threat_model>

<verification>
- `grep -c "fonts.googleapis.com" src/index.css` == 0 and `! grep -rq "fonts.googleapis.com" dist/` (no external font request anywhere — SC2).
- `grep -c "\[data-theme" src/index.css` == 0 (CSS half of dark-mode retirement — SC1).
- `grep -rn "'Outfit'" src/engine/` == 0 (engine literals repointed, symbols.ts comment updated).
- `dist/` CSS contains Fontaine `size-adjust`/`ascent-override` fallback faces (no-CLS — SC2).
- `npx tsc --noEmit` exits 0; `npm run build` exits 0; `npx vitest run` exits 0 with ≥ 240 passing (SC5).
</verification>

<success_criteria>
DESIGN-02 satisfied: self-hosted Newsreader/Archivo/JetBrains Mono via @fontsource, no external font request, Fontaine metric-fallback faces present (no-CLS). CSS half of DESIGN-01 satisfied: dual [data-theme] skins flattened to a single Atelier light :root. Engine symbol-font literals repointed with signatures frozen. Full Vitest suite green (≥240).
</success_criteria>

<output>
Create `.planning/phases/20-atelier-design-system-canvas-first-shell/20-01-SUMMARY.md` when done.
</output>
