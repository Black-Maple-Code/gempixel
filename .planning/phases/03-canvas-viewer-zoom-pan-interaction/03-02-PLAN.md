---
phase: 03-canvas-viewer-zoom-pan-interaction
plan: "02"
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - src/engine/viewer.ts
  - src/engine/__tests__/viewer.test.ts
autonomous: true
requirements:
  - VIEW-02
must_haves:
  truths:
    - "Offscreen canvas allocates size proportional to cell coordinates and is drawn onto display context"
    - "Round drill cells render as filled circles showing the backing slate color through corner gaps"
    - "Square drill cells render as filled rectangles covering the grid cells completely"
    - "Offscreen buffer redraws only when grid dimensions, style selections, or palette colors change"
  artifacts:
    - path: "src/engine/viewer.ts"
      provides: "Offscreen double-buffer rendering loops, Square/Round drill styling paths, and blit drawing updates"
    - path: "src/engine/__tests__/viewer.test.ts"
      provides: "Unit tests verifying offscreen buffer bounds, style toggling transitions, and redraw invalidations"
  key_links:
    - from: "src/engine/__tests__/viewer.test.ts"
      to: "src/engine/viewer.ts"
      via: "importing CanvasViewer class"
      pattern: "import .*from '\\.\\./viewer'"
---

<objective>
Implement the offscreen canvas double-buffering cache and Square/Round drill styles. This includes writing the loops that render grid colors to the offscreen context and blitting it to the screen during viewport transformations.
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-canvas-viewer-zoom-pan-interaction/03-CONTEXT.md
@.planning/phases/03-canvas-viewer-zoom-pan-interaction/03-RESEARCH.md
@.planning/phases/03-canvas-viewer-zoom-pan-interaction/03-PATTERNS.md
@.planning/phases/03-canvas-viewer-zoom-pan-interaction/03-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement offscreen canvas double-buffering and drill styles rendering</name>
  <files>src/engine/viewer.ts</files>
  <read_first>.planning/phases/03-canvas-viewer-zoom-pan-interaction/03-RESEARCH.md</read_first>
  <action>Extend src/engine/viewer.ts. Implement: (1) private variables offscreenCanvas and offscreenCtx; (2) setData(matches, cols, rows, candidates) storing grid matches, sizing, and candidate swatches, and building a fast color map; (3) setDrillStyle(style) updating style to square or round; (4) redrawOffscreen() which dimensions the offscreen canvas to cols * 16 x rows * 16, fills background with slate backing color #2D3748, and loops through matches drawing square filled rectangles or round filled circles; (5) draw() method clearing display canvas and blitting the offscreen canvas context with imageSmoothingEnabled = false.</action>
  <verify>Ensure compiling passes with npx tsc --noEmit</verify>
  <acceptance_criteria>
    - src/engine/viewer.ts allocates and sizes offscreen Canvas correctly
    - redrawOffscreen draws circles with a radius leaving corner gaps when round drill style is toggled
    - draw blits offscreen buffer cleanly with smoothing disabled
  </acceptance_criteria>
  <done>Double-buffering offscreen cache and styling rendering implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Implement unit tests for double-buffering, styles, and redraw logic</name>
  <files>src/engine/__tests__/viewer.test.ts</files>
  <read_first>src/engine/viewer.ts</read_first>
  <action>Extend src/engine/__tests__/viewer.test.ts. Write unit tests checking: (1) offscreen canvas sizing matches grid columns and rows; (2) changing drill style updates internal selections and triggers offscreen canvas redraws; (3) loading grid data builds candidate color maps and invokes buffer updates; (4) draw context blits offscreen coordinates successfully.</action>
  <verify>Run tests using npx vitest run src/engine/__tests__/viewer.test.ts to verify the entire test suite passes.</verify>
  <acceptance_criteria>
    - Unit tests cover offscreen size allocations, toggling style redraw calls, and data mapping triggers
    - Test runner reports all tests pass
  </acceptance_criteria>
  <done>Unit tests for double-buffering, drill styles, and redraw logic created and passed.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- Run the test suite: npx vitest run src/engine/__tests__/viewer.test.ts
- Verify that TypeScript compilation succeeds without errors: npx tsc --noEmit
</verification>
