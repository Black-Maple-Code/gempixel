---
phase: 03-canvas-viewer-zoom-pan-interaction
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/engine/viewer.ts
  - src/engine/__tests__/viewer.test.ts
autonomous: true
requirements:
  - VIEW-01
must_haves:
  truths:
    - "Clicking and dragging with pointer events correctly translates the viewport scale offsets"
    - "Scrolling the wheel updates the scale factor centered at the exact mouse cursor coordinates"
    - "Viewport scaling is constrained within minimum (0.5) and maximum (50.0) zoom limits"
  artifacts:
    - path: "src/engine/viewer.ts"
      provides: "CanvasViewer core viewport state, pointer drag event listeners, and cursor-centered zoom math"
    - path: "src/engine/__tests__/viewer.test.ts"
      provides: "Unit tests verifying screen translation panning offsets, mouse coordinate conversions, and zoom boundary clamping"
  key_links:
    - from: "src/engine/__tests__/viewer.test.ts"
      to: "src/engine/viewer.ts"
      via: "importing CanvasViewer class"
      pattern: "import .*from '\\.\\./viewer'"
---

<objective>
Implement the interactive Canvas Viewport with panning and cursor-centered zooming. This includes setting up pointer event listeners and designing the affine coordinate transformations for mouse scrolling.
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-canvas-viewer-zoom-pan-interaction/03-CONTEXT.md
@.planning/phases/03-canvas-viewer-zoom-pan-interaction/03-RESEARCH.md
@.planning/phases/03-canvas-viewer-zoom-pan-interaction/03-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement CanvasViewer viewport state and interaction listeners</name>
  <files>src/engine/viewer.ts</files>
  <read_first>.planning/phases/03-canvas-viewer-zoom-pan-interaction/03-RESEARCH.md</read_first>
  <action>Create src/engine/viewer.ts. Implement: (1) CanvasViewer class that accepts an HTMLCanvasElement; (2) private variables scale, offsetX, and offsetY; (3)setupListeners() setting up pointerdown, pointermove, pointerup, pointercancel, and wheel events. Implement pan offset translations during pointer drags. Implement cursor-centered zoom updates during wheel events, calculating pre-zoom canvas coordinate mouse positions, updating scale factor within [0.5, 50.0] bounds, and adjusting offsets; (4) getViewportState() returning scale, offsetX, and offsetY for validation checks.</action>
  <verify>Ensure compiling passes with npx tsc --noEmit</verify>
  <acceptance_criteria>
    - src/engine/viewer.ts exists and compiles cleanly
    - setupListeners binds pointer and wheel events to the canvas element
    - Zoom math centers zoom translations on the cursor and clamps scale within [0.5, 50.0] limits
  </acceptance_criteria>
  <done>Interactive zoom/pan canvas viewport implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Write unit tests for panning offsets and zoom bounds centering</name>
  <files>src/engine/__tests__/viewer.test.ts</files>
  <read_first>src/engine/viewer.ts</read_first>
  <action>Create src/engine/__tests__/viewer.test.ts. Stub out HTMLCanvasElement getContext methods. Write unit tests checking: (1) initial zoom offsets are default values; (2) dragging pointer events update offsetX/offsetY by matching drag deltas; (3) wheel events correctly modify scale, and calculate correct offset updates centering on mouse positions; (4) zoom levels clamp cleanly at 0.5 and 50.0.</action>
  <verify>Run tests using npx vitest run src/engine/__tests__/viewer.test.ts to verify viewport logic passes.</verify>
  <acceptance_criteria>
    - src/engine/__tests__/viewer.test.ts exists and compiles
    - Unit tests verify panning offsets, scale updates, cursor centering offsets, and clamp boundaries
    - Test runner reports all tests pass
  </acceptance_criteria>
  <done>Unit tests for viewport panning and zooming logic created and passed.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- Run the test suite: npx vitest run src/engine/__tests__/viewer.test.ts
- Verify that TypeScript compilation succeeds without errors: npx tsc --noEmit
</verification>
