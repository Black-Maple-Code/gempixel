---
phase: 07-symbol-overlay-canvas-margin-legends
plan: "02"
type: execute
wave: 2
depends_on:
  - "07-01-PLAN.md"
files_modified:
  - src/App.tsx
  - src/index.css
  - src/engine/__tests__/viewer.test.ts
autonomous: true
requirements:
  - SYMBOL-03
must_haves:
  truths:
    - "D-05: Support three viewport modes: grid (colors only), symbols (colors + symbols), and reference (original image)."
    - "D-06: Toggling viewport modes updates viewer.setViewMode and triggers redraw in <1ms without Preact DOM re-renders."
    - "D-07: Printable canvas sheet layout forces the symbols/icons mode to print, preventing printer exports of color-only grid or reference image."
    - "D-08: Position the color guide legend (DMC code, color swatch, symbol) on the left and right border margins of the printable canvas, separated by a dashed boundary line indicating the frame stretch fold."
  artifacts:
    - path: "src/App.tsx"
      provides: "3-way viewport switcher toggles, print-only left/right margin legends, and window beforeprint/afterprint event interceptor hooks"
    - path: "src/index.css"
      provides: "CSS print layout directives, landscape page setups, print-specific grid styles, and dashed guidelines for wrap fold"
    - path: "src/engine/__tests__/viewer.test.ts"
      provides: "Updated unit tests validating symbols view mode rendering and CanvasViewer state settings"
  key_links: []
---

<objective>
Implement the 3-way viewport switcher in the UI dashboard and orchestrate the printable margin legend fold layout. Intercept print requests to automatically switch the canvas to symbols mode, and structure print layouts to display the color guide on the left/right page margins separated by dashed stretch guidelines.
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-symbol-overlay-canvas-margin-legends/07-CONTEXT.md
@.planning/phases/07-symbol-overlay-canvas-margin-legends/07-RESEARCH.md
@.planning/phases/07-symbol-overlay-canvas-margin-legends/07-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 3-way viewport switcher state and UI selectors in App.tsx</name>
  <files>src/App.tsx</files>
  <read_first>src/App.tsx</read_first>
  <action>Modify src/App.tsx to add the 3-way viewport switcher logic (D-05, D-06), following the closest analog viewport mode selectors in src/App.tsx. Update the viewportMode state type definition to support 'grid' | 'symbols' | 'reference'. Update the floating center mode selector to render three pills: 'Grid Colors' (grid), 'Grid + Symbols' (symbols), and 'Original Photo' (reference). Synchronize the selected mode with the viewer via 'viewerRef.current.setViewMode(viewportMode)' inside the viewer synchronization useEffect. Ensure the canvas element is visible when viewportMode is 'grid' or 'symbols'. Generate the color-to-symbol allocation using 'generateSymbolAllocation(matchResult.matches, activeCandidates.map(c => c.dmc))' and pass it to the viewer via 'viewerRef.current.setSymbolMap(symbolMap)' during initialization and whenever activePaletteCodes or matches change.</action>
  <verify>Verify file compiles clean. <automated>npx tsc --noEmit</automated></verify>
  <acceptance_criteria>
    - viewportMode state supports 'grid', 'symbols', and 'reference' modes
    - 3-way selector switcher button group displays correctly in workspace
    - Switching viewport modes calls setViewMode and redraws canvas without lag
  </acceptance_criteria>
  <done>3-way viewport switcher UI controls integrated into App.tsx.</done>
</task>

<task type="auto">
  <name>Task 2: Implement print hooks to force symbol rendering</name>
  <files>src/App.tsx</files>
  <read_first>src/App.tsx</read_first>
  <action>Implement window print listeners inside a useEffect hook in src/App.tsx (D-07). On 'beforeprint', save the current viewportMode to a ref, force the viewportMode state to 'symbols', propagate it to the CanvasViewer, and trigger 'viewerRef.current.fitToContainer()' to ensure the canvas is scaled correctly for printing. On 'afterprint', restore the saved viewportMode state and redraw the canvas viewer. This guarantees that printable exports always show the symbol overlay grid regardless of screen state.</action>
  <verify>Verify tsc runs without errors. <automated>npx tsc --noEmit</automated></verify>
  <acceptance_criteria>
    - window.addEventListener registers beforeprint and afterprint event handlers
    - beforeprint handler intercepts print loop, sets viewMode to 'symbols', and fits to container
    - afterprint handler restores original viewportMode state on completion
  </acceptance_criteria>
  <done>Window print interceptors added to App.tsx.</done>
</task>

<task type="auto">
  <name>Task 3: Integrate print-only margin legends HTML and index.css print styling</name>
  <files>src/App.tsx, src/index.css</files>
  <read_first>src/index.css</read_first>
  <action>Add left and right print legend containers around the canvas element in src/App.tsx (following the print wrappers pattern from PATTERNS.md). Divide the active candidate color list into two halves: colors 1 to N/2 in the left list, and N/2+1 to N in the right list. Each list item displays the symbol, a colored swatch, and the DMC code. Add classnames to make these legend containers hidden on screen but visible on print (e.g. 'hidden print:flex'). Modify src/index.css (following closest analog print media queries in src/index.css as mapped in PATTERNS.md) to add print-specific styles under '@media print': format the sheet container with CSS grid 'grid-template-columns: 140px 1fr 140px', configure page size to A4 landscape with 8mm margins, apply dashed borders on the right of the left legend and left of the right legend to mark the wrap fold guidelines, and ensure print elements size correctly with print-color-adjust: exact.</action>
  <verify>Verify that tsc compiles and index.css modifications build correctly. <automated>npx tsc --noEmit</automated></verify>
  <acceptance_criteria>
    - Print sheet is divided into three sections: left legend, center canvas, right legend
    - Left and right margins display DMC codes, swatches, and symbols
    - Dashed fold guidelines mark the boundaries of the canvas print area
    - Background colors and swatches are preserved in prints using print-color-adjust: exact
  </acceptance_criteria>
  <done>Print-only margin legends and landscape print styles implemented.</done>
</task>

<task type="auto">
  <name>Task 4: Update CanvasViewer unit tests to verify symbol mode draw passes</name>
  <files>src/engine/__tests__/viewer.test.ts</files>
  <read_first>src/engine/__tests__/viewer.test.ts</read_first>
  <action>Modify src/engine/__tests__/viewer.test.ts to test CanvasViewer symbol support. Add unit tests asserting: (1) setViewMode correctly updates the internal viewport viewMode and triggers redraw; (2) setSymbolMap stores symbol associations; (3) when viewMode is 'symbols' and scale >= 10px, the canvas context fillText is called for each cell to render the symbols centered; (4) if cell size is below the 10px threshold, symbol rendering is skipped.</action>
  <verify>Run the updated tests via vitest. <automated>npx vitest run src/engine/__tests__/viewer.test.ts</automated></verify>
  <acceptance_criteria>
    - Unit tests verify viewMode setters and redraw logic
    - Tests assert fillText is called when viewMode === 'symbols' and cells are large enough
    - Tests assert symbols are skipped when zoom scale is below the threshold
    - All tests compile and run green
  </acceptance_criteria>
  <done>CanvasViewer unit tests updated to verify symbol rendering passes.</done>
</task>

</tasks>
