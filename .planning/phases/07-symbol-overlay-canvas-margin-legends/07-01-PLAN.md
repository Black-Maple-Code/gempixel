---
phase: 07-symbol-overlay-canvas-margin-legends
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/engine/symbols.ts
  - src/engine/viewer.ts
  - src/engine/__tests__/symbols.test.ts
autonomous: true
requirements:
  - SYMBOL-01
  - SYMBOL-02
must_haves:
  truths:
    - "D-01: Define a curated library of 80+ highly distinguishable symbols, explicitly omitting visually similar character pairs."
    - "D-02: Dynamically allocate symbols from the curated pool to active colors in order of color frequency."
    - "D-03: Adapt the text color of the centered symbol dynamically based on the cell's background luminance (black text for light backgrounds, white for dark backgrounds)."
    - "D-04: Render symbols centered inside grid cells only when the cell scale/zoom level is large enough to remain readable (cell size >= 10px)."
  artifacts:
    - path: "src/engine/symbols.ts"
      provides: "Curated symbol pool (80+ symbols), generateSymbolAllocation based on frequency, and getContrastColor luminance math"
    - path: "src/engine/viewer.ts"
      provides: "Canvas symbol overlay rendering inside the draw() viewport bounded clipping loop"
    - path: "src/engine/__tests__/symbols.test.ts"
      provides: "Unit tests verifying symbol allocation logic, uniqueness, frequency sorting, and contrast color calculation"
  key_links: []
---

<objective>
Implement the core dynamic symbol database and luminance-based canvas text overlay. Define the curated pool of 80+ unique characters, allocate them dynamically to active colors in order of frequency, and render them centering inside cells of the CanvasViewer when zoom level is sufficient, dynamically adapting the text color to background luminance.
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-symbol-overlay-canvas-margin-legends/07-CONTEXT.md
@.planning/phases/07-symbol-overlay-canvas-margin-legends/07-RESEARCH.md
@.planning/phases/07-symbol-overlay-canvas-margin-legends/07-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement symbol database utility functions and allocation logic</name>
  <files>src/engine/symbols.ts</files>
  <read_first>.planning/phases/07-symbol-overlay-canvas-margin-legends/07-RESEARCH.md</read_first>
  <action>Create a new file src/engine/symbols.ts (structuring it as a utility file following the closest analog pattern src/engine/color.ts from PATTERNS.md). In this file, define a static list of 100 curated highly distinguishable symbols (omitting visually similar pairs: 0/O, 1/I, 5/S, 8/B, 2/Z). Implement 'generateSymbolAllocation(gridMatches: string[], activePaletteCodes: string[])' which counts occurrences of active codes in grid matches, sorts active codes in descending order of frequency, and maps symbols from the curated pool to each code in order. Also implement 'getContrastColor(hexColor: string)' which computes BT.601 background luminance (Y = 0.299R + 0.587G + 0.114B) and returns '#000000' if luminance > 0.55, else '#FFFFFF'.</action>
  <verify>Verify file compiles clean. <automated>npx tsc --noEmit</automated></verify>
  <acceptance_criteria>
    - Curated symbol list contains at least 80 distinguishable symbols
    - generateSymbolAllocation correctly counts frequencies and orders symbol assignments
    - getContrastColor computes BT.601 luminance and handles both 3-character and 6-character hex codes
  </acceptance_criteria>
  <done>Symbol database and allocation engine implemented in src/engine/symbols.ts.</done>
</task>

<task type="auto">
  <name>Task 2: Update CanvasViewer with viewMode and symbol rendering overlay</name>
  <files>src/engine/viewer.ts</files>
  <read_first>src/engine/viewer.ts</read_first>
  <action>Modify src/engine/viewer.ts. Add private properties 'viewMode: "grid" | "symbols" | "reference"' (defaulting to 'grid') and 'symbolMap: Record<string, string>' (defaulting to empty object). Create setters 'setViewMode(mode)' and 'setSymbolMap(map)' that trigger redraw/re-render. Update the main 'draw()' method: if viewMode is 'symbols' and the scaled cell size (virtualCellSize * scale) is >= 10px, run an overlay draw pass. In this pass, configure text settings once (Outfit font, bold, centered alignment, middle baseline). Determine visible bounding cell coordinates (startCol to endCol, startRow to endRow) and draw each visible cell's allocated symbol at its center coordinate with contrast color calculated via 'getContrastColor'.</action>
  <verify>Run compiler validation to verify interface updates. <automated>npx tsc --noEmit</automated></verify>
  <acceptance_criteria>
    - CanvasViewer class supports setViewMode and setSymbolMap setters
    - draw() loop renders text overlay centering characters inside cell bounds
    - Dynamic text color contrast works correctly per cell background color
    - Clipping boundary logic skips symbol rendering outside visible viewport area
  </acceptance_criteria>
  <done>CanvasViewer updated to support symbol overlay rendering.</done>
</task>

<task type="auto">
  <name>Task 3: Implement unit tests for symbol allocation and luminance calculations</name>
  <files>src/engine/__tests__/symbols.test.ts</files>
  <read_first>src/engine/symbols.ts</read_first>
  <action>Create src/engine/__tests__/symbols.test.ts. Write unit tests to verify: (1) CURATED_SYMBOLS pool has >= 80 distinct characters; (2) generateSymbolAllocation handles frequency-based sorting correctly (assigning first symbol in pool to the most frequent color); (3) getContrastColor returns correct high-contrast black or white text colors for standard dark/light backgrounds (e.g. #000000 vs #FFFFFF); (4) duplicate colors do not share identical symbol allocations.</action>
  <verify>Run the new unit tests. <automated>npx vitest run src/engine/__tests__/symbols.test.ts</automated></verify>
  <acceptance_criteria>
    - All tests for allocation uniqueness and frequency ordering pass
    - getContrastColor tests verify standard BT.601 thresholds
    - Vitest execution succeeds without timeouts or warnings
  </acceptance_criteria>
  <done>Unit tests created and verified for symbols.ts.</done>
</task>

</tasks>
