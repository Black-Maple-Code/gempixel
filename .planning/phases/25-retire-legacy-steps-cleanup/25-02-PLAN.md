---
phase: 25-retire-legacy-steps-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/wizard/CanvasWorkspace.tsx
autonomous: true
requirements: []   # strangler close — no v4.0 REQ-ID by design (ROADMAP Phase 25)
must_haves:
  truths:
    - "The canvas viewport backdrop renders on the Atelier cream token (bg-bg), not bg-slate-950"
    - "The Grid / Grid+Symbols / Original view-mode switcher is docked to the BOTTOM of the viewport (absolute bottom, horizontally centered), no longer at the top viewport-hud position"
    - "The switcher keeps >= 8px clear of the canvas hint pill and never overlaps the canvas raster"
    - "The active switcher segment renders bg-accent text-on-accent; the loading progress fill renders bg-accent (not bg-sky-500 / bg-indigo-500)"
    - "The three zoom/HUD buttons keep min-h-[44px] min-w-[44px] and aria-labels (Zoom in / Zoom out / Fit to screen)"
    - "The loading overlay indeterminate phase reads the UI-SPEC pending copy and re-tokens to bg-bg/80 + text-ink/muted"
  artifacts:
    - "src/features/wizard/CanvasWorkspace.tsx re-tokened per the D-09 map with the switcher bottom-snapped"
  key_links:
    - "This file IS the confident D-01/D-09 theme sweep for the viewport — no slate-* token may survive here; the sweep must NOT reach the frame-scope fulfillment modals in App.tsx"
    - "The bottom-snapped switcher must compose with the Plan 05 D-05 fixed bottom action bar (it lives inside CanvasWorkspace's own viewport, absolute-positioned, independent of the shell zones)"
  prohibitions:
    - statement: "No structural/sizing/aria attribute is stripped during the re-token (only color tokens change)"
      status: preserved
      verification: "grep -c 'min-h-\\[44px\\]' src/features/wizard/CanvasWorkspace.tsx == 3; grep -c 'aria-label' on the three zoom buttons unchanged"
    - statement: "The re-token does not touch engine/export.ts, engine/checkout.ts, or any frame-scope App.tsx modal"
      status: preserved
      verification: "git diff --name-only HEAD only lists src/features/wizard/CanvasWorkspace.tsx"
---

<objective>
Re-token the dark `slate-*` canvas viewport, view-mode switcher, zoom HUD, canvas border, and
loading overlay in `CanvasWorkspace.tsx` to the semantic Atelier tokens (D-09), and move the
Grid / Grid+Symbols / Original switcher from the top `viewport-hud` to a bottom-snapped dock
(D-07). This is the confident half of the D-01 theme-remnant sweep — scoped entirely to this one
pure/props-only file so it never reaches the guardrail-protected fulfillment modals.

Purpose: SC8 (view-switcher snaps to the bottom without obstructing the canvas) + the D-09
"the viewport stops being dark" re-token, stopping the last consumers of the legacy remap shim.
Output: An Atelier-cream viewport with a bottom-docked switcher and touch-safe, labelled zoom HUD.
</objective>

<artifacts_produced>
## Created / changed symbols
- `CanvasWorkspace.tsx` viewport surface class `bg-slate-950` → `bg-bg`; view-switcher relocated to
  an absolute bottom-centered container; active-segment + progress-fill classes → `bg-accent` /
  `text-on-accent`; loading overlay → `bg-bg/80` + `text-ink`/`text-muted`; indeterminate loading
  label aligned to the UI-SPEC "Recomputing…" pending copy.

## Removed symbols (drift verification must EXCLUDE these)
- All `slate-*`, `sky-*`, `indigo-*` scale-name classes inside `CanvasWorkspace.tsx` (replaced by
  semantic tokens per the D-09 map).

## PRESERVED (do not touch)
- Every `min-h-[44px] min-w-[44px]` touch target and every `aria-label` on the zoom HUD buttons;
  the `onZoomIn`/`onZoomOut`/`onFit` prop wiring; the print-legend / print-mode markup.
</artifacts_produced>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/25-retire-legacy-steps-cleanup/25-CONTEXT.md
@.planning/phases/25-retire-legacy-steps-cleanup/25-UI-SPEC.md
@.planning/phases/25-retire-legacy-steps-cleanup/25-PATTERNS.md
@src/features/wizard/CanvasWorkspace.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply the D-09 re-token map to viewport, HUD, border, and loading overlay</name>
  <files>src/features/wizard/CanvasWorkspace.tsx</files>
  <read_first>
    - src/features/wizard/CanvasWorkspace.tsx lines 81 (viewport bg-slate-950), 84-113 (switcher), 113-160 (zoom HUD + aria-labels + 44px), 184 (canvas border), 233-250 (loading overlay bg-slate-950/80 + bg-indigo-500)
    - .planning/phases/25-retire-legacy-steps-cleanup/25-UI-SPEC.md §"Color" D-09 re-token map table and §"Accessibility (Visuals)"
    - .planning/phases/25-retire-legacy-steps-cleanup/25-PATTERNS.md §"CanvasWorkspace.tsx — re-token + switcher bottom-snap"
  </read_first>
  <action>
    Apply the UI-SPEC D-09 re-token map to `CanvasWorkspace.tsx` ONLY: viewport surface (~81)
    `bg-slate-950` → `bg-bg`; loading overlay (~234) `bg-slate-950/80` → `bg-bg/80`; canvas border
    (~184) and HUD dividers `border-slate-800` → `border-border`, `bg-slate-800` → `bg-border`;
    HUD/hint text `text-slate-300`/`-400`/`-500` → `text-ink`/`text-muted`/`text-faint`; active
    view-mode segment `bg-sky-500 text-white` → `bg-accent text-on-accent`; idle segment
    `text-slate-400 hover:text-slate-200` → `text-muted hover:text-ink`; the loading progress fill
    `bg-indigo-500` (~238,245) → `bg-accent`. Keep the three zoom buttons' `min-h-[44px] min-w-[44px]`
    and their `aria-label`s — align the strings to "Zoom in" / "Zoom out" / "Fit to screen" if the
    className is churned, never render a glyph-only button. Set the loading overlay indeterminate-phase
    label to the UI-SPEC pending copy so the D-02 auto-recompute pending state (Plan 04 fires it) reads
    correctly; keep the determinate phase as the existing "Matching colors: {progress}%". Do not change
    any structural flex/positioning class in this task except color tokens.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'slate-' src/features/wizard/CanvasWorkspace.tsx` counts only print-legend rules if any remain; NO `bg-slate-950`, `border-slate-800`, `bg-slate-800`, `text-slate-300/400/500` in the on-screen viewport/HUD/overlay (grep the specific tokens returns nothing).
    - `grep -c 'bg-sky-500\|bg-indigo-500' src/features/wizard/CanvasWorkspace.tsx` == 0.
    - `grep -c 'min-h-\[44px\] min-w-\[44px\]' src/features/wizard/CanvasWorkspace.tsx` == 3.
    - `grep -c 'aria-label' src/features/wizard/CanvasWorkspace.tsx` >= 3.
  </acceptance_criteria>
  <done>The viewport, HUD, border, and loading overlay render on semantic Atelier tokens; touch targets and aria-labels survive.</done>
</task>

<task type="auto">
  <name>Task 2: Bottom-snap the view-mode switcher and confirm no canvas overlap</name>
  <files>src/features/wizard/CanvasWorkspace.tsx</files>
  <read_first>
    - src/features/wizard/CanvasWorkspace.tsx lines 84-113 (current top viewport-hud switcher) and 221 (existing absolute bottom-centered hint pill: `absolute bottom-4 left-1/2 -translate-x-1/2`)
    - .planning/phases/25-retire-legacy-steps-cleanup/25-UI-SPEC.md D-07 row (dock to bottom, centered, >= 8px clear of hint pill, never overlap canvas)
  </read_first>
  <action>
    Move the Grid / Grid+Symbols / Original switcher (the `viewport-hud` block ~84-113) out of the
    top-anchored `viewport-hud` and into a bottom-snapped, horizontally-centered container inside the
    same relative viewport `<div>` — mirror the existing hint-pill positioning idiom
    (`absolute bottom-4 left-1/2 -translate-x-1/2 z-30 no-print`), stacked so the switcher and the
    `drag to pan · scroll to zoom · {N} drills` hint pill keep >= 8px vertical clearance (stack them or
    place the switcher just above the pill; do not overlap). Keep the switcher segment labels, the
    `no-print` class, and the aria/`aria-pressed` state. It must never overlap the canvas raster
    (`max-h-[75vh]` object-contain image) at desktop widths. Leave the zoom HUD where it is.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm test -- src/__tests__/integration.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - The switcher container uses an `absolute bottom-*` position (grep confirms a bottom-anchored switcher block), no longer the top `viewport-hud`.
    - The switcher keeps `no-print` and its three `aria-pressed` segment buttons.
    - `npm test` (integration + full suite in verification) stays green; no layout regression test fails.
  </acceptance_criteria>
  <done>The view-mode switcher docks to the bottom of the viewport, centered, clear of the hint pill, never obstructing the canvas.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Pure presentational re-token of a props-only component; no input, no network, no storage change |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25-03 | Tampering (a11y regression) | Class-name churn stripping aria-labels / 44px targets on the zoom HUD | low | mitigate | UI-SPEC mandates aria + 44px survive; acceptance greps assert `min-h-[44px]` count == 3 and aria-label presence |
| T-25-04 | Tampering (scope creep) | Theme sweep leaking outside CanvasWorkspace into a fulfillment modal | low | mitigate | files_modified is a single file; `git diff --name-only` must list only CanvasWorkspace.tsx |
| T-25-SC | Tampering | npm/pip/cargo installs | n/a | accept | Zero new dependencies this phase; no install step |

No new attack surface: presentational token remap on a client-side pure component. The outbound
Diamond Drills USA cart URL (`engine/checkout.ts`) is not referenced or touched here.
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0.
- `npm test` exits 0, suite green.
- No `slate-*`/`sky-*`/`indigo-*` token remains on the on-screen viewport/HUD/overlay classes.
- `git diff --name-only HEAD` lists only `src/features/wizard/CanvasWorkspace.tsx`.
</verification>

<success_criteria>
SC8 view-switcher bottom-snap satisfied; D-09 re-token complete for the viewport surface; the
last shim-dependent viewport classes are gone; a11y touch/label contract preserved.
</success_criteria>

<output>
Create `.planning/phases/25-retire-legacy-steps-cleanup/25-02-SUMMARY.md` when done.
</output>
