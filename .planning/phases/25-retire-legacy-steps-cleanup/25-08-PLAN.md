---
phase: 25-retire-legacy-steps-cleanup
plan: 08
type: execute
wave: 5
depends_on: ["25-05"]   # extends the shipped fixed 3-zone shell (25-05, Zone 2 scroll region); complete
files_modified:
  - src/features/screens/SuppliesScreen.tsx
  - src/features/screens/__tests__/SuppliesScreen.test.tsx
autonomous: true
gap_closure: true
requirements: []
tags: [ui, screens, supplies, atelier, d-05, gap-2, sc9]

must_haves:
  truths:
    - "On desktop (md+), scrolling the Supplies step keeps the order-summary panel AND the fixed Next CTA in view and hittable at maximum scroll — the order-summary panel is sticky within the content scroll region while the drill-quantity list scrolls past it."
    - "On mobile the Supplies step keeps its natural single-column flow with NO sticky/height constraint, so the change composes with — not fights — the Phase 24 container-query reflow."
    - "The single-source order-summary quote (line items + total), the supply-table rows, the honesty affordances (unavailable canvas / unpriced bag), and the totals caption are all unchanged; the full Vitest suite stays green."
  artifacts:
    - "src/features/screens/SuppliesScreen.tsx — order-summary panel pinned md:sticky md:top-0 md:self-start"
    - "src/features/screens/__tests__/SuppliesScreen.test.tsx — asserts the sticky order-summary contract (desktop) and no unconditional sticky (mobile)"
  key_links:
    - "The order-summary <div> sticks within AtelierShell Zone 2's scroll region (Zone 2 = flex-1 min-h-0 overflow-y-auto, D-05); the Zone 1 step-bar and the Zone 3 Back/Next are already fixed, so pinning the summary completes the SC9 static-chrome behavior inside the Supplies step."
    - "md:self-start makes the panel content-height (not stretched) so it has room to stick; all sticky classes are md:-prefixed so mobile is untouched."
---

<objective>
Close **GAP-2 (UAT Test 29, criterion SC9)**: on the Supplies (drill order) step the whole step
content currently scrolls together, so the order-summary panel does not stay put while the
drill-quantity list is scrolled. Pin the order-summary panel so that on desktop it — together with
the already-fixed step bar and Next CTA (D-05) — stays static and hittable at any scroll position
while the drill list scrolls past it. Keep mobile's natural single-column flow untouched.

Purpose: apply the SC9 static-chrome behavior (Next always reachable, chrome stays put) inside the
Supplies step, which the Plan 25-05 fixed shell established at the wizard level but did not extend
to the in-step order-summary panel.
Output: a scoped, desktop-only sticky treatment on the order-summary panel in `SuppliesScreen.tsx`
plus a locking test — contained to `SuppliesScreen`, zero new deps, screen stays pure/props-only.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-retire-legacy-steps-cleanup/25-CONTEXT.md
@.planning/phases/25-retire-legacy-steps-cleanup/25-UI-SPEC.md
@.planning/phases/25-retire-legacy-steps-cleanup/25-UAT.md
@.planning/phases/25-retire-legacy-steps-cleanup/25-05-SUMMARY.md

# Live targets
@src/features/screens/SuppliesScreen.tsx
@src/features/screens/__tests__/SuppliesScreen.test.tsx
@src/features/wizard/AtelierShell.tsx
</context>

<constraints>
- **Contained to SuppliesScreen (+ its test).** Do NOT touch `App.tsx` (the `min-h-full`
  content wrappers and Zone 2 scroll region stay as-is), `AtelierShell.tsx`, or any other screen —
  the Phase 24 mobile reflow + the D-05 fixed shell must keep working unchanged.
- **Compose with Phase 24 mobile (P24 D-01/D-03).** Scope every layout change to `md+`
  (`md:` prefix). Mobile keeps its natural single-column stacked flow — no sticky, no clamp.
- **Screen stays pure/props-only (P20 D-01).** No new state, no cents math, no prop-shape change —
  this is CSS/class-only. The single-source `quote` render (D-07) is untouched.
- **Strangler guardrail (D-01).** Do not touch `Step3Canvas.tsx`, the fulfillment handlers, the
  coupled `slate-*` modals, or `flags.ts`.
- **Zero new dependencies (CLAUDE.md).** Tailwind v4 sticky/self-start utilities only — no scroll
  library.
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: Pin the order-summary panel sticky on desktop in SuppliesScreen</name>
  <files>src/features/screens/SuppliesScreen.tsx</files>
  <read_first>
    - src/features/screens/SuppliesScreen.tsx (the `<section>` ~L62–66 `flex ... md:flex-row`; the left drill-table `<div>` ~L68; the right order-summary panel `<div>` ~L161 currently `flex w-full flex-col gap-3 border-l border-border bg-panel p-6 md:w-[320px] md:shrink-0`)
    - src/features/wizard/AtelierShell.tsx (Zone 2 is `flex-1 min-h-0 overflow-y-auto` — the scroll ancestor the panel will stick within; the Zone 1 step-bar and the Zone 3 Back/Next are already fixed via D-05)
    - .planning/phases/25-retire-legacy-steps-cleanup/25-05-SUMMARY.md (the fixed 3-zone shell that makes the summary's sticky ancestor the internally-scrolling Zone 2)
  </read_first>
  <action>
    Edit ONLY the right order-summary panel `<div>` in `src/features/screens/SuppliesScreen.tsx`
    (the `<div>` that renders the "Order summary" heading + the `quote.lineItems` `<dl>` + the
    "Est. total"). Add the desktop-scoped sticky utilities `md:sticky md:top-0 md:self-start` to
    its existing className, keeping all current classes (`flex w-full flex-col gap-3 border-l
    border-border bg-panel p-6 md:w-[320px] md:shrink-0`).

    Rationale for each added class:
    - `md:self-start` — in the `md:flex-row` section, flex items default to `stretch` (full row
      height), which leaves a sticky element no room to move; `self-start` makes the panel
      content-height on desktop so it can stick as the taller drill-list column scrolls past.
    - `md:sticky md:top-0` — pins the panel to the top of the Zone 2 scroll viewport (just under
      the fixed step bar) so it stays in view at any scroll position while the drill list scrolls.

    All three are `md:`-prefixed, so at mobile widths the panel keeps its current
    `w-full flex-col` stacked flow (no sticky, no self-start effect since it is `w-full`) — the
    Phase 24 reflow is unaffected. Do NOT change the `<section>` wrapper, the left drill-table
    column, the table markup, the totals caption, the "Why these bags?" disclosure, or any prop.
    Do NOT introduce a fixed pixel/viewport height (no magic-number `max-h`) — the sticky pin needs
    no explicit height and the summary is short.
  </action>
  <acceptance_criteria>
    - `grep -c "md:sticky" src/features/screens/SuppliesScreen.tsx` == 1 and the same panel `<div>` also carries `md:top-0` and `md:self-start`.
    - The sticky classes are on the order-summary panel `<div>` (the one containing the "Order summary" heading and `data-testid="supplies-est-total"`), NOT on the `<section>` or the drill-table column.
    - No un-prefixed `sticky`/`self-start` is added (mobile stays natural flow): `grep -c "\bsticky\b" src/features/screens/SuppliesScreen.tsx` shows only the `md:`-prefixed occurrence.
    - No `App.tsx`, `AtelierShell.tsx`, or other-screen edits; no prop-shape change; `npx tsc --noEmit` passes.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>The Supplies order-summary panel is `md:sticky md:top-0 md:self-start` — it pins on desktop within the Zone 2 scroll region while mobile keeps its stacked flow.</done>
</task>

<task type="auto">
  <name>Task 2: Lock the sticky order-summary contract in the SuppliesScreen test</name>
  <files>src/features/screens/__tests__/SuppliesScreen.test.tsx</files>
  <read_first>
    - src/features/screens/__tests__/SuppliesScreen.test.tsx (the existing render-contract suite — mirror its `setup()` helper, the `container.querySelector('[data-testid="supplies-est-total"]')` idiom, and the class-name assertion style; jsdom does not compute layout, so assert on className tokens like print.test does)
    - src/features/screens/SuppliesScreen.tsx (the order-summary panel `<div>` the assertion targets — reachable as the closest positioned ancestor of `[data-testid="supplies-est-total"]`, i.e. the panel that also contains the "Order summary" heading)
  </read_first>
  <action>
    Add one test case to `src/features/screens/__tests__/SuppliesScreen.test.tsx` (reusing the
    existing `setup()` helper) that renders `SuppliesScreen` and locks the GAP-2 SC9 contract by
    class-name assertion (jsdom applies no CSS, so — like `print.test.tsx` — assert the className
    carries the required tokens):

    - Locate the order-summary panel element: find the ancestor `<div>` of
      `[data-testid="supplies-est-total"]` that also contains the "Order summary" heading (e.g.
      `container.querySelector('[data-testid="supplies-est-total"]')` then walk up to the panel
      `<div>`, or select the panel by its heading text).
    - Assert its `className` contains `md:sticky`, `md:top-0`, and `md:self-start` (desktop pin).
    - Assert the desktop pin is desktop-scoped: the className does NOT contain a bare, un-prefixed
      `sticky` token (guard that mobile keeps natural flow — the sticky is `md:`-gated).
    - Keep the assertion resilient to the existing classes (`md:w-[320px]`, `border-l`, etc.) — only
      assert the presence of the three sticky tokens and the absence of an unprefixed `sticky`.

    Do not weaken or remove any existing assertion (single-source total, supply rows, honesty
    affordances, totals caption, "Why these bags?" disclosure all stay).
  </action>
  <acceptance_criteria>
    - The new test case renders SuppliesScreen and asserts the order-summary panel className contains `md:sticky`, `md:top-0`, and `md:self-start`, and does NOT contain an unprefixed `sticky`.
    - All pre-existing SuppliesScreen test cases still pass unchanged.
    - `npm test -- src/features/screens/__tests__/SuppliesScreen.test.tsx` is green.
  </acceptance_criteria>
  <verify>
    <automated>npm run build && npm test</automated>
  </verify>
  <done>The SuppliesScreen test suite locks the desktop-sticky order-summary contract (and its mobile-safe md: scoping), and the full build + vitest suite is green.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this plan produces

**Changed layout (no new symbol)**
- The `SuppliesScreen` order-summary panel `<div>` gains the Tailwind utilities
  `md:sticky md:top-0 md:self-start`. No new component, prop, class token, or CSS rule is
  introduced; no `SuppliesScreenProps` change; no `src/index.css` change.

**New test case**
- One added test in `src/features/screens/__tests__/SuppliesScreen.test.tsx` asserting the
  sticky order-summary contract.
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Pure client-side CSS/class change on an existing pure/props-only screen. No new network, storage, file, or user-input surface; no data crosses any new boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25-08-01 | Tampering | SuppliesScreen render props (quote, rows) | low | accept | Unchanged — the single-source `quote`/`rows` render path (D-07) is not touched; only presentational sticky classes are added. No untrusted input reaches the screen. |
| T-25-08-02 | Denial of Service | Sticky/scroll layout on md+ | low | accept | Pure CSS sticky within the existing Zone 2 scroll region; no JS scroll handler, no reflow loop, no engine call. Mobile flow is `md:`-scoped out. |
| T-25-08-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package installs — zero new dependencies (CLAUDE.md). No supply-chain surface added. |

No high-severity threats (ASVS L1 block-on-high satisfied). No secrets, auth, or PII surface touched.
</threat_model>

<verification>
- `npm run build` (`tsc && vite build`) exits 0.
- `npm test` (full vitest) is green (0 failed); ≥ 365 passed plus the new SuppliesScreen assertion; 7 skipped unchanged.
- Structural: `grep -c "md:sticky" src/features/screens/SuppliesScreen.tsx` == 1 on the order-summary panel; no unprefixed `sticky` added.
- Behavioral (existing coverage preserved): all pre-existing SuppliesScreen tests (single-source total, supply rows, honesty affordances, totals caption, disclosure) stay green.
- Guardrail: `git diff --name-only` for this plan touches only `SuppliesScreen.tsx` + its test — NOT `App.tsx`, `AtelierShell.tsx`, `Step3Canvas.tsx`, `flags.ts`, or any engine file.
</verification>

<success_criteria>
GAP-2 (SC9) is closed when: on desktop (md+) the Supplies order-summary panel is sticky and stays
in view and hittable — together with the already-fixed step bar and Next CTA — at maximum scroll
while the drill-quantity list scrolls past it; on mobile the step keeps its natural single-column
flow (no sticky), composing with the Phase 24 reflow; the single-source quote and supply table are
unchanged; and `npm run build` + `npm test` are green. A re-walk of UAT Test 29 SC9 will pass.
</success_criteria>

<output>
Create `.planning/phases/25-retire-legacy-steps-cleanup/25-08-SUMMARY.md` when done.
</output>
