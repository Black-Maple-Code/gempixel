---
phase: 25-retire-legacy-steps-cleanup
plan: 05
type: execute
wave: 3
depends_on:
  - 25-04
files_modified:
  - src/App.tsx
  - src/features/wizard/AtelierShell.tsx
  - src/index.css
  - src/__tests__/print.test.tsx
autonomous: true
requirements: []   # strangler close — no v4.0 REQ-ID by design (ROADMAP Phase 25)
must_haves:
  truths:
    - "AtelierShell renders three flex zones: fixed top step-bar (shrink-0) → flex-1 min-h-0 internally-scrolling content → fixed bottom action bar (shrink-0)"
    - "The Back/Next footer lives in the fixed bottom zone; #wizard-back-btn and #wizard-next-btn keep their ids and gating; Next is reachable without page scroll at desktop widths"
    - "Long Supplies/drill content scrolls INSIDE the content zone; the step bar and Next stay fixed"
    - "The single-mount canvas <main> is re-zoned into the content region, never remounted (the step-2 re-fit effect still fires on entering Refine)"
    - "The canvas <main> is print:block on ALL four steps, so a plain Ctrl+P prints the canvas grid from any step"
    - "The dedicated Supply-Report (print-only-report-mode) and legend (print-only-legend-mode) print paths remain intact and independent (no double-print conflict)"
    - "A successful image ingest auto-advances Upload → Refine (step 2), respecting the image-required gate; the recent-image load path advances for parity"
  artifacts:
    - "src/features/wizard/AtelierShell.tsx 3-zone shell with a bottomBar slot"
    - "src/App.tsx with the relocated footer, print:block on <main>, and wizard.goTo(2) on ingest"
    - "src/__tests__/print.test.tsx asserting print:block on non-Refine steps"
  key_links:
    - "The bottom action bar composes with the Plan 02 bottom-snapped view-switcher (switcher lives inside the canvas viewport; the action bar is the shell's own Zone 3) — verify they do not overlap"
    - "min-h-0 on the content zone is the load-bearing detail that lets the inner scroll region shrink inside a flex column"
    - "Desktop-scoped: the P24 @max-[640px] mobile container-query reflow + sticky-canvas classes on the canvas wrapper stay intact"
  prohibitions:
    - statement: "The canvas <main> is re-zoned, never remounted (P20 D-14) — it is never gated behind a step conditional that unmounts it"
      status: preserved
      verification: "the <main> stays an always-rendered display-toggled sibling; the layout regression + mobile tests stay green; a canvas element is present on entering Refine"
    - statement: "The Supply-Report and legend print paths are untouched (only the plain-canvas print gate changes)"
      status: preserved
      verification: "grep -c 'print-only-report-mode' src/index.css and 'print-only-legend-mode' unchanged; printReport/printLegendSheetOnly wiring intact"
    - statement: "engine/export.ts, engine/checkout.ts, Step3Canvas.tsx, flags.ts remain untouched"
      status: preserved
      verification: "git diff --name-only HEAD excludes those four paths"
---

<objective>
Land the fixed wizard chrome and the flow/print fixes: restructure `AtelierShell` into a fixed
3-zone shell (top step-bar → internally-scrolling content → fixed bottom Back/Next), so Next is
always reachable without page scroll and long lists scroll internally (D-05 / SC9); make the
single-mount canvas `<main>` `print:block` on every step so a plain Ctrl+P prints the grid from
anywhere (D-03 / WR-01 / SC6); and auto-advance Upload → Refine on a successful ingest (D-08 / SC5).
Reuse the existing `beforeprint`→`fitToContainer` hook and keep the dedicated report/legend print
paths independent.

Purpose: SC9 (static wizard chrome), SC6 (Ctrl+P prints the canvas grid from every step), SC5
(auto-advance on upload — also unblocks UAT Test 29's first step).
Output: A fixed app-shell with a hittable Next, canvas-grid print from any step, and no dead-end
no-op on upload.
</objective>

<artifacts_produced>
## Created / changed symbols
- `AtelierShell.tsx`: 3-zone flex structure; a new `bottomBar?: ComponentChildren` slot for the
  relocated Back/Next; content wrapped in `flex-1 min-h-0 overflow-y-auto`.
- `App.tsx`: Back/Next markup moved into the `bottomBar` slot (ids preserved); `<main>` className
  composed as `print:block` + step-gated on-screen classes; `wizard.goTo(2)` added to the ingest
  success path (fresh upload + recent-image parity).
- `print.test.tsx`: new assertion that `<main>` carries `print:block` on Upload/Supplies/Order.

## PRESERVED
- The `beforeprint`→`fitToContainer` hook (App.tsx ~706), the `.print-canvas-wrapper canvas`
  max-width rule (index.css ~280), and the `print-only-report-mode` / `print-only-legend-mode`
  paths. The `@max-[640px]` mobile reflow classes on the canvas wrapper. All fulfillment code.
</artifacts_produced>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/25-retire-legacy-steps-cleanup/25-CONTEXT.md
@.planning/phases/25-retire-legacy-steps-cleanup/25-RESEARCH.md
@.planning/phases/25-retire-legacy-steps-cleanup/25-PATTERNS.md
@.planning/phases/25-retire-legacy-steps-cleanup/25-UI-SPEC.md
@src/features/wizard/AtelierShell.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restructure AtelierShell into a fixed 3-zone shell and relocate Back/Next</name>
  <files>src/features/wizard/AtelierShell.tsx, src/App.tsx</files>
  <read_first>
    - src/features/wizard/AtelierShell.tsx lines 15-52 (props + root `flex flex-col h-dvh overflow-hidden` + header), 52-82 (children render)
    - src/App.tsx lines 1607-1640 (the flex-row justify-center canvas+panels wrapper with @max-[640px] classes + single-mount <main>), 1799-1830 (in-flow Back/Next footer with #wizard-back-btn / #wizard-next-btn — already simplified by Plan 04)
    - .planning/phases/25-retire-legacy-steps-cleanup/25-PATTERNS.md §"App.tsx + AtelierShell.tsx — fixed 3-zone shell (D-05)" (copy-ready zone breakdown + footer excerpt)
    - .planning/phases/25-retire-legacy-steps-cleanup/25-RESEARCH.md Pattern 4 + Pitfall 4 (do not zero the canvas measure / break the mobile reflow)
    - .planning/phases/25-retire-legacy-steps-cleanup/25-UI-SPEC.md D-05 row + §"Spacing" (bottom bar padding px-4 py-3 to match header)
  </read_first>
  <action>
    In `AtelierShell.tsx`: keep the root `@container flex flex-col h-dvh overflow-hidden` and the
    existing `<header>` (Zone 1, `shrink-0 no-print`). Wrap `{children}` in a Zone-2 container with
    `flex-1 min-h-0 overflow-y-auto` (the `min-h-0` is load-bearing). Add a new optional
    `bottomBar?: ComponentChildren` prop and render it in a Zone-3 `shrink-0 no-print` bottom bar
    (`px-4 py-3` to match the header, top border `border-t border-border`). In `App.tsx`: move the
    Back/Next footer markup (~1799-1830) out of the panels tree and pass it into `<AtelierShell bottomBar={...}>`,
    keeping `id="wizard-back-btn"` and `id="wizard-next-btn"` and the gating verbatim (Plan 04 already
    dropped `nextBlockedByStale`, so Next is `disabled={!wizard.canEnter(wizard.step + 1)}` with
    `onClick={wizard.next}`). Keep the canvas+panels wrapper (~1612) and the single-mount `<main>` inside
    Zone 2 exactly as siblings — do NOT remount the canvas and KEEP its `@max-[640px]` mobile classes.
    Desktop-scope the change so the P24 mobile container-query reflow + sticky-canvas pane still govern
    below 640px. Verify the step-2 `fitToContainer` effect still fires on entering Refine.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm test -- src/__tests__/integration.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - AtelierShell renders three zones; `grep -c 'min-h-0' src/features/wizard/AtelierShell.tsx` >= 1 and a `flex-1 min-h-0 overflow-y-auto` content wrapper exists.
    - `grep -c 'wizard-next-btn' src/App.tsx` == 1 and `grep -c 'wizard-back-btn' src/App.tsx` == 1 (ids preserved, single footer).
    - The `<main>` canvas remains an always-rendered display-toggled sibling (not step-gated to unmount); `grep -c '@max-\[640px\]' src/App.tsx` unchanged for the canvas wrapper (mobile classes intact).
    - `npm test` (integration layout regression + mobile tests) stays green.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>The wizard chrome is a fixed 3-zone shell with the Back/Next in the fixed bottom zone; the canvas is re-zoned not remounted; mobile reflow intact.</done>
</task>

<task type="auto">
  <name>Task 2: print:block the canvas on every step (WR-01) and extend the print test</name>
  <files>src/App.tsx, src/index.css, src/__tests__/print.test.tsx</files>
  <read_first>
    - src/App.tsx line 1619 (the `<main>` className `wizard.step === 2 ? '…print:block…' : 'hidden'`) and 706-731 (the beforeprint→fitToContainer hook)
    - src/index.css lines 184-210 (plain @media print — does NOT hide `main`), 273-281 (.print-canvas-wrapper canvas max-width:100%), 295-395 (print-only-legend-mode / print-only-report-mode)
    - src/__tests__/print.test.tsx lines 1-40 (jsdom-queryable `hidden print:block` assertions)
    - .planning/phases/25-retire-legacy-steps-cleanup/25-PATTERNS.md §"App.tsx — canvas print from every step (D-03/WR-01)" (compose print:block unconditionally) and .planning/.../25-RESEARCH.md Pattern 3 / Code Examples (the one-line class fix)
  </read_first>
  <action>
    Change the `<main>` className (~1619) so `print:block` is applied UNCONDITIONALLY, composed with the
    on-screen step gate: base `print:block` plus (step===2 ? the existing on-screen flex + `@max-[640px]:…`
    classes : `hidden`). Off-Refine this yields `hidden print:block` — `display:none` on screen, `block`
    in print — so a plain Ctrl+P prints the canvas grid from Upload/Supplies/Order too. Do NOT change the
    `beforeprint`→`fitToContainer` hook (it already re-fits the backing store even while `display:none`)
    and do NOT touch the `print-only-report-mode` / `print-only-legend-mode` CSS or the report/legend
    buttons — those remain separate button-driven actions (no double-print conflict). Confirm
    `.print-canvas-wrapper canvas { max-width:100% }` already fits the raster (index.css ~280); only add
    a print rule if the raster overflows. Extend `print.test.tsx` to assert `<main>` carries `print:block`
    while on a non-Refine step (e.g. Upload/step 1).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm test -- src/__tests__/print.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `<main>` className includes `print:block` in the non-step-2 branch too (grep: the `<main>` line contains `print:block` outside the ternary consequent).
    - `grep -c 'print-only-report-mode' src/index.css` and `grep -c 'print-only-legend-mode' src/index.css` unchanged from baseline.
    - print.test.tsx asserts `print:block` on a non-Refine step and passes.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Plain Ctrl+P prints the canvas grid from any step; the dedicated report/legend print paths stay intact; the print test covers the new behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Auto-advance Upload → Refine on successful ingest</name>
  <files>src/App.tsx</files>
  <read_first>
    - src/App.tsx lines 958-998 (loadImageFile → img.onload success path, after setImage/commit) and 1041-1050 (recent-image load path running the same handler)
    - the useWizard gate: canEnter(2) requires an uploaded image (P20 SC criterion)
    - .planning/phases/25-retire-legacy-steps-cleanup/25-PATTERNS.md §"App.tsx — auto-advance on ingest (D-08)" and 25-RESEARCH.md Code Examples (D-08 snippet + A4 recent-load parity)
    - .planning/phases/25-retire-legacy-steps-cleanup/25-UI-SPEC.md D-08 row (silent advance; the step change is the feedback)
  </read_first>
  <action>
    In the `img.onload` success handler (~985-998), AFTER the existing `setImage(img)` / matchInputs
    commit, call `wizard.goTo(2)` to advance Upload → Refine. The wizard `canEnter(2)` gate already
    requires an image, which is now set, so the advance is legal; no new gating is needed and no copy is
    added (the step change is the feedback). Apply the same `wizard.goTo(2)` to the recent-image load path
    (~1041-1050) for parity so re-loading a recent image also lands on Refine. Do not advance on a load
    error or when the image fails to decode.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'wizard.goTo(2)\|goTo(2)' src/App.tsx` >= 1 inside the ingest success path.
    - After a successful upload the app is on step 2 (Refine) — covered by an integration/App test (add or extend one asserting `data-step-panel="2"` becomes `contents` after ingest).
    - `npm test` exits 0, >= 240 passing; `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>A successful upload (fresh or recent) auto-advances to Refine, respecting the image-required gate; the suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local image ingest → wizard advance | Already-validated decode gates the auto-advance (no new input surface) |
| (no others) | Client-side only; no network egress, no auth, no server |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25-09 | Tampering (functional regression) | Shell restructure remounting the canvas or zeroing its measure (blank/mis-sized canvas) | medium | mitigate | Canvas stays an always-rendered display-toggled sibling (P20 D-14); keep @max-[640px] mobile classes; verify the step-2 fitToContainer effect fires; layout regression + mobile tests are the gate |
| T-25-10 | Tampering (regression) | Print gate change breaking the report/legend print paths (double-print) | low | mitigate | Only the plain-canvas `<main>` gate changes; print-only-report/legend CSS + buttons untouched; print.test.tsx extended |
| T-25-11 | Availability (UX) | Auto-advance firing on a failed/undecoded image (dead-end on step 2 with no canvas) | low | mitigate | goTo(2) only in the img.onload success path, gated by canEnter(2)=image-present; never on error |
| T-25-SC | Tampering | npm/pip/cargo installs | n/a | accept | Zero new dependencies (hand-rolled flex shell + CSS); no install step |

No new attack surface: layout/flex + a print CSS gate + a client-side wizard step advance. No
network call added; the Diamond Drills USA cart URL (checkout.ts) is untouched.
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0.
- `npm test` exits 0, >= 240 passing (incl. layout regression, mobile, print tests).
- `<main>` carries `print:block` on all steps; report/legend print CSS unchanged.
- `git diff --name-only HEAD` excludes export.ts, checkout.ts, Step3Canvas.tsx, flags.ts.
</verification>

<success_criteria>
SC9 (static wizard chrome — Next always reachable), SC6 (Ctrl+P prints the canvas grid from every
step; report/legend paths intact), and SC5 auto-advance are satisfied; the suite is green.
</success_criteria>

<output>
Create `.planning/phases/25-retire-legacy-steps-cleanup/25-05-SUMMARY.md` when done.
</output>
