---
phase: 25-retire-legacy-steps-cleanup
plan: 06
type: execute
wave: 4
depends_on:
  - 25-02
  - 25-05
files_modified: []
autonomous: false
requirements: []   # strangler close — no v4.0 REQ-ID by design (ROADMAP Phase 25)
must_haves:
  truths:
    - "npm run build (tsc && vite build) exits 0 and npm test is green (>= 240 passing) on the composed phase"
    - "The real-photo journey Upload → Refine → Supplies → Order (UAT Test 29) completes with the final in-viewport layout"
    - "Upload auto-advances to Refine; a SizeCard change auto-recomputes and re-fits without a zoom jump; the canvas defaults to fit; the rail fits the browser; the view switcher is bottom-snapped and never obscures the canvas; Next is reachable without page scroll; plain Ctrl+P prints the canvas grid"
    - "The still-live Diamond Drills USA cart + canvas/legend PNG export path (Step3Canvas) is intact and reachable (preserved for Phase 26)"
  artifacts:
    - ".planning/phases/25-retire-legacy-steps-cleanup/25-06-SUMMARY.md capturing the UAT Test 29 re-verification result"
  key_links:
    - "This plan writes no source — it is the phase-close regression gate + human UAT re-verification across all prior plans (SC7)"
  prohibitions:
    - statement: "Step3Canvas + the 3 fulfillment handlers + the 3 frame-scope slate-* modals + flags.ts survived the whole phase (Phase 26 owns their deletion)"
      status: preserved
      verification: "test -f src/features/wizard/steps/Step3Canvas.tsx; grep -c 'handleShopifyCheckout' src/App.tsx > 0; git diff --name-only <phase-base>..HEAD excludes engine/export.ts, engine/checkout.ts, Step3Canvas.tsx, flags.ts"
    - statement: "No dead path can resurrect dark mode or the old sidebar (SC2 grep-clean)"
      status: verified
      verification: "the three legacy Step files are absent; no bg-slate-950 remains on the viewport surface; tsc is clean"
---

<objective>
Close Phase 25 by proving the composed result: run the full build + test regression gate, verify
the SC1/SC2/SC10 grep-clean/guardrail invariants hold across all prior plans, then re-verify the
real-photo end-to-end journey (UAT Test 29: Upload → Refine → Supplies → Order) against the final
in-viewport layout with every Phase 25 UX fix in place (SC7).

Purpose: SC7 (real-photo E2E re-verified against the final layout) and a phase-level confirmation
that SC1–SC10 composed without regression and the SC10 fulfillment guardrail held.
Output: A signed-off UAT Test 29 re-verification and a green regression gate.
</objective>

<artifacts_produced>
## Created
- `.planning/phases/25-retire-legacy-steps-cleanup/25-06-SUMMARY.md` (the re-verification record).

## Verified (no source changes)
- The composed Phase 25 behavior across Plans 01–05.
</artifacts_produced>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/25-retire-legacy-steps-cleanup/25-CONTEXT.md
@.planning/phases/25-retire-legacy-steps-cleanup/25-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full regression gate + SC1/SC2/SC10 invariant checks</name>
  <files>(verification only — no source changes)</files>
  <read_first>
    - .planning/ROADMAP.md §"Phase 25" Success Criteria SC1–SC10
    - .planning/phases/25-retire-legacy-steps-cleanup/25-PATTERNS.md §"SC10 Fulfillment Guardrail" (the preserve set)
    - the SUMMARY files of Plans 01–05 (what each landed)
  </read_first>
  <action>
    Run `npm run build` (which runs `tsc && vite build`) and confirm it exits 0. Run `npm test` and
    confirm the full Vitest suite is green with at least 240 passing (SC4 floor). Then assert the phase
    invariants by inspection: (SC1/SC2) the three legacy Step files are absent and the dark slate
    viewport backdrop is fully re-tokened (no legacy dark-viewport class remains on the CanvasWorkspace
    viewport surface); (SC10) `Step3Canvas.tsx` still exists, the three
    fulfillment handlers still exist in `App.tsx`, and `git diff --name-only` for the phase excludes
    `src/engine/export.ts`, `src/engine/checkout.ts`, `src/features/wizard/steps/Step3Canvas.tsx`, and
    `src/features/screens/flags.ts`. If any check fails, stop and report which plan must be revisited —
    do not paper over a failure here.
  </action>
  <verify>
    <automated>npm run build && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0; `npm test` exits 0 with >= 240 passing.
    - `test ! -f src/features/wizard/steps/Step1Ingest.tsx` (and Step2Palette, Step4Export); `test -f src/features/wizard/steps/Step3Canvas.tsx`.
    - `grep -n 'bg-slate-950' src/features/wizard/CanvasWorkspace.tsx` returns nothing (viewport re-tokened).
    - `grep -c 'handleShopifyCheckout' src/App.tsx` > 0.
  </acceptance_criteria>
  <done>The composed phase builds and tests green; the SC1/SC2 grep-clean and SC10 preserve-guardrail invariants hold.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human UAT re-verification of the real-photo journey (UAT Test 29)</name>
  <files>(manual verification — no source changes)</files>
  <action>
    Pause for the developer to walk the real-photo journey per the how-to-verify steps below and confirm
    the SC5/SC6/SC8/SC9/SC10 behaviors on the final in-viewport layout. Do not advance until the
    developer approves or reports issues.
  </action>
  <what-built>
    The full Phase 25 result is composed: legacy Step1/2/4 deleted; the Refine viewport re-tokened to
    the Atelier cream with a bottom-snapped view switcher; fit-to-container is the default zoom;
    dimension changes auto-recompute and re-fit with no zoom jump; the Refine rail is width-capped; the
    wizard chrome is a fixed 3-zone shell with an always-reachable Next; a plain Ctrl+P prints the canvas
    grid from any step; and a successful upload auto-advances to Refine. Step3Canvas + the Diamond Drills
    USA cart + canvas/legend PNG export are preserved intact for Phase 26.
  </what-built>
  <how-to-verify>
    1. Run `npm run dev` and open http://localhost:5173 in a normal-size desktop browser window.
    2. Upload a real photo (a detailed color photo, not a flat graphic). Confirm the app auto-advances
       from Upload to Refine on ingest (no dead-end no-op) — SC5.
    3. In Refine, confirm the canvas opens fit-to-container (whole grid visible, not an arbitrary zoom).
       Click Small / Medium / Large / XL SizeCards: the preview must re-fit cleanly (no zoom jump) and
       recompute automatically with a brief pending overlay — no "Recompute" button appears — SC5/SC8.
    4. Confirm the controls rail fits the browser width with no horizontal scroll and does not encroach
       on the canvas; the Grid / Grid+Symbols / Original switcher sits at the bottom of the viewport and
       never covers the canvas — SC8. Open the "Advanced" disclosure and confirm it reads as clickable.
    5. Advance Upload → Refine → Supplies → Order (UAT Test 29). On the Supplies/drill list, confirm the
       list scrolls internally while the step bar and Next stay fixed and hittable without scrolling the
       page — SC9.
    6. From at least two different steps, press Ctrl+P and confirm the print preview shows the canvas
       grid (not a blank page) — SC6. Separately, confirm the dedicated Supply-Report and legend print
       buttons still produce their own reports.
    7. Confirm the Order step still exposes the Diamond Drills USA drill-cart handoff and the canvas/PNG
       export via the preserved path (Step3Canvas is still wired on panel 3) — SC10 (no orphaned feature).
    8. Note any layout overlap, zoom jump, blank print, or unreachable Next.
  </how-to-verify>
  <resume-signal>Type "approved" if UAT Test 29 passes with all SC5/SC6/SC8/SC9/SC10 behaviors, or describe the issues found (which step, which behavior).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Verification-only plan; no source change, no input/network/storage change |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25-12 | Tampering (regression escaping to ship) | A composed-behavior regression not caught by unit tests (layout/print/zoom) | low | mitigate | Human UAT Test 29 walkthrough across all steps + the automated build/test gate before the checkpoint |
| T-25-13 | Tampering (orphaned feature) | The fulfillment path silently broken by the cumulative cleanup | low | mitigate | Explicit SC10 checkpoint step verifying the Diamond Drills USA cart + PNG export are still reachable |
| T-25-SC | Tampering | npm/pip/cargo installs | n/a | accept | Zero new dependencies this phase; no install step |

No new attack surface: a build/test gate plus a manual UAT walkthrough of a client-side app. The
outbound Diamond Drills USA cart URL (checkout.ts) is verified reachable, not modified.
</threat_model>

<verification>
- `npm run build` exits 0; `npm test` exits 0, >= 240 passing.
- The SC1/SC2/SC10 invariant checks pass (files absent/present, no viewport slate, fulfillment intact).
- Human UAT Test 29 approved with SC5/SC6/SC8/SC9/SC10 behaviors confirmed.
</verification>

<success_criteria>
SC7 (real-photo E2E re-verified against the final in-viewport layout) is signed off, and the phase
composes SC1–SC10 with no regression and the SC10 fulfillment guardrail intact.
</success_criteria>

<output>
Create `.planning/phases/25-retire-legacy-steps-cleanup/25-06-SUMMARY.md` when done (record the UAT
Test 29 result, the regression-gate output, and any follow-ups).
</output>
