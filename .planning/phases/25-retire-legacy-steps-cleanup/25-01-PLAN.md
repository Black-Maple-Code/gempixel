---
phase: 25-retire-legacy-steps-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/App.tsx
  - src/features/wizard/steps/Step1Ingest.tsx
  - src/features/wizard/steps/Step2Palette.tsx
  - src/features/wizard/steps/Step4Export.tsx
  - src/__tests__/App.test.tsx
  - src/__tests__/integration.test.tsx
autonomous: true
requirements: []   # strangler close — no v4.0 REQ-ID by design (ROADMAP Phase 25)
must_haves:
  truths:
    - "Panels 1/2/4 render ONLY their new Atelier screens (UploadScreen/RefineScreen/OrderScreen); no legacy Step ternary else-branch remains for those three panels"
    - "The three legacy component files Step1Ingest.tsx, Step2Palette.tsx, Step4Export.tsx no longer exist on disk"
    - "npx tsc --noEmit exits 0 (no dangling import, no unused state left by the Step deletion)"
    - "npm test stays green (>= 240 passing; runtime DOM unchanged because the USE_NEW_* flags were already true)"
    - "safeStorage.removeItem('gempixel_theme') startup cleanup is retained (idempotent dark-mode-pref removal)"
  artifacts:
    - "src/App.tsx with 3 collapsed panel ternaries and Step1/2/4 imports removed"
    - "src/features/wizard/steps/ containing ONLY Step3Canvas.tsx (three siblings deleted)"
  key_links:
    - "App.tsx panel-3 ternary (USE_NEW_SUPPLIES ? SuppliesScreen : Step3Canvas) and the Step3Canvas import MUST remain intact — panel 3 still reads the flag (D-01)"
    - "noUnusedLocals/noUnusedParameters (tsconfig) is the deterministic gate that surfaces every state symbol orphaned by the Step deletion"
  prohibitions:
    - statement: "Step3Canvas.tsx is neither deleted nor modified (fulfillment-live, Phase 26 owns it)"
      status: preserved
      verification: "test -f src/features/wizard/steps/Step3Canvas.tsx; git diff --name-only HEAD -- src/features/wizard/steps/Step3Canvas.tsx is empty"
    - statement: "flags.ts is left as-is (all four USE_NEW_* still true; panel 3 depends on USE_NEW_SUPPLIES)"
      status: preserved
      verification: "git diff --name-only HEAD -- src/features/screens/flags.ts is empty"
    - statement: "engine/export.ts and engine/checkout.ts (canvas PNG + Diamond Drills USA cart) are untouched"
      status: preserved
      verification: "git diff --name-only HEAD -- src/engine/export.ts src/engine/checkout.ts is empty"
    - statement: "The three fulfillment handlers (handleShopifyCheckout, handleDownloadCanvasOnly, handleDownloadCombinedCanvasSheet) and the frame-scope slate-* fulfillment modals (Checkout Warning :1931, Save Project :2029, Artist Resources :1835) remain in App.tsx"
      status: preserved
      verification: "grep -c 'handleShopifyCheckout' src/App.tsx > 0; grep -c 'checkoutWarning' src/App.tsx > 0; grep -c 'saveModalOpen' src/App.tsx > 0"
---

<objective>
Complete the v4.0 strangler close: delete the residual legacy `Step1Ingest`, `Step2Palette`,
`Step4Export` component files and collapse their now-dead `USE_NEW_* ? Screen : <StepN>` ternary
else-branches in `App.tsx` (panels 1, 2, 4), then remove the App-level state/handlers that
`tsc` proves orphaned by that deletion. Panel 3 (Supplies) keeps its ternary + `Step3Canvas`
import untouched (D-01 / SC10 — Phase 26 owns that deletion).

Purpose: Satisfy SC1/SC2 (residual Step files + dead branches removed; no dead path can
resurrect the old sidebar) while the 240+ Vitest suite stays green at every commit (SC4).
Output: A single-panel path for Upload/Refine/Order; a `steps/` folder holding only the
preserved `Step3Canvas.tsx`.
</objective>

<artifacts_produced>
## Created / changed symbols
- `App.tsx` panels 1/2/4 collapsed to `<UploadScreen/>` / `<RefineScreen {...refineProps}/>` /
  `<OrderScreen {...orderProps}/>` (ternary + legacy branch removed).

## Removed symbols (drift verification must EXCLUDE these — they are intentionally gone)
- Files: `src/features/wizard/steps/Step1Ingest.tsx`, `Step2Palette.tsx`, `Step4Export.tsx`.
- Imports at `App.tsx:18,19,21` (`Step1Ingest`, `Step2Palette`, `Step4Export`).
- Whatever App state/handlers `tsc --noEmit` reports unused after the JSX deletion. High-confidence
  candidates (verify each against tsc, do NOT blind-delete): `imageSourceOpen`/`setImageSourceOpen`,
  `recentUploadsOpen`/`setRecentUploadsOpen`, `imageFitMode`/`setImageFitMode`, `recsOpen`/`setRecsOpen`,
  `excludeListOpen`/`setExcludeListOpen`, and any `Step*`-only preset/substitution handler.

## Explicitly PRESERVED (NOT artifacts of this phase — do not touch)
- `Step3Canvas.tsx`, its import (`App.tsx:20`), panel-3 ternary (`App.tsx:1737-1770`),
  `flags.ts`, `engine/export.ts`, `engine/checkout.ts`, the 3 fulfillment handlers, and the
  frame-scope `slate-*` fulfillment modals.
</artifacts_produced>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-retire-legacy-steps-cleanup/25-CONTEXT.md
@.planning/phases/25-retire-legacy-steps-cleanup/25-RESEARCH.md
@.planning/phases/25-retire-legacy-steps-cleanup/25-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete Step1/2/4 files, collapse panels 1/2/4 ternaries, remove imports</name>
  <files>src/App.tsx, src/features/wizard/steps/Step1Ingest.tsx, src/features/wizard/steps/Step2Palette.tsx, src/features/wizard/steps/Step4Export.tsx</files>
  <read_first>
    - src/App.tsx lines 18-21 (Step imports), 1641-1703 (panel 1 ternary), 1705-1735 (panel 2 ternary), 1737-1770 (panel 3 — PRESERVE), 1772-1794 (panel 4 ternary)
    - .planning/phases/25-retire-legacy-steps-cleanup/25-PATTERNS.md §"Panel-ternary collapse (view compose) — D-01" (copy-ready before/after) and §"SC10 Fulfillment Guardrail" modal-trace table
    - .planning/phases/25-retire-legacy-steps-cleanup/25-RESEARCH.md Pitfall 3 (delete import AND collapse ternary together)
  </read_first>
  <action>
    Delete the three files `src/features/wizard/steps/Step1Ingest.tsx`, `Step2Palette.tsx`,
    `Step4Export.tsx`. In `src/App.tsx`: remove the three imports at lines 18, 19, 21
    (`Step1Ingest`, `Step2Palette`, `Step4Export`) — KEEP the `Step3Canvas` import at line 20.
    Collapse each of the three panel ternaries to the new screen only: panel-1 (`data-step-panel="1"`,
    ~1642) becomes just `<UploadScreen ... />` with the `USE_NEW_UPLOAD ? ( ) : (<Step1Ingest .../>)`
    wrapper and its 30+ -prop else-branch removed; panel-2 (~1706) becomes just
    `<RefineScreen {...refineProps} />`; panel-4 (~1773) becomes just `<OrderScreen {...orderProps} />`.
    Do NOT touch panel-3 (`data-step-panel="3"`, ~1737): its `USE_NEW_SUPPLIES ? <SuppliesScreen/> :
    <Step3Canvas .../>` ternary, the three fulfillment handler props on `Step3Canvas`
    (`handleShopifyCheckout`, `handleDownloadCanvasOnly`, `handleDownloadCombinedCanvasSheet`), and the
    `Step3Canvas` import stay verbatim. Leave `flags.ts` unchanged. Do NOT remove any `slate-*` class
    from the frame-scope modals (Checkout Warning ~1931, Save Project ~2029, Artist Resources ~1835) —
    those are guardrail-protected per PATTERNS SC10 trace; the confident theme sweep does not reach them.
  </action>
  <verify>
    <automated>test ! -f src/features/wizard/steps/Step1Ingest.tsx && test ! -f src/features/wizard/steps/Step2Palette.tsx && test ! -f src/features/wizard/steps/Step4Export.tsx && test -f src/features/wizard/steps/Step3Canvas.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `src/features/wizard/steps/` contains ONLY `Step3Canvas.tsx` (three siblings gone).
    - `grep -n "steps/Step1Ingest\|steps/Step2Palette\|steps/Step4Export" src/App.tsx` returns nothing (imports gone).
    - `grep -c "steps/Step3Canvas" src/App.tsx` is >= 1 (import preserved).
    - Panels 1/2/4 no longer contain a `USE_NEW_* ? ` ternary; panel-3 still does (`grep -c "USE_NEW_SUPPLIES ?" src/App.tsx` >= 1).
  </acceptance_criteria>
  <done>The three legacy files are deleted, their imports removed, panels 1/2/4 collapsed to the single new screen each; panel 3 + Step3Canvas untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Remove tsc-orphaned dead state and confirm typecheck green</name>
  <files>src/App.tsx</files>
  <read_first>
    - The `npx tsc --noEmit` error output produced after Task 1 (the authoritative list of orphaned symbols)
    - src/App.tsx lines 158-230 (useState block), 780-900 (size/preset handlers), 190-200 (gempixel_theme cleanup)
    - .planning/phases/25-retire-legacy-steps-cleanup/25-RESEARCH.md §"Runtime State Inventory" (gempixel_theme is the only surviving stored symbol; keep the idempotent removeItem)
  </read_first>
  <action>
    Run `npx tsc --noEmit`. `tsconfig` has `noUnusedLocals` and `noUnusedParameters` enabled, so every
    state variable, setter, and handler orphaned by the Step deletion surfaces as a "declared but never
    read" error. Remove ONLY the symbols tsc flags — delete the `useState` declaration and any now-dead
    handler that fed exclusively the deleted Steps (likely `imageSourceOpen`/`setImageSourceOpen`,
    `recentUploadsOpen`/`setRecentUploadsOpen`, `imageFitMode`/`setImageFitMode`, `recsOpen`/`setRecsOpen`,
    `excludeListOpen`/`setExcludeListOpen`). Do NOT remove any symbol still referenced by `UploadScreen`,
    `refineProps`, `suppliesProps`, `orderProps`, the match/worker pipeline, or the preserved modals —
    if tsc does not flag it, it stays. KEEP `safeStorage.removeItem('gempixel_theme')` at ~line 198 (it
    is an idempotent stale-dark-mode-pref removal, not dead code). Iterate tsc until it exits 0. Do not
    leave the removed identifiers echoed in explanatory comments.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0 with no "declared but never read" errors.
    - `grep -c "gempixel_theme" src/App.tsx` is >= 1 (the removeItem cleanup is retained).
    - `grep -n "recsOpen\|recentUploadsOpen\|imageSourceOpen" src/App.tsx | grep -v '//'` returns nothing (representative orphans gone — adjust to the actual tsc-flagged set).
  </acceptance_criteria>
  <done>App.tsx typechecks clean with all Step-orphaned state removed and the gempixel_theme cleanup retained.</done>
</task>

<task type="auto">
  <name>Task 3: Prune obsolete skipped legacy-panel tests and confirm suite green</name>
  <files>src/__tests__/App.test.tsx, src/__tests__/integration.test.tsx</files>
  <read_first>
    - src/__tests__/App.test.tsx lines 220-235, 525-535, 620-630, 790-800, 880-890, 1130-1140 (the TODO(25) legacy-panel notes / skipped tests)
    - src/__tests__/integration.test.tsx lines 505-512 (historical DELETED(23-03) recent-uploads comment)
    - .planning/phases/25-retire-legacy-steps-cleanup/25-PATTERNS.md §"Test retargeting" (P23 retarget-vs-intentional-retirement precedent)
  </read_first>
  <action>
    Grep `src/__tests__/App.test.tsx` and `src/__tests__/integration.test.tsx` for `TODO(25)`,
    `it.skip`, and `describe.skip`. For each skipped legacy-panel test whose behavior now lives in a new
    Atelier screen, either delete it (behavior covered elsewhere — note removal inline) or unskip and
    retarget it onto the new-screen DOM (P23 precedent). Do NOT touch the D-02 soft-invalidate stale
    tests at App.test.tsx ~1473-1535 (Plan 04 owns those) and do NOT retire any assertion exercising the
    preserved Step3Canvas fulfillment path. Tidy the now-inaccurate historical Step-name comments where
    they would read as live coupling. Run the full suite and typecheck.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` exits 0 with >= 240 passing (baseline SC4 floor held).
    - No `it.skip`/`describe.skip` remains that is annotated TODO(25) for a panel-1/2/4 legacy body (grep confirms).
    - The D-02 stale test block (~1473-1535) is unmodified by this plan (Plan 04 owns it).
  </acceptance_criteria>
  <done>Obsolete skipped legacy-panel tests are pruned/retargeted; the full Vitest suite and tsc pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local image file → in-browser ingest | Already-sandboxed local `File`/`img` decode; unchanged by this plan (no new parsing) |
| (no others) | 100% client-side app: no network egress, no auth, no server, no untrusted remote input |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25-01 | Tampering (functional regression) | Deleting a `slate-*` modal or fulfillment handler coupled to `engine/checkout.ts` / `engine/export.ts` | high | mitigate | Confident sweep scoped to deleted Step files only; the PATTERNS SC10 modal-trace table + must_haves.prohibitions keep Step3Canvas / the 3 handlers / the 3 frame-scope modals / flags.ts untouched. `git diff --name-only` MUST NOT include export.ts, checkout.ts, Step3Canvas.tsx, flags.ts |
| T-25-02 | Denial of Service (self) | Orphaned state removal breaking the match pipeline | low | mitigate | tsc-driven removal (only unused symbols) + full Vitest suite green gate |
| T-25-SC | Tampering | npm/pip/cargo installs | n/a | accept | Zero new dependencies this phase (hard zero-new-deps constraint); no install step exists to attack |

No new attack surface: client-side only, no network egress, no untrusted input beyond the
already-sandboxed local image ingest. The one existing outbound surface — the Diamond Drills USA
cart URL built by `engine/checkout.ts` `compileShopifyCartLink` — is NOT touched by this plan (SC10).
</threat_model>

<verification>
- `test ! -f` for each of the three deleted Step files; `test -f Step3Canvas.tsx`.
- `npx tsc --noEmit` exits 0.
- `npm test` exits 0, >= 240 passing.
- `git diff --name-only HEAD` excludes `src/engine/export.ts`, `src/engine/checkout.ts`, `src/features/wizard/steps/Step3Canvas.tsx`, `src/features/screens/flags.ts`.
</verification>

<success_criteria>
SC1 (residual Step files + dead branches removed) and SC2 (no dead path can resurrect the old
sidebar) are met for panels 1/2/4; SC4 (suite green) holds; SC10 guardrail (Step3Canvas +
fulfillment path preserved) is verifiably intact.
</success_criteria>

<output>
Create `.planning/phases/25-retire-legacy-steps-cleanup/25-01-SUMMARY.md` when done.
</output>
