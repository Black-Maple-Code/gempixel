---
phase: 25-retire-legacy-steps-cleanup
plan: 07
type: execute
wave: 5
depends_on: ["25-02", "25-05"]   # extends the shipped switcher (25-02) + fixed 3-zone shell (25-05); both complete
files_modified:
  - src/features/wizard/CanvasControlBar.tsx
  - src/features/wizard/__tests__/CanvasControlBar.test.tsx
  - src/features/wizard/CanvasWorkspace.tsx
  - src/features/wizard/AtelierShell.tsx
  - src/App.tsx
autonomous: true
gap_closure: true
requirements: []
tags: [ui, wizard, canvasworkspace, atelier, d-05, d-07, gap-1, sc8]

must_haves:
  truths:
    - "The view-mode switcher (Grid Colors / Grid + Symbols / Original Photo, with its active-segment readout) AND the zoom in/out/fit controls render inside the wizard's bottom action zone (an in-flow strip in AtelierShell Zone 3, immediately above Back/Next) as normal-flow chrome — never absolutely positioned over the canvas raster."
    - "On Refine the canvas viewport fills the vertical space between the fixed top step-bar and the bottom control/Back·Next chrome; the default fit view is centered and uncropped (no top-pinned HUD eating vertical space)."
    - "The switcher never overlaps the canvas raster; the low-zoom warning still appears in symbols mode below the ~10px cell threshold, and the drag/scroll drills hint pill still shows."
    - "The single-mount canvas is never remounted; toggling Original Photo still hides the canvas and reveals the reference image; the full Vitest suite stays green."
  artifacts:
    - "src/features/wizard/CanvasControlBar.tsx — new pure/props-only bottom control strip"
    - "src/features/wizard/CanvasWorkspace.tsx — top zoom HUD + floating switcher removed"
    - "src/features/wizard/AtelierShell.tsx — canvasControls Zone-3 slot above bottomBar"
    - "src/App.tsx — composes CanvasControlBar into the bottom zone on Refine (step 2)"
    - "src/features/wizard/__tests__/CanvasControlBar.test.tsx — locks the relocated-control contract"
  key_links:
    - "App threads its already-owned viewportMode / setViewportMode / onZoomIn / onZoomOut / onFit / zoomScale / image props into CanvasControlBar (rendered in Zone 3); CanvasWorkspace keeps only viewportMode to drive the canvas hidden/reference toggle and the hint pill."
    - "AtelierShell renders canvasControls as a shrink-0 no-print strip above bottomBar, so Zone 2 (the canvas region) fills the remaining height between the top step-bar and the bottom chrome."
---

<objective>
Close **GAP-1 (UAT Test 29, criterion SC8)**: relocate the Refine canvas chrome — the
Grid / Grid+Symbols / Original view-mode switcher AND the zoom in/out/fit controls — off the
canvas raster and onto the wizard's fixed bottom action zone, so the canvas viewport fills the
full height between the top step-bar and the bottom bar and nothing floats over the raster.

Today (shipped by Plan 25-02) the zoom HUD is pinned top-center of the viewport and the
view-mode switcher floats mid-canvas as an `absolute` overlay above the hint pill — its comment
claims a "bottom-snapped dock (D-07)" but it is NOT on the task bar. The top HUD eats vertical
space so the fit view is pushed down and does not fill the height. This plan completes D-05
(full-height content) + D-07 (switcher bottom-snap).

Purpose: finish the D-07 bottom-snap correctly (in-flow chrome in Zone 3, not an absolute
overlay) and give the Refine canvas the full vertical frame.
Output: a new pure `CanvasControlBar`, a trimmed `CanvasWorkspace`, a `canvasControls` slot on
`AtelierShell`, and App wiring — all client-side layout, zero new deps, single-mount canvas
never remounted, engine signatures untouched.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-retire-legacy-steps-cleanup/25-CONTEXT.md
@.planning/phases/25-retire-legacy-steps-cleanup/25-UI-SPEC.md
@.planning/phases/25-retire-legacy-steps-cleanup/25-UAT.md
@.planning/phases/25-retire-legacy-steps-cleanup/25-02-SUMMARY.md
@.planning/phases/25-retire-legacy-steps-cleanup/25-05-SUMMARY.md

# Live targets
@src/features/wizard/CanvasWorkspace.tsx
@src/features/wizard/AtelierShell.tsx
@src/App.tsx
@src/__tests__/integration.test.tsx
</context>

<constraints>
- **Preserve the strangler guardrail (D-01):** do NOT touch `Step3Canvas.tsx`, the three
  fulfillment handlers (`handleShopifyCheckout`, `handleDownloadCanvasOnly`,
  `handleDownloadCombinedCanvasSheet`), the coupled `slate-*` fulfillment modals, or `flags.ts`.
- **App.tsx stays the sole state owner (P20 D-01):** `viewportMode`, `setViewportMode`, the
  zoom callbacks, and `zoomScale` remain App state; new screens/strips are pure/props-only.
- **Single-mount canvas never remounts (P20 D-14):** the canvas `<main>` and its
  `<canvas ref={canvasRef}>` are re-zoned only — never gated behind a new conditional that would
  unmount them.
- **Engine signatures are frozen (Phase 22):** do NOT change `viewer.ts` — this is pure
  layout/composition. The existing `onZoomIn/onZoomOut/onFit` App wrappers around `viewerRef`
  are reused verbatim.
- **Zero new dependencies (CLAUDE.md):** hand-rolled Preact + Tailwind v4 only.
- **Compose with Phase 24 mobile (P24 D-01/D-03):** do not alter the canvas `<main>`'s
  `@max-[640px]` sticky-pane classes; this is a desktop-web relocation of controls into the
  already-existing fixed bottom zone.
- **Preserve copy + a11y (25-UI-SPEC):** switcher labels stay **Grid Colors / Grid + Symbols /
  Original Photo**; zoom buttons keep aria-labels **Zoom in / Zoom out / Fit to screen** and the
  **min-h-[44px] min-w-[44px]** touch targets; low-zoom badge stays **⚠️ Low Zoom** (warn token);
  active switcher segment stays `bg-accent text-on-accent`.
- **Do NOT modify `src/index.css`:** the `.viewport-hud` rule simply becomes unused (harmless);
  the bottom-bar controls use the default (upward-opening) `tooltip-group` / `tooltip-box` idiom.
</constraints>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the pure CanvasControlBar bottom control strip + its unit test</name>
  <files>src/features/wizard/CanvasControlBar.tsx, src/features/wizard/__tests__/CanvasControlBar.test.tsx</files>
  <read_first>
    - src/features/wizard/CanvasWorkspace.tsx (the current top zoom HUD ~L82–141 and the floating switcher ~L199–230 are the exact markup being relocated — copy the button structure, tooltips, aria-labels, active-segment styling, and low-zoom logic from here)
    - src/features/wizard/AtelierShell.tsx (Zone-3 bottomBar idiom this strip sits beside)
    - .planning/phases/25-retire-legacy-steps-cleanup/25-UI-SPEC.md (Copywriting Contract + Color "Accent reserved" list + a11y labels)
    - src/features/screens/__tests__/SuppliesScreen.test.tsx (the pure-component jsdom render-contract test idiom to mirror)
  </read_first>
  <behavior>
    - Renders nothing when `image` is null (no image → no canvas chrome).
    - When `image` is set: renders exactly three view-mode segment buttons labelled "Grid Colors", "Grid + Symbols", "Original Photo", each carrying `aria-pressed` reflecting whether it is the active `viewportMode`.
    - Clicking a segment calls `setViewportMode` with the matching mode ('grid' | 'symbols' | 'reference').
    - In grid OR symbols mode: renders three zoom buttons with aria-labels "Zoom in", "Zoom out", "Fit to screen"; clicking them calls `onZoomIn`, `onZoomOut`, `onFit` respectively; each zoom button keeps min-h-[44px] min-w-[44px].
    - In reference mode: the zoom buttons are NOT rendered (zoom is meaningless on the full-res photo) but the switcher still renders so the user can leave reference mode.
    - Low-zoom warning ("⚠️ Low Zoom") renders only in symbols mode when `zoomScale * 16 < 10`; hidden otherwise.
    - The component root is normal-flow (no `absolute`/`fixed`/`bottom-16` positioning) — it lays out inline inside its parent bar.
  </behavior>
  <action>
    Create `src/features/wizard/CanvasControlBar.tsx` as a PURE / props-only Preact component
    (P20 D-01 — owns no state, imports no engine value). Export an interface
    `CanvasControlBarProps` with: `image: HTMLImageElement | null`,
    `viewportMode: 'grid' | 'symbols' | 'reference'`,
    `setViewportMode: (mode: 'grid' | 'symbols' | 'reference') => void`,
    `onZoomIn: () => void`, `onZoomOut: () => void`, `onFit: () => void`, `zoomScale: number`.

    Render: if `image` is falsy, return null. Otherwise return a single normal-flow row —
    outer wrapper `mx-auto flex w-full max-w-[1180px] items-center justify-center gap-3 no-print`
    (the `max-w-[1180px]` matches the card frame + the Back/Next row so the controls align).

    Left group — the view-mode switcher: a `flex rounded-lg border border-border p-0.5` segment
    group mapping `['grid','symbols','reference']`. Each segment is a `tooltip-group` wrapping a
    `<button>` with `aria-pressed={mode === viewportMode}`, `onClick={() => setViewportMode(mode)}`,
    the label from the map (grid → "Grid Colors", symbols → "Grid + Symbols", reference →
    "Original Photo"), classes `text-[10px] uppercase tracking-wider px-3 py-1.5 rounded-md
    font-bold transition-all cursor-pointer` and, when active, `bg-accent text-on-accent shadow
    shadow-accent/20`, else `text-muted hover:text-ink`; plus a `tooltip-box` with the short
    tooltip ("Canvas colors" / "Colors + Symbols" / "Original photo"). Reuse the DEFAULT
    (upward-opening) `tooltip-group`/`tooltip-box` classes — do NOT wrap in `.viewport-hud`, so
    tooltips open upward from the bottom bar.

    Right group — the zoom controls, rendered only when `viewportMode === 'grid' || 'symbols'`:
    a `flex items-center gap-1` holding three `tooltip-group` buttons — Zoom in (glyph ➕,
    `aria-label="Zoom in"`, `onClick={onZoomIn}`), Zoom out (glyph ➖, `aria-label="Zoom out"`,
    `onClick={onZoomOut}`), Fit to screen (glyph ⛶, `aria-label="Fit to screen"`,
    `onClick={onFit}`). Each button keeps `min-h-[44px] min-w-[44px] p-1.5 rounded-lg
    hover:bg-border text-muted hover:text-ink transition-colors cursor-pointer flex items-center
    justify-center`, each with its `tooltip-box` ("Zoom In" / "Zoom Out" / "Fit to Screen").

    Low-zoom warning — when `viewportMode === 'symbols' && zoomScale * 16 < 10`: the existing
    warn-token badge (`bg-warn/15 border border-warn/40 text-warn text-[10px] font-bold ...
    animate-pulse`) reading "⚠️ Low Zoom" with its tooltip, placed after the zoom group.

    Do NOT apply any `absolute` / `fixed` / `bottom-16` positioning anywhere — the strip is
    positioned by its parent (AtelierShell Zone 3), not by itself. No fenced code, no engine import.

    Then create `src/features/wizard/__tests__/CanvasControlBar.test.tsx` (jsdom, mirroring the
    SuppliesScreen test idiom) covering every bullet in <behavior> above: null render on no image;
    three labelled segments with aria-pressed reflecting the active mode; segment click fires
    setViewportMode with the right mode; zoom buttons present with the three aria-labels in
    grid/symbols and clicks fire the callbacks; zoom buttons absent in reference mode; low-zoom
    badge shown only in symbols mode below threshold; and a structural assertion that the rendered
    root className does NOT contain an absolute-positioning token (normal flow).
  </action>
  <acceptance_criteria>
    - `src/features/wizard/CanvasControlBar.tsx` exists and default-imports cleanly (tsc passes).
    - `grep -c "Grid Colors" src/features/wizard/CanvasControlBar.tsx` ≥ 1 and the file also contains "Grid + Symbols" and "Original Photo".
    - `grep -c "min-h-\[44px\]" src/features/wizard/CanvasControlBar.tsx` == 3 (three 44px zoom targets).
    - `grep -c "aria-label" src/features/wizard/CanvasControlBar.tsx` == 3 (Zoom in / Zoom out / Fit to screen).
    - The new test file runs green and asserts: no-image → null; three aria-pressed segments; setViewportMode called with 'reference' on the "Original Photo" click; onZoomIn/onZoomOut/onFit fired by their buttons; low-zoom badge only in symbols mode under threshold; root is not absolutely positioned.
    - CanvasControlBar imports no value from `src/engine/*` (pure/props-only).
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && npm test -- src/features/wizard/__tests__/CanvasControlBar.test.tsx</automated>
  </verify>
  <done>A pure CanvasControlBar renders the relocated switcher + zoom + low-zoom warning in normal flow, and its unit test proves the click/label/threshold contract.</done>
</task>

<task type="auto">
  <name>Task 2: Strip the top zoom HUD + floating switcher from CanvasWorkspace</name>
  <files>src/features/wizard/CanvasWorkspace.tsx</files>
  <read_first>
    - src/features/wizard/CanvasWorkspace.tsx (the whole file — targeting the top HUD overlay block ~L82–141 and the floating centered switcher block ~L199–230; KEEP the print legends ~L142–197, the reference-mode image ~L184–195, the hint pill ~L232–237, and the loading overlay ~L239–264)
    - src/__tests__/integration.test.tsx (the "supports toggling between Grid View and Original Photo modes" test at ~L423 — it finds the switcher buttons by text across the whole container and asserts the canvas gains/loses the `hidden` class and the reference `img[alt="Original reference full size"]` appears; this behavior must survive)
  </read_first>
  <action>
    Edit `src/features/wizard/CanvasWorkspace.tsx`:

    1. Delete the top-center floating zoom-HUD overlay block — the element gated on
       `image && (viewportMode === 'grid' || viewportMode === 'symbols')` near the top of the
       returned JSX that holds the zoom buttons and the low-zoom warning (the block styled with
       the floating-overlay HUD class and `onClick`/`onPointerDown` stop-propagation wrappers).
       Those controls now live in CanvasControlBar (Task 1).

    2. Delete the centered floating view-mode switcher block lower in the JSX — the element gated
       on `image` that is positioned as an absolute overlay above the hint pill (the one whose
       comment claims a bottom-snapped dock and maps `['grid','symbols','reference']` to the
       segment buttons). It now lives in CanvasControlBar.

    3. KEEP unchanged: the outer `flex-1 relative flex items-center justify-center overflow-hidden
       bg-bg viewport-dots ...` wrapper; the print-legend sheet (left/center canvas/right legend);
       the reference-mode full-res `<img alt="Original reference full size">` block (still gated on
       `viewportMode === 'reference'`); the bottom hint pill; and the two-phase loading overlay.

    4. Trim `CanvasWorkspaceProps` and the destructure: REMOVE `setViewportMode`, `onZoomIn`,
       `onZoomOut`, `onFit`, and `zoomScale` (all now owned by CanvasControlBar). KEEP
       `viewportMode` — it still drives the canvas `hidden` class (grid/symbols vs reference), the
       reference-image gate, and the hint-pill gate. Keep `canvasRef`, `image`, `matchResult`,
       `cols`, `rows`, `symbolMap`, `leftLegendColors`, `rightLegendColors`, `loading`,
       `loadingPhase`, `progress`. Update the leading JSDoc so it no longer describes an in-file
       zoom HUD / switcher (they moved to the bottom control bar).

    The removed blocks are pure presentation; no engine call or ref wiring changes here.
  </action>
  <acceptance_criteria>
    - `npx tsc --noEmit` passes (no unused-prop or missing-prop type errors) after the props trim.
    - `CanvasWorkspaceProps` no longer declares `setViewportMode`, `onZoomIn`, `onZoomOut`, `onFit`, or `zoomScale`; it still declares `viewportMode`.
    - The reference-mode `<img alt="Original reference full size">`, the hint pill, and the loading overlay remain in CanvasWorkspace.tsx.
    - After Task 3 wiring, the integration test "supports toggling between Grid View and Original Photo modes" still passes (canvas gains `hidden` in reference mode, reference img appears, canvas loses `hidden` back in grid mode).
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>CanvasWorkspace no longer renders any zoom HUD or view-mode switcher over the raster; it keeps the canvas, legends, reference image, hint pill, and loading overlay, and its props no longer carry the zoom/switcher callbacks.</done>
</task>

<task type="auto">
  <name>Task 3: Add the canvasControls Zone-3 slot to AtelierShell and compose CanvasControlBar in App</name>
  <files>src/features/wizard/AtelierShell.tsx, src/App.tsx</files>
  <read_first>
    - src/features/wizard/AtelierShell.tsx (the 3-zone shell + existing optional `bottomBar` slot ~L34, ~L94–98)
    - src/App.tsx (the AtelierShell composition ~L1434–1477 with the `bottomBar` Back/Next; the `<CanvasWorkspace ... />` usage ~L1560–1579; App already owns `viewportMode`/`setViewportMode`/`zoomScale` and passes `onZoomIn`/`onZoomOut`/`onFit` as `() => viewerRef.current?.…` wrappers)
    - src/features/wizard/CanvasControlBar.tsx (the component from Task 1)
  </read_first>
  <action>
    Part A — `AtelierShell.tsx`: add an optional prop `canvasControls?: ComponentChildren` to
    `AtelierShellProps` (documented as the in-flow canvas control strip for Zone 3), destructure it
    in the function signature. In the render, immediately BEFORE the existing `{bottomBar && (…)}`
    block (i.e. between the Zone-2 scroll `<div>` and the bottomBar bar), render
    `{canvasControls && (<div className="no-print shrink-0 border-t border-border bg-panel px-4 py-2">{canvasControls}</div>)}`.
    Both the canvasControls strip and the bottomBar bar are `shrink-0`, so Zone 2 (`flex-1 min-h-0
    overflow-y-auto`) still fills the remaining height — the canvas region now spans from the top
    step-bar down to this control strip. Do not otherwise change the 3-zone structure.

    Part B — `src/App.tsx`:
    1. Import `CanvasControlBar` from `./features/wizard/CanvasControlBar`.
    2. On the `<AtelierShell …>` element add a `canvasControls={wizard.step === 2 ? (
       <CanvasControlBar image={image} viewportMode={viewportMode} setViewportMode={setViewportMode}
       onZoomIn={() => viewerRef.current?.zoomIn()} onZoomOut={() => viewerRef.current?.zoomOut()}
       onFit={() => viewerRef.current?.fitToContainer()} zoomScale={zoomScale} /> ) : undefined}`
       prop — so the control strip renders only on Refine (step 2), the only step where the canvas
       is visible. Reuse the SAME viewer-wrapper callbacks already used for CanvasWorkspace.
    3. In the `<CanvasWorkspace … />` usage, REMOVE the now-deleted props `setViewportMode`,
       `onZoomIn`, `onZoomOut`, `onFit`, and `zoomScale` (they moved to CanvasControlBar). Keep
       `viewportMode`, `canvasRef`, `image`, `matchResult`, `cols`, `rows`, `symbolMap`,
       `leftLegendColors`, `rightLegendColors`, `loading`, `loadingPhase`, `progress`.

    Do NOT touch the Back/Next `bottomBar` block, the canvas `<main>` classes (incl. `print:block`
    and the `@max-[640px]` mobile pane), the fulfillment modals, or any `viewer.ts` call.
  </action>
  <acceptance_criteria>
    - `grep -c "canvasControls" src/features/wizard/AtelierShell.tsx` ≥ 1 (prop declared + rendered) and the strip wrapper carries `shrink-0` and `no-print`.
    - `grep -c "CanvasControlBar" src/App.tsx` ≥ 1 (imported + composed) and it is gated on `wizard.step === 2`.
    - The `<CanvasWorkspace />` usage in App no longer passes `onZoomIn`/`onZoomOut`/`onFit`/`setViewportMode`/`zoomScale`.
    - `npx tsc --noEmit` passes; App still passes the `onZoomIn/onZoomOut/onFit` viewer wrappers to CanvasControlBar (not CanvasWorkspace).
    - The canvas `<main>` retains its `print:block` and `@max-[640px]` classes unchanged (Phase 24 + D-03 preserved).
    - The Back/Next `bottomBar` block and its `#wizard-back-btn` / `#wizard-next-btn` ids are unchanged.
  </acceptance_criteria>
  <verify>
    <automated>npm run build && npm test</automated>
  </verify>
  <done>On Refine, the switcher + zoom controls render as an in-flow strip in AtelierShell Zone 3 above Back/Next; the canvas region fills the height between the top bar and that strip; the full suite (build + vitest) is green.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this plan produces

**New component**
- `CanvasControlBar` (`src/features/wizard/CanvasControlBar.tsx`) — pure/props-only bottom control
  strip. Exported interface `CanvasControlBarProps { image, viewportMode, setViewportMode,
  onZoomIn, onZoomOut, onFit, zoomScale }`. Renders the view-mode switcher (Grid Colors / Grid +
  Symbols / Original Photo), the zoom in/out/fit buttons, and the low-zoom warning as normal-flow
  chrome.

**New prop / slot**
- `AtelierShell` gains optional `canvasControls?: ComponentChildren` — an in-flow, `shrink-0
  no-print` strip rendered in Zone 3 immediately above the `bottomBar` Back/Next bar.

**Changed interface**
- `CanvasWorkspaceProps` loses `setViewportMode`, `onZoomIn`, `onZoomOut`, `onFit`, `zoomScale`
  (relocated to `CanvasControlBar`); it retains `viewportMode` for the canvas hidden/reference
  toggle and the hint pill.

**New test**
- `src/features/wizard/__tests__/CanvasControlBar.test.tsx`.

No new Tailwind classes/tokens are introduced beyond existing Atelier utilities; no `src/index.css`
change; no engine signature change.
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Pure client-side layout relocation. No new network, storage, file, or user-input surface is introduced; no data crosses any new boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25-07-01 | Tampering | CanvasControlBar props (viewportMode, zoomScale) | low | accept | Props are App-owned state (P20 D-01) with a closed union type; no external/untrusted input reaches the strip. Type system constrains `viewportMode` to the three-member union. |
| T-25-07-02 | Denial of Service | Single-mount canvas re-zone | low | mitigate | Canvas `<main>` and `<canvas>` are re-zoned only, never gated behind a new conditional (P20 D-14); the step-2 `fitToContainer` re-fit effect is untouched, so no remount/measure thrash. Verified by the always-green layout/integration suite. |
| T-25-07-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package installs — zero new dependencies (CLAUDE.md). No supply-chain surface added by this plan. |

No high-severity threats (ASVS L1 block-on-high satisfied). No secrets, auth, or PII surface touched.
</threat_model>

<verification>
- `npm run build` (`tsc && vite build`) exits 0.
- `npm test` (full vitest) is green (0 failed); ≥ 365 passed plus the new CanvasControlBar tests; 7 skipped unchanged.
- Structural: `grep -c "CanvasControlBar" src/App.tsx` ≥ 1; `grep -c "canvasControls" src/features/wizard/AtelierShell.tsx` ≥ 1; the CanvasControlBar file carries the three switcher labels + three 44px zoom targets + three aria-labels.
- Behavioral (existing coverage preserved): the integration test "supports toggling between Grid View and Original Photo modes" still finds the switcher buttons and toggles the canvas `hidden` class + reference image.
- Guardrail: `git diff --name-only` for this plan touches only the five files in `files_modified` — NOT `Step3Canvas.tsx`, `engine/checkout.ts`, `engine/export.ts`, `flags.ts`, `viewer.ts`, or `src/index.css`.
</verification>

<success_criteria>
GAP-1 (SC8) is closed when: the view-mode switcher and the zoom in/out/fit controls render in the
wizard's bottom action zone (AtelierShell Zone 3, an in-flow strip above Back/Next) as normal-flow
chrome — never `absolute` over the raster; the Refine canvas viewport fills the vertical space
between the top step-bar and the bottom chrome with a centered, uncropped default fit view; the
switcher never overlaps the canvas raster; the low-zoom warning + hint pill behaviors are
preserved; the single-mount canvas is not remounted; and `npm run build` + `npm test` are green.
A re-walk of UAT Test 29 SC8 will pass.
</success_criteria>

<output>
Create `.planning/phases/25-retire-legacy-steps-cleanup/25-07-SUMMARY.md` when done.
</output>
