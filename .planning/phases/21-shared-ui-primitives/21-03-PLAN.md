---
phase: 21-shared-ui-primitives
plan: 03
type: execute
wave: 2
depends_on:
  - 21-01
files_modified:
  - src/ui/SizeCard.tsx
  - src/ui/__tests__/SizeCard.test.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "SizeCard renders a selectable native <button type=\"button\" aria-pressed={selected}> that displays label, gridDims, inches, and drillCount taken directly from props (D-05, SC1)."
    - "drillCount renders in JetBrains Mono (font-mono) as a data figure; the selected state shows the green accent border + tint, the default state a neutral border (clear, non-ambiguous selection)."
    - "Clicking the card calls onSelect (fully controlled; App owns selection — D-04)."
    - "SizeCard imports NO engine module and performs no derivation — every displayed value is a fully-computed prop (D-05)."
    - "The full Vitest suite stays green and npx tsc --noEmit exits 0, with package.json + package-lock.json byte-unchanged (SC3, SC4)."
  artifacts:
    - src/ui/SizeCard.tsx
    - src/ui/__tests__/SizeCard.test.tsx
  key_links:
    - "SizeCard imports cn from '../cn' (created in 21-01) and merges className last (D-02)."
    - "Its props interface is the ONLY seam to Phase 22's density helper / merged drill count — Phase 23 formats and passes those values in; no import coupling now (D-05)."
  prohibitions:
    - "D-05: SizeCard must NOT import any engine module and must NOT derive inches/counts itself — props are fully-computed display values."
    - "D-03: Do NOT add any dependency to package.json or package-lock.json. Verified by a byte-level diff check in this plan (the phase-wide SC3 gate)."
---

<objective>
Build `SizeCard` — the dumb, selectable presentational card (D-05) — into `src/ui/`, and enforce the phase-wide zero-dependency gate. SizeCard receives fully-computed display values (`label`, `gridDims` like `"120×160"`, `inches` formatted string, `drillCount` number, `selected`, `onSelect`, optional `tag`), imports NO engine module, and does no derivation — Phase 22's density helper + merged drill count reach it only through props in Phase 23. This plan also owns the explicit SC3 `package.json` diff check (the D-03 hard constraint) and the SC4 suite-green gate.

Purpose: Give the Refine screen a token-driven, unit-testable size-selection card now, fully decoupled from the not-yet-built Phase 22 engine (D-05), and prove the whole phase added zero dependencies (SC3).
Output: `src/ui/SizeCard.tsx`, its co-located test, and a passing zero-dependency + green-suite verification.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-shared-ui-primitives/21-CONTEXT.md
@.planning/phases/21-shared-ui-primitives/21-RESEARCH.md
@.planning/phases/21-shared-ui-primitives/21-PATTERNS.md
@.planning/phases/21-shared-ui-primitives/21-UI-SPEC.md
@.planning/codebase/CONVENTIONS.md

# Analog / reference source:
@src/features/wizard/StepBar.tsx
@src/features/wizard/AtelierShell.tsx
@src/features/wizard/__tests__/StepBar.test.tsx
@src/ui/cn.ts
@src/index.css
</context>

## Artifacts this phase produces

The full Phase 21 public surface (this plan owns the SizeCard row):

| Symbol | Kind | File | Plan |
|--------|------|------|------|
| `cn(...classes)` | function | `src/ui/cn.ts` | 21-01 |
| `Button` + `ButtonProps` + `ButtonVariant` | component + types | `src/ui/Button.tsx` | 21-01 |
| `Pill` + `PillProps` + `PillVariant` | component + types | `src/ui/Pill.tsx` | 21-01 |
| `SegmentedControl` + `SegmentedControlProps<T>` + `SegmentOption<T>` | component + types | `src/ui/SegmentedControl.tsx` | 21-02 |
| `Slider` + `SliderProps` | component + types | `src/ui/Slider.tsx` | 21-02 |
| `SizeCard` + `SizeCardProps` | component + types | `src/ui/SizeCard.tsx` | 21-03 |

`SizeCardProps` extends `Omit<ComponentProps<'button'>, 'onSelect'>` with `{ label: string; gridDims: string; inches: string; drillCount: number; selected: boolean; onSelect: () => void; tag?: string }`.
Named export; no default, no barrel.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SizeCard dumb selectable card (D-05) + render test</name>
  <files>src/ui/SizeCard.tsx, src/ui/__tests__/SizeCard.test.tsx</files>
  <read_first>
    - .planning/phases/21-shared-ui-primitives/21-CONTEXT.md — D-05 (SizeCard is a dumb presentational card; props are fully-computed display values; imports no engine module).
    - .planning/phases/21-shared-ui-primitives/21-UI-SPEC.md — §Component Inventory (SizeCard props + `aria-pressed` selectable semantics) and §Color (selected = 2px green border + `#EAF2EF` fill).
    - src/features/wizard/StepBar.tsx — the props-only, no-state, token-styled button pattern to mirror.
    - src/features/wizard/AtelierShell.tsx — card chrome reference.
    - src/features/wizard/__tests__/StepBar.test.tsx — jsdom raw-render harness.
    - src/ui/cn.ts — the class-join helper.
    - src/index.css — `--radius-card` (12px) and `--shadow-card` tokens (arbitrary-value only), `border-accent`/`border-border` utilities.
    - The files being created: src/ui/SizeCard.tsx, src/ui/__tests__/SizeCard.test.tsx.
  </read_first>
  <behavior>
    - SizeCard renders a native <button type="button"> with aria-pressed reflecting the `selected` prop.
    - The rendered text contains the `label`, `gridDims`, `inches`, and the `drillCount` (as a number).
    - drillCount renders inside a font-mono element (data figure).
    - selected=true applies `border-accent` (and the accent-tint fill); selected=false applies `border-border` — the two states are visually distinct.
    - Clicking the card calls onSelect exactly once.
    - When `tag` is provided, its text renders; when omitted, no tag element renders.
    - The card imports no engine module and performs no numeric derivation.
    - A consumer className appears in the class attribute (merged last).
  </behavior>
  <action>
Create `src/ui/SizeCard.tsx`. Declare `export interface SizeCardProps extends Omit<ComponentProps<'button'>, 'onSelect'> { label: string; gridDims: string; inches: string; drillCount: number; selected: boolean; onSelect: () => void; tag?: string }` (import `type { ComponentProps } from 'preact'`). Export `function SizeCard({ label, gridDims, inches, drillCount, selected, onSelect, tag, className, ...rest }: SizeCardProps)` rendering a native `<button type="button" aria-pressed={selected} onClick={onSelect}>`. Build the class list with `cn(...)` over base card classes `flex flex-col gap-1 p-6 text-left rounded-[var(--radius-card)] border-2 transition-all cursor-pointer`, then the selected ternary `selected ? 'border-accent bg-[#EAF2EF]' : 'border-border bg-panel-2'` (the UI-SPEC selected recipe: 2px green border + `#EAF2EF` accent-tint fill; default neutral), then `className` LAST. Inside: render `label` (Archivo control label), `gridDims` and `inches` as captions, and `drillCount` inside a `font-mono` element (JetBrains Mono data figure). When `tag` is truthy, render it as a small inline `<span>` label (keep SizeCard self-contained — do NOT import Pill; Phase 23 may swap in Pill later). CRITICAL (D-05): import NO engine module and do NO derivation — every value is displayed exactly as received; the JSDoc references D-05 as the reason (phrase it as "props are fully-computed; performs no derivation" without writing an import path). Hold no internal state (D-04). Match naming/JSDoc/export conventions; strict noUnusedLocals/noUnusedParameters (destructure only what is used).

Create `src/ui/__tests__/SizeCard.test.tsx` with the jsdom raw-render harness. Assert the behaviors above via `container.querySelector('button')`, `aria-pressed`, `textContent` (label/gridDims/inches/drillCount present), `container.querySelector('.font-mono')`, class-attribute checks for `border-accent` vs `border-border` across `selected` true/false renders, `.click()` firing a `vi.fn()` onSelect once, and the `tag` present/absent cases.
  </action>
  <acceptance_criteria>
    - `src/ui/SizeCard.tsx` contains `export function SizeCard` and imports `cn` from `'./cn'`.
    - `grep -c "aria-pressed" src/ui/SizeCard.tsx` >= 1 and `grep -c "font-mono" src/ui/SizeCard.tsx` >= 1.
    - `grep -nE "^\s*import.*engine" src/ui/SizeCard.tsx` produces NO output — no engine import (D-05).
    - `grep -c "useState" src/ui/SizeCard.tsx` == 0 (controlled/props-only).
    - `npx tsc --noEmit` exits 0.
    - `npx vitest run src/ui/__tests__/SizeCard.test.tsx` exits 0 — includes the selected/unselected border assertions and the onSelect-click assertion.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/ui/__tests__/SizeCard.test.tsx</automated>
  </verify>
  <done>SizeCard is a dumb, props-only selectable card with aria-pressed, mono drill count, distinct selected styling, optional tag, and zero engine coupling; its test passes asserting rendered props, both selection states, onSelect firing, and the tag present/absent cases.</done>
</task>

<task type="auto">
  <name>Task 2: Zero-dependency + green-suite gate (SC3 / SC4)</name>
  <files>(verification only — creates/modifies no source file)</files>
  <read_first>
    - .planning/phases/21-shared-ui-primitives/21-CONTEXT.md — D-03 (zero new dependencies, enforced by package.json diff check) and D-05.
    - package.json and package-lock.json — confirm dependencies/devDependencies are the phase baseline (deps: preact, culori, @fontsource*; devDeps: vite, vitest, tailwindcss, typescript, jsdom, fontaine, @preact/preset-vite, @tailwindcss/vite).
    - CLAUDE.md — the commands table (`npm test`, `npx tsc --noEmit`).
  </read_first>
  <action>
Run the phase-wide SC3/SC4 verification. Confirm ZERO dependency changed (D-03): run `git diff --exit-code -- package.json package-lock.json` and require empty output; independently confirm no forbidden package name (`clsx`, `classnames`, `cva`, `class-variance-authority`, `tailwind-merge`, `@testing-library`, any `slider`/`range` UI lib) appears anywhere under `dependencies`/`devDependencies`. Confirm the D-05 no-engine-import invariant holds for the finished card: `grep -nE "^\s*import.*engine" src/ui/SizeCard.tsx` returns nothing. Confirm the whole `src/ui/` tree is state-free at the value layer: `grep -rc "useState" src/ui/*.tsx` sums to 0. Run `npx tsc --noEmit` (must exit 0) and `npm test` (must exit 0 with the suite green, >= 255 tests). Do NOT modify any file to make these pass — if a gate fails, the fault is in the corresponding component task and should be fixed there. This task changes no source.
<!-- planner-discipline-allow: useState -->
  </action>
  <acceptance_criteria>
    - `git diff --exit-code -- package.json package-lock.json` produces no output (D-03 / SC3).
    - `grep -nE "^\s*import.*engine" src/ui/SizeCard.tsx` produces no output (D-05).
    - `npx tsc --noEmit` exits 0.
    - `npm test` exits 0 — full suite green (>= 255 tests; SC4).
  </acceptance_criteria>
  <verify>
    <automated>git diff --exit-code -- package.json package-lock.json && npx tsc --noEmit && npm test</automated>
  </verify>
  <done>package.json and package-lock.json are byte-unchanged (zero new dependency — SC3/D-03), SizeCard has no engine import (D-05), tsc is clean, and the full Vitest suite is green (SC4).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| consumer (App.tsx / Phase 23) → SizeCard props | First-party trusted; fully-computed display strings + numbers flow in. SizeCard reads no engine module and performs no derivation (D-05). |

Stateless, render-only presentational card. No network, storage, auth, routing, or data mutation.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-06 | Tampering / XSS | SizeCard label / gridDims / inches / tag text | low | mitigate | Preact escapes text children and attribute values by default; no `dangerouslySetInnerHTML` and no HTML-string sink — all display props render as text nodes only. |
| T-21-07 | Tampering (supply chain) | package.json / package-lock.json | low | mitigate | Zero new dependencies (D-03). Enforced by the byte-level diff check in Task 2 (the phase-wide SC3 gate). No `npm install` occurs. |

Expected block-worthy (high) threats: none. No package installs occur this plan; no `[ASSUMED]`/`[SUS]` legitimacy checkpoint is required.
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0 (strict).
- `npm test` exits 0 — full suite stays green (SC4) with the new SizeCard test.
- `git diff --exit-code -- package.json package-lock.json` produces no output — ZERO new dependencies (D-03 / SC3).
- `grep -nE "^\s*import.*engine" src/ui/SizeCard.tsx` returns nothing (D-05).
</verification>

<success_criteria>
- SizeCard is a dumb, props-only, token-driven selectable card (D-05, SC1) with `aria-pressed` selection semantics.
- SizeCard imports no engine module and derives nothing (D-05).
- Fully controlled — App owns selection (D-04).
- Phase adds ZERO dependencies (D-03 / SC3) and the full suite stays green + tsc clean (SC4).
</success_criteria>

<output>
Create `.planning/phases/21-shared-ui-primitives/21-03-SUMMARY.md` when done.
</output>
