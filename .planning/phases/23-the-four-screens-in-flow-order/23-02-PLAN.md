---
phase: 23-the-four-screens-in-flow-order
plan: 02
type: execute
wave: 2
depends_on: ["23-01"]
files_modified:
  - src/features/screens/UploadScreen.tsx
  - src/features/screens/flags.ts
  - src/App.tsx
  - src/features/screens/__tests__/UploadScreen.test.tsx
autonomous: true
requirements: [UPLOAD-01]
must_haves:
  truths:
    - "The user can drag/drop or browse a photo and it ingests via the existing path (handleFileChange / handleDrop / loadImageFile), no size fields present (D-10, SC1)"
    - "An inline recent-projects list from projectStore.list() renders with thumbnails + name; clicking a chip calls loadProject(id) and rehydrates state with no worker run (UPLOAD-01, D-10)"
    - "Canvas-size selection is absent from Upload — it is owned by Refine (SC1/D-10)"
    - "USE_NEW_UPLOAD is true, the Upload screen renders in data-step-panel 1, and the full suite stays green"
  artifacts:
    - src/features/screens/UploadScreen.tsx
    - src/features/screens/__tests__/UploadScreen.test.tsx
  key_links:
    - "UploadScreen ↔ handleFileChange / handleDragOver / handleDragLeave / handleDrop / dropZoneRef props (App.tsx:855-878,465)"
    - "recent-project chip onClick ↔ loadProject(id) (App.tsx:292-354); list source projectsRegistry (App.tsx:143 = projectStore.list())"
    - "recent-image chip ↔ loadRecentImage / deleteRecentImage (App.tsx:940,978)"
---

<objective>
Swap in the real Upload screen (D-10, UPLOAD-01): the simplest journey step, landed first. `UploadScreen` composes the existing ingest surface (drag/drop + browse via `Button variant="primary"`) plus an inline recent-projects list sourced from `projectStore.list()` (`projectsRegistry`, has thumbnails) whose chips call `loadProject(id)` to rehydrate state and advance into the flow — no modal picker. Canvas-size selection is intentionally NOT here (it moved to Refine per SC1/D-10). Then build the App-side `uploadProps`, pass it into the panel-1 slot, and flip `USE_NEW_UPLOAD` to `true`.

Purpose: Deliver UPLOAD-01 as the first flag flip, proving the strangler swap end-to-end while the suite stays green (Pitfall 7).
Output: full `UploadScreen.tsx` + its render test + the App wiring + `USE_NEW_UPLOAD=true`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-the-four-screens-in-flow-order/23-CONTEXT.md
@.planning/phases/23-the-four-screens-in-flow-order/23-UI-SPEC.md
@.planning/phases/23-the-four-screens-in-flow-order/23-PATTERNS.md
@src/features/wizard/steps/Step1Ingest.tsx
@src/ui/Button.tsx
@src/ui/Pill.tsx
</context>

<artifacts_produced>
## Artifacts this phase produces (this plan)
- `src/features/screens/UploadScreen.tsx` — full `UploadScreen` + expanded `UploadScreenProps` (ingest surface + recent lists, NO size fields).
- `src/features/screens/__tests__/UploadScreen.test.tsx` — jsdom render test.
- Modified: `src/features/screens/flags.ts` (`USE_NEW_UPLOAD = true`), `src/features/screens/__tests__/flags.test.ts` (update the Upload assertion), `src/App.tsx` (build + pass `uploadProps`).
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Implement UploadScreen.tsx (dropzone + browse + recent-projects list)</name>
  <files>src/features/screens/UploadScreen.tsx</files>
  <read_first>
    - src/features/wizard/steps/Step1Ingest.tsx (the existing ingest markup + the prop-bag it receives at App.tsx:1407-1446 — copy the ingest handlers, DROP the size fields)
    - 23-UI-SPEC.md §"A1 · Upload" (dropzone 560×250, 2px dashed #C9BFA6, RECENT micro-label, project chips, copywriting, interaction states) + §"Copywriting Contract" (Upload rows)
    - 23-RESEARCH.md §"projectStore Recent-Projects API" (saved projects = projectStore.list() / ProjectSummary with thumbnails = the D-10 list; raw recents = recents.list())
    - src/ui/Button.tsx, src/ui/Pill.tsx, src/ui/cn.ts
  </read_first>
  <action>
    Expand `UploadScreenProps` to the ingest surface App already owns, MINUS all size fields (drop standardSizes/selectedPreset/unit/width+heightInput/cols/rows/drillStyle and the preset/size handlers — D-10 moves size to Refine): keep `image`, `imageName`, `dropZoneRef`, `isDragOver`, `handleFileChange`, `handleDragOver`, `handleDragLeave`, `handleDrop`, `imageFitMode`, `setImageFitMode`, `recentImages`, `loadRecentImage`, `deleteRecentImage`, and ADD `projectsRegistry: ProjectSummary[]` + `loadProject: (id: string) => void` (import `ProjectSummary` as a type from `../../engine/projectStore`).
    Render the UI-SPEC A1 layout: centered column with the display title "Photo → Diamond chart", muted subtitle; a `dropZoneRef`-bound dropzone (~560×250, `2px dashed #C9BFA6`, bg `--panel`, radius 14px) with a `handleDragOver/Leave/Drop` binding, a hidden file input driven by `handleFileChange`, the "Drag & drop, or" hint, and a green `Button variant="primary"` labeled "Browse files"; the drag-over state (`isDragOver`) shifts the border to `--accent` and bg to `#EAF2EF`.
    Below the dropzone render an inline "RECENT" mono micro-label followed by a horizontal row of project chips built from `projectsRegistry` (thumbnail + `name`); clicking a chip calls `loadProject(chip.id)`. When `projectsRegistry` is empty, omit the RECENT row entirely (no empty-state card). Render filenames/names as TEXT only — never `dangerouslySetInnerHTML` (T-23-02-01).
    For the destructive "remove a recent" affordance, use the existing `deleteRecentImage` via a ghost control with inline "Remove? Yes / Cancel" (no modal, no red button) per the UI-SPEC destructive-action row. Keep the component PURE/props-only — no engine calls, no domain useState (a tiny local "which chip is confirming removal" UI flag is the only permitted presentational state).
    Do NOT render any canvas-size control here (D-10).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `UploadScreenProps` contains the ingest surface + `projectsRegistry` + `loadProject`, and contains NO size/preset/unit/cols/rows/drillStyle fields.
    - The component imports no `src/engine/*` value (types-only import of `ProjectSummary` is allowed); no `dangerouslySetInnerHTML` anywhere.
    - Dropzone binds `dropZoneRef` and the four drag/file handlers; the browse action uses `Button variant="primary"`.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>UploadScreen renders the dropzone + recent-projects list from props; no size UI; compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Build uploadProps in App, pass into slot 1, flip USE_NEW_UPLOAD</name>
  <files>src/App.tsx, src/features/screens/flags.ts, src/features/screens/__tests__/flags.test.ts</files>
  <read_first>
    - src/App.tsx:1406-1447 (panel-1 slot + the Step1Ingest prop-bag to source the same handlers from)
    - src/App.tsx:143 (projectsRegistry state) and 292-354 (loadProject)
  </read_first>
  <action>
    In the panel-1 `data-step-panel` slot, replace the bare `<UploadScreen />` (from 23-01) with `<UploadScreen {...uploadProps} />`, where `uploadProps` supplies the fields the new interface expects, sourced from existing App state/handlers: `image`, `imageName`, `dropZoneRef`, `isDragOver`, `handleFileChange`, `handleDragOver`, `handleDragLeave`, `handleDrop`, `imageFitMode`, `setImageFitMode`, `recentImages`, `loadRecentImage`, `deleteRecentImage`, `projectsRegistry`, `loadProject`. Leave the legacy `<Step1Ingest {...} />` branch untouched.
    Set `USE_NEW_UPLOAD = true` in `src/features/screens/flags.ts` (leave the other three `false`). Update `flags.test.ts` to assert `USE_NEW_UPLOAD === true` and the other three `=== false` (one flag flip per commit — Pitfall 7).
    Do NOT modify the useDiamondArtMatch call, the viewer, or any engine module.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm test</automated>
  </verify>
  <acceptance_criteria>
    - Panel-1 renders `<UploadScreen {...uploadProps} />` on the true branch; `USE_NEW_UPLOAD === true`; other flags `false`.
    - `flags.test.ts` reflects the single flip and passes.
    - `npx tsc --noEmit` exits 0; `npm test` full suite green.
  </acceptance_criteria>
  <done>Upload screen is live behind its flag; suite green; exactly one flag flipped.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: UploadScreen render test (jsdom)</name>
  <files>src/features/screens/__tests__/UploadScreen.test.tsx</files>
  <behavior>
    - Renders the "Photo → Diamond chart" title and a "Browse files" button; the browse control is a native button.
    - With a non-empty `projectsRegistry`, renders one chip per project showing its `name`; clicking a chip calls the stubbed `loadProject` with that project's id.
    - With an empty `projectsRegistry`, renders NO "RECENT" label (empty-state omission, UI-SPEC).
    - A crafted project name containing markup (e.g. an `<img onerror>` string) appears as escaped text, not as an element (no XSS sink — Preact escapes by default).
    - Renders NO canvas-size control (assert absence of a size/cols/rows input) — size moved to Refine.
  </behavior>
  <read_first>
    - src/features/wizard/__tests__/StepBar.test.tsx (the exact jsdom harness: `// @vitest-environment jsdom`, render into a detached div, `render(null, container)` in afterEach, `vi.fn()` stubs)
  </read_first>
  <action>
    Create `src/features/screens/__tests__/UploadScreen.test.tsx` with the `// @vitest-environment jsdom` pragma, mirroring `StepBar.test.tsx`. Stub every callback prop with `vi.fn()`. Assert the behaviors in the `<behavior>` block: title + Browse button present, chip rendering + `loadProject` call on click, empty-registry omits RECENT, crafted-name is escaped text (query that no injected element node exists), and no size input is rendered. No real worker/canvas.
  </action>
  <verify>
    <automated>npx vitest run src/features/screens/__tests__/UploadScreen.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - All render-test cases pass; the crafted-name case proves no element was injected (text-escaped).
    - `npx vitest run src/features/screens/__tests__/UploadScreen.test.tsx` exits 0.
  </acceptance_criteria>
  <done>UploadScreen behavior + the filename-escaping mitigation are test-covered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage → render | Saved-project names/thumbnails from `projectStore` (user- or import-controlled) render in the Upload recent list. |
| file picker/drop → app | User-chosen image file enters via the existing ingest path (already handled by `loadImageFile`). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-23-02-01 | Tampering / Info-disclosure | recent-project chip (name/filename) | medium | mitigate | Render names as text only; NO `dangerouslySetInnerHTML` (Preact escapes by default). Covered by the crafted-name render test in Task 3. |
| T-23-02-02 | Denial of Service | image ingest | low | accept | Reuses the existing, already-hardened `loadImageFile`/decode path (bad file → existing inline error banner, W5). No new sink introduced. |
| T-23-SC | Tampering | npm installs | high | accept | No package installs this phase (RESEARCH audit: zero external packages). |
</threat_model>

<verification>
- `npx tsc --noEmit` → 0 errors.
- `npm test` → full suite green (Upload swapped in; legacy Step1 body dormant behind the false-less branch).
- `npx vitest run src/features/screens/__tests__/UploadScreen.test.tsx` → passes.
- Grep: `flags.ts` shows only `USE_NEW_UPLOAD` true; UploadScreen has no `dangerouslySetInnerHTML` and no size input.
</verification>

<success_criteria>
- UPLOAD-01 delivered: drag/drop + browse + inline recent-projects list that rehydrates via `loadProject`; no size selection on Upload.
- `USE_NEW_UPLOAD=true`, one flag flipped; full Vitest suite + tsc green.
</success_criteria>

<output>
Create `.planning/phases/23-the-four-screens-in-flow-order/23-02-SUMMARY.md` when done.
</output>
