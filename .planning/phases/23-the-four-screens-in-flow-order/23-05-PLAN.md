---
phase: 23-the-four-screens-in-flow-order
plan: 05
type: execute
wave: 5
depends_on: ["23-04"]
files_modified:
  - src/features/screens/orderPacket.ts
  - src/features/screens/OrderScreen.tsx
  - src/features/screens/flags.ts
  - src/App.tsx
  - src/features/screens/__tests__/orderPacket.test.ts
  - src/features/screens/__tests__/OrderScreen.test.tsx
autonomous: true
requirements: [ORDER-01, ORDER-02]
must_haves:
  truths:
    - "Order shows an auto-filled, LOCKED spec (Rolled Canvas fixed, size from gridToInches(matchCols,matchRows), finish) + finish selection + ship-to + the same itemized quote as Supplies (ORDER-01, D-08)"
    - "buildOrderPacket produces a versioned (schemaVersion), self-contained packet: design/chart ref, locked canvas spec, gem-bag list from OrderSupplyPlan.rows, integer-cents OrderQuote snapshot, client-only ship-to; packetId via generateUUID (ORDER-02, D-08)"
    - "The user completes the flow by downloading the packet (anchor+Blob idiom); the terminal state is an honest 'Packet downloaded — take this to the vendor' with NO order number, NO receipt, NO payment UI (ORDER-02, D-09)"
    - "Ship-to stays client-side — embedded in the downloaded file only, never transmitted (D-08)"
    - "USE_NEW_ORDER is true and the full suite stays green"
  artifacts:
    - src/features/screens/orderPacket.ts
    - src/features/screens/OrderScreen.tsx
    - src/features/screens/__tests__/orderPacket.test.ts
    - src/features/screens/__tests__/OrderScreen.test.tsx
  key_links:
    - "OrderScreen price ↔ the SAME orderQuote App computed in 23-04 (single source, D-07)"
    - "download ↔ App handler mirroring export.ts:260-288 anchor+Blob+deferred-revoke over an application/json Blob; packetId = generateUUID() (projectStore.ts:75-84)"
    - "packet.gemBags ↔ OrderSupplyPlan.rows; packet.quote ↔ OrderQuote snapshot; packet.canvasSpec.size ↔ gridToInches(matchCols,matchRows)"
---

<objective>
Swap in the final Order screen — an honest, client-side handoff (ORDER-01/02, D-08/D-09). Add a pure `orderPacket.ts` serializer (`buildOrderPacket` → versioned, self-contained `OrderPacket` with a `schemaVersion` field), an `OrderScreen` that renders the auto-filled LOCKED spec (Rolled Canvas fixed, size from `gridToInches(matchCols,matchRows)`, finish), a finish selection, a ship-to card, and the SAME itemized `orderQuote` as Supplies, and completes the flow by downloading the packet (mirroring the `export.ts` anchor+Blob idiom). The terminal state is an honest "Packet downloaded — take this to the vendor" — NO order number, NO receipt, NO payment UI. Ship-to stays client-side, embedded in the file only. Then flip `USE_NEW_ORDER` — the last swap; every screen is now live behind its flag.

Purpose: Deliver ORDER-01/02, closing the four-screen journey while honoring the honesty constraints (D-09) and the single-source quote (D-07).
Output: `orderPacket.ts` + `OrderScreen.tsx` + two tests + App wiring (finish/ship-to state, download handler, orderProps) + `USE_NEW_ORDER=true`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-the-four-screens-in-flow-order/23-CONTEXT.md
@.planning/phases/23-the-four-screens-in-flow-order/23-RESEARCH.md
@.planning/phases/23-the-four-screens-in-flow-order/23-UI-SPEC.md
@.planning/phases/23-the-four-screens-in-flow-order/23-PATTERNS.md
@src/engine/quote.ts
@src/engine/density.ts
@src/engine/export.ts
</context>

<artifacts_produced>
## Artifacts this phase produces (this plan)
- `src/features/screens/orderPacket.ts` — `OrderPacket` interface (with `schemaVersion`) + pure `buildOrderPacket(input): OrderPacket`.
- `src/features/screens/OrderScreen.tsx` — full `OrderScreen` + expanded `OrderScreenProps` (locked spec + finish + ship-to + quote + onDownloadPacket + downloaded flag).
- `src/features/screens/__tests__/orderPacket.test.ts` — pure serializer unit test (version, self-containment, no-PII-beyond-shipTo).
- `src/features/screens/__tests__/OrderScreen.test.tsx` — jsdom render test.
- Modified: `src/App.tsx` — NEW `finish` + `shipTo` state, `handleDownloadOrderPacket` (buildOrderPacket + anchor/Blob download + generateUUID), `orderProps`. `src/features/screens/flags.ts` (`USE_NEW_ORDER = true`) + `flags.test.ts` update.
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure orderPacket.ts serializer (versioned, self-contained) + unit test</name>
  <files>src/features/screens/orderPacket.ts, src/features/screens/__tests__/orderPacket.test.ts</files>
  <behavior>
    - `buildOrderPacket(input)` returns an object with a string `schemaVersion` (e.g. "1.0"), a `packetId` (from the injected id), a `createdAt` ISO string, a `design` chart reference (cols/rows + grid encoding + drill shape/type), a LOCKED `canvasSpec` (product "Rolled Canvas", widthIn/heightIn, finish, vendor), a `gemBags` list derived from the supply-plan rows, a `quote` integer-cents snapshot (lineItems + totalCents + ratesAsOf), and a `shipTo` block.
    - The packet is JSON-round-trippable: `JSON.parse(JSON.stringify(packet))` deep-equals the packet (self-contained, no functions/DOM/handles).
    - The packet contains NO PII beyond the passed `shipTo` and NO secrets/tokens (assert only the expected top-level keys exist).
    - `product` is always "Rolled Canvas" regardless of input (locked, D-08).
  </behavior>
  <read_first>
    - 23-RESEARCH.md §"Order Packet Serialization (D-08/ORDER-02)" (the recommended schema sketch) + §"buildOrderQuote / planOrderSupply Shapes" (OrderSupplyPlan.rows / OrderQuote shapes)
    - src/engine/quote.ts (OrderQuote type) + src/engine/bagPlanner.ts (OrderSupplyPlan / row shape) + src/engine/projectStore.ts:75-84 (generateUUID — id is INJECTED into buildOrderPacket, not called inside, so the serializer stays pure/deterministic for tests)
  </read_first>
  <action>
    Create `src/features/screens/orderPacket.ts` exporting an `OrderPacket` interface (with a `schemaVersion` string field) and a PURE `buildOrderPacket(input)` that assembles the packet from injected data only — take `packetId`, `createdAt`, the design/chart data (cols/rows + grid encoding + drillShape/drillType), the finish + vendor, the `supplyPlan` (for `gemBags` from its `rows`), the `quote` (OrderQuote snapshot), and `shipTo` as inputs. Do NOT call `generateUUID()` or `new Date()` inside the serializer (inject both) so it is deterministic and unit-testable. Field naming is Claude's discretion (D-08) as long as it is self-contained, versioned, and forward-compatible with the future v5.0 backend. Pin `canvasSpec.product = "Rolled Canvas"` unconditionally (locked). No engine-signature changes — this is a new pure module composing frozen types.
    Create `src/features/screens/__tests__/orderPacket.test.ts` (node env; pure module — no jsdom) asserting the `<behavior>` block: schemaVersion present, JSON round-trip deep-equals, only the expected top-level keys (no stray PII/secret), and product locked to "Rolled Canvas".
  </action>
  <verify>
    <automated>npx vitest run src/features/screens/__tests__/orderPacket.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `buildOrderPacket` is pure (no `generateUUID`/`Date`/DOM inside), returns a `schemaVersion`-bearing self-contained object, and locks `product` to "Rolled Canvas".
    - JSON round-trip deep-equals; top-level keys are exactly the intended set (no extra PII/secret).
    - `npx vitest run src/features/screens/__tests__/orderPacket.test.ts` exits 0.
  </acceptance_criteria>
  <done>Versioned, testable packet serializer exists and is green.</done>
</task>

<task type="auto">
  <name>Task 2: App wiring — finish/shipTo state, handleDownloadOrderPacket, orderProps, flip USE_NEW_ORDER</name>
  <files>src/App.tsx, src/features/screens/flags.ts, src/features/screens/__tests__/flags.test.ts</files>
  <read_first>
    - src/engine/export.ts:260-288 (the anchor + URL.createObjectURL(blob) + deferred-revoke download idiom to mirror for an application/json Blob)
    - src/App.tsx:216 (selectedVendor), 515-516 (matchCols/matchRows), the `orderQuote`/`orderPlan` derivations added in 23-04, and the panel-4 slot
    - src/engine/projectStore.ts:75-84 (generateUUID) + src/engine/density.ts (gridToInches)
  </read_first>
  <action>
    Add App state for the Order-only fields (App stays sole state owner, D-01): `finish` (a fixed UI enum, e.g. 'trimmed' | 'wrap', default 'trimmed') and `shipTo` (an address object, client-side only). Add `handleDownloadOrderPacket`: build the packet via `buildOrderPacket({ packetId: generateUUID(), createdAt: new Date().toISOString(), design: {cols: matchCols, rows: matchRows, grid: matchResult.matches (or its index encoding), drillShape: drillStyle, drillType}, finish, vendor: selectedVendor, canvasSpec size via gridToInches(matchCols, matchRows), supplyPlan: orderPlan, quote: orderQuote, shipTo })`, then serialize to a JSON Blob and trigger a client-side download mirroring `export.ts:260-288` (create anchor, `URL.createObjectURL`, click, deferred `revokeObjectURL`), filename `gempixel-order-<packetId.slice(0,8)>.json`. Wrap in try/catch and surface the existing `actionError` banner on failure ("Couldn't build the order packet. Please try again."). Set a `packetDownloaded` flag on success to drive the honest terminal confirmation. The ship-to is embedded in the packet ONLY — make NO network call (D-08).
    Assemble `orderProps`: the locked spec values (product "Rolled Canvas", size string via gridToInches/formatInches(matchCols,matchRows), `finish`/`onFinishChange`), `shipTo`/`onShipToChange`, `quote` (=`orderQuote`), `onDownloadPacket` (=`handleDownloadOrderPacket`), and `packetDownloaded`. Replace the bare `<OrderScreen />` in the panel-4 slot with `<OrderScreen {...orderProps} />`; leave the legacy `<Step4Export {...} />` branch untouched.
    Set `USE_NEW_ORDER = true` in flags.ts (all four now true) and update `flags.test.ts`. Touch no engine module.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm test</automated>
  </verify>
  <acceptance_criteria>
    - App owns `finish` + `shipTo` state and a `handleDownloadOrderPacket` that builds the packet and downloads via the anchor/Blob idiom with `generateUUID()` for the id; the handler makes no network/`fetch` call.
    - Panel-4 renders `<OrderScreen {...orderProps} />`; `USE_NEW_ORDER === true` (all four flags now true); `flags.test.ts` updated.
    - `npx tsc --noEmit` exits 0; `npm test` full suite green.
  </acceptance_criteria>
  <done>App builds + downloads the packet client-side; Order screen wired; suite green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement OrderScreen.tsx (locked spec + finish + ship-to + quote + honest terminal) + render test</name>
  <files>src/features/screens/OrderScreen.tsx, src/features/screens/__tests__/OrderScreen.test.tsx</files>
  <behavior>
    - Renders the locked spec: a "Rolled Canvas" row with a LOCKED `Pill variant="neutral"`, the SIZE derived from props (e.g. "24 × 16 in · from {cols}×{rows} grid"), and the FINISH; the spec is presentational/non-editable (ORDER-01).
    - Renders two finish cards (Trimmed default + "BEST FOR ART" tag; Image wrap); selecting one calls `onFinishChange`.
    - Renders the ship-to card (name + address) as TEXT (no dangerouslySetInnerHTML) and the price block from `quote.lineItems` + `quote.totalCents` verbatim (single source, no local cents math).
    - The primary CTA is "Download order packet" (no price in the label); clicking it calls `onDownloadPacket`.
    - When `packetDownloaded` is true, an honest terminal confirmation renders ("Packet downloaded — take this to the vendor") and there is NO order number, NO receipt, and NO payment/"Place order"/price-bearing CTA anywhere.
  </behavior>
  <read_first>
    - 23-UI-SPEC.md §"A4 · Order" (proof column, locked spec strip, finish cards, ship-to, price, terminal state) + §"Copywriting Contract" (Order rows — the CTA is "Download order packet", NOT "Place order")
    - src/ui/Pill.tsx, src/ui/Button.tsx + src/engine/quote.ts (OrderQuote type) + src/engine/money.ts (cents formatting helpers)
    - src/features/wizard/__tests__/StepBar.test.tsx (jsdom harness)
  </read_first>
  <action>
    Expand `OrderScreenProps`: the locked spec fields (product/sizeLabel/`finish`/`onFinishChange`), `shipTo`/`onShipToChange`, `quote: OrderQuote`, `onDownloadPacket: () => void`, `packetDownloaded: boolean`. Implement the UI-SPEC A4 layout: the auto-filled LOCKED spec strip (Rolled Canvas + LOCKED Pill, size, finish; caption "Everything the print lab needs is carried over. Nothing to re-enter."), the CANVAS FINISH two-card selector (Trimmed default + tag / Image wrap) bound to `finish`/`onFinishChange`, the SHIP TO card (rendered as text; client-side only), and a price block mapping `quote.lineItems` + `quote.totalCents` verbatim (no cents math). Primary action: green `Button variant="primary"` "Download order packet" → `onDownloadPacket`. When `packetDownloaded`, render the honest terminal confirmation and ensure NO order number / receipt / payment UI / "Place order" / price-in-CTA is present (D-09). Keep PURE/props-only.
    Create `src/features/screens/__tests__/OrderScreen.test.tsx` (`// @vitest-environment jsdom`) asserting the `<behavior>` block — especially: CTA text is "Download order packet" and never "Place order"; clicking it calls `onDownloadPacket`; `packetDownloaded` shows the honest confirmation with no order-number/receipt/payment text; the price total equals the stub `quote.totalCents`; the LOCKED Pill is present.
  </action>
  <verify>
    <automated>npx vitest run src/features/screens/__tests__/OrderScreen.test.tsx && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - The CTA reads "Download order packet" (no price); a grep of OrderScreen finds no "Place order", no "receipt", no order-number/payment UI.
    - The terminal state renders only on `packetDownloaded` and is honest (no fake receipt); ship-to renders as text (no `dangerouslySetInnerHTML`).
    - Price total equals the stub `quote.totalCents` (single source).
    - `npx vitest run .../OrderScreen.test.tsx` exits 0 and `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>OrderScreen renders the honest handoff from props; behavior test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ship-to form → packet file | User PII (name/address) is typed into the ship-to form and embedded in the downloaded packet. |
| app → filesystem | The packet leaves the app ONLY as a user-initiated local Blob download — no network egress. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-23-05-01 | Information disclosure | ship-to PII | high | mitigate | Ship-to is client-side only — embedded in the packet and downloaded locally; `handleDownloadOrderPacket` makes NO `fetch`/network call (grep-clean). Rendered as text (no injection sink). Verified: download handler has no network egress. |
| T-23-05-02 | Information disclosure | packet contents | medium | mitigate | Packet contains only design/spec/gem-bags/quote/ship-to — no secrets/tokens; `orderPacket.test.ts` asserts the top-level key set (no stray PII/secret). |
| T-23-05-03 | Spoofing / integrity | packet id | low | mitigate | `packetId` via `generateUUID()` (CSPRNG, projectStore.ts:75-84) — never `Math.random()`. |
| T-23-05-04 | Repudiation / honesty | terminal confirmation | medium | mitigate | No order number, no receipt, no payment UI (D-09) — the terminal state truthfully reads "packet downloaded"; asserted by the OrderScreen test (no "Place order"/receipt). |
| T-23-SC | Tampering | npm installs | high | accept | No package installs this phase (RESEARCH audit: zero external packages). |
</threat_model>

<verification>
- `npx tsc --noEmit` → 0 errors; engine untouched.
- `npm test` → full suite green (all four screens now live behind their flags).
- `npx vitest run src/features/screens/__tests__/orderPacket.test.ts` + `.../OrderScreen.test.tsx` → pass.
- Grep: `handleDownloadOrderPacket` / OrderScreen have no `fetch`/network egress; OrderScreen has no "Place order"/receipt/payment text; `flags.ts` shows all four flags true.
</verification>

<success_criteria>
- ORDER-01/02 delivered: auto-filled LOCKED spec + finish + ship-to + single-source itemized quote, completed by a versioned self-contained packet download; honest terminal state (no payment, no fake receipt); ship-to client-side only.
- `USE_NEW_ORDER=true` — all four flags true; full Vitest suite + tsc green. The four-screen journey is complete behind the strangler flags.
</success_criteria>

<output>
Create `.planning/phases/23-the-four-screens-in-flow-order/23-05-SUMMARY.md` when done.
</output>
