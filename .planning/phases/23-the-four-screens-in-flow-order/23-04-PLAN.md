---
phase: 23-the-four-screens-in-flow-order
plan: 04
type: execute
wave: 4
depends_on: ["23-03"]
files_modified:
  - src/features/screens/SuppliesScreen.tsx
  - src/features/screens/flags.ts
  - src/App.tsx
  - src/features/screens/__tests__/SuppliesScreen.test.tsx
autonomous: true
requirements: [SUPPLIES-01, SUPPLIES-02]
must_haves:
  truths:
    - "The supply table renders one row per color from planOrderSupply: symbol (symbolMap) · swatch (DMC_PALETTE hex) · DMC code + name · drills incl. +10% safety · bags (bagsText), plus a 'why these bags?' inline disclosure = DYE_LOT_WHY_SENTENCE (SUPPLIES-01, D-07)"
    - "An inline order-summary panel renders buildOrderQuote(...).lineItems verbatim + totalCents with zero local cents math (SUPPLIES-02, D-07)"
    - "An unpriced canvas (canvasPriced=false) or hasUnpricedSize color surfaces an est./unavailable affordance, never a silent $0 (honesty, Pitfall 6)"
    - "USE_NEW_SUPPLIES is true and the full suite stays green"
  artifacts:
    - src/features/screens/SuppliesScreen.tsx
    - src/features/screens/__tests__/SuppliesScreen.test.tsx
  key_links:
    - "App adds const orderQuote = buildOrderQuote({ supplyPlan: orderPlan, canvasBaseCost, vendor: selectedVendor }) (import from ./engine/quote) — orderPlan already at App.tsx:1076; legacy totalCostSafetyCents at :1143-1148 stays untouched"
    - "SuppliesScreen rows ↔ pre-joined sortedMatches (App.tsx:1079-1104) + symbolMap; summary ↔ orderQuote.lineItems / totalCents / canvasPriced / ratesAsOf"
---

<objective>
Swap in the Supplies screen (SUPPLIES-01/02, D-07) — both the legend/supply table and the inline itemized order-summary reading from the single-source quote so Supplies and Order can never diverge. App adds ONE derived value — `const orderQuote = buildOrderQuote({ supplyPlan: orderPlan, canvasBaseCost, vendor: selectedVendor })` (orderPlan already computed at App.tsx:1076; the legacy inline total stays for the legacy Step3/4 bodies until Phase 25) — and passes it plus the already-joined supply rows into `SuppliesScreen`. The screen renders the table (symbol · swatch · DMC code+name · drills incl. +10% · bags + "why these bags?") and the order-summary panel (line items rendered verbatim + total), with zero local cents math (D-07) and honest est./unavailable affordances (never a silent $0). Then flip `USE_NEW_SUPPLIES`.

Purpose: Deliver SUPPLIES-01 (wiring) + the UI consumption of the already-complete SUPPLIES-02 engine support, proving the single-source quote seam.
Output: full `SuppliesScreen.tsx` + render test + App `orderQuote` derivation + `suppliesProps` + `USE_NEW_SUPPLIES=true`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-the-four-screens-in-flow-order/23-CONTEXT.md
@.planning/phases/23-the-four-screens-in-flow-order/23-UI-SPEC.md
@.planning/phases/23-the-four-screens-in-flow-order/23-PATTERNS.md
@src/engine/quote.ts
@src/engine/bagPlanner.ts
@src/ui/Pill.tsx
</context>

<artifacts_produced>
## Artifacts this phase produces (this plan)
- `src/features/screens/SuppliesScreen.tsx` — full `SuppliesScreen` + expanded `SuppliesScreenProps` (supply rows + orderQuote).
- `src/features/screens/__tests__/SuppliesScreen.test.tsx` — jsdom render test.
- Modified: `src/App.tsx` — `buildOrderQuote` import + `const orderQuote = buildOrderQuote(...)` derivation + `suppliesProps`. `src/features/screens/flags.ts` (`USE_NEW_SUPPLIES = true`) + `flags.test.ts` update.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: App — add the single-source orderQuote derivation + suppliesProps, flip USE_NEW_SUPPLIES</name>
  <files>src/App.tsx, src/features/screens/flags.ts, src/features/screens/__tests__/flags.test.ts</files>
  <read_first>
    - src/engine/quote.ts (buildOrderQuote input `{ supplyPlan, canvasBaseCost, vendor }` → OrderQuote { lineItems, totalCents, ratesAsOf, canvasPriced })
    - src/App.tsx:1076-1123 (orderPlan = planOrderSupply(...) + the sortedMatches row join with name/hex/safety/packets/bagsText/hasUnpricedSize) + 1143-1148 (legacy totalCostSafetyCents — leave untouched) + 216-217 (selectedVendor, canvasBaseCost) + 26 (DYE_LOT_WHY_SENTENCE)
  </read_first>
  <action>
    Import `buildOrderQuote` from `./engine/quote`. After `orderPlan` is computed (App.tsx:1076), add `const orderQuote = buildOrderQuote({ supplyPlan: orderPlan, canvasBaseCost, vendor: selectedVendor })`. Do NOT modify or remove the legacy `totalCostSafetyCents` assembly (App.tsx:1143-1148) — it still feeds the legacy Step3/Step4 bodies until Phase 25 (SC5 strangler).
    Assemble `suppliesProps` from already-derived values: `rows` (=`sortedMatches` — already joined with `code`, `name`, `hex`, `safety` (+10%), `packets`, `bagsText`, `hasUnpricedSize`, App.tsx:1079-1104), `symbolMap`, `dyeLotWhy` (=`DYE_LOT_WHY_SENTENCE`), and `quote` (=`orderQuote`). Replace the bare `<SuppliesScreen />` in the panel-3 slot with `<SuppliesScreen {...suppliesProps} />`. Leave the legacy `<Step3Canvas {...} />` branch untouched.
    Set `USE_NEW_SUPPLIES = true` in flags.ts (others unchanged) and update `flags.test.ts` (one flip per commit). Touch no engine module.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm test</automated>
  </verify>
  <acceptance_criteria>
    - App computes `orderQuote` via `buildOrderQuote` from the existing `orderPlan`/`canvasBaseCost`/`selectedVendor`; the legacy `totalCostSafetyCents` remains present and unchanged.
    - Panel-3 renders `<SuppliesScreen {...suppliesProps} />`; `USE_NEW_SUPPLIES === true`; `flags.test.ts` updated.
    - `npx tsc --noEmit` exits 0; `npm test` full suite green.
  </acceptance_criteria>
  <done>App exposes the single-source quote to Supplies; suite green; legacy total intact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement SuppliesScreen.tsx (supply table + verbatim order-summary + honesty)</name>
  <files>src/features/screens/SuppliesScreen.tsx</files>
  <behavior>
    - Renders one table row per `rows` entry: the symbol glyph (`symbolMap[code]`), a swatch styled with `hex`, the DMC `code` (mono bold) + `name` (muted), the `safety` drill count (+10%), and the `bagsText` bag string.
    - Renders each `quote.lineItems` entry verbatim (label + formatted cents) and the total from `quote.totalCents` — with NO local summation of cents in the component.
    - When `quote.canvasPriced` is false OR a row `hasUnpricedSize` is true, an "est./unavailable" affordance renders for that figure and NO `$0` is shown in its place.
    - The "Why these bags?" native `<details>` renders and its body equals the `dyeLotWhy` string.
  </behavior>
  <read_first>
    - src/App.tsx:1079-1104 (the exact row shape being passed) + 23-UI-SPEC.md §"A3 · Supplies" (table columns, order-summary line items, honesty) + §"Copywriting Contract" (Supplies rows)
    - src/engine/quote.ts (QuoteLineItem shape: key/label/cents/estimate/note) + src/ui/Pill.tsx
    - src/engine/money.ts (fromCents/formatUSD — for formatting cents to display; import types/helpers as needed, or receive pre-formatted strings — planner's call, but do NOT sum cents in the screen)
  </read_first>
  <action>
    Expand `SuppliesScreenProps`: `rows` (the joined supply-row array — reuse the App row shape), `symbolMap` (`Record<string,string>` or the `ColorSymbolMap` type), `dyeLotWhy: string`, and `quote: OrderQuote` (import the type from `../../engine/quote`). Implement the UI-SPEC A3 layout: a left supply table with mono micro-label header `(symbol) · (swatch) · DMC COLOR · DRILLS · BAGS`, a "counts include +10% safety" caption, and per-row cells as in the `<behavior>` block (row divider `--border-2`); an inline "Why these bags?" `<details>` whose body is `dyeLotWhy`. A right order-summary panel titled "Order summary" that maps `quote.lineItems` to mono rows (label + the line's formatted dollars) and an "Est. total" figure from `quote.totalCents` in accent green — rendered VERBATIM, with zero `.reduce`/summation of cents in the component (D-07 anti-pattern). Honesty: for `quote.canvasPriced === false` or a row's `hasUnpricedSize`, render an "est./unavailable" affordance (e.g. `Pill` or inline note) instead of a `$0` figure (Pitfall 6). Format cents with `money.ts` helpers (or render App-preformatted strings) — never hand-roll cents math. Keep PURE/props-only.
    This task writes SuppliesScreen.test.tsx-covered behavior; the test lands in Task 3 (its assertions mirror the `<behavior>` block).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - The component renders `quote.lineItems` + `quote.totalCents` verbatim and contains NO cents summation (no `.reduce` over cents, no re-adding line cents).
    - The unpriced/`canvasPriced=false` path renders an est./unavailable affordance, never a literal `$0` substitute.
    - The "Why these bags?" `<details>` body is the `dyeLotWhy` prop.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>SuppliesScreen renders the table + single-source summary + honesty affordance from props.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: SuppliesScreen render test (jsdom)</name>
  <files>src/features/screens/__tests__/SuppliesScreen.test.tsx</files>
  <behavior>
    - Given a stub `quote` with known `lineItems`/`totalCents`, the rendered summary shows each line's label + amount and the total, matching the quote exactly (proves single-source, no local math).
    - Given a `rows` array, renders that many supply rows with the expected `code`, `name`, `safety`, and `bagsText`.
    - Given `quote.canvasPriced: false`, an est./unavailable affordance renders and the canvas figure is NOT "$0.00".
    - The "Why these bags?" details renders the `dyeLotWhy` text.
  </behavior>
  <read_first>
    - src/features/wizard/__tests__/StepBar.test.tsx (jsdom harness)
  </read_first>
  <action>
    Create `src/features/screens/__tests__/SuppliesScreen.test.tsx` with `// @vitest-environment jsdom`, mirroring `StepBar.test.tsx`. Build a representative stub `quote` (OrderQuote) and `rows`. Assert the behaviors above — in particular that the displayed total equals the stub `quote.totalCents` (single-source) and that a `canvasPriced:false` stub yields an est. affordance and never a "$0" canvas line.
  </action>
  <verify>
    <automated>npx vitest run src/features/screens/__tests__/SuppliesScreen.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - All cases pass; the total-equals-quote and no-silent-$0 assertions are explicit.
    - `npx vitest run src/features/screens/__tests__/SuppliesScreen.test.tsx` exits 0.
  </acceptance_criteria>
  <done>Single-source rendering + honesty are test-locked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tampered/legacy price → quote | A tampered/legacy canvas price can yield `canvasPriced:false` or an `hasUnpricedSize` color reaching the summary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-23-04-01 | Tampering / integrity | order-summary line items | medium | mitigate | Render `quote.lineItems`/`totalCents` verbatim (never recompute); surface `canvasPriced:false` / `hasUnpricedSize` as est./unavailable, never a silent `$0` (Pitfall 6). Covered by the `canvasPriced:false` render test. |
| T-23-04-02 | Denial of Service | tampered money into render | low | accept | Money is sanitized at the App load boundary (`sanitizeMoney`, App.tsx:323-324) and routed through `money.ts` in `buildOrderQuote` (throws-loud on non-finite); the screen only reads pre-sanitized cents. |
| T-23-SC | Tampering | npm installs | high | accept | No package installs this phase (RESEARCH audit: zero external packages). |
</threat_model>

<verification>
- `npx tsc --noEmit` → 0 errors; engine untouched.
- `npm test` → full suite green.
- `npx vitest run src/features/screens/__tests__/SuppliesScreen.test.tsx` → passes.
- Grep: SuppliesScreen has no cents `.reduce`; `flags.ts` shows Upload+Refine+Supplies true, Order false.
</verification>

<success_criteria>
- SUPPLIES-01 delivered + SUPPLIES-02 UI-consumed: supply table + "why these bags?" + inline order-summary, all from `planOrderSupply` + the single-source `buildOrderQuote`; honest est./unavailable, never a silent $0.
- `USE_NEW_SUPPLIES=true`, one flag flipped; full Vitest suite + tsc green.
</success_criteria>

<output>
Create `.planning/phases/23-the-four-screens-in-flow-order/23-04-SUMMARY.md` when done.
</output>
