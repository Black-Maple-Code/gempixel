---
phase: 23-the-four-screens-in-flow-order
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/screens/flags.ts
  - src/features/screens/UploadScreen.tsx
  - src/features/screens/RefineScreen.tsx
  - src/features/screens/SuppliesScreen.tsx
  - src/features/screens/OrderScreen.tsx
  - src/features/screens/__tests__/flags.test.ts
  - src/App.tsx
autonomous: true
requirements: [UPLOAD-01, REFINE-01, REFINE-05, SUPPLIES-01, ORDER-01]
must_haves:
  truths:
    - "With all four flags false, the app renders the legacy Step1–4 bodies byte-for-byte unchanged and the full Vitest suite stays green (D-02, strangler)"
    - "flags.ts exports USE_NEW_UPLOAD / USE_NEW_REFINE / USE_NEW_SUPPLIES / USE_NEW_ORDER, all initialized false (D-02)"
    - "Each data-step-panel slot renders a per-flag ternary; the legacy branch is untouched and the single CanvasViewer in <main> never remounts (D-01/D-14, Pitfall 5)"
  artifacts:
    - src/features/screens/flags.ts
    - src/features/screens/UploadScreen.tsx
    - src/features/screens/RefineScreen.tsx
    - src/features/screens/SuppliesScreen.tsx
    - src/features/screens/OrderScreen.tsx
    - src/features/screens/__tests__/flags.test.ts
  key_links:
    - "App data-step-panel slots (App.tsx:1406-1526) ↔ flags.ts boolean imports"
    - "Each ternary legacy branch ↔ untouched StepNIngest/Palette/Canvas/Export component"
---

<objective>
Lay the strangler foundation for the four-screen swap (D-01, D-02): create `src/features/screens/` holding a `flags.ts` module (four `const` booleans, all `false`) and four pure/props-only screen shells (`UploadScreen`, `RefineScreen`, `SuppliesScreen`, `OrderScreen`), then wire each `data-step-panel` slot in App.tsx to a per-flag ternary that renders the new screen when its flag is true and the existing legacy Step body otherwise. All flags stay `false`, so behavior is unchanged and the 240+ Vitest suite stays green — one clean, zero-risk commit that every subsequent per-screen plan builds on.

Purpose: Establish the swap mechanism (D-02) once so each later plan flips exactly one flag per commit with the suite green (Pitfall 7), preserving the Phase 20 D-14 single-mount viewer.
Output: `flags.ts` + four shell components + the App slot wiring + a flags test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-the-four-screens-in-flow-order/23-CONTEXT.md
@.planning/phases/23-the-four-screens-in-flow-order/23-RESEARCH.md
@.planning/phases/23-the-four-screens-in-flow-order/23-PATTERNS.md
@src/features/wizard/StepBar.tsx
@src/features/wizard/__tests__/StepBar.test.tsx
</context>

<artifacts_produced>
## Artifacts this phase produces (created in THIS plan)
- `src/features/screens/flags.ts` — exports `USE_NEW_UPLOAD`, `USE_NEW_REFINE`, `USE_NEW_SUPPLIES`, `USE_NEW_ORDER` (all `false` here).
- `src/features/screens/UploadScreen.tsx` — pure shell + `UploadScreenProps` (minimal; expanded in 23-02).
- `src/features/screens/RefineScreen.tsx` — pure shell + `RefineScreenProps` (minimal; expanded in 23-03).
- `src/features/screens/SuppliesScreen.tsx` — pure shell + `SuppliesScreenProps` (minimal; expanded in 23-04).
- `src/features/screens/OrderScreen.tsx` — pure shell + `OrderScreenProps` (minimal; expanded in 23-05).
- `src/features/screens/__tests__/flags.test.ts` — asserts all four flags default `false`.
- Modified: `src/App.tsx` — four `data-step-panel` ternaries importing the flags + shells.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Create flags.ts + the four minimal pure screen shells</name>
  <files>src/features/screens/flags.ts, src/features/screens/UploadScreen.tsx, src/features/screens/RefineScreen.tsx, src/features/screens/SuppliesScreen.tsx, src/features/screens/OrderScreen.tsx</files>
  <read_first>
    - src/features/wizard/StepBar.tsx (the props-only shape to mirror: `<Component>Props` interface immediately above a named-export function, JSDoc block, no default export, no useState)
    - .planning/phases/23-the-four-screens-in-flow-order/23-PATTERNS.md §"Shared Conventions" + §"flags.ts (config module — NO ANALOG)"
    - src/ui/cn.ts (class-merge helper the screens will use later)
  </read_first>
  <action>
    Create `src/features/screens/flags.ts` exporting four named `const` booleans — `USE_NEW_UPLOAD`, `USE_NEW_REFINE`, `USE_NEW_SUPPLIES`, `USE_NEW_ORDER` — each assigned `false`, with a header JSDoc explaining the per-screen strangler swap (D-02) and the "flip exactly one per commit" rule (Pitfall 7). Plain `const` booleans, no typed record, no env plumbing.
    Create the four shell components under `src/features/screens/`, each mirroring `StepBar.tsx`: a `<Screen>Props` interface immediately above a named-export function component (`UploadScreen`, `RefineScreen`, `SuppliesScreen`, `OrderScreen`), each with a JSDoc noting PURE/props-only (D-01) and which D-decision it will satisfy. Keep each `<Screen>Props` interface MINIMAL for now (no required props — full interfaces land in 23-02..23-05); the body renders a single placeholder element (e.g. a `<section data-screen="upload">` etc.) using Atelier token classes only. No `useState` for domain data, no engine imports, no default exports.
    Do NOT wire these into App yet — that is Task 2.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `flags.ts` exports exactly the four named booleans, all `false`.
    - Each screen file exports a named function component with a `<Screen>Props` interface declared immediately above it; none uses `useState` or imports from `src/engine/`.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>flags.ts and four pure shell components compile; no App wiring yet.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the four data-step-panel slots to per-flag ternaries (flags off)</name>
  <files>src/App.tsx</files>
  <read_first>
    - src/App.tsx:1401-1526 (the four `data-step-panel` sibling slots rendering Step1Ingest / Step2Palette / Step3Canvas / Step4Export)
    - src/App.tsx:1-20 (import block) and 1583 (`<main>` single-mount CanvasViewer — must NOT move)
    - .planning/phases/23-the-four-screens-in-flow-order/23-PATTERNS.md §"src/App.tsx (modified — strangler swap)" (Pattern 3 ternary)
  </read_first>
  <action>
    Import the four flags from `./features/screens/flags` and the four shell components from `./features/screens/*` at the top of App.tsx.
    Inside EACH always-mounted `data-step-panel` div (do NOT change the `contents`/`hidden` CSS-toggle or the div itself — Pitfall 5), wrap the existing legacy Step body in a ternary: panel 1 → `USE_NEW_UPLOAD ? <UploadScreen /> : <Step1Ingest {...existing props} />`; panel 2 → `USE_NEW_REFINE ? <RefineScreen /> : <Step2Palette {...} />`; panel 3 → `USE_NEW_SUPPLIES ? <SuppliesScreen /> : <Step3Canvas {...} />`; panel 4 → `USE_NEW_ORDER ? <OrderScreen /> : <Step4Export {...} />`. The legacy branch keeps its exact current props (untouched until Phase 25). The new-screen branch renders the shell with no props (minimal interface). Leave all flags `false` in flags.ts, so every legacy body still renders and behavior is byte-identical.
    Do NOT touch the hoisted `<CanvasViewer>` in `<main>`, the AtelierShell wiring, or `useWizard`.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm test</automated>
  </verify>
  <acceptance_criteria>
    - Each of the four `data-step-panel` divs now contains a `USE_NEW_*` ternary; the `contents`/`hidden` class expression is unchanged.
    - The single `<CanvasViewer>` remains in `<main>` (grep confirms exactly one `new CanvasViewer(` / `<CanvasViewer` mount site is unchanged).
    - `npx tsc --noEmit` exits 0 and `npm test` (vitest run) passes the full existing suite (240+ it cases green).
  </acceptance_criteria>
  <done>App renders legacy bodies via flag-off ternaries; suite green; viewer single-mount preserved.</done>
</task>

<task type="auto">
  <name>Task 3: Add flags.test.ts asserting the all-false default</name>
  <files>src/features/screens/__tests__/flags.test.ts</files>
  <read_first>
    - src/features/wizard/__tests__/StepBar.test.tsx (vitest structure; note default env is node — this test needs no jsdom pragma since it only imports constants)
  </read_first>
  <action>
    Create `src/features/screens/__tests__/flags.test.ts` importing all four flags from `../flags` and asserting each strictly equals `false`. Add a short comment: this is the strangler guard — a plan that flips a flag updates this test in the same commit so the "one flag per commit" invariant is observable (Pitfall 7). No jsdom pragma required (constants only).
  </action>
  <verify>
    <automated>npx vitest run src/features/screens/__tests__/flags.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The test asserts all four flags are `false` and passes.
    - `npx vitest run src/features/screens/__tests__/flags.test.ts` exits 0.
  </acceptance_criteria>
  <done>flags.test.ts passes; the swap-flag default is guarded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan adds an inert flag module + placeholder shells + a render ternary. No new input crosses any boundary; no data, network, or persistence surface is introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-23-01-01 | Tampering | strangler swap in App.tsx | low | accept | Flags are compile-time `const`s (no runtime/env input); the legacy branch is untouched so existing mitigations remain in force. |
| T-23-SC | Tampering | npm/pip/cargo installs | high | accept | No package installs this phase (RESEARCH Package Legitimacy Audit: zero external packages). No `[ASSUMED]`/`[SUS]` packages, so no legitimacy checkpoint required. |
</threat_model>

<verification>
- `npx tsc --noEmit` → 0 errors.
- `npm test` → full existing suite green (no behavior change; flags all false).
- `npx vitest run src/features/screens/__tests__/flags.test.ts` → passes.
- Manual/grep: exactly one `<CanvasViewer>` mount site in `<main>`; all four `data-step-panel` divs retain their `contents`/`hidden` toggle.
</verification>

<success_criteria>
- `src/features/screens/` exists with `flags.ts` (four false booleans) + four pure shell components + a passing flags test.
- App.tsx renders each step panel through a `USE_NEW_*` ternary with the legacy body intact and all flags off.
- Full Vitest suite + tsc green (strangler invariant: green at every commit).
</success_criteria>

<output>
Create `.planning/phases/23-the-four-screens-in-flow-order/23-01-SUMMARY.md` when done.
</output>
