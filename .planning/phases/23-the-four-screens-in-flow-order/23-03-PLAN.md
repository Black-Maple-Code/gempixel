---
phase: 23-the-four-screens-in-flow-order
plan: 03
type: execute
wave: 3
depends_on: ["23-02"]
files_modified:
  - src/features/screens/RefineScreen.tsx
  - src/features/screens/flags.ts
  - src/App.tsx
  - src/features/screens/__tests__/RefineScreen.test.tsx
autonomous: true
requirements: [REFINE-01, REFINE-02, REFINE-03, REFINE-04, REFINE-05]
must_haves:
  truths:
    - "Size cards show grid dims + true derived inches (gridToInches/formatInches, 2.5mm/dot) + a live drill count; selecting a card sets live cols/rows only and updates the card counts instantly (REFINE-01, D-05)"
    - "A custom-size entry (cols/inches) with sane clamps is available and reuses the existing App width/height clamp precedent (REFINE-02, D-05)"
    - "A size change marks downstream steps stale + surfaces the single Recompute CTA; it does NOT re-fire the worker per click (D-04, worker tier)"
    - "Edge-cleanup SegmentedControl (Off/Light/Med/Strong → smoothing 0-3) and the color-count Slider re-render the chart live with no worker re-fire and no staleness (REFINE-03/04, D-03, post-process tier)"
    - "The color slider max equals detectedColorCount (raw-keyed, stable under drag); lowering it merges orphan drills with no visible picture change (REFINE-04, D-04, Pitfall 3)"
    - "Advanced disclosure (closed by default) holds kit (default all), color-exclusion, drill-shape (default square) (REFINE-05, D-06)"
    - "USE_NEW_REFINE is true and the full suite stays green"
  artifacts:
    - src/features/screens/RefineScreen.tsx
    - src/features/screens/__tests__/RefineScreen.test.tsx
  key_links:
    - "size onSelect ↔ setCols/setRows (live) → existing soft-invalidate (App.tsx:507-510) → stale/onRecompute props (handleRecomputeMatch App.tsx:522-532)"
    - "edge-cleanup ↔ enableSmoothing + smoothingStrength (App.tsx:203-209) → hook matchResult memo (post-process, no worker)"
    - "color slider ↔ NEW enableReduce + targetColorCount App state → hook params (useDiamondArtMatch.ts:40-47,271-275); max = detectedColorCount (hook return :49-57,241-244)"
    - "Advanced ↔ selectedBaseKit / excludedColors+toggleColorExclusion / drillStyle (App.tsx:185-188)"
---

<objective>
Swap in the keystone Refine screen and wire the two-tier reactivity seam (D-03/D-04) exactly. `RefineScreen` (right rail inside the panel-2 slot; the single-mount viewer stays in `<main>`) composes: SizeCards showing grid dims + true derived inches (computed in App via `gridToInches`/`formatInches`) + a live drill count, plus a custom-size entry with clamps (REFINE-01/02); a `SegmentedControl` edge-cleanup mapped to the already-wired `enableSmoothing`/`smoothingStrength` (REFINE-03); a `Slider` color-count whose `max = detectedColorCount` driving NEW App `enableReduce`/`targetColorCount` state (REFINE-04); and an "Advanced" `<details>` (kit/exclude/shape, defaults kit=all, shape=square) (REFINE-05). Size = worker tier (sets live cols/rows only → existing soft-invalidate → single Recompute CTA, NO per-click worker re-fire). Edge-cleanup + color slider = pure main-thread post-process (live, no worker, no staleness). Then build `refineProps`, pass into panel 2, flip `USE_NEW_REFINE`.

Purpose: Deliver REFINE-01..05 — the highest-risk screen — sequenced after Upload, keeping the suite green (Pitfall 7) and honoring the locked two-tier model (critical_design_resolution).
Output: full `RefineScreen.tsx` + render test + App wiring (two new state vars + hook pass-through + `detectedColorCount` destructure + preset list + derived inch strings) + `USE_NEW_REFINE=true`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-the-four-screens-in-flow-order/23-CONTEXT.md
@.planning/phases/23-the-four-screens-in-flow-order/23-RESEARCH.md
@.planning/phases/23-the-four-screens-in-flow-order/23-UI-SPEC.md
@.planning/phases/23-the-four-screens-in-flow-order/23-PATTERNS.md
@src/features/match/useDiamondArtMatch.ts
@src/engine/density.ts
@src/ui/SizeCard.tsx
@src/ui/SegmentedControl.tsx
@src/ui/Slider.tsx
</context>

<artifacts_produced>
## Artifacts this phase produces (this plan)
- `src/features/screens/RefineScreen.tsx` — full `RefineScreen` + expanded `RefineScreenProps` (size + edge-cleanup + color slider + Advanced + stale/onRecompute).
- `src/features/screens/__tests__/RefineScreen.test.tsx` — jsdom render test asserting the two-tier seam.
- Modified: `src/App.tsx` — NEW `enableReduce` + `targetColorCount` state, passed into `useDiamondArtMatch`; `detectedColorCount` destructured from the hook return; a curated SizeCard preset list; App-derived inch strings; `refineProps`. `src/features/screens/flags.ts` (`USE_NEW_REFINE = true`) + `flags.test.ts` update.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: App wiring — enableReduce/targetColorCount state, hook pass-through, detectedColorCount, preset list, refineProps</name>
  <files>src/App.tsx</files>
  <read_first>
    - src/App.tsx:482-493 (the useDiamondArtMatch call — currently omits enableReduce/targetColorCount and does not read detectedColorCount)
    - src/App.tsx:128-160 (cols/rows/matchInputs/widthInput/heightInput state) + 507-532 (isStale, staleFromStep, handleRecomputeMatch) + 720-853 (handleWidthChange/handleHeightChange clamp precedent)
    - src/App.tsx:185-188 (drillStyle / selectedBaseKit / excludedColors) + 469-480 (baseCandidates / activeCandidates) + 817 (toggleColorExclusion)
    - src/engine/density.ts (gridToInches, formatInches) and 23-RESEARCH.md §"App.tsx State → Screen Props Map" (Refine row) + Open Question Q2 (curated preset list)
  </read_first>
  <action>
    Add two new App state vars: `enableReduce` (boolean, default `false`) and `targetColorCount` (number). Pass BOTH into the existing `useDiamondArtMatch({...})` call (they are already-accepted-but-omitted params, useDiamondArtMatch.ts:40-47) and destructure `detectedColorCount` from the hook's return (currently unread). Keep the worker-tier inputs (`image`/`cols`/`rows` sourced from `matchInputs`) unchanged — enableReduce/targetColorCount are post-process only (D-03/D-04), so adding them must NOT change the worker effect (it keys only on image/cols/rows/candidatesKey). Initialize `targetColorCount` sensibly (e.g. to `detectedColorCount` once a match exists / a large default) so the slider starts at the top and reduce stays a no-op until the user lowers it (SC5 byte-identical default). Clamp `targetColorCount` to `[8, detectedColorCount]` when set.
    Define a curated SizeCard preset list (array of `{ label, cols, rows, tag? }`) — verify count/labels against the A2-refine design render (23-UI-SPEC A2 / RESEARCH Q2); do NOT reuse the mixed-unit `STANDARD_SIZES` directly. For each preset compute its inch string in App via `gridToInches(preset.cols, preset.rows)` + `formatInches(...)` and its drill count (`preset.cols * preset.rows`), so the card receives fully-computed strings/numbers (Pattern 2 — no derivation in the card).
    Assemble `refineProps` from existing + new state/handlers: `sizePresets` (with pre-derived `inches` + `drillCount`), `cols`, `rows`, `onSelectSize` (sets live `setCols`/`setRows` only — NEVER commits matchInputs or fires the worker), `widthInput`, `heightInput`, `onWidthChange` (=`handleWidthChange`), `onHeightChange` (=`handleHeightChange`), `edgeCleanup` value derived from `enableSmoothing`+`smoothingStrength` and `onEdgeCleanupChange` setting them (Off → enableSmoothing=false; 1/2/3 → enableSmoothing=true + smoothingStrength=n), `colorTarget` (=targetColorCount), `detectedColorCount`, `currentColorCount` (=`Object.keys(matchResult?.counts ?? {}).length`, the post-reduce distinct count), `onColorTargetChange` (sets enableReduce=true + targetColorCount), `selectedBaseKit`/`onKitChange`, `drillStyle`/`onShapeChange`, `excludedColors`/`onToggleExclude` (=`toggleColorExclusion`), `baseCandidates`, `stale` (=`isStale`), `onRecompute` (=`handleRecomputeMatch`). Do NOT render RefineScreen yet (interface expands in Task 2) — for this task it is enough that App compiles with the bare `<RefineScreen />` still in the slot.
    Touch NO engine module (engine frozen). Leave the legacy `Step2Palette` branch untouched.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm test</automated>
  </verify>
  <acceptance_criteria>
    - App declares `enableReduce` + `targetColorCount` state and passes both into `useDiamondArtMatch`; `detectedColorCount` is destructured from the hook return.
    - The worker effect deps in useDiamondArtMatch.ts are unchanged (no engine diff); grep shows `src/engine/` and `src/features/match/` untouched by this plan.
    - A curated preset list exists with App-computed `inches` (via gridToInches/formatInches) + drillCount per preset.
    - `npx tsc --noEmit` exits 0; `npm test` full suite green (reduce is a no-op default, so matchResult stays byte-identical — SC5).
  </acceptance_criteria>
  <done>App owns the Refine state surface + derived props; suite green; engine untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Implement RefineScreen.tsx (size / edge-cleanup / color slider / Advanced) + flip USE_NEW_REFINE</name>
  <files>src/features/screens/RefineScreen.tsx, src/features/screens/flags.ts, src/features/screens/__tests__/flags.test.ts</files>
  <read_first>
    - src/ui/SizeCard.tsx (dumb card contract: label/gridDims/inches/drillCount/selected/onSelect/tag — NO derivation in the card)
    - src/ui/SegmentedControl.tsx (controlled radiogroup: value/onChange/options/label) + src/ui/Slider.tsx (native range, onInput, value/onChange/min/max/ariaLabel/ariaValueText)
    - src/features/wizard/StepBar.tsx (the stale prop shape to mirror) + 23-UI-SPEC.md §"A2 · Refine" (rail layout, copywriting, two-tier reactivity table, slider states)
    - 23-PATTERNS.md §"RefineScreen.tsx (keystone)" + 23-RESEARCH.md §"The Two-Tier Reactivity Seam"
  </read_first>
  <action>
    Expand `RefineScreenProps` to the surface App assembles in Task 1 (see that props list). Implement the rail (UI-SPEC A2): heading "How big should it be?"; a vertical stack of `SizeCard`s from `sizePresets` (each receives the App-computed `inches` string + `drillCount`; `selected = cols===preset.cols && rows===preset.rows`; `onSelect={() => onSelectSize(preset.cols, preset.rows)}`; `tag` → the card's badge); a "Bigger canvas = more detail & more drills. Custom size" footer where the accent "Custom size" link toggles a custom cols/inches entry bound to `widthInput`/`heightInput` + `onWidthChange`/`onHeightChange` (the clamps live in App's handlers — the screen just forwards strings).
    Edge cleanup: a `SegmentedControl` with options Off/Light/Med/Strong bound to `edgeCleanup`/`onEdgeCleanupChange`, micro-label "EDGE CLEANUP · smooths ragged edges".
    Color count: a `Slider` with `value={colorTarget}`, `min={8}`, `max={detectedColorCount}`, `onChange={onColorTargetChange}`, `ariaLabel` + `ariaValueText` (e.g. "{currentColorCount} of {detectedColorCount} matched"); micro-label header "COLOR COUNT · {currentColorCount} of {detectedColorCount} matched" and the caption from the UI-SPEC. Use the Slider primitive as-is (it uses `onInput` internally — do NOT re-wrap with onChange, Pitfall 2). Show `currentColorCount` beside the slider so a smoothing dead-zone reads as "already at N" (Specifics note).
    Advanced: a native `<details>` (closed by default) holding kit (`selectedBaseKit`/`onKitChange`, options 100/200/all, default all), color-exclusion (`excludedColors`/`onToggleExclude` over `baseCandidates`), and drill-shape (`drillStyle`/`onShapeChange`, square/round, default square).
    Stale/recompute: accept `stale` + `onRecompute`; render the existing "out of date → Recompute match" affordance for the size (worker) tier ONLY. The screen NEVER fires the worker itself — size selection just calls `onSelectSize` (which sets live cols/rows in App); the existing soft-invalidate owns the re-match.
    Keep PURE/props-only — no useState for domain data (a local "custom-size expanded" presentational flag is the only permitted state), no engine imports beyond types.
    Replace the bare `<RefineScreen />` in the panel-2 slot with `<RefineScreen {...refineProps} />`. Set `USE_NEW_REFINE = true` in flags.ts (others unchanged) and update `flags.test.ts` accordingly (one flip per commit).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - RefineScreen composes SizeCard + SegmentedControl + Slider + native `<details>`; the Slider `max` prop is `detectedColorCount` (NOT a matchResult length).
    - `onSelectSize` is called with `(cols, rows)` on card click and the screen makes no worker/engine call; edge-cleanup + slider handlers are the App post-process setters (no staleness path).
    - Advanced `<details>` is closed by default; kit default all, shape default square.
    - `USE_NEW_REFINE === true`; `flags.test.ts` updated; `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>RefineScreen live behind its flag; two-tier wiring in place; compiles.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: RefineScreen render test — assert the two-tier seam + slider max + defaults</name>
  <files>src/features/screens/__tests__/RefineScreen.test.tsx</files>
  <behavior>
    - Renders one SizeCard per preset with its `inches` string + `drillCount`; the currently-selected preset has `aria-pressed="true"`.
    - Clicking a size card calls the stubbed `onSelectSize(cols, rows)` and does NOT call any recompute/worker stub (worker tier is App's soft-invalidate, not the screen).
    - The color Slider renders with `max` equal to the stubbed `detectedColorCount` (assert the range input's max attribute), and dragging/input calls `onColorTargetChange` with a number.
    - The edge-cleanup control is a `role="radiogroup"` with four options; selecting one calls `onEdgeCleanupChange`.
    - The Advanced `<details>` is present and closed by default (no `open` attribute); kit shows "all" selected and shape shows "square" selected by default.
    - When `stale` is true, the Recompute affordance renders and clicking it calls `onRecompute`.
  </behavior>
  <read_first>
    - src/features/wizard/__tests__/StepBar.test.tsx (jsdom harness template)
  </read_first>
  <action>
    Create `src/features/screens/__tests__/RefineScreen.test.tsx` with `// @vitest-environment jsdom`, mirroring `StepBar.test.tsx`. Stub all callbacks with `vi.fn()` and pass representative props (e.g. `detectedColorCount: 26`, a small `sizePresets` array, `stale: false` then `true`). Assert every behavior in the `<behavior>` block — especially: slider `max === detectedColorCount`; a size-card click fires `onSelectSize` but NOT `onRecompute`; edge-cleanup radiogroup + option select; Advanced closed by default with kit=all/shape=square; stale → Recompute → `onRecompute`.
  </action>
  <verify>
    <automated>npx vitest run src/features/screens/__tests__/RefineScreen.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - All cases pass; the "size click does not trigger recompute" and "slider max === detectedColorCount" assertions are explicit (they encode D-03/D-04 + Pitfall 3).
    - `npx vitest run src/features/screens/__tests__/RefineScreen.test.tsx` exits 0.
  </acceptance_criteria>
  <done>The two-tier seam, stable slider max, and Advanced defaults are test-locked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| custom-size inputs → grid dims | Free-typed cols/inches strings become `cols`/`rows` that size the grid and the (deferred) worker match. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-23-03-01 | Denial of Service | custom-size numeric entry | medium | mitigate | Clamp/validate via the EXISTING App `handleWidthChange`/`handleHeightChange` precedent (App.tsx:720-853) — the screen forwards strings; App owns the clamp. No NaN/`Infinity`/DoS-scale grid reaches cols/rows. `targetColorCount` clamped to `[8, detectedColorCount]`. |
| T-23-03-02 | Tampering | reduce no-op default | low | accept | `enableReduce` defaults false; matchResult stays byte-identical until the user lowers the slider (SC5). No integrity risk. |
| T-23-SC | Tampering | npm installs | high | accept | No package installs this phase (RESEARCH audit: zero external packages). |
</threat_model>

<verification>
- `npx tsc --noEmit` → 0 errors; engine + useDiamondArtMatch signatures unchanged (grep `src/engine/` clean of this plan's diffs).
- `npm test` → full suite green (reduce no-op default keeps matchResult byte-identical, SC5).
- `npx vitest run src/features/screens/__tests__/RefineScreen.test.tsx` → passes (two-tier seam locked).
- Grep: `flags.ts` shows `USE_NEW_UPLOAD` + `USE_NEW_REFINE` true; others false. Slider `max={detectedColorCount}` in RefineScreen (not a counts length).
</verification>

<success_criteria>
- REFINE-01..05 delivered: live size cards + custom size (worker tier via soft-invalidate/Recompute), live edge-cleanup + color slider (post-process, no worker), stable slider max = detectedColorCount, Advanced disclosure with sane defaults.
- `USE_NEW_REFINE=true`, one flag flipped; full Vitest suite + tsc green.
</success_criteria>

<output>
Create `.planning/phases/23-the-four-screens-in-flow-order/23-03-SUMMARY.md` when done.
</output>
