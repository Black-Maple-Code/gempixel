---
phase: 23-the-four-screens-in-flow-order
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - src/App.tsx
  - src/features/wizard/CanvasWorkspace.tsx
  - src/__tests__/App.test.tsx
  - src/__tests__/print.test.tsx
autonomous: true
gap_closure: true
requirements: [REFINE-01, UPLOAD-01]
user_setup: []

must_haves:
  truths:
    - "The single CanvasViewer mounts exactly once and never remounts on a step change (Phase 20 D-14 preserved through the extraction)."
    - "No duplicate upload prompt exists: the legacy center-canvas hero dropzone is gone; UploadScreen (D-10) is the only ingestion surface."
    - "The full Vitest suite is green and tsc is clean after the extraction (byte-behavior-equivalent refactor)."
    - "App.test.tsx and print.test.tsx no longer reach the legacy 'My Images' left-drawer to load a project; they load via the UploadScreen recent-project chips instead."
  artifacts:
    - "src/features/wizard/CanvasWorkspace.tsx — a pure/props-only region hosting the single-mount <canvas>, viewport HUD, zoom controls, loading overlay, print legends, and match/action error banners."
  key_links:
    - "canvasRef flows App → CanvasWorkspace unchanged, so the CanvasViewer init effect (App.tsx ~L654) keeps binding the same element (single mount)."
    - "UploadScreen recent chips call the same App loadProject(id) the retired left drawer called — so pre-migrated tests exercise identical state restoration."
---

<objective>
Gap closure for Phase 23 UAT Test 26 (layout gap), step 1 of 3. This plan does the SAFE PREP that makes the shell flip (Plan 08) green: it extracts the center-canvas workspace into a single reusable component, deletes the duplicate hero upload prompt, and re-points the project-load test helpers off the soon-to-be-retired left "My Images" drawer and onto the UploadScreen recent chips. The legacy dark 3-column shell is still rendered at the end of this plan — nothing user-visible flips yet — so every commit stays green on the existing shell.

Purpose: The canvas must become Refine's preview column in the new viewport frame while remaining a single mount (D-14). Extracting it first, and severing test coupling to legacy-only chrome, lets Plan 08 swap the shell in one atomic, green commit.
Output: `src/features/wizard/CanvasWorkspace.tsx`; a hero-dropzone-free `<main>`; App.test.tsx + print.test.tsx loading projects via UploadScreen chips.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-the-four-screens-in-flow-order/23-UAT.md
@.planning/phases/23-the-four-screens-in-flow-order/23-CONTEXT.md
@.planning/phases/23-the-four-screens-in-flow-order/23-UI-SPEC.md

# Integration site + the pieces being re-parented
@src/App.tsx
@src/features/wizard/AtelierShell.tsx
@src/features/screens/UploadScreen.tsx

# Test files being pre-migrated
@src/__tests__/App.test.tsx
@src/__tests__/print.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract CanvasWorkspace and delete the duplicate hero upload prompt</name>
  <files>src/features/wizard/CanvasWorkspace.tsx, src/App.tsx</files>
  <action>
Extract the entire center-canvas region currently inside `<main>` (App.tsx ~L1900-2140 — the `viewport-dots` container: the viewport HUD with the Grid/Symbols/Original mode toggle and zoom controls, the low-zoom warning, the `print-canvas-sheet` with the single `<canvas ref={canvasRef}>` and the left/right print legends, the loading overlay, the matchError banner, and the actionError banner) into a NEW pure/props-only component `src/features/wizard/CanvasWorkspace.tsx`.

Follow the project convention (PascalCase.tsx, a `CanvasWorkspaceProps` interface above the component, named export, no engine-value imports beyond types). The component owns NO domain state — App stays the sole state owner (Phase 20 D-01). Thread every value it needs as props: `canvasRef`, `viewportMode` + `setViewportMode`, the viewer zoom callbacks (wrap `viewerRef.current?.zoomIn/zoomOut/fitToContainer` as `onZoomIn`/`onZoomOut`/`onFit` handler props so the ref stays in App), `image`, `matchResult`, `loading`, `loadingPhase`, `progress`, `zoomScale`, `cols`, `rows`, `symbolMap`, `leftLegendColors`, `rightLegendColors`, `matchError`, `actionError`, `onDismissActionError`. Preserve every className, `data-*`/`id` attribute, `no-print`/`print:*` class, and the `print-legend-left`/`print-legend-right` markup EXACTLY — this is a byte-behavior-equivalent move, not a redesign.

CRITICAL (D-14 single mount): `canvasRef` is passed through as a prop and attached to the same `<canvas>` element. Do not create a second canvas and do not gate the canvas element behind a step conditional here. The CanvasViewer init effect (App.tsx ~L654, keyed on image/matchResult) must keep binding the identical element — verify canvasRef identity is unchanged after the extraction.

Delete the duplicate hero empty-state dropzone: the legacy `(image || matchResult) ? <grid> : <hero dropzone with #hero-file-upload>` else-branch (App.tsx ~L2036-2068) is removed. When there is no image/match, CanvasWorkspace renders nothing in the canvas slot (UploadScreen owns ingestion per D-10 — this removes the cream-on-dark duplicate upload prompt the UAT flagged). No test references `#hero-file-upload`, so this is safe.

In App.tsx, replace the extracted JSX inside `<main>` with `<CanvasWorkspace ...props />`. Leave the legacy dark 3-column shell, the left `<aside>`, and the right `<aside>` untouched for now — they are retired in Plan 08. This plan must remain green on the existing shell.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm test</automated>
  </verify>
  <done>CanvasWorkspace.tsx exists and is rendered once inside `<main>`; the `#hero-file-upload` hero dropzone is gone; canvasRef binds the same element (viewer never remounts); full suite + tsc green.</done>
</task>

<task type="auto">
  <name>Task 2: Re-point App.test.tsx + print.test.tsx project loads onto UploadScreen chips</name>
  <files>src/__tests__/App.test.tsx, src/__tests__/print.test.tsx</files>
  <action>
Sever these two suites' dependence on the legacy left "My Images" drawer so Plan 08 can delete it without breaking them. Every helper that finds the drawer toggle by its "My Images" text and clicks a drawer row to load a saved project must instead click the equivalent UploadScreen recent-project chip.

Concretely: the current pattern finds a button whose text includes `My Images`, clicks it to open the drawer, then clicks a saved-project row. Replace it with: locate the UploadScreen recent chip (the button inside `[data-screen="upload"]` whose text contains the seeded project's name) and click it — that button calls the same App `loadProject(id)`, so state restoration is identical. The chips render whenever `projectsRegistry.length > 0` and are present in the DOM regardless of the current step (panels are always-mounted, display-toggled), so no drawer open is required. Update the shared `openDrawer`/toggle helpers accordingly (or delete them and inline the chip click).

Do NOT change navigation or reset hooks: `#wizard-next-btn`, `#wizard-back-btn`, `#new-project-btn`, and `#upload-file-input` are preserved through the flip, so keep those assertions as-is. Do NOT touch the sidebar-collapse assertions in App.test.tsx (~L224-247) — those are retired in Plan 07.

This runs against the still-legacy shell (UploadScreen already renders on the step-1 panel there), so the migrated tests must pass immediately.
  </action>
  <verify>
    <automated>npm test -- src/__tests__/App.test.tsx src/__tests__/print.test.tsx</automated>
  </verify>
  <done>Neither App.test.tsx nor print.test.tsx locates a project via a "My Images" drawer toggle; both load via UploadScreen recent chips; those two suites and the full suite stay green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage → UI | Saved project/image names originate from user/imported data and cross into rendered chips. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-23-06-01 | Tampering/XSS | UploadScreen recent chips (project names from localStorage) | medium | mitigate | Names render as escaped text only (Preact default; no dangerouslySetInnerHTML) — already in force (T-23-02-01), unchanged by this refactor. |
| T-23-06-02 | Information disclosure | CanvasWorkspace extraction | low | accept | Pure DOM re-parenting, no new data flow, no network egress; single-mount canvas identity preserved. |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0.
- `npm test` runs the full suite green (baseline was 347 passed / 12 skipped; count may shift only via the chip-load helper edits, never a regression to failures).
- `git grep -n "hero-file-upload" src/App.tsx` returns nothing (duplicate prompt removed).
- CanvasWorkspace is referenced exactly once in App.tsx.
</verification>

<success_criteria>
Single-mount canvas is extracted and still mounts once; the duplicate hero upload prompt is deleted; App.test.tsx + print.test.tsx load projects via UploadScreen chips; suite + tsc green. The legacy shell is otherwise untouched (retired in Plan 08).
</success_criteria>

<output>
Create `.planning/phases/23-the-four-screens-in-flow-order/23-06-SUMMARY.md` when done.
</output>
