---
phase: 23-the-four-screens-in-flow-order
plan: 08
type: execute
wave: 3
depends_on: [23-06, 23-07]
files_modified:
  - src/App.tsx
  - src/__tests__/App.test.tsx
  - .planning/ROADMAP.md
autonomous: false
gap_closure: true
requirements: [UPLOAD-01, REFINE-01, SUPPLIES-01, ORDER-01]
user_setup: []

must_haves:
  truths:
    - "Each screen (Upload/Refine/Supplies/Order) is the PRIMARY content of the Atelier shell, hosted in a centered ~1180px frame — not inside a 320px dark aside (UAT Test 26 gap closed)."
    - "On Refine (step 2) the layout is [live CanvasWorkspace preview | 360px RefineScreen rail]; the canvas is hidden on Upload/Supplies/Order via CSS and never unmounts (D-14)."
    - "The legacy dark 3-column chrome is gone: no bg-slate-950 shell wrapper, no left 'My Images' menu, no right Color-Legend/DMC aside, no legacy hero prompt."
    - "Still-needed legacy options live in the viewport: recent projects + Save + New/Reset are reachable without a side menu; Back/Next keep their #wizard-next-btn/#wizard-back-btn ids and gating."
    - "An integrated full-App layout test guards 'new screens host the viewport, legacy menu retired'; the full Vitest suite is green and tsc is clean."
  artifacts:
    - "src/App.tsx — AtelierShell body is the centered viewport frame; both legacy asides and the dark shell wrapper are deleted."
    - "src/__tests__/App.test.tsx — a new integrated layout regression test (gap missing item #4)."
  key_links:
    - "CanvasWorkspace is an always-mounted sibling in the frame, shown only when step===2 (single-mount canvas preserved across all steps)."
    - "guardedGoTo + wizard.next/back + StepBar still own navigation; the relocated Back/Next buttons keep their ids and disabled/stale gating so navigation tests pass unchanged."
    - "matchError/actionError banners are hoisted to frame scope so they surface on any step (not only while the canvas is visible)."
---

<objective>
Gap closure for Phase 23 UAT Test 26, step 3 of 3 — the flip. Replace the legacy dark 3-column shell (bg-slate-950 wrapper + 320px left "My Images" aside + center `<main>` + right Color-Legend/DMC aside + in-aside Back/Next) with a centered ~1180px cream viewport frame that hosts the four screens as the shell's primary content, per 23-UI-SPEC. Relocate the still-needed options (recent projects, Save, New/Reset, Back/Next) into the viewport, delete the retired chrome and its dead collapse/drawer state, add the integrated layout regression test, and reconcile the ROADMAP so Phase 25 shrinks to a final grep-clean.

Because Plans 06 and 07 already severed all test coupling to the legacy asides and re-pointed project loads/color-exclusion to the surviving screens, this shell swap lands as ONE atomic, green commit.

Purpose: Deliver exactly what the user asked — "a simplified UI where the user can live within the viewport… retire the left hand menu and move those options into the viewport."
Output: Viewport-first App shell; integrated layout test; reconciled ROADMAP note.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-the-four-screens-in-flow-order/23-UAT.md
@.planning/phases/23-the-four-screens-in-flow-order/23-CONTEXT.md
@.planning/phases/23-the-four-screens-in-flow-order/23-UI-SPEC.md
@.planning/phases/20-atelier-design-system-canvas-first-shell/20-CONTEXT.md

# The shell being rewritten + the pieces it composes
@src/App.tsx
@src/features/wizard/AtelierShell.tsx
@src/features/wizard/CanvasWorkspace.tsx
@src/features/screens/UploadScreen.tsx
@src/features/screens/RefineScreen.tsx
@src/features/screens/SuppliesScreen.tsx
@src/features/screens/OrderScreen.tsx
@src/__tests__/App.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Swap the legacy dark shell for the centered viewport frame</name>
  <files>src/App.tsx</files>
  <action>
Rewrite the AtelierShell body (App.tsx ~L1544-2160) from the dark 3-column shell into a single centered viewport frame. Keep everything OUTSIDE the shell body — the AtelierShell top bar, the page-level stale banner (~L1525), and the modals (resources / sizing / save, ~L2376+) — as-is.

TARGET STRUCTURE (per 23-UI-SPEC A1-A4):
- Replace the `bg-slate-950 text-slate-100` wrapper with a light frame: a scroll container whose inner content is centered and width-capped at the fixed 1180px card frame (`max-w-[1180px] mx-auto`), on the Atelier cream background (`bg-bg` / cream tokens). No dark classes.
- Inside the frame, keep the four always-mounted, display-toggled `data-step-panel="1..4"` siblings (preserve these attributes — tests and the single-mount contract depend on them). The visible panel shows; the others are `hidden` (display:none). This is the existing D-14 sibling-toggle mechanism, just re-homed.
- Mount `<CanvasWorkspace>` ONCE as a stable frame sibling (NOT nested inside any step panel), shown only when `wizard.step === 2` and `hidden` otherwise. On Refine, the frame lays out as a flex row so the visible children read as [CanvasWorkspace preview | step-2 panel = RefineScreen 360px rail]. On steps 1/3/4 the CanvasWorkspace is display:none and the visible panel is the sole content. Keeping CanvasWorkspace as an always-mounted sibling (never behind `{step===2 && …}`) is what preserves the single canvas mount (D-14) — the element stays in the DOM on every step.
- Panel 1 (Upload): UploadScreen centered in the frame (single column, no canvas). Panel 3 (Supplies): SuppliesScreen (its own table + 320px summary). Panel 4 (Order): OrderScreen (its own 470px proof + right column). These screens already render their full internal layout — do not wrap them in extra rails.

RELOCATE the still-needed legacy options INTO the frame (do not lift state into the screens — App stays the state owner, D-01):
- Recent projects / load / remove: already delivered by UploadScreen (D-10) on step 1 — no new work, just ensure it is the step-1 content.
- Save: keep the AtelierShell top-bar Save pill (onSave → save modal). Preserve the save modal and any `#save-project-btn` affordance the save flow needs; if `#save-project-btn` currently lives only in the deleted left drawer, re-home the same button (same id, same handler) into the frame (e.g. a small action row) so the save-flow keeps working.
- New / Reset: re-home the `#new-project-btn` button (same id, same `resetWorkspace` handler) into the frame as an inline "Start over / New" affordance. Keep the id — reset tests query it directly.
- Back / Next: re-home the wizard nav footer (currently ~L1839-1866) as a frame-level footer bar. Preserve `#wizard-next-btn` and `#wizard-back-btn` verbatim: same ids, same `disabled={!wizard.canEnter(step+1) || nextBlockedByStale}` gating, same back handler, same "final step hides Next" behavior. Navigation tests depend on this exactly.

HOIST the error banners: move the matchError and actionError banners OUT of CanvasWorkspace to frame scope (render them at the top of the frame, above the panels) so they surface on any step, not only while the canvas is visible. CanvasWorkspace keeps the HUD, zoom, loading overlay, canvas, and print legends. (Adjust CanvasWorkspaceProps to drop the two banner props if you move them; or keep them in CanvasWorkspace only if you also render a frame-level actionError — pick one home per banner, no duplicates.)

DELETE the retired chrome and its now-dead state:
- The left `<aside>` (Collapse button, My Images drawer, Artist Resources footer, in-aside sticky nav), the right `<aside>` (Color Legend / DMC supply list), the `<main>` wrapper's dark styling and the expand buttons (`title="Expand Sidebar"`, `title="Expand color legend"`), and the `bg-slate-950` frame wrapper.
- Remove the now-unused state and handlers: `leftPanelCollapsed`, `rightPanelCollapsed`, `imagesDrawerOpen` and their setters and any JSX-only references. Leave `resetWorkspace`, `loadProject`, `projectStore` wiring, and all match/quote/order state intact.

RE-FIT ON REFINE: because the canvas is now display:none on steps 1/3/4, add a `useEffect` keyed on `wizard.step` that calls `viewerRef.current?.fitToContainer()` when the step becomes 2 and a viewer exists — so re-entering Refine re-measures the now-visible canvas. Do not remount the viewer.

Keep the `no-print`/`print:*` classes coherent so the existing print path (print legends in CanvasWorkspace + the print report) still works. Run the FULL suite — Plans 06/07 pre-migrated every coupled test, so it must be green.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm test</automated>
  </verify>
  <done>The AtelierShell body is a centered ~1180px cream frame hosting the four screens; Refine shows [canvas | rail]; no bg-slate-950 wrapper / left menu / right aside remain; `#wizard-next-btn`/`#wizard-back-btn`/`#new-project-btn` preserved; canvas mounts once and re-fits on entering Refine; full suite + tsc green.</done>
</task>

<task type="auto">
  <name>Task 2: Add the integrated full-App layout regression test + reconcile the ROADMAP</name>
  <files>src/__tests__/App.test.tsx, .planning/ROADMAP.md</files>
  <action>
Add an integrated full-App layout regression test (the UAT gap's missing item #4) so "new screens host the viewport, legacy menu retired" can never silently regress. Render `<App />` and assert, at the whole-App level (not a per-component unit render):

POSITIVE (screens host the viewport):
- The step-1 primary content is UploadScreen: `[data-step-panel="1"]` contains `[data-screen="upload"]`, and that panel is the visible (non-`hidden`) panel at initial render.
- Navigating to step 2 (via `#wizard-next-btn` after loading a project through a UploadScreen chip, matching the Plan 06 helper) makes `[data-step-panel="2"]` visible with `[data-screen="refine"]` present AND the single `<canvas>` present as a visible sibling (the CanvasWorkspace preview) in the same frame.
- Steps 3 and 4 host `[data-screen="supplies"]` and `[data-screen="order"]` respectively.

NEGATIVE (legacy chrome retired):
- No element with the legacy dark shell class remains (query the rendered container for the retired wrapper class and assert none) — express this as a not-present assertion so the literal is only referenced in the test's expectation, never written into App source.
- There is no legacy left "My Images" drawer toggle and no right "Color Legend" aside: `container.querySelectorAll('aside')` no longer yields the two legacy panels; assert the count matches the new frame's structure (0 legacy asides).
- Exactly one `<canvas>` exists across all steps (single mount): assert `container.querySelectorAll('canvas').length === 1` and that it is the same node before and after a step change (capture the node, advance a step and back, assert identity) — this is the D-14 regression guard.

Keep the test resilient to jsdom (no layout): rely on `hidden` class presence / `data-*` attributes, not computed geometry.

Then reconcile the ROADMAP (use scoped edits, not a full rewrite):
- Update the Phase 25 entry (`### Phase 25: Retire Legacy Steps + Cleanup`, and its one-line summary ~L69) to note that the Phase 23 UAT gap fix (Plans 06-08) already retired the legacy dark 3-column shell, the left "My Images" menu, and the right Color-Legend/DMC aside — so Phase 25 shrinks to a FINAL GREP-CLEAN of residual `Step1..4` component files, theme remnants, and any leftover dead preset state, not a from-scratch strangler close. Do not delete Phase 25; just narrow its scope with a dated note referencing this gap fix (Test 26).
  </action>
  <verify>
    <automated>npm test -- src/__tests__/App.test.tsx</automated>
    <human-check>Run `npm run dev`, load a photo, and walk Upload → Refine → Supplies → Order: confirm each screen fills the centered ~1180px frame with cream Atelier styling, Refine shows the live chart preview beside the 360px rail, and no dark left menu / right legend / duplicate upload prompt appears (UAT Test 26). Deferred to the end-of-phase human verification pass (human_verify_mode=end-of-phase).</human-check>
  </verify>
  <done>An integrated App-level test asserts the four screens host the frame, the legacy asides are gone, and exactly one canvas persists across step changes; the ROADMAP Phase 25 entry is narrowed to a final grep-clean with a dated Test-26 note; suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage → UI | Project/image names + restored project data cross into the viewport-hosted screens (UploadScreen chips, restored match). |
| client-only | No new network egress; ship-to and packet stay client-side (D-08/D-09), unchanged. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-23-08-01 | Tampering/XSS | UploadScreen recent chips, restored project fields | medium | mitigate | Names/fields render as escaped text (Preact default, no dangerouslySetInnerHTML); money-typed loads sanitized at the state boundary (existing CR-01 guard) — unchanged by the shell swap. |
| T-23-08-02 | Denial of service | Single-mount CanvasViewer under CSS show/hide | low | mitigate | CanvasWorkspace stays mounted as a frame sibling (never behind a step conditional) + explicit re-fit on entering Refine; the integrated test asserts exactly one persistent canvas node, guarding accidental remount churn. |
| T-23-08-03 | Elevation/lost-gating | Relocated Back/Next + reset controls | low | mitigate | `#wizard-next-btn`/`#wizard-back-btn`/`#new-project-btn` keep their ids, handlers, and disabled/stale gating; navigation + reset tests exercise them unchanged. |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0 and `npm test` full suite green.
- `git grep -nE "bg-slate-950|leftPanelCollapsed|rightPanelCollapsed|imagesDrawerOpen" src/App.tsx` returns nothing (dark shell + dead state removed).
- The integrated layout test passes: four `data-screen` hosts present as primary panels, 0 legacy asides, exactly one persistent `<canvas>`.
- ROADMAP Phase 25 entry narrowed to a grep-clean with a Test-26 dated note.
- Human verification of the desktop journey is deferred to the end-of-phase pass (Test 26 re-verify, plus the deferred Test 29 end-to-end photo journey).
</verification>

<success_criteria>
UAT Test 26 is closed: the four Atelier screens are the primary viewport content in a centered ~1180px frame, the legacy left menu and dark 3-column chrome are retired with their options relocated into the viewport, the single CanvasViewer never remounts, an integrated layout test regression-guards the outcome, and the ROADMAP reflects that Phase 25 is now a final grep-clean. Suite + tsc green.
</success_criteria>

<output>
Create `.planning/phases/23-the-four-screens-in-flow-order/23-08-SUMMARY.md` when done.
</output>
