---
phase: 23-the-four-screens-in-flow-order
plan: 07
type: execute
wave: 2
depends_on: [23-06]
files_modified:
  - src/__tests__/integration.test.tsx
  - src/__tests__/App.test.tsx
autonomous: true
gap_closure: true
requirements: [REFINE-01, SUPPLIES-01]
user_setup: []

must_haves:
  truths:
    - "No test asserts the existence or collapse behavior of the legacy left sidebar or the right Color-Legend/DMC aside (those asides are deleted in Plan 08)."
    - "Color-exclusion coverage is retained but re-pointed to the RefineScreen 'Advanced' disclosure (D-06), which survives the flip."
    - "Retired legacy-only behaviors (sidebar collapse, right-aside legend collapse, sortable DMC list, highlight-on-legend-row-click) are removed as tests, documented as intentional strangler retirement — not silent coverage loss."
    - "The full Vitest suite is green on the still-legacy shell after the retirements."
  artifacts:
    - "src/__tests__/integration.test.tsx — legacy-aside assertions removed or re-pointed to surviving screens."
    - "src/__tests__/App.test.tsx — sidebar-collapse test removed."
  key_links:
    - "The color-exclusion checklist lives in RefineScreen Advanced (step-2 panel) both before and after the flip, so the re-pointed test is shell-agnostic."
---

<objective>
Gap closure for Phase 23 UAT Test 26, step 2 of 3. Plan 06 severed the project-load coupling; this plan severs the remaining test coupling to the legacy left sidebar and the right Color-Legend/DMC aside. It re-points the still-valid color-exclusion coverage onto the RefineScreen "Advanced" disclosure (which survives), and retires the tests that assert genuinely-legacy behavior being removed by the strangler (sidebar collapse, right-aside legend collapse, sortable DMC list, highlight-on-legend-row-click). After this plan, nothing in the suite depends on chrome that Plan 08 deletes, so the shell flip lands green.

Purpose: These behaviors (side-drawer collapse, the interactive right-aside legend) are exactly what the user asked to retire ("retire the left hand menu and move those options into the viewport"). Their tests must go with them, explicitly, so the flip is a clean green commit rather than a wall of red.
Output: integration.test.tsx + App.test.tsx with no legacy-aside coupling; color-exclusion coverage preserved against RefineScreen.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-the-four-screens-in-flow-order/23-UAT.md
@.planning/phases/23-the-four-screens-in-flow-order/23-UI-SPEC.md

# The surviving color-exclusion home
@src/features/screens/RefineScreen.tsx

# Test files being retired/re-pointed
@src/__tests__/integration.test.tsx
@src/__tests__/App.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-point color-exclusion coverage and retire right-aside legend tests in integration.test.tsx</name>
  <files>src/__tests__/integration.test.tsx</files>
  <action>
The right `<aside>` (Color Legend / DMC supply list, App.tsx ~L2143) is deleted in Plan 08. Sever every integration.test.tsx assertion that reaches it via `container.querySelectorAll('aside')[1]`:

1. Color-exclusion test (~L175-185): currently finds "Exclude Colors" inside the right aside and toggles its checkboxes. Re-point it to the RefineScreen "Advanced" disclosure — the surviving color-exclusion home (D-06). Scope to the step-2 panel (`[data-step-panel="2"]`), open the native `<details>` "Advanced", and assert the exclude checkboxes there (RefineScreen mounts the exclusion list only while Advanced is open — open it first). This keeps REFINE color-exclusion coverage alive against the component that survives the flip.

2. Highlight-on-legend-row-click test (~L284-285): retire it. The interactive sortable legend that wired a row click to `setHighlightedColor` lives only in the legacy right aside and is being removed; the new SuppliesScreen table is display-only by design (SUPPLIES-01). Delete the test with a one-line comment noting it covered retired legacy right-aside behavior (Phase 25 strangler retirement pulled forward).

3. Sidebar-collapse test (~L491-513, "Collapse Sidebar" / `w-80` / `w-0`): retire — the left sidebar and its collapse affordance are deleted.

4. Right-aside supply-list toggle/table test (~L520-545): retire — the legacy DMC supply list is deleted (SuppliesScreen is its replacement, already covered by SuppliesScreen unit tests).

5. Right-sidebar collapse test (~L638-661, `w-96` / "Collapse Workspace" / "Expand color legend"): retire — the right aside is deleted.

6. Sortable-DMC-header test (~L741-746): retire — sorting lived only on the legacy right-aside list.

For each retirement, remove the whole `it(...)` block and leave a short comment block above the neighboring test recording WHAT was retired and WHY (legacy chrome removed by the Phase 23 UAT gap fix, formerly scheduled for Phase 25). Do not leave dangling helpers. Run the file against the still-legacy shell — the re-pointed exclusion test must pass and the retirements must not leave references to deleted symbols.
  </action>
  <verify>
    <automated>npm test -- src/__tests__/integration.test.tsx</automated>
  </verify>
  <done>integration.test.tsx contains no `querySelectorAll('aside')[1]` usage and no "Collapse Sidebar"/"Collapse Workspace"/"Expand color legend" assertions; color-exclusion is asserted via the RefineScreen Advanced disclosure; the file passes.</done>
</task>

<task type="auto">
  <name>Task 2: Retire the sidebar-collapse test in App.test.tsx</name>
  <files>src/__tests__/App.test.tsx</files>
  <action>
Remove the App.test.tsx test that asserts the two asides' collapse state (~L220-247: `container.querySelectorAll('aside')`, `asides[0]`/`asides[1]`, `w-0` toggling). Both asides are deleted in Plan 08, so this behavior ceases to exist. Delete the whole `it(...)`/`test(...)` block and leave a short comment recording it as intentional strangler retirement of the legacy sidebar-collapse chrome.

Confirm no other App.test.tsx assertion references `aside`, `w-80`, `w-96`, or `w-0` after this edit. Leave all navigation/reset/ingestion hooks (`#wizard-next-btn`, `#wizard-back-btn`, `#new-project-btn`, `#upload-file-input`) and the UploadScreen-chip loads from Plan 06 intact. Run against the still-legacy shell; the suite must stay green.
  </action>
  <verify>
    <automated>npm test -- src/__tests__/App.test.tsx && npm test</automated>
  </verify>
  <done>App.test.tsx no longer references `aside`/`w-80`/`w-96`/`w-0`; the collapse test is gone with a retirement note; full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test suite → shipped behavior | Retiring tests removes regression coverage; the risk is silently dropping coverage for behavior that should survive. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-23-07-01 | Repudiation/coverage-loss | Retired legacy-aside tests | medium | mitigate | Color-exclusion coverage is re-pointed (not dropped) to RefineScreen Advanced; SuppliesScreen table is covered by its own unit tests; only genuinely-deleted chrome (collapse, sortable legend, highlight) loses tests, each documented inline as intentional retirement. |
</threat_model>

<verification>
- `npm test` full suite green after both retirements (still on the legacy shell).
- `git grep -n "querySelectorAll('aside')" src/__tests__` returns nothing.
- `git grep -nE "Collapse Sidebar|Collapse Workspace|Expand color legend|w-80|w-96" src/__tests__` returns nothing.
- Color-exclusion is asserted against `[data-screen="refine"]` / `[data-step-panel="2"]` Advanced.
</verification>

<success_criteria>
No test depends on the legacy left or right aside; color-exclusion coverage survives against RefineScreen; retired legacy behaviors are documented; suite + tsc green — Plan 08's flip can now delete the asides without breaking a single test.
</success_criteria>

<output>
Create `.planning/phases/23-the-four-screens-in-flow-order/23-07-SUMMARY.md` when done.
</output>
