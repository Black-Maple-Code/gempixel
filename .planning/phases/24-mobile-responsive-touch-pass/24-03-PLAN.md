---
phase: 24-mobile-responsive-touch-pass
plan: 03
type: execute
wave: 2
depends_on: ["24-01", "24-02"]
files_modified: []
autonomous: false
requirements: [MOBILE-01, MOBILE-02]
must_haves:
  truths:
    - "npm run build exits 0 (tsc + vite build succeed after the reflow + touch changes)."
    - "npm test passes with 240+ tests green (no regression from the container-query reflow or the multi-touch pinch branch)."
    - "Desktop layout is provably unregressed: base classes (flex-row shell body, w-[360px] rail) are intact and only descendant @max-[640px] overrides were added."
    - "A human confirms at ~300px: one portrait column, every control inline (no drawer), nothing overflows, Refine canvas-first sticky pane, pinch-to-zoom + pan work, and the page does not scroll under the gesture — while desktop is unchanged."
  artifacts:
    - ".planning/phases/24-mobile-responsive-touch-pass/24-03-SUMMARY.md — records the build/test results, the desktop-unregressed grep evidence, and the human-verify sign-off."
  key_links:
    - "Criterion 3 (desktop unregressed + 240+ suite green) is proven here after both Wave-1 plans land, not assumed."
    - "The human-verify checkpoint is the only way to confirm the visual reflow + real pinch gesture that jsdom cannot assert."
---

<objective>
Prove Phase 24 Success Criterion 3 — desktop layout unregressed and the 240+ Vitest suite green — and human-verify the mobile reflow (MOBILE-01) and touch pinch/pan (MOBILE-02) end to end. This is a verification-only plan: it edits no source files.

Purpose: The user's top stated concern is regressions. Wave 1 delivers the reflow (Plan 01) and touch (Plan 02) in parallel; this Wave-2 plan gates the phase by running the full build + test suite, asserting the desktop base classes survived, and having a human confirm the actual portrait-column reflow + live pinch gesture that jsdom cannot exercise.
Output: A recorded regression gate (build + full suite + desktop-class grep evidence) and a human-verify sign-off captured in the SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-mobile-responsive-touch-pass/24-CONTEXT.md
@.planning/phases/24-mobile-responsive-touch-pass/24-01-PLAN.md
@.planning/phases/24-mobile-responsive-touch-pass/24-02-PLAN.md
</context>

## Artifacts this phase produces

This plan produces no source symbols. It produces one artifact: `.planning/phases/24-mobile-responsive-touch-pass/24-03-SUMMARY.md`, capturing the build result, the full-suite test count/result, the desktop-unregressed grep evidence, and the human-verify outcome. It runs read-only assertions against the symbols introduced by Plans 01–02 (listed in those plans' "Artifacts this phase produces" sections).

<tasks>

<task type="auto">
  <name>Task 1: Full-suite + build regression gate; prove desktop unregressed (Criterion 3)</name>
  <read_first>
    - .planning/ROADMAP.md §"Phase 24" — Success Criteria 1–3 (esp. Criterion 3: desktop unregressed + 240+ Vitest green).
    - .planning/phases/24-mobile-responsive-touch-pass/24-01-PLAN.md and 24-02-PLAN.md — the acceptance criteria + "Artifacts this phase produces" lists (the exact classes/symbols this gate greps for).
    - src/features/wizard/AtelierShell.tsx, src/App.tsx (shell body ~L1612 + canvas <main> ~L1619), src/features/screens/RefineScreen.tsx (root ~L108), src/index.css (~L495-576), src/engine/viewer.ts — to confirm the intended end-state after Wave 1.
    - CLAUDE.md Commands table — `npm run build` (tsc && vite build) and `npm test` (vitest run) are the check commands.
  </read_first>
  <files>none — verification only (read-only inspection of the Wave-1 targets: AtelierShell.tsx, App.tsx, RefineScreen.tsx, index.css, viewer.ts)</files>
  <action>
    Run the full regression gate and record results in the SUMMARY:
    1. `npm run build` — must exit 0 (tsc typecheck + vite production build clean).
    2. `npm test` — must exit 0 with the full suite green; record the passing test count and confirm it is ≥ 240 (Criterion 3 baseline).
    3. Desktop-unregressed grep evidence (base classes intact, responsiveness added only as descendant overrides):
       - `src/App.tsx` shell body still contains `flex-row justify-center` AND now `@max-[640px]:flex-col`.
       - `src/features/screens/RefineScreen.tsx` root still contains `w-[360px]` AND now `@max-[640px]:w-full`.
       - `src/features/wizard/AtelierShell.tsx` root still contains `flex flex-col h-dvh overflow-hidden` AND now `@container`.
       - `src/index.css` returns 0 for `drawer-backdrop` and retains `.viewport-hud` (≥ 2 occurrences).
       - `src/engine/viewer.ts` contains `activePointers` and sets `touchAction` to `'none'` on the canvas.
    If build fails, the suite drops below 240 green, or any base class is missing, STOP and report the exact failure for gap closure rather than proceeding to the human checkpoint.
    <!-- planner-discipline-allow: drawer-backdrop -->
  </action>
  <acceptance_criteria>
    - `npm run build` exits 0.
    - `npm test` exits 0 and the reported passing count is ≥ 240.
    - Grep confirms: App shell body has both `flex-row` and `@max-[640px]:flex-col`; RefineScreen root has both `w-[360px]` and `@max-[640px]:w-full`; AtelierShell root has both `h-dvh overflow-hidden` and `@container`.
    - `grep -c 'drawer-backdrop' src/index.css` returns 0 and `grep -c '.viewport-hud' src/index.css` returns ≥ 2.
    - `grep -c 'activePointers' src/engine/viewer.ts` returns ≥ 3 and `touchAction` is set to `'none'` on the canvas.
  </acceptance_criteria>
  <verify>
    <automated>npm run build && npm test</automated>
  </verify>
  <done>Build and the full 240+ Vitest suite are green; grep evidence proves the desktop base classes are intact with only `@max-[640px]` overrides added and the dead drawer CSS removed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human-verify mobile reflow (MOBILE-01) + touch pinch/pan (MOBILE-02)</name>
  <action>Human-verification checkpoint — no code is written. The executor pauses after the automated gate (Task 1) is green and asks the human to walk the verification steps below on the running dev server, then resumes on the sign-off.</action>
  <what-built>
    MOBILE-01 container-query reflow (single portrait column at ~300px, canvas-first sticky Refine pane, all controls inline, dead drawer CSS removed) and MOBILE-02 touch pinch-to-zoom + pan with `touch-action: none` on the canvas and touch-friendly on-screen zoom buttons. Automated build + the full 240+ Vitest suite are already green (Task 1).
  </what-built>
  <how-to-verify>
    1. Start the dev server: `npm run dev` and open http://localhost:5173.
    2. **Desktop unregressed:** at a normal desktop width, walk Upload → Refine → Supplies → Order. Confirm the layout looks exactly as before Phase 24 — Refine shows the canvas preview on the left and the 360px controls rail on the right; no visual change.
    3. **Mobile reflow (MOBILE-01):** open DevTools responsive mode and set width ≈ 300–360px (e.g. iPhone SE). On every step confirm: one portrait column, every control inline (NO drawer, side menu, or overlay), and nothing overflows horizontally (no sideways scrollbar).
    4. **Refine canvas-first (D-03):** on Refine at ~320px, confirm the canvas + zoom HUD sit at the top and stay pinned (sticky) while you scroll the controls rail (SizeCards → custom size → edge cleanup → color count → Advanced) beneath. If the custom-size number inputs are unusable/cramped under the pinned pane with the on-screen keyboard, note it — the documented fallback is to drop `sticky` (non-sticky canvas-first), NOT a JS hybrid.
    5. **Touch pinch/pan (MOBILE-02):** using a touch device or the browser's touch emulation, load an image, go to Refine, and pinch to zoom the chart and drag to pan. Confirm the chart zooms/pans and the PAGE DOES NOT scroll or browser-zoom under the gesture. Confirm the on-screen ➕ / ➖ / ⛶ zoom buttons also work and are comfortably tappable.
  </how-to-verify>
  <resume-signal>Type "approved" if all five checks pass, or describe the issue (e.g. "custom-size input cramped — drop sticky", "page scrolls under pinch", "control X overflows at 300px") for gap closure.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Verification-only plan: runs build/test and a manual review. No source edits, no new code path, no network/auth/storage/data-flow change. GemPixel remains 100% client-side and privacy-first. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-24-03-V | Tampering | verification gate | low | accept | This plan only observes (build/test/grep + human review); it introduces no attack surface. Its purpose is to catch regressions from Plans 01–02 before the phase closes. |

No package installs in this plan (no `## Package Legitimacy Audit` required).
</threat_model>

<verification>
- `npm run build` exits 0; `npm test` exits 0 with ≥ 240 tests passing.
- Desktop base classes intact (grep evidence) with only `@max-[640px]` overrides added; dead drawer CSS gone; `.viewport-hud` and viewer `activePointers`/`touch-action` present.
- Human-verify checkpoint approved: single-column reflow, inline controls, no overflow, canvas-first sticky Refine, pinch/pan works, page does not scroll under gesture, desktop unchanged.
</verification>

<success_criteria>
Phase 24 Success Criterion 3 is proven: the build and full 240+ Vitest suite are green, the desktop layout is unregressed (base classes intact, descendant-only overrides), and a human has confirmed the mobile reflow (MOBILE-01) and touch pinch/pan (MOBILE-02) work with `touch-action: none` holding the page still.
</success_criteria>

<output>
Create `.planning/phases/24-mobile-responsive-touch-pass/24-03-SUMMARY.md` when done.
</output>
