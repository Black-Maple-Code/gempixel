---
phase: 11-storage-robustness-error-feedback
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/engine/safeStorage.ts
  - src/hooks/usePersistentState.ts
  - src/engine/__tests__/safeStorage.test.ts
  - src/hooks/__tests__/usePersistentState.test.ts
autonomous: true
requirements: [STORE-01, STORE-02]

must_haves:
  truths:
    - "safeStorage.getItem returns null (never throws) when localStorage access throws (blocked/private mode)"
    - "safeStorage.setItem returns false (never throws) when localStorage access throws; removeItem swallows"
    - "usePersistentState falls back to the initial value when the stored raw value is corrupt (parse throws) or storage is blocked"
    - "codecs preserve the CURRENT on-disk formats: bool as 'true'/'false', int as decimal string with NaN guard, string raw, json via JSON.stringify"
  artifacts:
    - src/engine/safeStorage.ts
    - src/hooks/usePersistentState.ts
    - src/engine/__tests__/safeStorage.test.ts
    - src/hooks/__tests__/usePersistentState.test.ts
  key_links:
    - "usePersistentState reads/writes exclusively through safeStorage (import from '../engine/safeStorage')"
    - "codecs.int(fallback).parse uses Number.isFinite to reject NaN and return fallback (IN-05)"
---

<objective>
Build the reusable, framework-agnostic storage-safety foundation this phase depends on: a pure `safeStorage` guard module and a thin `usePersistentState` Preact hook with format-preserving codecs. Both are unit-tested in isolation before any `src/App.tsx` wiring happens.

Purpose: This is the single-audit-point that lets a blocked/private-mode browser never throw during render (STORE-01) and lets all persisted settings share one safe helper (STORE-02). It has NO dependency on `App.tsx`, so it lands first (Wave 1) and the App.tsx migration (Plan 11-02) consumes it.
Output: Two new source modules (`src/engine/safeStorage.ts`, `src/hooks/usePersistentState.ts`) plus two new Vitest unit files, all green.
</objective>

<artifacts_this_phase_produces>
Every NEW symbol/file introduced across Phase 11. Source-grounding verification MUST exclude these as "newly created" (they will not exist in prior source):

- `src/engine/safeStorage.ts` — new file; exports the `safeStorage` object with methods `getItem`, `setItem`, `removeItem`, `isAvailable`.
- `src/hooks/usePersistentState.ts` — new file; exports the `Codec<T>` interface, the `codecs` object (`bool`, `int`, `string`, `json`), and the `usePersistentState<T>` hook.
- `src/engine/__tests__/safeStorage.test.ts` — new test file.
- `src/hooks/__tests__/usePersistentState.test.ts` — new test file.
- New App.tsx state (Plan 11-03): `actionError` / `setActionError`.
- `src/hooks/` — new directory.
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/11-storage-robustness-error-feedback/11-RESEARCH.md
@.planning/phases/11-storage-robustness-error-feedback/11-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create pure safeStorage guard module + unit tests</name>
  <files>src/engine/safeStorage.ts, src/engine/__tests__/safeStorage.test.ts</files>
  <read_first>
    - src/engine/checkout.ts (role analog: pure, named-export-only engine module with per-export JSDoc, defensive try/catch — see 11-PATTERNS.md "safeStorage.ts")
    - src/engine/__tests__/checkout.test.ts (Vitest engine-unit style: `import { describe, it, expect, vi } from 'vitest'`)
    - src/__tests__/App.test.tsx:1-47 (jsdom directive + `vi.spyOn` + `afterEach` mock-restore harness)
    - .planning/phases/11-storage-robustness-error-feedback/11-RESEARCH.md (§"Layer 1 — pure guard" for the exact object shape; §"Simulating a throwing/blocked localStorage")
  </read_first>
  <behavior>
    - Test: `safeStorage.getItem(k)` returns the stored string when localStorage works.
    - Test: with `Storage.prototype.getItem` spied to throw a `DOMException('blocked','SecurityError')`, `safeStorage.getItem(k)` returns `null` and does NOT throw.
    - Test: `safeStorage.setItem(k,v)` returns `true` on success; with `Storage.prototype.setItem` spied to throw, returns `false` and does NOT throw.
    - Test: `safeStorage.removeItem(k)` swallows a throwing `Storage.prototype.removeItem` (no throw).
    - Test: `safeStorage.isAvailable()` returns `true` when a probe write+remove round-trips, `false` when setItem throws.
  </behavior>
  <action>
    Create `src/engine/safeStorage.ts` as a pure module (NO preact import) exporting a single `safeStorage` object literal with four methods, matching 11-RESEARCH.md §"Layer 1" exactly: `getItem(key: string): string | null` (try `localStorage.getItem`, `catch` returns `null`); `setItem(key: string, value: string): boolean` (try then `return true`, `catch` returns `false`); `removeItem(key: string): void` (try, `catch` ignore); `isAvailable(): boolean` (probe key `__gp_probe__`: setItem+removeItem in a try, `return true`, catch `return false`). Catch blocks are SILENT — return the safe fallback, no `console.*` (per CONVENTIONS.md logging rule, unlike checkout.ts which logs). Add a one-line JSDoc per method. Then write `src/engine/__tests__/safeStorage.test.ts` with `// @vitest-environment jsdom` at the top (jsdom needed for the `Storage`/`localStorage` globals), covering the five behaviors above. Use `vi.spyOn(Storage.prototype, 'getItem'|'setItem'|'removeItem').mockImplementation(() => { throw new DOMException('blocked','SecurityError'); })` and restore with `afterEach(() => vi.restoreAllMocks())` so spies never leak into other suites.
  </action>
  <verify>
    <automated>npx vitest run src/engine/__tests__/safeStorage.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Source: `src/engine/safeStorage.ts` exists and exports a `safeStorage` object with `getItem`, `setItem`, `removeItem`, `isAvailable`; no `import ... 'preact'` appears in the file.
    - Behavior: `npx vitest run src/engine/__tests__/safeStorage.test.ts` exits 0 with all five behaviors asserted.
    - Behavior: with `Storage.prototype.getItem`/`setItem` mocked to throw, the module's methods return `null`/`false` respectively and no exception propagates.
  </acceptance_criteria>
  <done>safeStorage guards both access failure surfaces (throw-on-access → null/false), is unit-tested, and its test file exits 0.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create usePersistentState hook + format-preserving codecs + unit tests</name>
  <files>src/hooks/usePersistentState.ts, src/hooks/__tests__/usePersistentState.test.ts</files>
  <read_first>
    - src/features/match/useDiamondArtMatch.ts:1-18 (hook analog: framework import first, then relative engine imports, multi-line JSDoc above the hook)
    - src/engine/safeStorage.ts (created in Task 1 — the hook imports `{ safeStorage }` from '../engine/safeStorage')
    - src/App.tsx:129-155 (the 7 lazy-init `useState` reads this hook generalizes; note bool='true'/'false', int=parseInt, string=raw, unmappedLog=JSON)
    - src/__tests__/App.test.tsx:37-47, 254-296 (container render harness + `localStorage.setItem(key, value)` seeding + `beforeEach(localStorage.clear())`)
    - .planning/phases/11-storage-robustness-error-feedback/11-RESEARCH.md (§"Layer 2 — the hook", §"Migration mapping", Pitfall 1)
  </read_first>
  <behavior>
    - Test (fallback on blocked): with `Storage.prototype.getItem` spied to throw, `usePersistentState('k', initial, codecs.bool)` initializes to `initial` and does not throw.
    - Test (fallback on corrupt): seed `localStorage.setItem('gempixel_substitution_threshold','NaNsense')`; `codecs.int(15).parse` returns `15` (Number.isFinite guard, IN-05).
    - Test (fallback on corrupt json): seed `'{bad'`; `usePersistentState('k', [], codecs.json())` initializes to `[]` (parse throws → initial).
    - Test (round-trip format fidelity): after `setValue(true)` the on-disk value is exactly `'true'`; after `setValue(15)` it is `'15'`; after `setValue('mytag')` it is raw `'mytag'` — so legacy tests that assert exact stored formats stay green.
    - Test: `codecs.string.parse('mytag')` returns `'mytag'` unchanged (a raw string is NOT run through JSON.parse — Pitfall 1).
  </behavior>
  <action>
    Create `src/hooks/usePersistentState.ts` exactly per 11-RESEARCH.md §"Layer 2". Export: (1) `interface Codec<T>` with `parse(raw: string): T` (may throw; caller catches) and `serialize(value: T): string`; (2) a `codecs` object with `bool` (`Codec<boolean>`: parse `r === 'true'`, serialize `v.toString()`), `int(fallback: number): Codec<number>` (parse uses `parseInt(r,10)` then `Number.isFinite(n) ? n : fallback` — the IN-05 NaN guard — serialize `v.toString()`), `string` (`Codec<string>`: identity parse/serialize), and `json<T>(): Codec<T>` (parse `JSON.parse`, serialize `JSON.stringify`); (3) `function usePersistentState<T>(key: string, initial: T, codec: Codec<T>)` using a lazy `useState` initializer — call `safeStorage.getItem(key)`, return `initial` if `null`, else `try codec.parse(raw) catch return initial` — and a `useEffect(() => { safeStorage.setItem(key, codec.serialize(value)); }, [value])`. Return `[value, setValue] as const` (deliberate tuple exception to the repo's object-return convention — document this in the hook JSDoc, mirrors native useState). Import `{ useState, useEffect } from 'preact/hooks'` and `{ safeStorage } from '../engine/safeStorage'`. Do NOT add a "skip first write" guard (Pitfall 5). Then write `src/hooks/__tests__/usePersistentState.test.ts` with `// @vitest-environment jsdom`, `beforeEach(() => localStorage.clear())`, `afterEach(() => vi.restoreAllMocks())`, rendering the hook via a minimal test component into a container (App.test.tsx harness) OR asserting codec functions directly for the pure-codec cases. Cover all five behaviors above.
  </action>
  <verify>
    <automated>npx vitest run src/hooks/__tests__/usePersistentState.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Source: `src/hooks/usePersistentState.ts` exports `Codec`, `codecs`, and `usePersistentState`; imports `safeStorage` from '../engine/safeStorage' and never references `localStorage` directly.
    - Source: `codecs.int` is a factory taking a numeric fallback; its `parse` uses `Number.isFinite` (NaN guard).
    - Behavior: `npx vitest run src/hooks/__tests__/usePersistentState.test.ts` exits 0.
    - Behavior: round-trip test proves stored formats are `'true'`/`'15'`/raw string (no format drift); corrupt-value tests prove fallback-to-initial with no throw.
  </acceptance_criteria>
  <done>usePersistentState + codecs exist, preserve current on-disk formats losslessly, fall back to initial on blocked/corrupt storage, and the unit file exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage → app | Every stored value is untrusted: another tab, an extension, or a manual edit can corrupt it or make access throw (private mode). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-01 | Denial of Service | `localStorage` access during state init | high | mitigate | `safeStorage.getItem/setItem/removeItem` wrap all access in try/catch and return `null`/`false`/void so a blocked/private-mode browser cannot throw (STORE-01). Unit-tested via `Storage.prototype` throwing spies. |
| T-11-02 | Tampering | corrupt stored value (`'NaNsense'`, `'{bad'`) | medium | mitigate | `usePersistentState` catches `codec.parse` throws and falls back to `initial`; `codecs.int` NaN-guards via `Number.isFinite` (IN-05). All stored values treated as untrusted input (ASVS V5). |
| T-11-SC | Tampering | npm/pip/cargo installs | high | accept | No package installs in this phase — pure code change, no dependency added (RESEARCH: Package Legitimacy Audit is N/A). Supply-chain surface unchanged. |
</threat_model>

<verification>
- `npx vitest run src/engine/__tests__/safeStorage.test.ts` exits 0.
- `npx vitest run src/hooks/__tests__/usePersistentState.test.ts` exits 0.
- `npm test` stays fully green (no regressions in existing suites).
- `npx tsc --noEmit` passes (new files typecheck cleanly).
</verification>

<success_criteria>
- `safeStorage` never throws on any method under blocked/throwing storage (returns null/false/void).
- `usePersistentState` initializes to `initial` on blocked or corrupt storage without throwing, and its codecs write back the exact legacy on-disk formats.
- Both new unit test files exit 0; full suite green.
</success_criteria>

<output>
Create `.planning/phases/11-storage-robustness-error-feedback/11-01-SUMMARY.md` when done.
</output>
