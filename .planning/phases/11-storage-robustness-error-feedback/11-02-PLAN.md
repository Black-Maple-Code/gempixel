---
phase: 11-storage-robustness-error-feedback
plan: 02
type: execute
wave: 2
depends_on: [11-01]
files_modified:
  - src/App.tsx
  - src/features/wizard/steps/Step3Canvas.tsx
  - src/__tests__/App.test.tsx
autonomous: true
requirements: [STORE-01, STORE-02]

must_haves:
  truths:
    - "The 7 persisted settings in App.tsx read/write exclusively through usePersistentState — no remaining inline lazy-init `localStorage.getItem` or write-effect `localStorage.setItem` for those 7 keys"
    - "App mounts and renders its shell (h1 'GemPixel') with Storage.prototype.getItem/setItem throwing — no unguarded storage throw during render (STORE-01)"
    - "The canvasTemplate heartfuldiamonds→adiamondpainting normalization is preserved via a custom codec (no regression)"
    - "The theme DOM side-effect (document.documentElement.dataset.theme) is retained after migrating theme storage"
    - "Step3Canvas 'Clear Log' removeItem routes through safeStorage.removeItem (no unguarded throw)"
  artifacts:
    - src/App.tsx
    - src/features/wizard/steps/Step3Canvas.tsx
    - src/__tests__/App.test.tsx
  key_links:
    - "App.tsx imports { usePersistentState, codecs } from './hooks/usePersistentState' and { safeStorage } from './engine/safeStorage'"
    - "Persisted key NAMES and serialized FORMATS are unchanged (existing tests seed exact keys/values — App.test.tsx L295-296, 586, 616)"
---

<objective>
Migrate the 7 unguarded persisted settings in `src/App.tsx` onto the Wave-1 `usePersistentState` helper, deleting the duplicated lazy-init + write-effect boilerplate (IN-01), and route the one remaining unguarded storage call in `Step3Canvas.tsx` through `safeStorage`. Add a regression test proving `<App/>` mounts under fully-blocked storage.

Purpose: Satisfy STORE-01 (app mounts under blocked/private storage — no unguarded read/write throws during render) and STORE-02 (all persisted settings flow through one safe helper). Keys and on-disk formats are FROZEN — this changes HOW storage is accessed, never WHAT is stored.
Output: A slimmer `App.tsx` (7 boilerplate pairs collapsed to 7 one-liners), a guarded `Step3Canvas` clear-log button, and a new blocked-storage mount test.
</objective>

<artifacts_this_phase_produces>
Phase 11 introduces these NEW symbols/files — source-grounding verification MUST treat them as newly created (absent from prior source):

- `src/engine/safeStorage.ts` → `safeStorage` object (`getItem`, `setItem`, `removeItem`, `isAvailable`) [Plan 11-01].
- `src/hooks/usePersistentState.ts` → `Codec<T>` interface, `codecs` object (`bool`, `int`, `string`, `json`), `usePersistentState<T>` hook [Plan 11-01].
- `src/engine/__tests__/safeStorage.test.ts`, `src/hooks/__tests__/usePersistentState.test.ts` [Plan 11-01].
- App.tsx state `actionError` / `setActionError` [Plan 11-03].
- New directory `src/hooks/`.
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/11-storage-robustness-error-feedback/11-RESEARCH.md
@.planning/phases/11-storage-robustness-error-feedback/11-PATTERNS.md
@.planning/phases/11-storage-robustness-error-feedback/11-01-SUMMARY.md
@src/engine/safeStorage.ts
@src/hooks/usePersistentState.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate the 7 persisted settings in App.tsx to usePersistentState</name>
  <files>src/App.tsx</files>
  <read_first>
    - src/App.tsx:1-14 (import block ordering — add the two new relative imports here)
    - src/App.tsx:95-215 (the 9 persisted-key sites: theme L97-111, the 4 substitution/smoothing states L129-172, unmappedLog L149-155, affiliate/canvasTemplate L190-214, and the canvasTemplate migration L198-201)
    - src/hooks/usePersistentState.ts, src/engine/safeStorage.ts (the helpers to consume)
    - .planning/phases/11-storage-robustness-error-feedback/11-RESEARCH.md (§"Migration mapping", §"Complete Persisted-Key Inventory", Note A, Note B, Pitfalls 1/2/4/5)
    - .planning/phases/11-storage-robustness-error-feedback/11-PATTERNS.md ("src/App.tsx — banner + migration + guarded parse", Theme note)
  </read_first>
  <action>
    Add to the App.tsx import block: `import { usePersistentState, codecs } from './hooks/usePersistentState';`, `import type { Codec } from './hooks/usePersistentState';` (the `Codec` type is referenced by the affiliateApp cast in (6) and the `customTemplateCodec: Codec<string>` in (7) below — omitting it fails `tsc --noEmit`), and `import { safeStorage } from './engine/safeStorage';`. Replace each of the 7 setting state declarations AND delete its paired write `useEffect` (per RESEARCH §"Migration mapping"):
    (1) `enableSubstitution` → `usePersistentState('gempixel_enable_substitution', true, codecs.bool)`;
    (2) `substitutionThreshold` → `usePersistentState('gempixel_substitution_threshold', 15, codecs.int(15))`;
    (3) `enableSmoothing` → `usePersistentState('gempixel_enable_smoothing', true, codecs.bool)`;
    (4) `smoothingStrength` → `usePersistentState('gempixel_smoothing_strength', 1, codecs.int(1))`;
    (5) `affiliateTag` → `usePersistentState('gempixel_affiliate_tag', '', codecs.string)`;
    (6) `affiliateApp` → `usePersistentState<'ref'|'rfsn'|'none'>('gempixel_affiliate_app', 'ref', codecs.string as unknown as Codec<'ref'|'rfsn'|'none'>)` (raw-string union — do NOT use json codec);
    (7) `canvasTemplate` → `usePersistentState('gempixel_canvas_template', '<default adiamondpainting URL>', <customTemplateCodec>)` where `customTemplateCodec` is a locally-defined `Codec<string>` whose `parse` reproduces the L198-201 normalization (if the stored value includes `heartfuldiamonds`, return the adiamondpainting default; else return the raw value) and whose `serialize` is identity — Pitfall 4, do NOT drop this migration.
    Delete the now-orphaned write effects at L158-172 and L204-214. For `theme` (Note A): migrate the storage read/write onto `usePersistentState('gempixel_theme', 'light', codecs.string as ...)` but KEEP a separate one-line `useEffect(() => { document.documentElement.dataset.theme = theme; }, [theme])` for the DOM side-effect — do NOT fold the DOM write into the generic hook. For `unmappedLog` (Note B): replace only the lazy-init read with `usePersistentState('gempixel_unmapped_colors_log', [], codecs.json<string[]>())`; its imperative writes in checkout are handled in Plan 11-03 — do not touch them here. Keys and serialized formats stay IDENTICAL (Pitfall 2). Do NOT touch `src/engine/projectStore.ts` or any project load/save path (Pitfall 3, scope fence).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Source: App.tsx contains `import { usePersistentState, codecs } from './hooks/usePersistentState'` and `import { safeStorage } from './engine/safeStorage'`.
    - Source: the 7 settings (`enableSubstitution`, `substitutionThreshold`, `enableSmoothing`, `smoothingStrength`, `affiliateTag`, `affiliateApp`, `canvasTemplate`) are each declared via `usePersistentState(...)`, and their former standalone `useEffect(() => { localStorage.setItem(...) })` write effects are removed.
    - Source: a custom template codec preserves the `heartfuldiamonds`→`adiamondpainting` normalization; the `theme` DOM effect (`document.documentElement.dataset.theme = theme`) still exists as its own effect.
    - Behavior: `npx tsc --noEmit` passes; `npm test` stays green (existing App.test.tsx assertions on exact seeded keys/formats at L295-296, 586, 616 still pass — proves formats unchanged).
  </acceptance_criteria>
  <done>All 7 settings persist through usePersistentState, the 7 boilerplate write-effects are gone, canvasTemplate migration and theme DOM effect are preserved, keys/formats unchanged, and typecheck + full suite pass.</done>
</task>

<task type="auto">
  <name>Task 2: Guard Step3Canvas clear-log + add blocked-storage mount regression test</name>
  <files>src/features/wizard/steps/Step3Canvas.tsx, src/__tests__/App.test.tsx</files>
  <read_first>
    - src/features/wizard/steps/Step3Canvas.tsx:388-402 (the unguarded `localStorage.removeItem('gempixel_unmapped_colors_log')` clear-log button)
    - src/engine/safeStorage.ts (the guard to route through)
    - src/__tests__/App.test.tsx:1-56 (jsdom directive, worker/viewer mocks, container lifecycle, existing `h1`/'GemPixel' shell assertion to mirror)
    - .planning/phases/11-storage-robustness-error-feedback/11-RESEARCH.md (§"Phase Requirements → Test Map" STORE-01 row; §"Simulating a throwing/blocked localStorage")
    - .planning/phases/11-storage-robustness-error-feedback/11-PATTERNS.md ("Step3Canvas.tsx — clear-log button", "App.test.tsx — new cases")
  </read_first>
  <action>
    In `Step3Canvas.tsx`: add `import { safeStorage } from '../../../engine/safeStorage';` (3 levels under src/) and change the clear-log button `onClick` to call `safeStorage.removeItem('gempixel_unmapped_colors_log')` instead of raw `localStorage.removeItem(...)`; keep the existing `setUnmappedLog([])` call (this component stays pure-presentational — no new state). Silent failure is acceptable here. In `src/__tests__/App.test.tsx`: add a new `it('mounts under blocked storage without throwing', ...)` case — BEFORE `render(<App/>, container)`, `vi.spyOn(Storage.prototype, 'getItem').mockImplementation(() => { throw new DOMException('blocked','SecurityError'); })` and the same for `setItem`; assert the shell renders (query `h1`, expect `textContent` `'GemPixel'`, mirroring L54-56) and that no error is thrown. Restore spies in the suite's existing `afterEach` (add `vi.restoreAllMocks()` so `Storage.prototype` spies do not leak into other tests — RESEARCH Wave 0 gap).
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/App.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Source: `Step3Canvas.tsx` imports `safeStorage` from '../../../engine/safeStorage' and its clear-log `onClick` calls `safeStorage.removeItem('gempixel_unmapped_colors_log')` (no raw `localStorage.removeItem` remains in that handler).
    - Behavior: a new App.test.tsx case renders `<App/>` with `Storage.prototype.getItem`/`setItem` mocked to throw and asserts the `h1` 'GemPixel' shell renders with no exception (STORE-01).
    - Behavior: `vi.restoreAllMocks()` runs in `afterEach`; `npx vitest run src/__tests__/App.test.tsx` exits 0 and the full `npm test` suite stays green.
  </acceptance_criteria>
  <done>Step3Canvas clear-log is guarded, a blocked-storage mount test proves the app renders without throwing, and the App suite exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage → App render | State initializers read persisted values during render; a blocked/private-mode browser makes access throw, which would abort mount. |
| localStorage → Step3Canvas action | The clear-log button writes to storage on user click; a blocked store would throw. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-03 | Denial of Service | App.tsx state init (7 settings + theme + unmappedLog read) | high | mitigate | All reads route through `usePersistentState`/`safeStorage` which never throw; regression test mounts `<App/>` with `Storage.prototype` throwing and asserts the shell renders (STORE-01). |
| T-11-04 | Tampering | corrupt `canvas_template` / affiliate / int settings from another tab or manual edit | medium | mitigate | Format-preserving codecs fall back to defaults on parse failure; custom template codec normalizes legacy hosts; NaN-guarded int codec (IN-05). Keys/formats frozen so no silent data loss (Pitfall 1/2). |
| T-11-05 | Denial of Service | Step3Canvas clear-log `removeItem` | low | mitigate | Routed through `safeStorage.removeItem`, which swallows throws (silent is acceptable for a clear action). |
| T-11-SC | Tampering | npm/pip/cargo installs | high | accept | No package installs in this phase — pure code change; supply-chain surface unchanged. |
</threat_model>

<verification>
- `npx tsc --noEmit` passes.
- `npx vitest run src/__tests__/App.test.tsx` exits 0, including the new blocked-storage mount case.
- `npm test` fully green — existing suites that seed exact keys/formats (App.test.tsx L295-296, 586, 616; projectStore.test.ts) still pass, proving keys/formats unchanged.
- Grep confirms the 7 removed write-effects no longer call `localStorage.setItem` for the migrated keys.
</verification>

<success_criteria>
- App mounts under fully-blocked storage with the shell rendered and no thrown exception (STORE-01).
- All 7 settings persist via the single `usePersistentState` helper; duplicated lazy-init/effect boilerplate removed (STORE-02, IN-01).
- canvasTemplate migration and theme DOM effect preserved; keys and formats frozen; full suite green (no regressions).
</success_criteria>

<output>
Create `.planning/phases/11-storage-robustness-error-feedback/11-02-SUMMARY.md` when done.
</output>
