---
phase: 01-core-engine-color-mathematics
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - vite.config.ts
  - src/engine/types.ts
  - src/engine/color.ts
  - src/engine/__tests__/color.test.ts
autonomous: true
requirements:
  - ENGINE-01
  - ENGINE-02
must_haves:
  truths:
    - "Arbitrary colors convert from sRGB to CIELAB space within a tolerance of 0.05 units"
    - "Semi-transparent pixels blend with a solid white background color before matching"
    - "sRGB colors match the nearest reference DMC/Art Dot color code using CIEDE2000 distance"
    - "Exact RGB matches are cached using an in-memory Map key of 24-bit integers"
    - "Color ties are resolved stably by choosing the first encountered candidate in the active list"
  artifacts:
    - path: "src/engine/types.ts"
      provides: "Common type definitions including DmcColor and LabCoordinates to resolve circular/wave compile-time dependencies"
    - path: "src/engine/color.ts"
      provides: "Color conversions, blending, distance calculation, caching, stable nearest-match logic, and flat matching grid pipelines"
    - path: "src/engine/__tests__/color.test.ts"
      provides: "Unit test suite verifying conversions, blending, caching, stable tie resolution, and flat grid matching"
  key_links:
    - from: "src/engine/color.ts"
      to: "src/engine/types.ts"
      via: "importing DmcColor and LabCoordinates interfaces"
      pattern: "import .*from '\\./types'"
    - from: "src/engine/__tests__/color.test.ts"
      to: "src/engine/color.ts"
      via: "importing blendAlpha, matchColor, matchPixelGrid, rgbToLab functions"
      pattern: "import .*from '\\.\\./color'"
---

<objective>
Establish the project foundation and implement the core color matching engine. This includes configuring TypeScript, Vite, and Vitest, and writing the mathematical functions for sRGB-to-CIELAB conversion, alpha blending, caching, and CIEDE2000 color distance calculations.
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-core-engine-color-mathematics/01-CONTEXT.md
@.planning/phases/01-core-engine-color-mathematics/01-RESEARCH.md
@.planning/phases/01-core-engine-color-mathematics/01-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold project configuration and dependencies</name>
  <files>package.json, tsconfig.json, vite.config.ts</files>
  <read_first>.planning/PROJECT.md</read_first>
  <action>Initialize package.json, tsconfig.json, and vite.config.ts for a Vite-based TypeScript and Vitest project. Install standard dependencies: "culori" (^4.0.2). Install devDependencies: "typescript" (^5.0.0), "vitest" (^3.0.0 or compatible version), and "vite" (^6.0.0). Configure TypeScript with strict type checking, standard ESNext target/module resolution, and paths for src. Set up vitest configurations inside vite.config.ts to execute tests under the src directory.</action>
  <verify>Run the test runner using pnpm test or npm test to verify it executes successfully (even with no tests).</verify>
  <acceptance_criteria>
    - package.json contains culori, typescript, and vitest dependencies
    - tsconfig.json has strict compiler options enabled
    - vite.config.ts includes a vitest configuration block
  </acceptance_criteria>
  <done>Configuration files created and test command executes without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Implement and test core color science functions and grid matching pipeline</name>
  <files>src/engine/types.ts, src/engine/color.ts, src/engine/__tests__/color.test.ts</files>
  <read_first>.planning/phases/01-core-engine-color-mathematics/01-RESEARCH.md, .planning/phases/01-core-engine-color-mathematics/01-PATTERNS.md</read_first>
  <action>Create src/engine/types.ts defining the DmcColor and LabCoordinates interfaces, separating types from the static catalog to prevent wave compilation blockers. Create src/engine/color.ts. Import tree-shakable functions from culori/fn (D-01) and register modes (modeRgb, modeXyz65, modeLab) manually (D-04). Implement: (1) rgbToLab(r, g, b) returning CIELAB L/a/b coordinates using converter('lab'); (2) blendAlpha(r, g, b, a) which blends transparent/semi-transparent pixels with a solid white (#FFFFFF) background color (D-02); (3) matchColor(r, g, b, activeCandidates) using differenceCiede2000 (D-10) with exact in-memory cache mapping RGB 24-bit integers to DMC color objects (D-03); (4) stable tie-breaking logic using strict inequality (D-11); and (5) matchPixelGrid(pixels, candidates) which accepts a flat Uint8ClampedArray of RGBA values and an array of active DmcColor candidates (D-09), and returns a flat array of matched codes and the aggregated count summary object (D-12). Create src/engine/__tests__/color.test.ts with Vitest unit tests verifying conversions, alpha blending, caching, stable tie resolution, and matchPixelGrid. Crucially, the unit tests must use local mock color candidates instead of importing DMC_PALETTE or types from palette.ts, ensuring no dependencies on files from Wave 2.</action>
  <verify>Run the newly created unit tests using the test runner command to verify that all color conversions, blending, caching, stable tie-breaking, and grid matching cases pass.</verify>
  <acceptance_criteria>
    - src/engine/types.ts defines LabCoordinates and DmcColor interfaces
    - src/engine/color.ts exists and correctly implements rgbToLab, blendAlpha, matchColor, and matchPixelGrid
    - Unit tests in src/engine/__tests__/color.test.ts verify conversion boundaries within 0.05 tolerance, exact pixel grid matching with flat inputs, and aggregated counts
    - Unit tests are completely self-contained with mock candidates, without importing src/engine/palette.ts or DMC_PALETTE
    - Test runner reports all tests passing successfully
  </acceptance_criteria>
  <done>Color science types, functions, and flat grid matching pipeline implemented and automated unit tests verify correct behavior.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- Run the test suite: npm run test or npx vitest run
- Verify that TypeScript compilation succeeds without errors: npx tsc --noEmit
</verification>

<success_criteria>
- package.json, tsconfig.json, and vite.config.ts are properly configured.
- src/engine/color.ts correctly handles D-01, D-02, D-03, D-04, D-09, D-10, D-11.
- Unit tests verify conversion, blending, caching, and tie-breaking behavior within tolerance.
- All tests pass and type-checking succeeds.
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-engine-color-mathematics/01-01-SUMMARY.md`
</output>
