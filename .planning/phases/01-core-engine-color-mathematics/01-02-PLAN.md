---
phase: 01-core-engine-color-mathematics
plan: "02"
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - scratch/generate-palette.js
  - src/engine/palette.ts
  - src/engine/__tests__/palette.test.ts
autonomous: true
requirements:
  - PALETTE-01
  - PALETTE-02
must_haves:
  truths:
    - "The system supports the Art Dot 100-color kit index containing its specific DMC mappings"
    - "The system supports the Art Dot 200-color kit index containing its specific DMC mappings"
    - "Overlapping colors in 100 and 200 kits are represented under a single unified catalog definition with kit membership metadata"
    - "Reference catalog entries include pre-calculated CIELAB coordinates to bypass conversion during matches"
  artifacts:
    - path: "src/engine/palette.ts"
      provides: "DMC_PALETTE constant containing unified list of DmcColor objects with pre-calculated Lab coordinates and kit tags"
    - path: "scratch/generate-palette.js"
      provides: "A batch compilation utility converting raw RGB/hex codes to pre-calculated Lab coordinates via Culori"
    - path: "src/engine/__tests__/palette.test.ts"
      provides: "Unit tests verifying catalog integrity, size of kits (100 and 200), and overlapping metadata"
  key_links:
    - from: "src/engine/palette.ts"
      to: "src/engine/types.ts"
      via: "importing DmcColor and LabCoordinates interfaces"
      pattern: "import .*from '\\./types'"
    - from: "src/engine/__tests__/palette.test.ts"
      to: "src/engine/palette.ts"
      via: "importing DMC_PALETTE reference catalog"
      pattern: "import .*from '\\.\\./palette'"
---

<objective>
Compile the static manufacturer color databases for Art Dot 100-color and 200-color kits. Build a utility script that processes raw color tables, pre-calculates their CIELAB coordinates, and writes a single unified, deduplicated TypeScript catalog containing kit membership metadata. Verify catalog integrity via unit tests.
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-core-engine-color-mathematics/01-CONTEXT.md
@.planning/phases/01-core-engine-color-mathematics/01-RESEARCH.md
@.planning/phases/01-core-engine-color-mathematics/01-PATTERNS.md
@.planning/phases/01-core-engine-color-mathematics/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create batch palette generator and compile unified reference index</name>
  <files>scratch/generate-palette.js, src/engine/palette.ts</files>
  <read_first>src/engine/types.ts, src/engine/color.ts</read_first>
  <action>Create a palette generation script at scratch/generate-palette.js. Define the list of DMC reference colors included in standard Art Dot 100-color and 200-color assortments, capturing each color's DMC code (string, e.g. "310", "BLANC", "ECRU" as per D-06), descriptive name, and standard digital RGB hex code. The 100 and 200 kits have overlapping colors; structure the input as a unified set where each entry has membership metadata (e.g. kits: ["100", "200"] as per D-07, D-08). The script must use Culori's converter('lab') (configured identically to Plan 01) to calculate CIELAB coordinates for each color. Finally, the script must write a clean TypeScript file at src/engine/palette.ts. Crucially, palette.ts must import the DmcColor and LabCoordinates interfaces from src/engine/types.ts (rather than defining them locally) and statically export the DMC_PALETTE constant array (D-05).</action>
  <verify>Run scratch/generate-palette.js using node and verify that src/engine/palette.ts is successfully created, imports DmcColor and LabCoordinates from src/engine/types.ts, and has valid TypeScript syntax.</verify>
  <acceptance_criteria>
    - scratch/generate-palette.js executes without errors using standard Node.js
    - src/engine/palette.ts contains the exported DMC_PALETTE constant and imports DmcColor/LabCoordinates from types.ts
    - Every color in DMC_PALETTE has pre-calculated lab: { l, a, b } coordinates (D-08) and a kits array containing "100" and/or "200" (D-07)
  </acceptance_criteria>
  <done>Reference catalog generated with pre-calculated color space coordinates and external types imported.</done>
</task>

<task type="auto">
  <name>Task 2: Implement palette catalog integrity tests</name>
  <files>src/engine/__tests__/palette.test.ts</files>
  <read_first>src/engine/palette.ts</read_first>
  <action>Create src/engine/__tests__/palette.test.ts. Write Vitest unit tests to verify: (1) DMC_PALETTE contains all unique colors without code duplication; (2) filtering for kit "100" returns exactly the standard assortment of colors; (3) filtering for kit "200" returns exactly the standard assortment of colors; and (4) reference coordinates (like "310" black and "BLANC" white) have coordinates corresponding to expected CIELAB space boundaries.</action>
  <verify>Run the test runner to verify that all palette tests execute and pass successfully.</verify>
  <acceptance_criteria>
    - src/engine/__tests__/palette.test.ts exists and compiles cleanly
    - Tests assert kit sizes match expected Art Dot 100 and 200 sizes respectively
    - Vitest reports successful passes for all suite tests
  </acceptance_criteria>
  <done>Palette unit tests written and passing, certifying static data integrity.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- Run the palette generator: node scratch/generate-palette.js
- Run the full test suite: npm run test or npx vitest run
- Run TypeScript compile check: npx tsc --noEmit
</verification>

<success_criteria>
- scratch/generate-palette.js is written and successfully generates src/engine/palette.ts.
- src/engine/palette.ts complies with D-05, D-06, D-07, D-08.
- Unit tests pass and verify the size and deduplication of the kits (100 and 200).
- TypeScript compiles cleanly.
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-engine-color-mathematics/01-02-SUMMARY.md`
</output>
