---
phase: 04-supply-planning-customization-exports
plan: "02"
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - src/engine/viewer.ts
  - src/App.tsx
  - src/__tests__/integration.test.tsx
autonomous: true
requirements:
  - PALETTE-03
  - VIEW-03
must_haves:
  truths:
    - "Toggling palette check items updates excluded candidates list and triggers instant Web Worker matches"
    - "Selecting a legend color row dims non-selected cells in the canvas viewport to 20% opacity"
    - "Highlighted color cells render at full opacity centered on their grid coordinate scales"
  artifacts:
    - path: "src/__tests__/integration.test.tsx"
      provides: "Integration tests verifying active sub-palette filter changes, highlight clicks, and opacity dims"
  key_links:
    - from: "src/__tests__/integration.test.tsx"
      to: "src/App.tsx"
      via: "importing App component"
      pattern: "import .*from '\\.\\./App'"
---

<objective>
Implement sub-palette exclusions and color highlight overlays. This includes filtering active matching candidates based on checklist states, and coding the canvas viewer's opacity blitting passes to dim non-selected cells.
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-supply-planning-customization-exports/04-CONTEXT.md
@.planning/phases/04-supply-planning-customization-exports/04-RESEARCH.md
@.planning/phases/04-supply-planning-customization-exports/04-PATTERNS.md
@.planning/phases/04-supply-planning-customization-exports/04-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement sub-palette check list filters and instant matching recalculations</name>
  <files>src/App.tsx</files>
  <read_first>src/App.tsx, .planning/phases/04-supply-planning-customization-exports/04-RESEARCH.md</read_first>
  <action>Modify src/App.tsx: (1) maintain excludedColors state (a Set of DMC codes); (2) render the sub-palette checklist in the right sidebar containing checkboxes for each candidate color; (3) add "Select All" and "Clear All" quick-action buttons; (4) on checkbox toggle, update the Set and immediately execute matching via MatcherClient.match(), passing only candidates whose codes are not in the excluded Set. In-flight jobs are aborted automatically.</action>
  <verify>Ensure compiling passes with npx tsc --noEmit</verify>
  <acceptance_criteria>
    - src/App.tsx compiles cleanly
    - Toggling sub-palette check boxes updates exclusions and triggers worker executions
    - Worker candidate inputs filter out excluded colors correctly
  </acceptance_criteria>
  <done>Sub-palette checklist exclusions and instant matching runs implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Build selection highlighting and cell opacity dimming in CanvasViewer</name>
  <files>src/engine/viewer.ts, src/App.tsx</files>
  <read_first>src/engine/viewer.ts, src/App.tsx</read_first>
  <action>Modify src/engine/viewer.ts: (1) add private highlightedColor: string | null = null; (2) add setHighlightedColor(code: string | null) updating the selection and invoking draw(); (3) update draw() method: if highlightedColor is set, configure globalAlpha = 0.2, blit the offscreen double-buffered image, reset globalAlpha = 1.0, and loop through cells drawing only the highlighted color cells fully opaque at their calculated destX/destY viewport positions. Modify src/App.tsx: (1) maintain highlightedColor state; (2) on clicking a row in the Legend supply table, toggle highlightedColor (set it if not selected, clear it if clicked again); (3) invoke viewer.setHighlightedColor(code) on row clicks.</action>
  <verify>Check that both files compile successfully with tsc.</verify>
  <acceptance_criteria>
    - src/engine/viewer.ts and src/App.tsx compile successfully
    - setHighlightedColor updates viewer selections and triggers redraws
    - Legend table row clicks update highlighted selections
    - draw() dims non-highlighted cells to 0.2 opacity and draws highlighted cells at full opacity
  </acceptance_criteria>
  <done>Canvas highlighting and cell opacity dimming implemented.</done>
</task>

<task type="auto">
  <name>Task 3: Implement integration tests for sub-palettes and highlighting</name>
  <files>src/__tests__/integration.test.tsx</files>
  <read_first>src/App.tsx</read_first>
  <action>Create src/__tests__/integration.test.tsx. Write Vitest integration tests verifying: (1) toggling sub-palette checkboxes filters candidates lists; (2) selecting legend rows updates highlighted color codes in the viewer; (3) canvas viewer draw context receives the correct globalAlpha parameters for highlight blending passes.</action>
  <verify>Run the integration tests using npx vitest run src/__tests__/integration.test.tsx to verify all pass.</verify>
  <acceptance_criteria>
    - src/__tests__/integration.test.tsx exists and runs
    - Integration tests check checklist filters, legend highlights, and viewer opacity calls
    - Test runner reports all tests pass
  </acceptance_criteria>
  <done>Integration tests for sub-palettes and highlights created and passed.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- Run the test suite: npx vitest run src/__tests__/integration.test.tsx
- Verify that TypeScript compilation succeeds without errors: npx tsc --noEmit
</verification>
