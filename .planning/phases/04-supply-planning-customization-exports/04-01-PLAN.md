---
phase: 04-supply-planning-customization-exports
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - vite.config.ts
  - tsconfig.json
  - index.html
  - src/App.tsx
  - src/main.tsx
  - src/index.css
  - src/__tests__/App.test.tsx
autonomous: true
requirements:
  - REPORT-01
  - INGEST-01
  - INGEST-03
  - INGEST-04
must_haves:
  truths:
    - "Preact and Tailwind CSS v4 compile cleanly into the Vite build pipeline"
    - "Dashboard layout displays sidebar menus, canvas view containers, and legend lists correctly"
    - "Dimensions input controls convert metric/imperial inputs and update canvas columns and rows"
  artifacts:
    - path: "src/App.tsx"
      provides: "Dashboard UI shell, left control sidebar, right palette sidebar, and legend table"
    - path: "src/main.tsx"
      provides: "Preact application entry point mounting App to the DOM"
    - path: "src/index.css"
      provides: "Tailwind CSS v4 base imports and print layout styles"
    - path: "src/__tests__/App.test.tsx"
      provides: "Unit tests verifying mounting, UI layouts, and input form controls"
  key_links:
    - from: "src/main.tsx"
      to: "src/App.tsx"
      via: "importing App component"
      pattern: "import .*from '\\./App'"
    - from: "src/__tests__/App.test.tsx"
      to: "src/App.tsx"
      via: "importing App component"
      pattern: "import .*from '\\.\\./App'"
---

<objective>
Configure Preact and Tailwind CSS v4 in the project and build the core dashboard UI shell. This includes wire-framing the left configuration sidebar, center canvas container, right sub-palette legend sidebar, and size converters.
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-supply-planning-customization-exports/04-CONTEXT.md
@.planning/phases/04-supply-planning-customization-exports/04-RESEARCH.md
@.planning/phases/04-supply-planning-customization-exports/04-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install and configure Preact + Tailwind CSS v4</name>
  <files>package.json, vite.config.ts, tsconfig.json, index.html</files>
  <read_first>.planning/phases/04-supply-planning-customization-exports/04-RESEARCH.md</read_first>
  <action>Install dependencies: "preact" (^10.25.0). Install devDependencies: "@preact/preset-vite" (^2.9.0), "tailwindcss" (^4.0.0), "@tailwindcss/vite" (^4.0.0). Update vite.config.ts to import and apply preact() and tailwindcss() plugins. Update tsconfig.json to add jsx: "react-jsx" and jsxImportSource: "preact" compiler options. Modify index.html to add a mounting container `<div id="app"></div>` and include the script tag `<script type="module" src="/src/main.tsx"></script>`.</action>
  <verify>Run vite build or tsc compile to check for configuration/installation errors.</verify>
  <acceptance_criteria>
    - package.json contains preact and tailwind dependencies
    - vite.config.ts includes preact and tailwind plugins
    - tsconfig.json has jsx compiler paths configured
    - index.html includes main.tsx module script reference
  </acceptance_criteria>
  <done>Preact and Tailwind v4 dependencies installed and configured.</done>
</task>

<task type="auto">
  <name>Task 2: Implement dashboard UI layout components</name>
  <files>src/App.tsx, src/main.tsx, src/index.css</files>
  <read_first>src/engine/viewer.ts, src/engine/worker-client.ts</read_first>
  <action>Create src/index.css importing tailwindcss. Create src/main.tsx mounting App to #app. Create src/App.tsx implementing the full dashboard layout: (1) left control sidebar with file input/drop zone, rows/cols numeric fields, physical width/height unit fields converting values to grid columns/rows dynamically, and square/round drill style selector; (2) central viewport holding the viewport `<canvas>` element and initializing CanvasViewer on it; (3) right legend sidebar containing checklists for sub-palette exclusion (starts with all checked) and Legend table displaying swatches, DMC codes, swatches, names, and exact counts. Connect sizing modifications and image loading changes to re-trigger MatcherClient.match() runs and redraw the viewport on complete.</action>
  <verify>Run vite build and tsc to check that files compile cleanly.</verify>
  <acceptance_criteria>
    - src/index.css exists and contains tailwind imports
    - src/main.tsx exists and mounts Preact App successfully
    - src/App.tsx implements control sidebars, legend tables, canvas wrappers, and coordinates triggers
  </acceptance_criteria>
  <done>Dashboard UI shell and controllers implemented.</done>
</task>

<task type="auto">
  <name>Task 3: Write unit tests for dashboard mounting and inputs</name>
  <files>src/__tests__/App.test.tsx</files>
  <read_first>src/App.tsx</read_first>
  <action>Create src/__tests__/App.test.tsx. Write Vitest unit tests checking: (1) App component mounts without errors; (2) file selector and dimension elements exist in the DOM; (3) resizing inputs update columns and rows correctly; (4) legend tables display matching swatches, DMC codes, and exact counts correctly.</action>
  <verify>Run the newly created tests using npx vitest run src/__tests__/App.test.tsx to check it passes successfully.</verify>
  <acceptance_criteria>
    - src/__tests__/App.test.tsx exists and runs on Vitest
    - Unit tests cover mounting, control fields, and legend table formatting
    - Test runner reports all tests pass
  </acceptance_criteria>
  <done>Unit tests for dashboard components created and passed.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- Run the test suite: npx vitest run src/__tests__/App.test.tsx
- Verify that TypeScript compilation succeeds without errors: npx tsc --noEmit
</verification>
