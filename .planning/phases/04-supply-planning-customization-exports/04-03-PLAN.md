---
phase: 04-supply-planning-customization-exports
plan: "03"
type: execute
wave: 3
depends_on: ["02"]
files_modified:
  - src/App.tsx
  - src/index.css
  - src/__tests__/print.test.tsx
autonomous: true
requirements:
  - REPORT-02
  - REPORT-03
must_haves:
  truths:
    - "Safety counts multiply exact drill quantities by +10% and round up to integers"
    - "Recommended packet counts calculate safety values divided by 200 and rounded up"
    - "Print media queries hide sidebar controls, maximize print width, and center print viewport grids"
    - "PDF Export button invokes the browser print dialog natively using window.print()"
  artifacts:
    - path: "src/__tests__/print.test.tsx"
      provides: "Unit tests verifying safety calculations, 200-multiple packet rounding, and boundary conditions"
  key_links:
    - from: "src/__tests__/print.test.tsx"
      to: "src/App.tsx"
      via: "importing safety/packet calculator functions"
      pattern: "import .*from '\\.\\./App'"
---

<objective>
Implement safety margin calculations, packet rounding, and CSS print PDF layouts. This includes displaying packet numbers in the legend table and setting up print media styles that format canvas previews and checklist indexes for letter/A4 pages.
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-supply-planning-customization-exports/04-CONTEXT.md
@.planning/phases/04-supply-planning-customization-exports/04-RESEARCH.md
@.planning/phases/04-supply-planning-customization-exports/04-PATTERNS.md
@.planning/phases/04-supply-planning-customization-exports/04-01-PLAN.md
@.planning/phases/04-supply-planning-customization-exports/04-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add safety margins and packet purchase rounding to legend reports</name>
  <files>src/App.tsx</files>
  <read_first>src/App.tsx, .planning/phases/04-supply-planning-customization-exports/04-RESEARCH.md</read_first>
  <action>Modify src/App.tsx: (1) create helper function calculateSafetyPurchase(exactCount) multiplying by 1.1 (safety margin), rounding safety count up to integer, dividing safety count by 200, and rounding up to find recommended packets; (2) update Legend table display: add "Safety Count" (+10%) and "Recommended Packets (200 drills/pk)" columns, rendering computed values next to exact counts.</action>
  <verify>Verify calculations compile cleanly and render inside DOM tests.</verify>
  <acceptance_criteria>
    - src/App.tsx helper function computes safety counts and packets correctly
    - Legend table displays safety counts and packet numbers in additional columns
  </acceptance_criteria>
  <done>Safety margin counts and packet purchase rounding implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Implement CSS print media queries and PDF export trigger</name>
  <files>src/index.css, src/App.tsx</files>
  <read_first>src/index.css, src/App.tsx</read_first>
  <action>Modify src/index.css: add `@media print` section hiding left and right sidebars (class `no-print`), widening canvas view and legend containers (class `print-area`) to 100% width, page-breaking tables cleanly, and styling canvas elements to scale within paper sheets. Modify src/App.tsx: add an "Export PDF" button (class `no-print`) in the header that invokes window.print() natively.</action>
  <verify>Build stylesheet and verify window.print is invoked successfully.</verify>
  <acceptance_criteria>
    - src/index.css print media queries compile cleanly
    - Export button is styled with no-print classes and invokes window.print() on click
  </acceptance_criteria>
  <done>CSS print styles and window.print PDF triggers implemented.</done>
</task>

<task type="auto">
  <name>Task 3: Write unit tests for safety margins and purchase calculations</name>
  <files>src/__tests__/print.test.tsx</files>
  <read_first>src/App.tsx</read_first>
  <action>Create src/__tests__/print.test.tsx. Write unit tests checking: (1) calculateSafetyPurchase applies +10% safety margin correctly; (2) packet counts round up correctly to multiples of 200; (3) boundary values (e.g. 0, 1, 199, 200) map to correct safety and packet purchase counts.</action>
  <verify>Run tests using npx vitest run src/__tests__/print.test.tsx to check assertions pass.</verify>
  <acceptance_criteria>
    - src/__tests__/print.test.tsx exists and runs
    - Unit tests cover safety margins, 200-multiple packets, and boundary values
    - Test runner reports all tests pass
  </acceptance_criteria>
  <done>Unit tests for safety margin and packet rounding created and passed.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- Run the test suite: npx vitest run src/__tests__/print.test.tsx
- Verify that TypeScript compilation succeeds without errors: npx tsc --noEmit
</verification>
